# 战斗数值与公式说明（BALANCE）

> 本文档汇总单挑（轮换出招回合制）的全部数值与公式，便于查阅与调整。
> 变量缩写：体=体力 · 武=武力 · 统=统帅 · 智=智力 · 政=政治 · 魅=魅力；
> 「己」=出招方，「敌/守」=承受方。实现见 `js/engine.js`，评级见 `js/app.js`。

## 一、体力 / 战意

- **体力上限 = 初始体力 = `体`**，归零即 KO。
- **战意初始** = `min(100, round(55 + 政 × 0.45))`
- **每回合末回战意** = `2 + 政 / 22`
- **出招战意消耗** = `max(2, round(招式基础耗 × (1 − min(0.34, 政/300))))`；基础耗为 0 则不耗。

| 招式 | 猛攻 | 普攻 | 格挡 | 谋攻 | 束缚 | 弱化 | 疗伤 | 蓄力 |
|---|---|---|---|---|---|---|---|---|
| 基础耗 | 20 | 0 | 0 | 7 | 12 | 10 | 11 | 4* |

\* 蓄力实际是「恢复战意」，并不扣除。

## 二、先手 / 回合

- **先手**：`spd = 统 + 随机(0~20)`，高者先手；之后双方**逐回合交替出招**。
- 被束缚的一方轮到自己时**跳过该回合**（消耗一层束缚）。

## 三、物理攻击伤害（猛攻 / 普攻）

```
base       = 武                              （只看攻方武力）
mitigation = (1 − min(0.50, 守.统/380))      （守方统帅减伤）
           × (1 − min(0.12, 守.政/1000))     （守方政治微韧性）
counter    = 相克表[招式][守方姿态]
critMul    = 蓄势命中 ? 2.0 : 1.0            （蓄力「暴发」翻倍）
luck       = 随机 0.82 ~ 1.18
atkMul     = 被「弱化」时 <1，否则 1
power      = 猛攻 1.0 / 普攻 0.5

伤害 = base × 0.32 × counter × mitigation × critMul × luck × atkMul × power
```

随后叠加**暴击**（见第五节），最后 `dmg = max(1, round(dmg))`。

## 四、谋攻伤害（智力系，无视格挡）

```
成功率 = clamp(0.55 + (己.智 − 守.智)/220, 0.12, 0.94)
  失败 → 未命中（0 伤害，"被识破"）
  成功 ↓
base       = 己.智
mitigation = 1 − min(0.55, 守.智/360)         （守方智力减伤）
其余 counter / critMul / luck / power(=1) 同物理；可暴击；忽略守方格挡
```

## 五、暴击（由魅力决定）

```
暴击率 = min(0.6, 攻.魅/280) + (处于蓄势 ? 0.35 : 0)
触发 → 伤害 × 1.7
```

猛攻 / 普攻 / 谋攻均可暴击。蓄势的 `×2.0 暴发` 与暴击 `×1.7` 可叠加。

## 六、相克倍率 counter（攻方招式 × 守方「最近姿态」）

> 守方姿态 = 它上一次的攻击招式（猛攻/普攻/谋攻）；用计、格挡后姿态记为「普通」。

| 攻＼守 | 格挡 | 谋攻 | 普攻 | 猛攻 | 蓄力 |
|---|---|---|---|---|---|
| 猛攻 | 0.55 | 1.35 | 1.05 | 1.0 | 1.45 |
| 普攻 | 0.85 | 1.05 | 1.0 | 0.95 | 1.2 |
| 谋攻 | 1.5 | 0.95 | 0.95 | 0.6 | 1.25 |

（实战中守方姿态只会是 普通/猛攻/普攻/谋攻 之一。）

## 七、格挡（必定成功）

```
减伤比例 frac = clamp(0.55 + (己.统 − 敌.武)/250, 0.2, 0.92)
被卸伤害 blocked = round(原伤害 × frac)
实际受伤 = 原伤害 − blocked
己方战意 += blocked（上限 100）   ← 卸掉的伤害全部转化为战意
```

- 仅对**下一次物理受击**生效，用后消失；**谋攻无视格挡**。

## 八、计策（智力系）

**通用成功率** = `clamp(base + (己.智 − 敌.智)/220, 0.12, 0.94)`

| 计策 | 成功率 base | 效果 |
|---|---|---|
| 束缚 | 0.30 | 敌被束缚 `(己.智 − 敌.智 > 45) ? 2 : 1` 回合（暂停出招） |
| 弱化 | 0.45 | 敌攻击 `×(1 − reduce)`，`reduce = min(0.55, 0.22 + 己.智/600)`；时长 `clamp(2 + round((己.智 − 敌.智)/30), 1, 4)` 回合 |
| 疗伤 | 0.62 | 回体力 `round(己.智 × (0.26~0.46 随机))`，不超上限 |
| 蓄力 | 0.66 | 战意 `+round(6 + 己.智 × 0.12)`，并进入「蓄势」（下次出招暴发 ×2.0、暴击率 +0.35） |
| 谋攻 | 0.55 | 见第四节（攻击型） |

- 束缚、弱化为**免费计策**：发动不占行动，同回合两者各可发动一次，之后仍可正常出招。
- 疗伤、蓄力占用本回合行动。

## 九、胜负判定

- 体力 ≤ 0 → KO。
- 自动模拟（阵营大战 / 世界杯 / 快捷观战）出手上限 160；耗尽则比剩余体力，高者胜。

## 十、评级 / 总评（图鉴与界面）

- 单维评级：`SS ≥ 100 · S ≥ 95 · A ≥ 90 · B ≥ 80 · C ≥ 70 · D ≥ 60 · E < 60`
- **总评 = 六维之和**；均值 = 和 ÷ 6（总评徽章按均值取整评级）。

## 十一、友谊系统（自选武将专属）

**获得途径**（主角出战的对应玩法中自动累计，上限 `Bond.MAX_FRIEND = 300`）：

| 互动方式 | 友谊值 |
|---|---|
| 历练 / 2v2 单挑击败对手 | 对手 +5 |
| 2v2 与副将并肩获胜 | 副将 +15 |
| 组队大战同队出战 | 每名队友 +3，获胜再 +3（共 +6） |
| 百人斩击败守将 | 每名被斩守将 +4 |
| 阵营大战并肩存活到终局 | 同阵营幸存同袍各 +2 |
| 世界杯同组交手 | 同组对手各 +3 |
| 国战战至终局仍在麾下 | 同袍各 +3，获胜 +6 |
| 赠送宝物（按「(武将,宝物)」首次赠送计算，不按天） | 按所赠宝物稀有度：普通 +10 / 精良 +30 / 稀有 +60 / 传说 +100 |
| 拜访（每人每【游戏日】限一次，宿营过夜后可再访；未开局时按自然日；无消耗；**友谊达上限 300 后禁用**，按钮置灰并提示「友谊已满」） | 随机 +1~+2 |
| 切磋（武将详情弹窗「⚔️ 切磋」按钮，经典单挑，每人每【游戏日】限一次，耗 1 行动力） | 获胜 +3~+5；落败不加；另见下方「切磋习得」 |

**实际增量与上限显示一致**：`Bond.addF(id,n)` 返回本次实际生效的增量（`min(300, 原值+n) - 原值`），而非传入的名义值 n。切磋获胜、赠送宝物、拜访等一切会展示「友谊 +N」的结算文案均改用该返回值渲染——若对方友谊已在 300 上限附近，文案会如实显示较小的实际增量甚至改为「友谊已至上限」提示，不会再出现友谊早已封顶却仍显示按名义值增长的错位文案。

**友谊等级**：陌生 0 → 相识 30 → 好友 80 → 挚友 150 → 生死之交 300（与招募所需的上限对齐）

**招募**（改为「满上限 + 重金」，不再有中间档可招募或免费档）：
- 友谊须达上限（≥300）才可谈及招募；未满上限时按钮固定置灰显示「🔒 友谊满上限（300）后可招募」
- 招募费 = `武将评分 × 10` 金（无免费档，`Bond.recruitCost`）
- 团队上限 = `5 + ⌊主角等级 / 10⌋`
- 队友用途：2v2 任选副将；同阵营队友在组队大战 / 国战 / 阵营大战必上阵
- **队友不可随意请出团队**；团队已满时再招募新武将，须从现有队友中选一名顶替，才能腾出位置（顶替流程仍走正常的招募友谊门槛与花费）

**切磋习得**：切磋结算时额外判定一次——触发概率随对手当前友谊值线性提升，`1% + (友谊/300)×30%`，友谊 0 时最低 1%、友谊满 300 时最高 31%（`RPG.trySparLearn`）。命中后比较双方按当前实际数值（含装备/惩罚/成长加成，`Armory.geared`）——取败方六维中最高一项，若严格大于胜方同一项，则胜方该项 +1：主角一方走 `RPG.char.alloc` 永久成长（110 封顶保护），对手一方写入战役内 `Campaign.mapState().statGrowth`（与刺杀惩罚 `statPenalty` 同构、符号相反，只影响本局展示与交战、不写回武将图鉴全局数值）。

**赠送宝物细节**（在武将详情页的装备栏为其选择宝物，即为赠送——不再有单独的赠礼弹窗）：
- 只能从玩家自己仓库中「已鉴定且未装备」的宝物里选（避免误赠正被自己或队友佩戴的装备）；宝物本身不再额外消耗金币——它的价值已体现在获取它所付出的代价（掉落运气 / 商店金 / 锻造材料）上。
- 赠出的宝物会直接为对方装备到对应槽位；若对方该槽位已有其他宝物，旧宝物会先卸下退回玩家仓库（不会被覆盖丢失），再换上新赠之物。
- **史实武将的装备槽没有「卸下」选项**，只能用另一件宝物替换（送出去的东西不能白白要回来，只能以新换旧）；「卸下」按钮仅在主角自己的装备栏（角色扮演主页）可用。
- 选择弹窗的「取消」按钮会回到武将详情页而非直接关闭整个信息面板。
- **防刷机制**：友谊奖励按「这件具体的宝物（以其唯一编号识别）是否第一次装到这位武将身上」发放，而非按自然日限次。同一件宝物无论卸下多少次、换给多少位武将、之后又换回来，都只在史上首次装到某位武将身上时给一次友谊——用两件宝物来回横跳无法重复刷分，唯一的加友谊途径是不断获得新的宝物（掉落 / 商店 / 锻造）。

## 十二、金币系统（自选武将专属）

**获得**（均在结算弹窗与经验同屏显示）：

| 来源 | 金币 |
|---|---|
| 历练 / 2v2 获胜 | +15 |
| 车轮大战 | 每连胜一阵 +8 |
| 百人斩 | 每攻克一层 +8 |
| 组队大战获胜 | +30 + 击杀 ×3 |
| 阵营大战获胜 | +40 |
| 国战 | 攻克每城 +40，一统天下再 +200 |
| 世界杯名次 | 夺冠 +100 / 止步半决赛·决赛 +50 |
| 世界杯竞猜 | 竞猜得分 ×1（第 r 轮每中一场 (r+1)×10 分，猜中冠军总分翻倍） |

**消耗**（提示均注明扣款与余额）：

| 用途 | 金币 | 效果 |
|---|---|---|
| 招募费 | 评分×10 | 友谊须满上限（300）方可招募，无免费档 |
| 机缘重抽 🎲 | 50 | 百人斩「天赐机缘」三选一重新随机 |
| 回魂丹 💊 | 100 | 主角在历练/2v2/百人斩/车轮单挑倒地时满血续战（每场限一次） |
| 重掷地图 🎲 | 30 | 国战开局（第1回合未行动前）重新生成地图 |
| 鉴宝 🔍 | 50 | 揭示战场拾获的「未鉴定」宝物细节，解锁装备与拆解 |

**天赐机缘六维上限**：角色扮演模式下（`Tower.rpg` 为真），`offerBuffs()` 生成三选一候选前先过滤 `Tower.BUFFS`——「疗养生息」（回体力，不涉及维度增长）恒可选，其余六项永久成长机缘（体力+16、其余五维各+7）若 `RPG.eff(RPG.char, k) + 该机缘固定加成量 > 110` 则移出候选池——不只排除已封顶的维度，连"当前值+固定加成量"会突破 110 上限的也一并排除（例如某维度当前 109，距上限仅差 1 点，但机缘固定 +7，109+7=116 会突破上限，同样不作为选项出现），避免选项本身诱导继续加点一个即将突破上限的维度；若过滤后候选不足 3 项（多数维度已封顶或临界）则回退为未过滤的全量 `BUFFS`，保证界面仍有三个选项可选。小游戏自由试玩（`Tower.rpg` 为假，无持久角色可参照）不受此限制。

## 十三、宝物系统（自选武将专属）

**五类宝物**：

| 类型 | 加成属性 | 举例 |
|---|---|---|
| 兵器 ⚔️ | 武力 | 青釭剑、方天画戟、村正、正宗 |
| 坐骑 🐎 | 统帅 | 赤兔马、的卢、川中岛骏马 |
| 书籍 📖 | 智力 或 政治（按典籍性质固定，同名书籍恒定同一属性） | 孙子兵法、六韬、五轮书 |
| 服饰 👘 | 魅力 | 蜀锦战袍、羽织家纹、凤纹腰带 |
| 奇珍 🔮 | 六选一战斗特效或行动力效果（见下表，每个模板固定绑定一种；传国玉玺/九鼎特殊——仅传说品阶为行动力效果，其余品阶走各自默认效果） | 传国玉玺、勾玉、南蛮令 |

每类各 20 件历史典故命名的模板，共 100 件；宝物图鉴按「是否曾获得过该名称」记录发现进度，并显示该件的属性/效果加成区间。

**书籍属性归类**：军事兵法类（孙子兵法、六韬、三略、太公兵法、五轮书、甲阳军鉴、武经总要、吴子兵法、尉缭子、司马法、三十六计、孙臏兵法、忍秘伝、兵法家伝书、太阁记，共 15 部）加**智力**；治国/史论/伦理类（贞观政要、战国策、资治通鉴、论语、汉书，共 5 部）加**政治**。

**稀有度与属性加成**（兵器/坐骑/书籍/服饰四类，以及奇珍的「体魄」效果；每件宝物生成时在对应区间内独立随机取值，而非固定单值，`Armory.rollBonus(rarityK)`）：

| 稀有度 | 权重（掉落/商店/锻造共用） | 属性加成区间 | 基础售价（≈加成基准值×1000） |
|---|---|---|---|
| 普通 | 55 | +1~+2 | 1000 金 |
| 精良 | 28 | +3~+5 | 3000 金 |
| 稀有 | 13 | +6~+8 | 6000 金 |
| 传说 | 4 | +9~+10 | 10000 金 |

售价（`Armory.shopPrice`）按稀有度基准加成值（`RARITIES[].bonus` = 1/3/6/10，即区间下限或代表值）×1000 计算，而非每件实际浮动后的加成——货摊/商店展示价格时宝物本身尚未生成，无从得知精确浮动值；对马黑市/行脚商队折扣时全场再八折。唯一奇珍（赤兔·千里神驹、千里靴）固定为传说级 +10，不参与随机浮动。

**奇珍的六种效果**（每件模板固定绑定其一，数值按普通/精良/稀有/传说四档；`apBonus` 是例外，见下方「行动力奇珍」小节）：

| 效果 | 数值（普/精/稀/传） | 实际作用 |
|---|---|---|
| 体魄 💪 | +1/+3/+6/+10 | 直接提升体力上限（与其他类型的属性加成表一致） |
| 暴击率 💥 | +2%/+4%/+7%/+12% | 直接叠加到暴击率判定，猛攻/普攻/谋攻均可触发 |
| 气血回复 💗 | +1/+3/+5/+8 | 每回合己方行动结束时额外回复体力 |
| 护体 🛡️ | +3%/+6%/+10%/+16% | 体力低于 35% 时，受到伤害额外减免该百分比 |
| 气盛 ⚡ | +2/+4/+8/+13 | 每回合额外恢复战意，出招更频繁 |
| 行动力上限 🚩 | +1（四档数值恒定） | 「传国玉玺」「九鼎」专属，仅传说品阶生效，见下方「行动力奇珍」小节 |

**掉落**（仅角色扮演出战时生效，胜利结算与经验/金币同屏展示；掉落的宝物均为「未鉴定」状态，见下）：

| 玩法 | 掉落几率（宝物 / 材料，随战绩规模浮动） |
|---|---|
| 历练 / 2v2 获胜 | 20% 宝物、30% 材料×1 |
| 车轮大战 | 宝物 min(60%, 连胜×5%)、材料 min(90%, 连胜×8%) |
| 百人斩 | 宝物 min(65%, 层数×5%)、材料 min(90%, 层数×7%) |
| 阵营大战 | 宝物 min(50%, 击杀×2%+获胜25%)、材料 min(85%, 击杀×3%+10%) |
| 组队大战 | 宝物 min(55%, 击杀×3%+获胜25%)、材料 min(85%, 击杀×4%+10%) |
| 国战 | 宝物 min(70%, 攻克×8%+获胜20%)、材料 min(95%, 攻克×10%+10%)；一统天下额外必得 1 件传说宝物 |
| 世界杯 | 夺冠必得传说宝物；四强/半决赛/决赛必得稀有宝物；其余名次 30% 宝物、40% 材料 |

**鉴宝**：战场拾获（掉落 / 一统天下 / 世界杯名次奖励）的宝物一律以「❔ 神秘宝物」状态入库，结算提示与仓库列表均不透露名称、稀有度、类型或效果——需在「宝物库·仓库」花 **50 金** 鉴宝，才能查看具体细节，并解锁装备与拆解。商店购买、锻造所得的宝物则即得即鉴定（购买/锻造本身就是明确的选择，无需二次鉴定）。

**宝物库**（角色扮演主页「🏪 宝物库」入口，四个页签，随战役进度变化；与下文全局「宝物阁」不同）：

- **仓库**：浏览已获得的全部宝物（含待鉴定的神秘宝物，固定排在已鉴定宝物之后，不按其尚未揭示的真实稀有度参与排序，避免用出现位置泄露信息）；已鉴定且未装备的宝物卡片底部并列两个各占 50% 宽度的按钮——**拆解**（换取材料：普通1／精良2／稀有3／传说5，二次确认）与**出售**（`Armory.sellItem`，换取金币：市价的 50%，即普通500／精良1500／稀有3000／传说5000 金，二次确认）。出售后该宝物从仓库移除，并以其自身名称/属性重建为一条货摊，加入「商店」页签的 `Armory.data.shop` 货架池，可再被购回（重新购得时属性加成重新随机）。
- **图鉴**：按五类分组展示 100 件模板的发现进度与各自的属性/效果加成区间，未发现的显示「？」。
- **商店**：每日自动刷新 6 件随机货架，明码标价购买（普通1000／精良3000／稀有6000／传说10000 金，对马岛黑市与行脚商队折扣期间八折）；也可花 20 金主动重刷；购买所得直接鉴定。玩家出售的宝物也会补充进这一货架池。
- **锻造**：每类消耗对应材料 6 份 + 40 金锻造一件随机宝物；连续 12 次未出稀有以上，下一次锻造保底出稀有以上；锻造所得直接鉴定。

**装备**：

- **400 名史实武将均可持有并穿戴宝物，不再局限于已招募的队友**：主角在「角色扮演」主页管理装备（可装备/更换/卸下）；任意武将（无论是否入队）在「武将详情」弹窗中、紧接六维数据下方有一排紧凑的单行五槽装备栏，点击即可装备/更换（仅已鉴定的宝物可选，且没有单独的「卸下」选项——只能以新宝物替换，见「友谊系统」）；选择弹窗的「取消」会回到武将详情页而非直接关闭整个信息面板。
- 每件宝物同一时间只能装备给一位武将；属性/效果加成由 `makeFighter` / `makeTroopUnit` 统一按武将 id 查询叠加，实时影响其出战的任何玩法（历练/车轮/百人斩/2v2/阵营/组队/国战/世界杯）——无论该武将是主角、队友，还是随机遇到的对手/守军/敌将。角色扮演主页、武将详情页、**乃至战斗界面点击头像弹出的武将信息**，六维数值旁都会正确标注装备带来的加成（如 `88(+6)`），不受该武将当时是否已处于战斗单位状态影响。
- 武将图鉴列表中，任何已装备宝物的武将都会在友谊值旁显示其携带的宝物图标（不再要求入队）。

## 十四、存档架构与开局向导（武将扮演版第一、二期）

**两层存档**：

| 层级 | 内容 | 存储 key | 生命周期 |
|---|---|---|---|
| 全局层 | 武将图鉴自定义/编辑的武将 | `wujiang_db_v1` | 永不因「新游戏」重置 |
| 全局层 | 宝物阁编辑/自建的宝物模板（overrides + custom） | `wujiang_armory_global_v1` | 永不因「新游戏」重置 |
| 战役层 | 角色（六维基线/加点/等级/经验/成长模式） | `wujiang_rpg_v1` | 随「新游戏」清空 |
| 战役层 | 金币/友谊/队伍/赠礼记录 | `wujiang_bond_v1` | 随「新游戏」清空 |
| 战役层 | 宝物背包（已获得的宝物实例）/材料/商店/保底进度 | `wujiang_armory_v1` | 随「新游戏」清空 |
| 战役层 | 战役元数据（创建时间、开局模式选择） | `wujiang_campaign_v1` | 「新游戏」时重写 |

「首页 → 新游戏」触发 `Campaign.reset(mode)`：清空 `wujiang_rpg_v1`，将 `wujiang_bond_v1`/`wujiang_armory_v1` 重置为初始结构（含金币归零、友谊/队伍清空、宝物背包清空、商店重新刷新），并写入新的战役元数据；若已存在角色会先展示覆盖确认。

**开局三步向导**：

1. **选角色**：
   - 自创武将：随机基线六维（45~80）+ 随机可分配加点（18~30），姓名/阵营自选。
   - 扮演史实武将：从图鉴任选一位，随后二选一成长模式——
     | 模式 | 初始六维 | 成长加成 | 说明 |
     |---|---|---|---|
     | 少年模式 🌱 | 默认值 × 60%（四舍五入，最低 1） | 全部维度成长 ×1；其中默认值最高的两项额外 ×1.5（「本命天赋」，UI 显示 ★） | 加点后数值可突破默认上限 |
     | 巅峰模式 👑 | 默认原值 | 全部维度成长 ×0.5 | 立即可用但后续成长慢 |

   两种途径统一获得 15 点可分配加点（`points`）。成长倍率实现：`allocate(k)` 每次花费 1 点加点时，实际叠加值 = `growthMul × (talents.includes(k) ? 1.5 : 1)`（可能为小数，`eff()` 用四舍五入取整显示/参战）。**六维基础值（不含装备加成）上限 110**——`allocate(k)` 在 `RPG.eff(c,k) >= 110` 时拒绝该维度继续加点并提示「该维度已达上限 110」；切磋习得、武道会名次奖励等一切主角基础值成长途径统一遵循此上限，装备/宝物加成不受影响、仍可叠加超出。
2. **选天下格局**：史实分布 / 群雄乱入，写入战役元数据 `mode.distribution` 并驱动 `buildCityAssignment()` 生成武将城池归属（见下节）。
3. **确认开局**：展示汇总与（若有旧档）覆盖警告，确认后创建角色、生成地图状态，直接进入「天下地图」。

**武将图鉴全局化**：首页「武将图鉴」固定只展示默认六维与资料，不显示友谊/装备等进度数据（`showDetail(g, { global: true })` 会跳过友谊面板、装备栏与装备加成增量计算）。战斗中/角色扮演中弹出的同一详情弹窗不受影响，仍正常显示装备加成与友谊面板。

**宝物阁全局化**：首页「宝物阁」一次性展示全部 100 件模板（不受「是否已发现」限制），可编辑名称/简介/属性（书籍的智力·政治、奇珍的五种效果二选其一）及四档稀有度加成值，也可自建全新模板；所有加成值统一限幅 **1~15**。编辑/自建的结果通过 `Armory.pool(type)`（内置模板 + overrides 覆盖 + custom 自建的合集）实时参与掉落、商店与锻造的随机池，对**下一局及当前进行中的战役**均生效。

**小游戏隔离**：首页「小游戏」的 8 个自由玩法均通过各自模块的 `rpg`/`rpgMode` 标记判断是否处于角色扮演语境；仅当该标记为真时才会调用触碰金币/友谊/宝物/经验的结算方法，因此从「小游戏」入口进入时，无论战役是否存在、进行多少局，都不会读写任何战役存档数据。

## 十五、天下地图与行动力系统（武将扮演版第二期）

**地图结构**：中原二十城（成都/汉中/长安/洛阳/许昌/邺城/徐州/荆州/柴桑/建业/天水/白帝城/上庸/江陵/宛城/汝南/下邳/寿春/合肥/武昌）+ 战国二十城（萨摩/安艺/京都/大坂/尾张/甲斐/骏府/小田原/越后/奥州/肥后/丰后/出云/备前/近江/越前/加贺/美浓/三河/常陆）+ 对马岛（海路中转，连接建业·徐州与萨摩），共 41 个节点，坐标为风格化相对位置（`CITIES` 表），道路为固定邻接关系（`ROADS` 表），非精确测绘；地图背景叠加风格化中日陆地剪影（`CHINA_LAND_PATH`/`JAPAN_LAND_PATH`，同一 0~100 相对坐标系手绘轮廓，非精确测绘但取意接近真实海岸线形状），令城池落于对应陆地色块之上。城池标记由"圆点+悬浮文字"改为矩形徽章（`.map-city`，圆角 3px），姓名直接嵌入徽章内（`.mcity-name` 6px，当前城池 8px），不再另起一行悬浮。地图支持**缩放/拖动**：鼠标拖拽、触控拖拽、双指捏合、滚轮、右上角四个按钮（＋/－缩放、🎯聚焦当前城市——以当前城池坐标为中心放大至 2.2 倍、🗺️显示全景——复位至 1 倍居中）均可调整（缩放范围 1~3 倍，`MapZoom` 模块级状态跨渲染持久），拖拽通过 document 级临时监听实现（不使用 `setPointerCapture`，避免其劫持 click 事件目标导致城池点击失效），拖动后紧邻的一次 click 会被判定为手势尾迹而非有效点击。天数/行动力/金币/名声四项状态统一显示在顶栏（标题「天下游历」与右侧音乐按钮下方另起一行的 `#map-topbar-status` 条，15px，`flex-basis:100%` 强制换行，不论折叠屏展开与否都固定两行，避免和标题/音乐按钮挤在同一行造成堆叠），不再与角色资料卡重复；等级信息移入角色资料卡（姓名下方、评分左侧）；「🏕️ 宿营」按钮不放在顶栏，而是并入当前城池的功能按钮网格（历练/集市/铁匠铺/设施同一排）。地图与城池面板随宽度自适应：< 600px 为竖屏纵向排列（地图 → 角色资料 → 本地武将 → 城池事务，逐段可滚动）；≥ 600px（折叠屏展开/平板/桌面）自动切换为「地图横贯顶部（约占可视高度 30%，吸顶固定）+ 下方左（角色资料+本地武将）右（城池事务）两栏」。全站返回逻辑（`handleBackAction`）现与手机系统/浏览器返回键同步——每次 `showScreen()` 正常推入一条历史记录，硬件返回键触发的 `popstate` 若检测到弹窗打开则仅关闭弹窗（不消耗画面层级），否则复用与左上角返回箭头完全相同的路由规则。

**武将城池分布**（`buildCityAssignment(distribution, seed)`）：

- 每名武将按其阵营被分配到该阵营 20 城之一，永久固定于当局战役（不随时间迁移）。
- **史实分布**：约 180 位主要武将（三国五虎上将/五子良将/江东十二虎臣/主要谋士，及日本主要大名与重臣，如"蜀→成都/汉中""织田→尾张""武田→甲斐""岛津→萨摩"等）依据 `CITY_HINTS` 表指定具体城池，覆盖范围为第二期上线时的原 20 城；第三期后续新增的 20 城（天水/白帝城等 10 座三国要地 + 肥后/丰后等 10 座战国分国）暂未逐一手工考据归属，其上武将全部按姓名字符串哈希稳定映射——同一局内固定，跨局也固定。其余次要/配角武将同样按哈希兜底映射到同阵营城池。这是风味化设计，非严谨考据。
- **群雄乱入**：全部武将（含前述主要武将）按"姓名 + 本局创建时间戳"哈希重新洗牌，每局分布都不同。

**渐进登场**：新游戏开局时，按城池分组，每城随机现身 2~3 名武将（`Campaign.reset` 内实现），其余「未登场」。此后每次宿营，若累计天数跨过登场节点（初始第 6 天，此后每 +5 天一次），会从全部「未登场」武将中随机揭示 3~5 名（不区分城池、不按名声分层——任何一位高手都可能在游戏早期就现身），并以「⚡ 天下快报」提示。城池面板「本地武将」区块以「已现身X/Y」精简展示当前城池数量，条目仅显示完整姓名（不再附带大字单姓头像图标），每行固定 3 个（`.mc-roster.narrow.triple`）；名录下方是与「🎭 角色详情」同一 `.cup-go` 样式的「🌐 全部武将」大按钮，打开独立的 `#screen-allgen` 页面——与武将图鉴（`#screen-db`）同一 `.db-table` 表格风格（可按列排序、三国/战国/全部筛选、姓名搜索），但去除新增/编辑/删除等修改操作，六维与评分改用 `Armory.geared(g, g.id)` 叠加当前已装备宝物后的实时数值（而非图鉴默认值），并新增「友谊」「所在城」两列；点击任意一行弹出的详情为**只读视图**（`showDetail(g,{readonly:true})`）——不显示拜访/切磋/招募等交友板块，携带宝物槽位仅供查看、不可点击更换。

**武将自行迁移**（`MapUI.wanderGenerals`）：每次宿营，每名已现身武将有 3% 概率迁往其阵营内、与当前城池相邻（`adjCities`，不含对马岛海路）的另一城池，使天下版图持续流动，不会永远静止在初始/首次登场的城池。迁移不受名声/行动力影响，纯随机触发，若当日无武将现身通知（天下快报），有武将迁移时会改为提示迁移人数。

**行动力（⚡ Action Point）**：

| 项目 | 数值/规则 |
|---|---|
| 初始上限 | 1 点/天 |
| 消耗 1 点的行动 | 移动一段路、历练（需先解锁，见下）、车轮/百人斩/2v2/阵营/组队/国战/世界杯（均在 `RPG.xxx()` 入口真正开战前扣减，「再来一次」链式点击同样计费）、接取悬赏、设施挑战、切磋（见「友谊系统」） |
| 免费行动 | 拜访本地武将（点击名录进入详情，含赠礼/招募）、逛集市、逛铁匠铺、查看悬赏榜、宿营 |
| 恢复方式 | 「🏕️ 宿营」：天数 +1，行动力回满；行动力耗尽时相关按钮自动置灰提示 |
| 上限成长 | `Campaign.recalcApMax()` = 1 + ⌊名声阶梯 ÷ 2⌋(0~4) + 已装备的行动力奇珍(传国玉玺/九鼎二选一传说品阶，+1)，封顶 6；武道会首冠不再贡献行动力上限；上限提升时同步补给当天差额，即时可用 |

**历练解锁门槛**：历练（`RPG.train()`，随机对手练级）需名声达到「小有名气」阶梯（`RPG.TRAIN_FAME_TIER = 2`）才开放；未解锁时地图「历练」按钮显示 🔒 与所需阶梯提示。开局阶段的名声/经验来源是拜访、切磋、悬赏与设施挑战——历练是这些手段打出声望后解锁的自由练级方式，而非起手可用的无限刷分入口。

**城池通用/特色功能**：📍拜访（本地已现身武将名录，复用既有武将详情弹窗）、🏋️历练、🏪集市（**本地货摊**：每城每游戏日按种子生成 4 件货品（名声达「略有耳闻」第 1 阶起为 5 件），价格按城市行情系数浮动——对马岛黑市固定 0.8，其余城按城名哈希稳定落在 0.90~1.20，行脚商队奇遇期间取更低者；已购摊位当日售罄，宿营翌日全城重新上货）、⚒️铁匠铺（**各城专精一类宝物**：锻专精类只需材料 5+30 金（名声达「名满天下」第 7 阶起再减为 4+20 金），其余类型 6+40 金，保底进度与宝物库锻造共享）、📋悬赏榜（每城 2~3 条，名声达「名动一国」第 5 阶时一次性为每城 +1 条永久空缺，见下节）、🏯特色设施（每城一项，见下节）。

**导航层级与返回逻辑**：天下地图为战役的顶层主界面（「新游戏」「继续游戏」均直接进入）；角色详情（原「角色扮演」主页，含六维加点/团队/装备）为地图下的子页，通过状态条「🎭 详情」按钮进入，其返回按钮固定回到地图；宝物库（仓库/商店/锻造）可从地图（集市/铁匠铺）或角色详情页两处进入，返回按钮通过 `homeBase` 机制智能回到发起它的那一层，而非固定某一处。所有战斗结算弹窗的「返回养成」类按钮统一改为调用 `goHome()`，同样遵循这一"回到发起处"的规则。

## 十六、名声、悬赏、奇遇、设施与终局（武将扮演版第三期）

**名声九阶**（`Campaign.FAME_TIERS`，数值上限 `Campaign.FAME_MAX = 9999`，`addFame` 封顶写入）：

| 阶梯 | 名声阈值 | 行动力上限贡献（⌊阶梯÷2⌋） | 对应解锁/变化 |
|---|---|---|---|
| 无名之辈 | 0 | +0 | 起点 |
| 略有耳闻 | 150 | +0 | 集市货摊 4→5 件 |
| 小有名气 | 500 | +1 | 解锁「历练」（`RPG.TRAIN_FAME_TIER = 2`） |
| 声名初显 | 1200 | +1 | — |
| 威震一方 | 2200 | +2 | 解锁「天下第一武道会」报名（`RPG.CUP_FAME_TIER = 4`） |
| 名动一国 | 3500 | +2 | 每城悬赏榜永久 +1 条空缺（一次性触发） |
| 威名远播 | 5000 | +3 | — |
| 名满天下 | 7000 | +3 | 铁匠铺专精类锻造再减免（5+30金 → 4+20金） |
| 威加四海 | 9000 | +4 | 声望封顶将至 |

**名声来源**：任意切磋（历练/悬赏/设施挑战等经典单挑或 2v2）获胜 +3；悬赏完成 +8~35（高级悬赏 35，普通视类型 8~16）；设施挑战胜利 +8（单挑/双人比武）/+10（车轮全清）/+15（阵营/组队）/+50（国战一统天下，设施加成再 +15~25）；击败威名榜高手 +25；世界杯按名次 +12~60（夺冠 60）。跨阶时弹出提示并立即重算行动力上限。阶梯阈值较早期 4 档版本进一步拉宽拆分为 9 档，避免开局几场切磋/悬赏就冲满全部阶梯，也让每次跨阶都有具体可感的功能/待遇变化，而不只是数字上涨。

**悬赏榜**（`genBounty`，每城 2~3 条，完成后立即刷新新的一条）：

| 类型 | 描述 | 判定 | 基础奖励 |
|---|---|---|---|
| 讨伐令 | 击败指定武将（本地已现身武将 60% 概率优先出题，否则任意武将） | 经典单挑取胜且对手为目标本人 | 金 60+评分×0.3、名声 +12 |
| 车轮令 | 车轮大战连胜 N 场（N=3/5/8 随机） | 车轮大战本次连胜 ≥ N | 金 50+N×12、名声 8+N |
| 登塔令 | 百人斩攀至第 N 层（N∈{5,10,15,20} 等概率随机，与 legendary 标记无关） | 百人斩本次攀至 ≥ N 层 | 金 50+N×10+N²×2、名声 N+round(N²/4)：5层150金/11名声 · 10层350/34 · 15层650/71 · 20层1050/120（按层数二次方加码，登塔难度随层数陡增故奖赏超线性） |
| 双雄令 | 携副将取胜一场 2v2 | 2v2 战斗（`BATTLE.duo` 存在）取胜 | 金 70、名声 15 |
| 刺杀令 | 潜入敌境刺杀该城已现身的敌方阵营本地武将 | 经典单挑取胜且对手为目标本人（`m.activeAssassin` 通道结算） | 金 80+评分×0.3、名声 45 |

刺杀令仅当生成悬赏的城池当前有敌方阵营（相对角色扮演武将所属阵营）已现身本地武将时才会计入候选，命中该条件时以 15% 概率优先生成，其余情形按讨伐/车轮/登塔/双雄 35%/15%/25%/25% 权重随机穿插（`genBounty`，`heroSide` 参数贯穿开局初始生成、名声跨阶追加、悬赏完成刷新三处调用）——车轮令反馈过于密集，本轮由原 40%/20%/20%/20% 调整为 35%/15%/25%/25%，压低车轮令占比，向登塔令/双雄令匀出空间；约 15% 概率生成「高级悬赏」（车轮令固定 N=10、刺杀令名声翻倍至 90；登塔令的层数不受此标记影响，见上表），奖励倍增（金额系数 0.6），且首次达成额外掉落唯一奇珍「千里靴」。悬赏在接取时即扣 1 行动力并直接发起对应战斗（`RPG.duoPicker(true)` 跳过二次扣行动力；刺杀令复用与主动刺杀相同的 `m.activeAssassin` 结算通道，因而额外叠加刺杀本身固定的 +15 名声与六维惩罚，与悬赏奖励的 45/90 名声各自独立发放）；未达成（战败或指标不足）不惩罚，悬赏本身仍保留在榜上可再次接取。城池面板悬赏榜两列布局，直接点击整张卡片即接取，无需二次确认按钮，行动力不足时按钮统一置灰。

**悬赏轮换**（第十七轮起）：每次宿营（`camp()`，在武将云游判定之后、月末/大会事件之前），**每座城的每一条悬赏各自独立掷一次 50% 概率**、命中则以 `genBounty` 静默换新——榜单刷新力度显著高于早期版本的"约 20% 概率仅换一城一条"，一次宿营后半数悬赏面貌一新；已接取中的悬赏结算按接取时写入 `m.activeBounty` 的独立快照进行，即便原槽位恰被轮换也不受影响（结算时若在榜上找不到原 uid，仅跳过"完成槽位刷新"这一步，奖励照发）。高级悬赏（legendary）与普通悬赏同等概率被轮换，不做保护。

**赶路奇遇**（移动时 30% 触发，六选一）：

| 事件 | 概率权重 | 效果 |
|---|---|---|
| 山贼拦路 | 30% | 按评分测算胜率（30~85%）瞬间判定，胜则获金 20~50 |
| 路遇奇珍 | 20% | 直接掉落一件神秘（未鉴定）宝物 |
| 行脚商队 | 15% | 集市折扣八折，持续至次日 |
| 世外高人 | 15% | 可分配加点 +1 |
| 路遇名将 | 15% | 与一位本地已现身武将直接结交，友谊 +5~15 |
| 客栈奇遇 | 5% | 当天行动力临时 +1（不影响永久上限，宿营后失效） |

**渡海风暴**：任何一段涉及对马岛的移动（建业/徐州⇄对马岛⇄萨摩）额外 20% 概率触发，50/50 为「延误」（当日行动力清零，原地滞留）或「漂流」（随机漂到除对马岛外的任意城池，仍计为完成本次移动）。**对马黑市**：停留对马岛时集市固定八折，与行脚商队折扣可视为同一机制（`shopDiscountActive()` 统一判定）。

**特色设施**（`CITY_FACILITY`，每城一项，7 种玩法循环覆盖全部 40 城）：

| 玩法 | 覆盖城池 |
|---|---|
| 天下擂台（经典单挑） | 洛阳、汉中、建业、天水、寿春、近江 |
| 车轮战武场 | 成都、长安、萨摩、白帝城、合肥、越前 |
| 百人斩道场 | 京都、许昌、安艺、上庸、武昌、加贺 |
| 双人比武场（2v2） | 大坂、邺城、尾张、江陵、肥后、美浓 |
| 军团攻城演（组队大战） | 小田原、徐州、甲斐、宛城、丰后、三河 |
| 远征校场（国战） | 奥州、荆州、骏府、汝南、出云、常陆 |
| 武道会预选（世界杯） | 柴桑、越后、下邳、备前 |

天下擂台/双人比武类设施会先弹出本地已现身武将供选择，其余直接调用对应角色扮演入口（内含行动力扣减），获胜额外记一笔名声。

**唯一奇珍**：

| 名称 | 装备类型 | 效果 | 获取方式 |
|---|---|---|---|
| 赤兔·千里神驹 | 坐骑 | 统帅 +10（传说级） | 百人斩首次达第 15 层 |
| 千里靴 | 服饰 | 魅力 +10（传说级） | 首个高级悬赏达成 |

全局各仅能获得一次（`uniqueOwned` 标记），纯粹的高稀有度专属装备，不再附带行动力上限效果（该效果已转移至常规奇珍池，见下）。

**行动力奇珍**（常规奇珍池，非全局唯一，可反复通过掉落/商店/锻造获得）：奇珍效果新增 `apBonus`（行动力上限），`CURIO_VALS.apBonus = [1,1,1,1]` 不随稀有度浮动（数值本身恒为 1，仅在下述条件下才会真正生效）。`Armory.TEMPLATES.curio` 中「传国玉玺」（默认 `effect:"critBonus"`）与「九鼎」（默认 `effect:"guardBonus"`）两个模板各新增 `legendEffect:"apBonus"` 字段——**仅当 `makeItem()` 将其生成为传说（legend）品阶时**才会切换 `stat` 为 `apBonus`（`bonus` 固定为 1，并置 `item.apBonus = 1` 供 `recalcApMax` 识别计数）；生成为普通/精良/稀有品阶时，仍走各自的默认 `effect`（暴击率/护体）按 `CURIO_VALS` 正常随稀有度浮动，不具备行动力效果。由于奇珍仅一个装备槽位，二者功能相同，佩戴其一即可，无法叠加出 +2。`Armory.equip`/`unequip` 会在换装主角（`owner==="hero"`）装备/卸下后立即调用一次 `Campaign.recalcApMax()`——此前该函数仅在获得唯一奇珍/名声跨阶/武道会夺冠三个时机被动调用，若玩家换下再换上奇珍并不会重算，属于第十三轮一并修复的既有缺口。

## 十七、天下历、城池归属、边境阵营大战与刺杀

**天下历**：`Campaign.mapState().day` 从第 1 天起递增，`calYMD`/`calLabel` 换算为每年 12 月、每月 30 天的「X 年 X 月 X 日」展示（年份自 0 起、月与日自 1 起）；`isMonthEnd(day)` 即 `day % 30 === 0`，为下述边境阵营大战的触发条件。

**城池归属**：`m.cityOwner`（城池 id → `'cn'|'jp'`）与武将分布沿用的静态 `c.side` 是两套独立数据——前者随战事可变，后者恒定不变。`initCityOwner()` 令唯一的海路中转站对马岛（`c.side === "sea"`）初始归属战国一方，其余城池归属与固有阵营一致。

**边境战触发**：每逢月末宿营（`isMonthEnd` 成立），`borderEdges(m)` 找出 `ROADS` 中两端 `cityOwnerSide` 不同的所有边——初始仅对马岛↔建业、对马岛↔徐州两条，随城池易主自然向外蔓延为新的前线；仅当边两端阵营各自都有「已现身武将」时才计入候选。**每月最多只从候选边中随机挑一条开战**（`edges[randInt(0,edges.length-1)]`），不会同时爆发多场。

**边境战结算——不亲征（或与主角阵营无关）**：双方各取「已现身武将」（不限本地驻军）随机等量人马（`Math.min(己方数,敌方数)`），依次用 `js/engine.js` 的 `autoBattle(g1,g2)` 单场极速判定，败者出列直到一方队列耗尽，即时弹出战报。

**边境战结算——亲征**：主角选择率团队亲征时，改用**组队大战**（`TeamBattle`）而非阵营大战——武将选择原则不变（双方均从「已现身武将」中取，主角与同阵营队友必上阵，其余随机补足，双方数量相等），仅因 `TeamBattle` 单场最多 10 将上阵，双方数量额外取 `Math.min(己方,敌方,10)`（与国战攻城 `Conquest` 的做法一致）。调用 `TeamBattle.begin(mine, heroSide, {exact:true, enemies:theirs, rpg:true, onDone})`——`exact:true` 跳过其默认的「不足 10 人从全库补满」逻辑，`rpg:true` 供全局返回键逻辑判断该局应回到发起它的那一层（地图）。战斗打完后经 `onDone(result)` 回调（`result = {playerWon, mySurvivors, theirSurvivors, kills}`）取代默认的「再来一场/返回菜单」收尾；亲征取胜额外获得名声 +20、经验（本方战场斩获数×15 + 60）与金币（30 + 斩获数×5），主角是否阵亡由 `mySurvivors` 中是否仍含 `id===-1` 判定，若阵亡则文案标注「阵中负伤」但不影响整体战果判定。

**夺城后的部署调整**：无论哪种结算方式，胜方都通过共用的 `applyBorderWarOutcome(m, edge, winnerSide)` 处理后续——`m.cityOwner[被占城]` 改写为胜方阵营；败方原驻守该城（`m.assign[gid] === 被占城`）的己方武将，各自退守至与该城相邻、仍由己方持有的城池（`adjCities` + `cityOwnerSide` 过滤，找不到时原地不动）；胜方从「已现身武将」中随机挑选 2~4 名（`randInt(2,4)`，排除已在该城的）进驻该城成为新的本地驻防。

**边境犒赏**：只要 `winnerSide === heroSide`（不亲征的自动模拟结算、亲征的组队大战结算均判定同一条件），主角额外获得 `MapUI.BORDER_WAR_WIN_BONUS = 1000` 金固定犒赏（`Bond.addGold`），与战斗本身的经验/金币结算分开显示（不亲征战报单独一行「🏆 己方大捷，边境犒赏」；亲征战报并入「你的战果」区块）。

**潜入敌境与刺杀**：`showDetail` 按目标与主角阵营关系互斥展示交友区（同阵营）或刺杀区（异阵营，非只读视图），刺杀按钮标注「-1⚡」耗行动力（与切磋同规格，行动力不足时置灰）。刺杀即经典单挑（`m.activeAssassin` 记目标，`Bond.data.assassinDay` 限每人每日一次），结算走 `RPG.onBattleEnd`：取胜则从六维中随机一维给敌将扣 1~3（`randInt(1,3)`）、主角名声 +15，落败则反过来扣主角自己同样一维 1~3；数值写入战役内 `m.statPenalty`（键为武将 id 或 `"hero"`），由 `Armory.geared()` 在既有装备加成之上一并减去，六维下限 10，且只影响本局战役展示与战斗数值、不写回武将图鉴全局默认值；展示侧的装备增量标注（`statRow`/`rpg-dim` 两处 `rd-gear`）按数值正负显示 `+N`/`-N`，避免负值被固定 `+` 前缀拼接成 `+-N`。为避免通用胜利结算里"不打不相识"友谊加成意外拉近与敌将关系，`Bond.addF` 在该分支被限定为仅同阵营对手才执行。

## 十八、阵营大战亲历模式与武将大会

**阵营大战亲历询问**（`War.start(hero, opts)` 内建功能，历史沿革见下）：快捷模式下每逢队列轮到的一方是主角本人（`id===-1`）或现有队友（`Bond.inTeam`）时，`isHeroOrMate()` 判定为真，弹窗 `askJoinDuel()` 询问「亲自应战」（走 `autoPlayBattle` 进入经典单挑画面）或「自动观战」（照常 `autoBattle` 瞬间结算）；`War.abort()` 对该弹窗 pending promise 做清理，避免中途退出留下悬空的 `await`。**注**：角色详情页曾短暂新增过「⚔️ 阵营大战」快捷入口以让这一功能可达，第十二轮已按反馈移除该入口——`War`（阵营大战）此后仅能从首页「小游戏」自由试玩进入，`askJoinDuel` 机制在该入口下同样生效，只是不再有 RPG 角色数据参与。

**武将大会触发**：`isTournamentDay(day)` 即 `calYMD(day)` 的 `month∈{2,5,8,11} && dom===1`，在 `camp()` 中于月末边境战判定之前检查（`MapUI.checkTournament`）。若当前无任何已现身武将则直接跳过（凑不出赛程）。

**报名与费用**：`checkTournament` 弹窗报名费按 `Math.round(ratingScore(heroGeneral())×2)` 计算（与招募费用同一量级公式），金币不足时报名按钮置灰。选择报名则立即 `Bond.spend(fee)`，若最终战报判定「杀入四强」（`heroPlacement().label` 命中 `/夺冠|决赛|半决赛/`，即至少打入四强赛轮次）则全额 `Bond.addGold(fee)` 退还。

**参赛名单**：除主角外的 31 席从 `m.appeared` 已现身武将（不分阵营、含队友）中按 `ratingScore(g)` 从高到低排序取前 31 名（`pool.slice().sort((a,b)=>ratingScore(b)-ratingScore(a))`，第十四轮由原先的随机抽取改为择优选取，使武将大会真正汇聚"当下天下最强的一批人物"），不足则以 `MapUI.byeFighter(i)` 生成的「轮空」占位武将（六维均为 10，几乎必败）补满，保证赛程恒定 32 强；`Tournament.size` 在开赛前强制重置为 32，避免沿用小游戏自由试玩残留的规模设置；`Tournament.draw()` 仍会 `shuffle(this.participants)` 打乱分组种子顺序，但只影响对阵签位，不影响已经选定的这 32 位参赛者本身。

**不参加时的后台判定**：`Tournament.simulate(parts)` 是新增的无 UI 纯逻辑赛程模拟（小组循环赛取前二 → 单败淘汰，全程 `autoBattle`），供主角选择不参加时仍在后台产生冠亚军并发放奖励，不占用/不影响 `Tournament` 模块的交互状态（`this.participants` 等）。

**参加时的完赛回调**：`Tournament` 新增 `onDone` 可选回调（`settlePrediction` 内两处收尾分支均优先判断 `this.onDone`），设置后取代默认的 `RPG.onCupResult`／试玩战报弹窗，交由调用方（`MapUI.runTournament`）自行展示战报与结算奖励。

**冠亚军奖励**：`MapUI.applyTournamentPrize(m, general, statAmt, isChampion)` 统一处理——冠军 `statAmt=3`、亚军 `statAmt=1`。主角六维加成经 `grantHeroStatGrowth`（`RPG.char.alloc[dim] += 加成`，逐维检查 `RPG.eff(c,k)<110` 挑选未封顶的维度，已全部封顶则加成为 0，不会突破上限）直接永久生效；非主角武将经 `grantNpcStatGrowth` 写入战役内新字段 `m.statGrowth[gid][dim]`（与 `m.statPenalty` 同构、符号相反），由 `Armory.geared()` 一并叠加展示，不写回武将图鉴全局数值。冠军额外触发 `grantChampionTreasure`：主角冠军 `Armory.guaranteedItem("legend")` 直接入库（未鉴定）；非主角冠军随机选定宝物类型 `Armory.makeItem(typeK,"legend")`，与其当前该类型已装备宝物比较稀有度/加成（`Armory.RARITIES` 顺序 + `bonus` 数值），更好则直接为其换装（原装备退回未装备状态），持平或更差（如双方皆为传说级）则改发一次 `grantNpcStatGrowth(m, championId, 3)`——与冠军的基础 +3 各自独立发放，因而运气不佳时会叠加为同一维度或不同维度累计 +6。

**宿营反向夜袭**：宿营结算顺序为「天数+1 → 月末边境战判定 → 夜袭判定 → 常规天下快报/迁移提示」。若当前城池本地已现身武将中存在敌方阵营成员，15% 概率弹出夜袭（随机挑一名敌将发起），「应战」直接复用与主动刺杀完全相同的结算通道，胜负判定对称。

**武道会与「天下无双」终局**：世界杯 32 强需名声达「威震一方」方可报名；首次夺冠仍记 `m.cupWon = true`（第十四轮起不再贡献行动力上限，见上「行动力」表格，该标记现仅用于下述终局条件判定）。`RIVAL_NAMES` 八大高手——吕布、关羽、张飞、赵云、织田信长、武田信玄、上杉谦信、本多忠胜（按姓名匹配，兼容武将图鉴改名/重建默认库）——在任意经典单挑/2v2 中击败即记入 `rivalsDefeated`。集齐八大高手战绩 + 至少一次武道会夺冠，触发一次性「🏆 天下无双」终局庆典弹窗（`ending` 标记防止重复触发），关闭后照常继续自由游历，不影响后续任何玩法。

## 十九、城市产业（城市经营一期）

**数据结构**：全存战役层 `m.estate = { [cityId]: { type, lastDay, pending, manager, matCarry?, matPending? } }`，新开局重置；旧档由 `Campaign.ensureMap()` 自动补建空档。每城产业类型由 `CITY_ESTATE`（40 城风味映射，对马岛无产业）固定给出，五类参数见下表（`ESTATE_TYPES`）：

| 类型 | 置办价 | 基础日进 | 特性 |
|---|---|---|---|
| 🍶 酒馆 tavern | 1200 金 | 35 金 | 无 |
| 🐎 马场 ranch | 1600 金 | 48 金 | 无 |
| ⛏️ 矿山 mine | 800 金 | 18 金 | 每满 5 天积攒 1 份本城铁匠铺专精材料（`Armory.TYPES[hashStr(cityId)%5]`，与铁匠铺同式），材料待收上限 3 份 |
| 🌾 农庄 farm | 800 金 | 25 金 | 无 |
| 🚢 商队 caravan | 2000 金 | 60 金 | 收取时实得 = 待收 × (0.6~1.4) 随机行情 |

**核心规则**：
- 置办耗 **1 行动力** + 置办价，敌占之城不可置办（按钮置灰）；收取**不耗行动力**，但必须身在该城。
- 收益**懒结算**（`Estate.accrue`）：任何读到该产业的时机（地图渲染总览、打开面板、收取、易主结算）按 `m.day - lastDay` 补记；待收上限 = 当前日进 × **15 天**，满仓即停。
- **掌柜**：候选 = 已现身 + 友谊满 300 + 不在团队 + 未在他处任职（不限阵营）；加成 = `min(50, round((智力+政治)/4))`%（取 `Armory.geared` 装备后数值），日进 = `round(基础日进 × (1+加成/100) × 繁荣倍率)`。委任/更换/解任前都会先按旧费率结清（`accrue`）。掌柜上任即 `m.assign[gid]=cityId` 常驻本城：`wanderGenerals` 跳过、边境战败军撤离也跳过；被 `Bond.recruit` 招募入队时经 `Estate.onRecruit` 自动卸任。**代收**（第十六轮起）：有掌柜的产业在 `accrue` 时金币不进待收而直接 `Bond.addGold` 入账（商队行情按整段一次判定），累计代收额记入 `est.banked` 供面板展示，不受 15 天积攒上限约束、也不触发经营事件；矿山材料不在代收之列，仍走 `matPending` 由本人收取。
- **查封**：`cityOwnerSide(m,cityId) !== 主角阵营` 即查封——不积攒、不可收取、不可变卖；`applyBorderWarOutcome` 在改写 `m.cityOwner` **之前**先 `Estate.accrue(m, capturedCity)`，保证易主前的天数不漏记、占领期间不多记；夺回后自动恢复积攒（已积待收原封保留）。
- **变卖**：非查封状态随时可卖，回收 = `round(置办价 × 0.5) + 全部待收`，档案删除后可再次置办。
- **总览**：地图左栏角色卡下方「🏪 名下产业 N 处 · 待收 X 金」，渲染时顺带对所有产业做一次懒结算。

## 二十、城市繁荣度、经营事件与一键拜访（城市经营二期）

**繁荣度（`Prosper`，存 `m.prosper[cityId] = {lv, pts}`）**：
- 星级 1~5；未记录的城取初始档：`PROSPER_INIT_HIGH`（洛阳/许昌/成都/建业/京都/大坂/尾张/骏府）3 星，`PROSPER_INIT_LOW`（白帝城/上庸/奥州/肥后/常陆）1 星，其余 2 星。
- 繁荣点来源：设施挑战获胜 +1（duel/duo、车轮全清、阵营/组队/国战的设施加成分支）、完成悬赏 +1（`completeBountyReward`）、置办产业 +2（`Estate.buy`）、己方边境战夺城/光复 +2（`applyBorderWarOutcome`，仅 `winnerSide === 主角阵营`）。每满 5 点升 1 星（可连升），满 5 星封顶后不再累计。
- 影响：产业收益倍率 `×(0.8 + 0.1×lv)`；集市货摊 `≥4 星 +1`（叠加名声第 1 阶的 4→5）；铁匠铺专精工钱 `≥4 星 ×0.7`（叠加名声第 7 阶的 30→20，四舍五入）。城名旁金色 ★ 实时显示。

**经营事件（仅亲自收取触发，掌柜代收静默）**：`MapUI.collectEstate` 单次 `Math.random()` 按固定区间判定——
| 区间 | 事件 | 效果 |
|---|---|---|
| [0, 0.12) | 🗡️ 强人劫掠（农庄免疫，命中视同无事件） | 待收先经 `Estate.seizeForRaid` 清账押走（商队行情此时判定）；**应战**走经典单挑（对手为 `makeRaider` 生成的浪人，六维贴主角 −12~+8 浮动、id=-77 不入图鉴），结算经 `RPG.onBattleEnd` 的 `m.activeEstateRaid` 通道：胜 = 追回 **2 倍** + 名声 +5，败 = 保住 **50%**；**破财消灾** = 保住 **70%**。矿山材料两种结局均全额送回 |
| [0.12, 0.22) | 📈 大市 | 本次金币收益 ×2（`Estate.collect` 的 `goldMult=2`） |
| [0.22, 0.32) | 🎉 贵客临门（酒馆特有） | 随机一位己方已现身武将友谊 +10（按实际增量），收益照常 |
| 其余 | 无事件 | 照常结算 |

**一键拜访（`MapUI.oneClickVisit`）**：本地武将区新增大按钮，一次访遍当前城池所有「已现身 + 己方阵营 + 友谊未满 300 + 今日未访」的武将——逐一写入 `visitDay` 并 `addF(randInt(1,2))`，与逐个拜访完全同规则同额度（无额外收益，纯省点击），汇总一条 toast（超过 8 人截断显示）。不消耗行动力。

## 二十一、城建捐修、产业升级链、守将与敌境自营（城市经营三·四期）

**城建捐修（`Buildings`，存 `m.builds[cityId][type] = lv`）**：
- 除对马岛外每城开放 3 种公共建筑：医馆/书院/驿站/城墙/演武场**五选三**，按 `hashStr(cityId+"|"+type)` 排序稳定选取（同一城每局相同）。
- 修至 1/2/3 级花 **500/1000/2000 金 + 0/1/2 份本城铁匠铺专精材料**，不耗行动力，每级 +2 繁荣点。
- 效果一览（均要求**当前所在城归属己方**，城墙除外——攻守对称）：

| 建筑 | 1 级 | 2 级 | 3 级 | 接入点 |
|---|---|---|---|---|
| 🏥 医馆 | 回魂丹 80 金 | 65 金 | 50 金 | `maybeRevive`（基准 100 金） |
| 📚 书院 | 单挑经验 ×1.1 | ×1.2 | ×1.3 | `onBattleEnd`（历练/切磋/悬赏/设施单挑均计） |
| 🏇 驿站 | 开通驿路 | 驿费 −25% | 驿费 −50% | 两端均须己方且建有驿站；1⚡ + 60 金×BFS 最短跳数 |
| 🏯 城墙 | 守备 +2 | +4 | +6 | 边境战报模拟中本城一方全员六维加成（敌占期间为敌所用；亲征不加成——胜负由你亲手打出） |
| ⚔️ 演武场 | 设施名声 +4 | +8 | +12 | duel/duo、车轮全清、阵营/组队/国战五处设施奖励分支 |

- 建筑归属跟随城池：城破为敌所用（面板只读可查看），夺回后原级保留、即刻恢复。

**产业升级链（`m.estate[cityId].lv`，缺省 1）**：
- 三段名号：酒馆→酒楼→天下名楼 · 马场→名驹牧场→天下第一厩 · 矿山→深矿→天下宝矿 · 农庄→庄园→天下粮仓 · 商队→商行→天下商盟。
- 扩建价 = 置办价 ×1.25（升 2 级）/ ×2（升 3 级），且城市繁荣须达 **★★ / ★★★**；扩建 +1 繁荣点、不耗行动力。
- 收益倍率 ×1 / ×1.6 / ×2.4 乘入 `dailyRate`（与掌柜加成、繁荣倍率连乘）；矿山出材周期 5/4/3 天一份、待收上限 3/4/5。
- **变卖回收 = 累计造价（`cumCost`，含历次扩建）× 0.5 + 全部待收**（原为仅置办价五成）。

**守将（`Guard`，存 `m.guards[cityId]=gid` 与 `m.captives[gid]={cityId,day}`）**：
- 候选：已现身 + 友谊满 300 + **己方阵营** + 不在团队 + 未任掌柜（守将与掌柜候选互斥）。
- 效果：常驻不云游；边境战报模拟必上阵（置于本方阵前）且六维 +3（与城墙叠加）；亲征时若战线上己方城池有守将则必编入队伍。
- **城破被俘**：从天下名录暂时消失（不可拜访/上阵/委任），囚于陷落之城。营救三途：付赎金（**评分 ×5 金**）、劫牢营救（1⚡ 经典单挑 vs 狱吏[主角六维 −8~+10 浮动]——胜 = 放还 + 名声 +10 + 友谊 +20，败 = 留待再来）、己方夺回该城自动放人。入队自动卸任。

**敌境自营（`AIDev.tick`，每次宿营执行）**：
- 敌方治下城市随机抽 **2 座**，各 **60%** 概率发生一次发展动作：45% 繁荣 +1 点（静默，升星才播报）/ 30% 修筑升级城建 / 25% 兴办·扩建敌营产业（`m.npcEstate[cityId]={lv}`，上限 3 级，与玩家产业互斥占位）。期望约 1.2 个动作/宿营，动向以「🏗️ 敌境风闻」并入宿营提示。
- **夺城红利**：城建直接为你所用；敌营产业可按**累计造价 × 0.5** 接管为自家产业（等级保留，耗 1⚡、+2 繁荣点）；未接管期间闲置，敌方夺回则复归敌营。

## 二十二、边境战亲历判定、医馆扩效、宿敌系统与杂项（第二十轮）

**边境战（重写 `resolveBorderWar`）**：
- 不再弹「是否亲征」二选一，改由**主角所在城池自动判定**：`m.curCity` 恰为交战两城之一且该城归属己方 → 亲历（`heroForced`）——主角+同阵营队友由 `War.start` 的既有强制上阵逻辑排在本方阵前；否则主角以 `heroGeneral()` 战力**混入本方候选池随机抽取**（不带队伍、无强制）。
- 无论亲历与否，战斗一律走**阵营大战（War）小游戏快捷模式可视化演出**（`customRoster` 双方等量、守将置前 +3、城墙 ±2×lv 全员加成——主角自身[id=-1]不叠加城墙/守将附加，其数值由 `heroGeneral()` 独立精算）；轮到主角/队友出战时仍会弹「亲自应战/自动观战」。
- 战报：亲历 = 个人战果（斩获×15 经验、胜 +20 名声、胜 30+杀×5 金）+ 边境犒赏；未亲历 = 简报 + 己方胜时照发犒赏 ≥1000 金。`War.rpg` 战后强制置真，保证硬件返回键回天下地图。

**医馆扩效（在原「回魂丹折价」上叠加，均要求所在城归属己方）**：
| 等级 | 回魂丹 | 单挑体力回复 | 带兵兵力恢复 |
|---|---|---|---|
| 1 | 80 金 | +2/回合 | 安抚/驰援 +3% 上限 |
| 2 | 65 金 | +4/回合 | +6% |
| 3 | 50 金 | +6/回合 | +9% |
- 体力回复经 `RPG.heroGeneral()` 注入 `regenBonus`（与奇珍「气血回复」同通道叠加，engine.js `endTurn` 结算）；兵力恢复在 `TeamBattle.doScheme` 中对己方 rally/reinforce 命中后追加（仅角色扮演模式）。

**地图**：新增道路 `["tsushima","higo"]`——对马岛两侧各两条航线（建业/徐州 ↔ 对马 ↔ 萨摩/肥后），渡海风暴规则照旧覆盖全部四条。

**城池功能区**：`.map-menu-free`/`.map-menu-ap` 由 3 列改 **2 列**；「历练」按钮移除（`RPG.train` 保留但无地图入口，练级走切磋/悬赏/设施/边境战等）。

**全部城市**：新增「日进」列（`dailyGold` 可排序）——已置办产业按当前费率（含等级/掌柜/繁荣倍率）、敌营产业按 `npcDailyRate` 标注（敌）、未置办显示基础费率为潜力值、查封显示「查封」。

**宿敌（`Nemesis`，存 `m.nemesis = {id, wins, nemesisWins, ambush, finaleDone}`）**：
- **结缘**：`RPG.onBattleEnd` 中主角首败于 `side !== 主角` 且 `id ≥ 0` 的武将即自动指定（已有宿敌不换）；兜底——名声达第 3 阶「声名初显」后每次宿营 8% 概率从敌方已现身武将中随缘指定。
- **战力**：`buffedOpponent` = 真实数值（含装备）+ 全维 `round((主角等级-1)×1.5) + 已被击败次数×4`，仅本场生效。
- **寻衅**（每次宿营 15%，终局已了则停）：⅓ 扬言拦路（置 `ambush` 标记，下次 `moveTo` 时强制应战，行动力照扣、照常抵达）、⅓ 抢先夺走本城随机一条悬赏（该槽位换新）、⅓ 踏营下战书（宿营流程末弹应战确认）。
- **对决结算**（`m.activeNemesis` 通道）：胜 = wins+1、+20 名声、80+wins×20 金；败 = nemesisWins+1。**wins 满 3 触发「恩怨了结」**：+100 名声（20+80）、必得一件传说级宝物（`guaranteedItem("legend")`，未鉴定入库）、`finaleDone=true` 后不再寻衅（宿敌卡仍可主动重温旧怨）。
- **UI**：地图左栏角色卡下方宿敌卡（头像/战绩/进度 + 「🗡️ 下战书」按钮，耗 1⚡）。

## 二十三、边境战组队化、宿营夜报、宿敌降幅与弹窗防误触（第二十一轮）

**边境战改用组队大战（TeamBattle）**（再次重写 `resolveBorderWar`，替代第二十轮的 War 快捷模式方案）：
- 参战名单构建不变（已现身武将、守将置前 +3、城墙 ±2×lv、主角亲历判定同前），但**每方上限收紧为 10 将**（`count = min(双方池, 10)`，组队大战棋盘规模），经 `TeamBattle.begin(myRoster, heroSide, { exact, enemies, rpg, observe, onDone })` 开打。
- **主角上阵（亲历强制 or 未亲历被抽中）→ 可亲自指挥的组队大战**：带兵攻击/计谋/挑唆/征兵全套操作可用，也可随时「委托 AI」；**主角未上阵 → `observe` 观战模式**（`begin` 新增 opts.observe → 开局即 `delegated=true`，己方全程 AI 代打，AI 间挑唆单挑亦以旁观模式自动演完）。
- 战报：主角上阵 = 个人战果（己方总歼敌 `kills`×12 经验、胜 +20 名声、胜 30+kills×4 金 / 败 10 金）+ 胜方犒赏；未上阵 = 简报 + 己方胜时照发犒赏。夺城/撤军/进驻/守将被俘等结算逻辑（`applyBorderWarOutcome`）不变。
- 返回键归属：`opts.rpg=true` 由 TeamBattle 自身语义承担，第二十轮 `War.rpg` 强制置真的补丁随 War 路径一并退役（War 的 `customRoster` 能力保留为通用接口）。

**宿营夜报**：宿营产生附加消息（敌境风闻/宿敌风闻）时，由一闪而过的 toast 改为「🏕️ 宿营夜报」弹窗逐条列出，点击屏幕任意处（或「知道了」）关闭；无附加消息时仍走原 toast。

**宿敌战力降幅**：临时加成由 `round((等级-1)×1.5) + wins×4` 下调为 **`round((等级-1)×0.6) + wins×2`，另设上限 25**——高等级主角面对的宿敌不再无限水涨船高。

**弹窗防误触卡死（modal 机制）**：`openOverlay(html, {modal:true})` 的弹窗点击遮罩不再关闭。修复病灶：单挑倒地的「回魂丹/认输」确认框是战斗循环 `await` 的 Promise，误触遮罩会连按钮带 Promise 出路一起关掉，战斗从此挂死（只能杀 APP）。已 modal 化的弹窗：回魂丹确认、阵营大战「亲自应战/自动观战」询问、组队大战战果回调（含边境战）、世界杯竞猜表、宿营夜袭、宿敌拦路/踏营应战、边境战事开战与战报。

## 二十四、掌柜代收材料、宿敌更替、敌将武备自集与单挑夺宝（第二十二轮）

**掌柜代收矿山材料**：`Estate.accrue` 中矿山按天折算出的材料份数，有掌柜时立即 `deliverMats` 直接入库（`matBanked` 累计、`matPending` 清零，不再受待收上限 3/4/5 份约束）；无掌柜仍走原「待收+上限+亲收」路径。产业面板文案同步（金币/材料代收额分列）。

**宿敌更替（承接 §22/§23）**：
- 恩怨了结（`finaleDone`）后地图左栏宿敌卡即刻隐去（`nemesisHtml` 提前返回空，「重温旧怨」入口随之退役）。
- `onHeroLoss`：已有宿敌但恩怨已了 → 败于**另一位**敌将时以其重新 `assign`（全新战绩，不与旧宿敌原地续约）；恩怨未了则一如既往不换人。
- `ensureFallback`：同样放行「旧怨已了」的情形，兜底随缘指定时排除旧宿敌本人。

**敌将武备自集（`AIGear.tick`，宿营时随 `AIDev` 一并结算）**：敌方已现身武将随机抽 2 人、各 25% 概率生成一件随机宝物（类型均匀、稀有度按掉落权重 55/28/13/4）——经 `Armory.npcAutoEquip` 择优穿戴：比同槽现役加成高才穿（换下的旧装弃毁、唯一奇珍退回玩家库），否则弃之不取；动向以「⚔️ 武备风闻」并入宿营夜报。

**单挑夺宝与赎回（`RPG.onBattleEnd` 尾段，仅敌我单挑 `opp.id≥0 && side≠主角`）**：
| 场景 | 概率 | 结果 |
|---|---|---|
| 主角胜 | 35% | 随机夺取败方随身一件宝物，直接入玩家宝物库（已鉴定） |
| 主角败 | 25% | 敌将随机夺取主角随身一件宝物（唯一奇珍除外），**当场弹 modal 赎回框** |
- 赎回价 = 市价五成（`LOOT_PRICE`：普通 500 / 精良 1500 / 稀有 3000 / 传说 5000），金币不足按钮置灰。
- 拒绝赎回 → `npcAutoEquip(opp.id, item)`：比其现役强 → 敌将随身穿戴（可再夺回）；否则销毁（就此遗失）。被夺可能涉及行动力奇珍，随手 `recalcApMax`。
- 结算文案并入战报 `extraHtml`；赎回框在战报弹窗之前出现（modal，误触遮罩不消失）。

## 二十五、夺宝概率对齐、赎金上调与全员成长（第二十三轮）

**单挑夺宝调整**：主角落败被夺概率 25% → **35%**（与获胜夺宝对齐）；赎回价由市价五成改为**市价一倍二**（`LOOT_PRICE`：普通 1200 / 精良 3600 / 稀有 7200 / 传说 12000）——敌将挟宝勒索，自然狮子大开口。

**全员成长（新 `Growth` 模块，按用户拍板的「B 岁月修行为主 + A 历战成长为辅」实现）**：
- **岁月修行 `Growth.tick`**（每次宿营）：随机抽 3 人（可重复），成长概率 = `clamp((750 − 武将评分) / 500, 0.1, 0.5)`——评分 500 约五成、650 约两成、750+ 仅一成（追赶机制）；命中后六维随机一项 +1，动向以「📈 修行风闻」并入宿营夜报。
- **历战成长 `Growth.battle`**（逢战结算）：参战真实武将（主角除外，其走经验升级）胜方 12%、败方 6% 概率，武力/统帅/体力随机一项 +1。挂钩点：`RPG.onBattleEnd`（单挑对手，战报追加「历战砥砺」行）与 `TeamBattle.finish`（战役内组队大战全员，战斗日志追加「历战磨砺：N 位武将六维有所精进」）。
- **封顶规则**：累计成长不设上限，但 `bump` 校验「武将基础值 + statGrowth ≥ 110 即不再 +1」——单项不带宝物不破 110，仅宝物加成可叠加超出；写入战役层 `m.statGrowth`（经 `Armory.geared` 全局生效、新开局重置、不污染图鉴）。

## 二十六、野战演武（两军对圆·斗将破阵，小游戏试验田，第二十四轮）

按用户拍板的「A 演义流骨架 + B 推线演出层」先做成独立小游戏（不接入战役），全流程数值如下：

**排兵布阵**：每方随机抽 10 将，按（武力+统帅）自动填 前锋/左翼/右翼/中军/后备 各 2 人（可点两人互换）。军令全场共 **4 道**（斥候探阵也占 1 道）。
- 阵形（相克者全军 +12%，克制环：锥形→方圆→雁行→鹤翼→锥形）：锥形=前锋/中军 +20%；鹤翼=两翼 +20%；方圆=被推挤幅度 -25%；雁行=每刻蚀敌士气 0.15。
- 地形（随机三选一）：平原无规则；山道两翼战力 ×0.5、可用火攻；河畔斗将士气波动 ×1.5、奇袭得手率 +15%。

**阵前斗将**（至多 3 阵，敌方派存活武力最高者搦战）：应战走单挑引擎（玩家亲自操刀），败者阵亡出局；胜方士气 +12/负方 -12（河畔 ×1.5），我方连斩三将敌军额外 -15（未战先乱）；避战 -6（河畔 -9）；可随时「全军掩杀」跳过。士气归零即未战先溃。

**挥军破阵**（600ms/刻实时推挤，战线计 0~100、始于 50）：
- 线战力 = Σ存活武将（武×0.6+统×0.4）× 阵形位加成 × 相克 ×（0.6+0.8×士气/100）× 地形；推移 = 战力差比 ×8 + 随机 ±1.4；胶着逾 100 刻进入「决胜时刻」烈度 ×2。
- 军令：擂鼓=该线战力 ×2（6 秒）；鸣金=该线 6 秒不退；后备=后备队（战力 ×0.7）永久并入一线（全场一次）；火攻（限山道）=该线 +18、敌士气 -8；奇袭=后备智力最高者出击，得手率 0.5+(智-70)/250（河畔 +0.15），成则敌中军 -25、敌士气 -12，败则该将阵亡、我士气 -10。
- 一线告破：破线方士气 +8、失线方 -15，溃兵冲乱邻线（±8）；连破 3 线或士气归零判负；四线俱了 2:2 时士气高者胜。敌军 AI：告急线自动投后备/擂鼓（×1.5）。

**追亡逐北**：穷追 60% 再斩 2~4 将，40% 中伏折损 1~2 将；收兵稳稳结算。战果只作展示，不写任何存档。

## 二十七、野战演武五项打磨（第二十五轮）

1. **按钮风格统一**：阵形卡/武将 chip/军令钮全部改金边（`rgba(212,175,55,.5)`）+ 纸色字，与其余小游戏按钮同系。
2. **阵位空间布局**：排兵改 3×3 网格（grid-template-areas）——前锋居上（面向敌军）、左右翼分列、中军居中、后路殿后（「后备」更名「后路」）。
3. **斗将规则收紧**：三阵各遣一将，已出战者（含胜者、中途退出者）从点将名单剔除、无将可遣时提示改走避战/掩杀；仅主角团队成员可亲自操控斗将单挑（`spectate` 按 `Bond.data.team` 判定），其余武将自动接战，名单上逐一标注「🎮 可操控 / 自动接战」。
4. **破阵必打**：斗将阶段双方士气一律保底 5、不再触发「未战先溃/不战自溃」的提前终局——连斩三将的 -15 依然狠，但挥军破阵这场正戏必然照打。
5. **五线破阵 + 主攻总攻**：战线由四条扩为五条（后路参战）；开打前必须点选一条**主攻战线**（我方该线 +12%，🚩 标记）再手动「击鼓总攻」启动实时推挤，敌军同时暗定己方主攻线（开战后 ⚠️ 揭示、同享 +12%）。「投入后备」军令随后路参战而退役（军令四式：擂鼓/鸣金/火攻/奇袭，奇袭改遣后路智将、失手阵亡直接削弱后路一线）；溃线连锁邻接表补 `center↔reserve`；终局判定改为连破 3 线 / 士气归零 / 五线俱了比线数。

## 二十八、野战演武四项体验打磨（第二十六轮）

1. **按钮风格再统一**：`fb-go`（擂鼓·两军对圆）、`fb-fight`（出将应战）、`fb-decline`（避战不出）、`fb-allout`（全军掩杀）、`fb-assault`（击鼓总攻）五枚主/次操作钮统一改 `cup-go`（主操作叠加 `primary` 金渐变）；单钮行（`fb-go`/`fb-assault`）改用 `cup-start-btns`（`flex:1` 撑满整行，左右手皆可从容点按），三钮行（阵前斗将）改用新增 `.fb-btnrow`（`flex-wrap` 防挤压）。
2. **士气条左右对垒**：`.fb-morales` 由纵向堆叠改横向并排，敌方一侧 `flex-direction: row-reverse` 镜像布局，血条填充锚定己方外侧、随士气下降向中央回缩——与单挑血条同一视觉语言。
3. **敌方斗将同守「不重复」**：新增 `usedFoeDuelists` 集合，`challenger()` 排除其中已记录的敌将 id（不论其是否阵亡）；`renderDuel()` 一旦选定挑战者即计入集合——避免同一敌将胜后反复搦战。
4. **总攻节奏放缓**：`tick` 间隔 600ms→**1100ms**，推移烈度系数 8→5、随机扰动 ±1.4→±1，「决胜时刻」阈值由第 100 刻提前到第 60 刻但烈度倍率由 ×2 降为 ×1.6（避免后段陡然失控）；擂鼓/鸣金军令持续时长 6 秒→**9 秒**，敌军 AI 擂鼓判定与增益时长同步拉长（每 8 刻→每 5 刻、5 秒→8 秒），使总攻阶段的节奏与军令覆盖窗口匹配，给玩家留出观察战线、择机下令的余裕。

## 二十九、野战演武：智谋军令、计谋与兵力体系（第二十七轮）

1. **军令数由智谋所定**：`calcOrders` = `clamp(round((全军智谋均值 − 30) / 12), 2, 6)`——均值 55 约 2 道、67 约 3 道、78 约 4 道、90 约 5 道；我军、敌军各自独立折算（`orders`/`foeOrders`），排兵界面明示双方道数。斥候探阵仍耗我方 1 道。
2. **移除「全军掩杀」**：阵前斗将只留「出将应战 / 避战不出」两选——斗将三阵是正戏铺垫，不再提供整段跳过；无将可遣时提示改为「唯有避战不出」。
3. **士气条同套变色**：弃用我金敌蓝的双色方案，双方均按数值走 `hpColor`（>50 绿 / >22 黄 / 以下红）——与单挑体力条同一语义，一眼可读危殆程度。
4. **兵力体系**：开局兵力 = Σ统帅×100（约七八万），斗将阶段静态展示、破阵阶段每刻互耗（对方线战力 ×(0.22~0.34)×决胜系数，五线累计约每刻 200~300）；破一线额外折损 1,500~3,000 众（战报记斩获/折兵）；兵力比影响战力（`×(0.7 + 0.3×存兵比)`）；兵力耗尽为新增终局条件；战果卡新增「余兵 现存/开局」行。
5. **敌军军令与计谋**：敌军每 5 刻 40% 概率消耗 1 道敌令（军令台实时显示「敌令余 N」）——半数擂鼓（吃紧线 ×1.5 增益 8 秒）、半数施计（择敌我智谋差最大的战线）；**计谋成功率 = clamp(0.45 + (施计方线上智谋和 − 对方线上智谋和) / 150, 0.15, 0.85)**，得手则该线 ±20、对方士气 −8，被看破则自损士气 −5。我方军令台同步新增「🧠 计谋」（同一公式，战报明示两侧智谋和），与擂鼓/鸣金/火攻/奇袭并列五式。

## 三十、野战演武：兵力色条、战线兵力制与调速（第二十八轮）

1. **兵力色条**：斗将/总攻页头新增兵力条一行，置于士气条**上方**，同为左右对垒布局；填充比例 = 现存兵力/开局兵力，配色与士气条同走 `hpColor`（比例 >50% 绿 / >22% 黄 / 以下红）。
2. **布阵显示智力**：武将 chip 由「武X 统X」改为「武X 统X 智X」——排兵时即可为军令数与计谋战线做打算。
3. **总攻调速**：野战顶栏音乐钮左侧新增调速钮（与单挑同位同式），×1/×2/×4 循环；总攻刻度间隔 = 1100ms ÷ 倍速，战中切换即时生效（重建定时器、代际保护不串场）。
4. **斗将士气摆幅减半**：胜负 ±12→**±6**（河畔 ×1.5 → 9），连斩三将追加 -15→**-8**，避战 -6/-9→**-4/-6**——斗将定势不定局，胜负主要还看破阵。
5. **战线兵力制（核心改动）**：各线兵力 = 该线两将统帅×100（开局约 1.4~1.7 万/线）；推挤 meter 不再直接判破线，只决定互耗的不对称度——己方折损 ∝ 对方线战力 ×(1.6 − 推进度)，对方折损 ∝ 我方线战力 ×(0.6 + 推进度)（含 ±0.1 随机、决胜时刻 ×1.6）；**战线只在该线兵力耗尽时告破**（双双耗尽按 meter 定胜负），全军士气归零仍是全局终局。线战力受该线存兵比衰减（×(0.7+0.3×存兵比)），每线实时显示双方士兵数。破线的额外折兵取消（败方本就归零），总兵力 = Σ 线兵力同步刷新。

## 三十一、野战演武：兵力对推条、战线攻防与各线士气（第二十九轮）

1. **对推条即兵力比**：五线颜色条宽度改为 `我线兵力 / (我线兵力 + 敌线兵力)`——条子被压到零即该线兵力耗尽告破，视觉与判定完全一致；兵力数字嵌入条内（我方靠左、敌方靠右，白字描影）。
2. **战线攻防公式（`laneStat`）**：攻 = 该线存活武将平均武力×0.65 + 平均统帅×0.35，防 = 平均统帅×0.65 + 平均武力×0.35；攻方吃阵形/地形/主攻/相克加成，方圆阵改为防 ×1.25；两者均乘该线士气系数 `(0.5 + 0.8×士气/100)`。每刻折损 = `220 × 敌攻/我守 ×(0.9~1.1)× 决胜系数`。擂鼓 = 攻 ×2（9 秒）、鸣金 = 守 ×2（9 秒）。
3. **各线独立士气**：总攻开始时各线士气取当时全军总士气，此后独立涨落（折损占优的一方缓慢提振、劣势方消沉；破线邻线 -8；计谋/奇袭/火攻直击该线士气；雁行乱箭全线流失），保底 5；全军总士气仍是全局终局条件与页头显示。
4. **战线排版**：线名（前锋等）居中、字号加大到 16px；线名左侧显示我方该线士气、右侧显示敌方该线士气（💪N）；武将名单移到条下一行两端分列。
5. **点将带总评**：点将名单每人名字右侧显示 `ratingScore` 总评分，敌方搦战卡同显总评——强弱一目了然，田忌赛马有据可依。
6. **奇袭改选线**：奇袭并入「点军令再点战线」流程，用户自选目标线——得手斩该线 1,500~2,500 众、该线敌士气 -10、敌全军 -6（仍由后路智力最高者执行、河畔 +15% 得手率，失手殉国）；火攻改烧该线 800~1,500 众 + 该线敌士气 -8；计谋改为该线敌士气 -15 + 敌全军 -5。敌军 AI 擂鼓改盯敌方存兵比最低线，施计改打击我方该线士气。

## 三十二、野战演武：擂鼓振军（魅力）、雁行修正与唯兵力定胜负（第三十轮）

1. **擂鼓改为振军**：不再是限时攻力翻倍，而是**即时提振该线士气**——幅度 = `max(3, round(该线存活武将魅力总值 / 12))`（魅力合计 ~150 约 +12），战报明示魅力合计；敌军 AI 擂鼓同公式（用其线上魅力）。攻守增益中仅保留鸣金（守 ×2、9 秒）。
2. **阵形/地形效果核对与修正**：锥形（前锋/中军攻 +20%）、鹤翼（两翼攻 +20%）、方圆（守 +25%）、相克（攻 +12%）、山道（两翼攻 ×0.5、火攻解锁）、河畔（斗将 ×1.5、奇袭 +15%）经核对均已在 `laneStat` 正确应用；**雁行乱箭**修正为直击敌**各线**士气且幅度由 0.15/刻 提到 **0.4/刻**（此前蚀的是总攻后已无意义的全军总士气，肉眼几乎不可见）；阵形卡描述文案同步改为攻/守口径。
3. **总攻页头精简**：总攻开始后各线士气各自为战，全军总士气已无意义——总攻页去除双方总士气数与色条，只留兵力行且字体放大（数值 17px、色条加高）；敌军兵力一侧「字样↔数值」对换，与我方镜像对称（数值居内、字样居外）。
4. **唯兵力定胜负**：战线士气再低只削战力、不判败退；全局终局条件只剩**一方五线兵力尽墨**（或五线俱了时存兵多者胜），移除全军士气归零判负与「连破三线」提前判负。破线/计谋/奇袭的全军士气加减一并移除（只动线士气），战果卡撤下「终局士气」行。

## 三十三、野战演武：阵形对等核对、去主攻线与暂停（第三十一轮）

1. **敌方阵形加成核对**：经逐项核对，`laneStat` 中锥形/鹤翼/方圆/雁行/相克/地形效果本就以「各用各的阵形」对等作用于敌我双方；本轮把敌军阵形效果摆上明面——总攻开战战报同时列出双方阵形及其效果说明（含相克方向提示）。
2. **去除主攻选线**：主攻战线（我 +12% / 敌暗定 +12%）机制整体移除——五线并进、无主次之分；总攻按钮即点即用，🚩/⚠️ 标记与选线横幅退役。移除后双方增益完全对称，呼应第 1 项。
3. **暂停/继续**：军令台新增「⏸ 暂停 / ▶ 继续」钮——暂停时战局完全冻结（tick 直接跳过），仍可从容点选军令与目标战线，继续后即时恢复；鸣金守备增益由「9 秒实时」改为「8 刻」计数，暂停期间不再白白流逝。

## 三十四、将魂 · 武将独特技能一期（野战侧，第三十二轮）

按用户拍板的「将魂」方案实施一期：**每员武将必有一技**——六维最突出一项定门派（通用技能），十二位名将持史实专属技能（⭐ 金橙标示）；全部被动自动发动、发动入战报；布阵 chip / 点将名单 / 敌方搦战卡均带技能标签（长按看说明）。

**六大门派通用技能**（按六维最高项归派，数值并列取先序 武>统>智>体>魅>政）：

| 门派 | 定维 | 野战效果 |
|---|---|---|
| 🗡️ 陷阵 | 武力 | 所在线每刻额外斩敌 `武力×0.35`（决胜时刻加倍） |
| 🛡️ 坚壁 | 统帅 | 所在线守备 ×1.1（同线两帅才可叠乘） |
| 🧠 连环 | 智谋 | 所在线我方计谋成功率 +15%、敌方计谋 -15% |
| 🐎 游击 | 体力 | 所在线我军兵力折损 ×0.88 |
| 👑 感召 | 魅力 | 所在线士气每刻 +0.2 |
| 📜 辎重 | 政治 | 所在线每刻补充兵力 15（不超开局编制） |

**十二名将专属技能**：

| 武将 | 技能 | 效果 |
|---|---|---|
| 吕布 | 无双 | 开战震慑：所在线敌军士气 -15 |
| 关羽 | 武圣 | 阵前斩将时己方士气收益翻倍 |
| 张飞 | 据水断桥 | 所在线敌军士气每刻 -0.3 |
| 诸葛亮 | 借东风 | 全军火攻不限地形且威力翻倍 |
| 曹操 | 奸雄 | 敌军计谋对我全军成功率减半（终乘 ×0.5） |
| 吕蒙 | 白衣渡江 | 开战 30% 概率令敌随机一员大将退场（计入阵亡） |
| 貂蝉 | 离间 | 开战敌随机一线内讧（该线士气 -20） |
| 织田信长 | 三段击 | 所在线每刻 35% 概率齐射，额外杀敌 60~120 |
| 武田信玄 | 风林火山 | 开战前 30 刻全军攻守 ×1.1 |
| 上杉谦信 | 军神 | 同武圣（战国镜像） |
| 本多忠胜 | 无伤 | 所在线守备 ×1.2 |
| 德川家康 | 隐忍 | 所在线兵力折损 ×0.8 |

计谋成功率统一公式：`clamp(0.45 + 智差/150 ± 连环修正, 0.15, 0.85) × 奸雄修正`（我方下令与敌 AI 同式）。敌我完全对等（敌军名将技能同样发动）。二期（单挑侧技能 + 更多具名）待本期试玩满意后再动工。

## 三十五、将魂二期：单挑侧技能 + 弹窗昭告 + 图鉴接入（第三十三轮）

**流程精简**：「击鼓总攻」确认页移除——阵前斗将三阵既毕（或无将可战）即刻转入挥军破阵自动开打；军令台直接就位，暂停钮仍在。

**技能可见性**：
- 开战一次性将魂（无双/离间/白衣渡江/风林火山）发动时以 **modal 弹窗**逐条昭告（「⭐ 将魂发动」，关闭后战局才开始走刻），战报同步留档；
- 单挑开场在战报顶部列出双方将魂（含单挑侧效果），七进七出复活等战中发动有专属提示行+音效；
- **武将图鉴新增「技能」列**（全员将魂标签，名将亮橙），**武将详情新增将魂描述卡**（野战/单挑双域效果全文）。

**单挑侧技能（二期主体，engine.js 钩子；每员武将同一将魂在单挑域的表现）**：

| 将魂 | 单挑效果 |
|---|---|
| 陷阵（武） | 伤害 ×1.1 |
| 坚壁（统） | 受创 ×0.9 |
| 连环（智） | 敌计策成功率 -15% |
| 游击（体） | 每回合回复体力 2 |
| 感召（魅） | 威压：对手起始战意 -15 |
| 辎重（政） | 出招战意消耗 ×0.8 |
| 吕布·无双 | 前三回合伤害 ×1.5 |
| 关羽·武圣 | 暴击率 +15% |
| 张飞·据水断桥 | 断喝：对手起始战意 -20 |
| 诸葛亮·借东风 | 己方计策成功率 +15% |
| 曹操·奸雄 | 敌计策成功率 -20% |
| 吕蒙·白衣渡江 | 首回合奇袭伤害 ×1.4 |
| 貂蝉·离间 | 开局魅惑：敌攻击 ×0.85（2 回合） |
| 织田信长·三段击 | 20% 概率追加半威力连击 |
| 武田信玄·风林火山 | 前三回合受创 ×0.8 |
| 上杉谦信·军神 | 伤害 ×1.12 |
| 本多忠胜·无伤 | 受创 ×0.85 |
| 德川家康·隐忍 | 体力低于 35% 时受创 ×0.7 |

**新增四位名将**（野战+单挑双域）：赵云「七进七出」（野战线折损 -10%；单挑首次倒地回复五成体力再战）、宫本武藏「二天一流」（野战双刀斩敌加伤；单挑 25% 连击）、许褚「虎痴」（野战线守备 +15%；单挑残血伤害 ×1.5）、真田幸村「日本一兵」（野战线攻守 +8%；单挑对体力上限更高的强敌伤害 ×1.2）——名将共十六位。

**接线方式**：`Skill.duelApply(g)` 把技能折算为 `sk*` 旗标（覆盖式写入、幂等），`engine.makeFighter` 经 `window.Skill` 自动盖章——交互单挑与 `autoBattle`（车轮/百人斩/世界杯/阵营/武将大会等）全部生效、敌我对等；开场技（威压/断喝/离间）由 `applyDuelOpeners` 在 `enterBattle`/`autoBattle` 双入口统一结算。

## 三十六、将魂三期：战线技能钮 + 冷却自动发动 + 双域可见性 + 名将倍增（第三十四轮）

按用户三点反馈实施：①野战/单挑双域均补齐显著的发动提醒；②名将专属技能由 16 位翻倍至 28 位；③主动型将魂从「一次性/常驻」改为**战线旁技能钮 + 冷却条**，冷却完毕自动发动，一场战斗中可反复触发——因此重新评估并调整了幅度以避免连续发动破坏平衡。

**主动（ACTIVE）／被动（PASSIVE）技能二分**：全部 22 种技能 `type` 划入两类，`Skill.ACTIVE_WAR_TYPES` 收录 11 种主动型（`awe/discord/infiltrate/roar/volley/dualblade/tempo/school-wu/school-ti/school-mei/school-zheng`），其余（坚壁/连环/武圣系/借东风/奸雄/无伤/隐忍/七进七出/虎痴/日本一兵）维持常驻被动加成，不占技能钮位——判断标准是"该技能是否读作一次具体的主动出击"（一击/一喝/一次偷袭/一轮调度）而非"天生固有特质"（防御力/反计/减员/减损）。

**战线技能钮机制**：每条战线两端各驻扎我/敌双方当前"持有者"（`laneActiveHolder`，同线多名同类技能武将取名将优先），钮位以 `conic-gradient` 冷却环呈现（`--cdpct` CSS 变量），冷却 `SKILL_CD = 8` 刻，冷却完毕 `tickSkillCooldowns()` 自动调用 `fireWarSkill` 发动——发动时战报（🌟 前缀）+ `toast` 悬浮提示 + 钮位闪光动画（`.fire` / `@keyframes fbSkillFire`）三重提醒，换人持有则重新起算冷却（不立即发动，避免"换人即秒放"）。

**主动技能单次发动幅度**（`fireWarSkill`，8 刻一轮，按同等窗口与一期常驻贡献大致对齐）：

| 类型 | 效果 | 幅度 |
|---|---|---|
| 陷阵（school-wu） | 突击斩敌 | `武力 × 2.8` |
| 游击（school-ti） | 补充兵力 | `体力 × 8` |
| 感召（school-mei） | 振奋士气 | `max(5, 魅力 ÷ 10)` |
| 辎重（school-zheng） | 调度补兵 | `政治 × 1.3` |
| 无双（awe） | 震慑敌军士气 | -10（一期开战一次性为 -15，本期改按次削弱） |
| 离间（discord） | 己方持有者所在线敌军自乱 | -12（一期为随机敌线 -20，改为固定所在线、降幅并更可预期） |
| 据水断桥／威震逍遥津（roar） | 断喝重挫敌军士气 | -10 |
| 白衣渡江／本能寺之变（infiltrate） | 偷袭斩敌 | `随机 700~1200`（**重大削弱**：一期为开战 30% 概率直接令敌大将退场，若按 8 刻一轮重复发动等同于一场长战里反复"斩将"，会让此系彻底压制其余流派，故改为有界的兵力突袭，杜绝重复消灭武将的破坏性） |
| 三段击系（volley） | 连番齐射 | `随机 190~320` |
| 二天一流系（dualblade） | 连番猛击 | `武力 × 3.2` |
| 风林火山（tempo） | 我方士气 +8、敌军受挫 | `武力`（伤害）+ 8（己方士气） |

**被动技能维持不变**（坚壁/连环/军神/借东风/奸雄/无伤/隐忍/七进七出/虎痴/日本一兵等）：仍是常驻乘区或 `laneStat()`/`tick()` 内固定折算，不受本期改动影响。

**单挑侧可见性（对应用户反馈①）**：`computeDamage()` 新增 `skillTags` 收集本回合触发的将魂（首合特攻/首击奇袭/暴击/残血暴走/巨人杀等），`buildHitText()` 在命中文案末尾追加「（⭐武将名将魂发动）」；`endTurn()` 补回 `skRegen`（游击系每回合回血）的返回值，`resolveTurn()` 据此推入独立的 `type:"skill"` 战斗事件（「⭐ 武将名 将魂发动，运功调息恢复体力 N！」），与既有的七进七出复活提示同一通道呈现，无需额外弹窗、不打断连续回合的战斗节奏。

**名将扩容至 28 位（对应用户反馈②）**：新增 12 位——三国 6 位（周瑜「火烧赤壁」/陆逊「火烧连营」/黄忠「老当益壮」/姜维「九伐中原」/张辽「威震逍遥津」/司马懿「鹰视狼顾」）、日本 6 位（伊达政宗「独眼龙的野望」/石田三成「智略关原」/前田利家「槍の又左」/明智光秀「本能寺之变」/立花宗茂「西国无双」/岛津义弘「捨て奸」），均复用既有平衡过的机制类型（`volley`/`dualblade`/`school-wu`/`roar`/`tempo`/`infiltrate`/`discord`/`adamant`/`endure` 等）配以史实典故命名与描述，仅少数几位单挑侧数值有历史契合的小幅微调（黄忠·暴击率 12% 代替通用伤害加成、张辽·敌起始战意 -22、司马懿·前三合受创 ×0.78、明智光秀·首合奇袭 ×1.45、姜维·追击率 22%、岛津义弘·残血受创 ×0.68），未引入新技能类型以控制复杂度。

## 三十七、野战演武 UI 精修 + 将魂冷却随将浮动 + 名将再倍增至 40 位（第三十五轮）

按用户五点反馈实施：

**① 技能钮移至战线框线左右两侧、加大并写出技能名**：原先技能钮位于姓名行下方单独一行（`.fb-lane-skl`，占满一行高度），改为 `.fb-lane` 整体变为横向 flex 容器——`skillBadgeHtml("my",k)` / 战线本体 `.fb-lane-body` / `skillBadgeHtml("foe",k)` 三者并排，技能钮尺寸由 24×24 的圆形小图标放大为 50×52 的圆角方形按钮，内部 `.face` 承载图标（`.ic`）与技能名（`.nm`，两行内自动换行），冷却环仍用 `conic-gradient(--cdpct)` 呈现，套在按钮外层作为 padding 边框。此举同时省下原先技能行占用的整行高度，为 ③ 的战线内部精简腾出空间。

**② 将魂冷却按持有者统帅（`tong`）浮动**：新增 `FieldBattle.warCD(g) = clamp(5, 12, round(13 - tong/10))`——统帅约 40 时冷却 9 刻，统帅约 100 时冷却 3 刻（clamp 后落在 5~12 刻区间），取代此前全员统一的 `SKILL_CD = 8` 刻硬编码常量（该常量降级为找不到冷却值时的兜底默认）。`tickSkillCooldowns()` 在新任持有者上任、以及每次发动后重置冷却时，都改为调用 `warCD(持有者)` 而非固定值，使不同武将不再机械地同刻步调发动，观感更自然、也让「谁来扛这条线的将魂位」多一层考量（不只看技能强弱，还要看统帅高低决定手速）。

**③ 战线内部布局重排，进一步压缩高度**：框内顺序由「（士气+战线名合一行）→ 兵力条 → 姓名 → 技能」调整为「战线名（独立一行居中）→ 士气（夹在战线名与姓名之间）→ 姓名（移到兵力色条上方）→ 兵力色条」。连同 ① 腾出的技能行高度，单条战线整体更紧凑，五条战线在手机屏幕上占用的总高度明显缩短。

**④ 点将出阵弹窗放大，10 人（5 行）无需滚动**：`pickDuelist()` 的弹窗外层加宽至 `min(520px,94vw)`（原受 `.detail-card` 默认 `min(420px,92vw)` 限制，武将卡片内容常被迫换行拉高单行高度）、外层 `max-height` 提至 96vh，内层武将列表 `max-height` 由 46vh 提至 74vh——两处一起放大后，五行（2 列 ×5 行）可完整显示，不再需要内部滚动条。

**⑤ 名将扩容至 40 位**：在三期 28 位基础上再增 12 位——三国 6 位（马超「西凉铁骑」dualblade / 甘宁「百骑劫营」infiltrate / 太史慈「神亭酣斗」volley / 徐晃「周亚夫之风」adamant / 郭嘉「十胜十败」firemaster / 庞统「连环献策」discord），日本 6 位（黑田官兵卫「九州军师」counterspy / 竹中半兵卫「稻叶山夜取」infiltrate / 毛利元就「三矢之训」tempo / 柴田胜家「瓶割破釜」tiger / 加藤清正「虎退治·熊本坚城」adamant / 福岛正则「贱岳一番枪」school-wu），均复用既有类型、仅个别数值按典故微调（甘宁首合奇袭 ×1.42、竹中半兵卫 ×1.38、毛利元就前三合受创 ×0.82、柴田胜家残血伤害 ×1.6、马超追击率 0.23、福岛正则暴击率 0.14），不引入新机制类型。

**验证**：test24 扩至 53 项全过——新增/更新断言：名将数改判定 ≥40（原 ≥24）、战线名字号改判 15px（原 16px，因 `.fb-lane-lbl` 独立成行后收窄字号腾出空间）、姓名行顺序落在兵力条之前、技能钮同时具备技能名文本与「分列 `.fb-lane` 左右两侧」两个条件（而非姓名下方单独一行）、抓取当局全部技能钮的冷却刻数标题属性验证确有差异（本轮实测样本 `6/5/5/5/5/6/6/6/5/5`，证实随统帅浮动而非固定值）、点将弹窗内容高度不超过可视高度（无需滚动）。回归 test21 22 项、test22 14 项、test23 10 项全过，本轮未改动 `computeDamage`/`endTurn`/`resolveTurn` 等共用引擎签名，回归风险主要来自 CSS/DOM 结构调整，已通过上述断言覆盖。

## 三十八、将魂冷却差距再拉大 + 战线单行合并布局 + 斗将按钮对换等宽（第三十六轮）

按用户三点反馈实施：①上一轮 `warCD` 5~12 刻的差距仍偏窄（常见统帅 70~90 区间只落在 5~6 刻）；②战线内部结构希望进一步整合——最左/最右为将魂技能钮，中间只留两行：上行「己方姓名·己方士气·战线名·敌方士气·敌方姓名」一字排开，下行兵力色条；③阵前斗将页的「出将应战」「避战不出」两按钮要互换左右位置且等宽。

**① 冷却差距拉大**：`warCD(g)` 由 `clamp(5, 12, round(13 - tong/10))` 改为 `clamp(4, 14, round(14 - (tong-40)/7))`——每 +7 点统帅减 1 刻冷却，全区间覆盖 4~14 刻（原区间实际有效跨度只有 7~8 刻附近的一两刻差距，因为多数武将统帅集中在 70~90 区间、落进旧公式的窄段）。新公式下同一区间（统帅 70~90）对应冷却 6~10 刻，肉眼可辨的差距明显拉开，武将不再扎堆同刻发动。

**② 战线内容合并为两行**：`laneHtml` 由「战线名独立一行 → 士气行 → 姓名行 → 兵力色条」四行压缩为「一字排开行（`.fb-lane-row1`：己方姓名 `flex:1` 省略号截断 + 己方士气 `flex:none` + 战线名 `flex:none` + 敌方士气 + 敌方姓名 `flex:1`）→ 兵力色条」两行，技能钮维持在 `.fb-lane` 最左/最右两端不变。姓名列表较长时以 CSS `text-overflow:ellipsis` 省略，不强行换行撑高整行。

**③ 斗将按钮对换 + 等宽**：`renderDuel()` 中 `#fb-decline`（避战不出）移到 `#fb-fight`（出将应战）之前；`.fb-btnrow .cup-go` 由 `flex:1 1 auto; min-width:100px`（按各自文案内容取自然宽度，导致长文案的「避战不出」比短文案的「出将应战」更宽）改为 `flex:1 1 0; min-width:0`（flex-basis 归零后按相同 grow 比例均分剩余空间，与文案长短无关），两按钮像素级等宽。

**验证**：test24 扩至 54 项全过——新增/更新断言：抓取实际渲染的技能钮标题冷却值计算极差（`Math.max-Math.min`），要求同局内出现 ≥1 刻差距（本轮实测样本极差 3~4 刻，相比上轮观测到的极差通常为 0~1 刻有明显提升）；战线一字排开行的 5 个子元素 class 顺序断言（`fb-lane-my → fb-lmor my → fb-lane-lbl → fb-lmor foe → fb-lane-foe`）；战线框体只剩两个子行（`fb-lane-row1` + `fb-push`）；斗将按钮顺序与像素宽度断言。过程中两次假阳性排查：一次是按钮宽度断言容差过严（`<2px` 卡在恰好 2px 的 flex 分配舍入误差上，实际两按钮已等宽，调宽容差到 `≤3px`）；一次是既有的「雁行阵乱箭蚀敌」断言（无关本轮改动，测的是敌方雁行阵五线平均士气应随刻流失）在观测窗口恰好被前一步「擂鼓」测试撞车（该测试固定对前锋线 +13 士气，若雁行乱箭当刻净流失不足以抵消这波单线大涨，5 线平均值短暂转为上升）——重跑一次即复现为通过，判定为无关本轮改动的既有时序巧合，非回归。回归 test21 22 项、test22 14 项、test23 10 项全过，本轮同样未改动共用引擎签名，改动全部集中在 `js/app.js` 的 `FieldBattle` 模块与 `css/style.css`。

**验证**：test24 扩至 49 项全过——新增「将魂名将专属技能扩充（28 位）」「主动型将魂在战线旁化作技能钮（无「将魂发动」弹窗）」「主动型将魂冷却完毕自动发动（战报含 🌟 将魂发动记录）」三项断言；回归 test21 22 项、test22、test23 全过，确认 `computeDamage`/`endTurn`/`resolveTurn` 的签名调整未影响车轮/百人斩/世界杯/阵营/组队/边境战等共用引擎的其余玩法。

**附：单挑鏖战护栏**（本轮回归测试中确认为既有机制的加固，非将魂直接引入的问题）：`takeTurn` 新增 300 手（150 回合）上限——极端拖长的对局（如高体力/回复流互相耗不动）按当前体力比例判定胜负，避免观战/委托模式下的组队大战因单场子对决迟迟不分胜负而卡住整场战局。

## 三十九、冷却环连续动画 + 门派分布 Z 分数平衡 + 每线双将各占技能钮（第三十七轮）

按用户三点反馈实施：①战线技能钮的冷却环每刻推进时是离散跳变，视觉不够顺滑；②六维门派技能是取六维原始数值最大项归入门派，但六维分布并不对称（体力普遍偏高且离散度小），导致「游击」门派严重扎堆；③部分战线看不到技能钮、且每线两名武将却只见一枚技能钮。

**① 冷却环改为 CSS 原生连续过渡**：此前 `--cdpct` 自定义属性只是每刻离散赋值一次，`conic-gradient()` 随之整环跳变。现注册 `@property --cdpct { syntax:'<number>'; inherits:false; initial-value:1; }` 使其成为浏览器可插值的类型化自定义属性，配合 `.fb-skillbtn { transition: --cdpct var(--fbtickms) linear; }`，冷却环从此逐帧平滑扫过而非每刻跳一格。`--fbtickms` 由新增的 `FieldBattle.applyTickMs()` 写在 `document.documentElement` 上、随 ×1/×2/×4 调速同步更新（1100ms/550ms/275ms），使过渡时长始终贴合实际刻间隔，调速后动画节奏仍跟手不落后。

**② 门派分布改用 Z 分数（相对全库均值/标准差）而非原始数值 argmax**：`ALL_GENERALS` 400 位武将六维统计显示体力均值 85.1、标准差仅 8.27（分布紧且偏高），远高于其余五维（均值 64~77、标准差 12~18），导致原始数值直接取最大项时体力独占鳌头——非名将中 242/400（60.5%）被判入「游击」门派。改为 `Skill._computeDimStats()` 预先算出六维各自的全库均值/标准差，`of(g)` 取 `(数值-均值)/标准差` 最大的一维（即该武将六维中「相对同类武将有多突出」而非「绝对数值多大」），非名将 360 位的分布随之变为 武87/统54/智50/体52/魅33/政84，最大占比从 60%+ 降到约 24%（政务「辎重」），六门派均有代表、不再由单一门派垄断。

**③ 每线每方最多两枚技能钮，每将各自独立冷却**：此前 `laneActiveHolder` 只挑该线该方「唯一」持主动将魂的武将（名将优先），若该线两将都是被动型（如坚壁/连环/隐忍等）则该侧完全不显示技能钮，若两将都是主动型也只有一将的技能会被使用、另一将形同虚设——这正是用户观察到「有些战线没有技能钮」「明明两名武将却只见一个按钮」的根因。现改为 `laneActiveSkilled` 返回该线该方全部存活的主动型武将（不再互斥），`tickSkillCooldowns` 为每员武将各自独立维护冷却槽（`L.mySkillCds[武将id]`），互不干扰、可同时冷却完毕各自发动；`skillBadgeHtml` 相应改为渲染 `.fb-skillstack`（含最多两枚 `.fb-skillbtn` 竖直堆叠），被动型武将也占一枚徽标位（灰色虚线环、无冷却），使该线在世武将始终各自可见一枚技能钮位，不再有战线空缺。

**均衡性联动修正**：允许同线两将独立触发主动将魂后，实测「雁行阵乱箭蚀敌」（原设计敌线士气应随刻持续流失）反被更频繁触发的正向效果（振奋/临阵调度等）压过甚至转为净上涨——因为原本每线每方效果吞吐量按「至多 1 名武将同时生效」设计强度，双将各自独立后吞吐量接近翻倍。修正：`fireWarSkill` 新增 `scale`，当该线该方同时有 ≥2 名主动型武将在世时，单次效果按 ×0.6 折算（两名合计约为单将基准的 ×1.2，而非 ×2），使总吞吐量适度提升但不冲垮既有的阵形/军令强度设定；伤害/兵力/士气类效果的战报文案也改用折算后的实际生效数值，避免显示值与实际改动值不一致。

**验证**：test24 扩至 59 项全过——新增门派 Z 分数分布断言（在浏览器内实跑 `ALL_GENERALS.forEach(g=>Skill.of(g))` 统计六类占比，断言「游击」占比 <30% 且无门派占比 >32%、六门派均有代表）；每线技能钮堆叠断言（前锋线我方/敌方各恰好 2 枚）；技能钮分列战线两端断言改为校验 `.fb-skillstack` 而非旧版单枚 `.fb-skillbtn` 直接作为 `.fb-lane` 子元素；被动型武将必出现技能徽标断言（用确定性查找而非依赖随机布阵抽样）；冷却环 CSS 连续过渡断言（`transitionProperty` 含 `--cdpct` 且 `transitionDuration` 非 `0s`）；调速联动断言（×4 下 `--fbtickms` 应为 275ms）。「雁行阵乱箭蚀敌」断言观测窗口由 6s 拉长到 10s（×4 下约 36 刻），使确定性的每刻衰减有足够样本量稳定压过敌方擂鼓军令等偶发正向噪声，重跑两次均稳定通过。回归 test21 22 项、test22 14 项、test23 10 项全过，改动集中在 `js/app.js` 的 `Skill`/`FieldBattle` 模块与 `css/style.css`，未触及共用单挑引擎签名。

## 四十、技能钮改水平放置+字体放大 + 调速/暂停合一钮（第三十八轮）

按用户两点反馈实施：①总攻界面每线两枚技能钮此前竖直堆叠，希望改水平放置且字体再大一点；②总攻界面独立的调速钮（音乐钮左侧）与军令台的暂停钮功能重复割裂，要求合并为一钮，循环显示「攻击（初始态，此时全局暂停）/×1/×2/×4/暂停」。

**① 技能钮堆叠改水平**：`.fb-skillstack` 由 `flex-direction: column` 改为 `row`，两枚技能钮左右并排而非上下叠放；配合钮体尺寸由 46×34px 调整为 44×42px（单行足够高，不必再为竖排两枚而压缩单枚高度），图标字号 12px→16px、技能名字号 7px→9px，肉眼可读性明显提升。

**② 调速与暂停合一钮**：移除 `index.html` 中音乐钮左侧的独立 `#fb-speed` 按钮及其绑定逻辑 `bindSpeedBtn()`，改由总攻页军令台内新增的 `#fb-ord-ctrl` 钮统一承担——按钮文案随 `FieldBattle.assaultCtrlLabel()` 在「攻击→×1→×2→×4→暂停→×1→…」间循环（首次显示「攻击」时全局处于暂停态，金色高亮吸引点击；其后循环不再出现「攻击」字样，暂停态显示「暂停」）。联动改造：`startClash()` 进入总攻页时不再立即开打，而是 `this.paused = true; this.everStarted = false`——斗将结束后总攻页面依旧无需中间确认页即可直接呈现（沿用第二十五轮设定），但节奏本身先按兵不动，玩家需亲自点击【攻击】钮下达总攻令，钮上同时兼管调速与暂停，逻辑集中在新增的 `assaultCtrlClick()` 中。

**验证**：test24 扩至 62 项全过——新增/更新断言：确认音乐钮左侧 `#fb-speed` 已彻底移除；总攻页进入后默认暂停、兵力保持不变，点击【攻击】后转为「×1」态且开始推进；技能钮堆叠改水平放置断言（`flexDirection:row`）及字体放大断言（图标>12px、技能名>7px）；调速与暂停合一钮循环断言（连点两次验证 ×1→×2→×4，再点一次验证进入「暂停」态且兵力冻结，最后一点验证回落「×1」并恢复厮杀）。中途排查一处假失败：「雁行阵乱箭蚀敌」断言在合一钮改造后一度不稳——因新设计里「暂停→×1」的循环会把倍速重置回 ×1（而非像旧版暂停钮那样保留暂停前的倍速），若不额外把倍速调回 ×4 就直接进入雁行观测窗口，×1 节奏下 10s 内实际推进的刻数不足以让确定性衰减稳定压过随机噪声；修正为暂停/继续测试后显式再点两次合一钮调回 ×4、观测前静置 500ms 避开调速瞬间、观测窗口进一步拉长到 15s，重跑三次均稳定通过——诊断为测试脚本自身未适配新合一钮的倍速重置语义，非游戏逻辑缺陷（用独立的 goosecheck 脚本单独验证过雁行乱箭在 ×4 下的衰减曲线本身完全正常，18s 内从 50.0 降到 41.2）。回归 test21 22 项、test22 14 项、test23 10 项全过。

## 四十一、总攻合一钮加宽改名「开始总攻」+ 全局隐藏滚动条防布局抖动 + 边境战事改走野战演武（第三十九轮）

按用户三点反馈实施：①总攻合一钮宽度不够、初始文案「攻击」改为「开始总攻」；②野战总攻界面运行一段时间后页面宽度会变窄，需排查修复；③把角色扮演·天下地图的「边境战事」由组队大战（TeamBattle）engine 换成野战演武（FieldBattle），相应调整关联要素。

**① 合一钮加宽改名**：`assaultCtrlLabel()` 的初始态文案由 `"攻击"` 改为 `"开始总攻"`；CSS 新增 `.fb-ord.ctrl { min-width: 96px; padding: 7px 14px; }`（其余 `.fb-ord` 钮维持原宽度不变），钮体明显加大不再显局促。

**② 布局宽度抖动排查**：用 Playwright 在战斗过程中逐帧量测 `document.documentElement.clientWidth`/`#screen-field .topbar` 宽度，未在本沙盒的无头 Chromium 下直接复现出数值抖动（全程稳定 390px，`.fb-log` 战报面板本身已有 `max-height:150px` 内部滚动、不会撑高外层 `.screen` 高度）。但经典（非悬浮）滚动条在内容首次超出可视高度、产生纵向滚动条时会临时占用一段宽度（内容变化、滚动条隐现之间即会被感知为"变窄/变宽"），这是浏览器环境相关的已知布局抖动成因，在本沙盒的无头环境恰好不复现（大概率使用悬浮式滚动条），但用户的实机环境很可能命中经典滚动条路径。修复：全局新增 `* { scrollbar-width: none; -ms-overflow-style: none; } *::-webkit-scrollbar { width: 0; height: 0; display: none; }`，彻底隐藏原生滚动条本体（触摸/滚轮滑动功能不受影响），从根源上让滚动条不再占用任何宽度、不再有出现/消失的宽度抖动，这一改动是防御性且零副作用的（游戏本身在其余所有可滚动区域也从未依赖可见滚动条样式），改动后全部回归套件（含大量弹窗/长列表的 test21~24）均无异常，滚动功能本身照常可用。

**③ 边境战事改走野战演武**：`FieldBattle` 新增 `beginExternal(myRoster, foeRoster, heroSide, opts)` 入口（`opts.rpg`/`opts.observe`/`opts.onDone`），与原有 `setup(side)`（标准小游戏入口，从图鉴随机抽点）共用重构出的 `_setupCommon()`；`RPG.resolveBorderWar()` 里原先的 `TeamBattle.begin(myRoster, heroSide, {exact:true, enemies:foeRoster, ...})` 直接替换为 `FieldBattle.beginExternal(myRoster, foeRoster, heroSide, {...})`，`myRoster`/`foeRoster`（含城墙/守将统计加成、主角强制编入等既有逻辑）原样复用、不做改动。
  - **主角亲历**（`heroIn=true`）：与自由试玩的野战演武体验完全一致——排兵布阵可选阵形、阵前斗将三阵各遣一将（可亲自操控或自动接战）、挥军破阵需点【开始总攻】才开打，边境战事从原本"只有组队大战一种打法"升级为拥有阵形相克、地形、军令等完整策略维度。
  - **主角未上阵**（`observe:true`）：新增全自动推演模式——`_setupCommon()` 检测到 `external.auto` 时跳过排兵布阵画面直接进入斗将流程，`renderDuel()` 自动抽点候选（无候选则视同避战）并强制 `spectate:true`，`startClash()` 自动点火且倍速直接拉到 ×4，`finishExternal()` 检测到 `auto` 时跳过追亡逐北二次确认直接结算，全程无需玩家任何点击。
  - **结果回调兼容**：`finishExternal(won, reason)` 把野战自身状态折算为 `{ playerWon, mySurvivors, kills }`（与组队大战原有结果对象同形），`resolveBorderWar` 的 `onDone` 回调、经验/金币/夺城结算逻辑完全不必改动。
  - **历战成长联动**：组队大战 `TeamBattle.finish()` 里挂钩的"历战成长"（真实参战武将胜负各有小概率六维精进）随边境战事迁移到野战引擎后不会自动跟着触发，为避免功能倒退，在 `finishExternal()` 内补一份等价逻辑——对 `this.mine`/`this.foes` 中的真实武将（主角除外）分别按各自胜负调用 `Growth.battle(gm, g, won/!won)`。
  - **返回键路由**：`FieldBattle` 新增 `this.rpg` 标记（`beginExternal` 时置真，`setup` 小游戏入口置假），`handleBackAction()` 里野战演武分支据此选择回天下地图（`goHome()`）还是回首页（`showScreen("home")`），与 War/TeamBattle/Conquest/Tournament 等既有角色扮演子玩法的返回路由约定一致。
  - **多人数容错**：`_setupCommon()` 内的 `deploy()` 由固定拆两两一组改为 `[sorted[i*2], sorted[i*2+1]].filter(Boolean)`，容许外部战场人数不足 10（边境两侧现身武将较少时）也能正常拆入五线、不产生 `undefined` 占位。
  - **联调发现并顺手修复的既有缺陷**：`FieldBattle` 此前只导出了 `Skill`（`window.Skill = Skill`）却未导出自身，导致任何外部脚本（包括本轮新增的自动化测试）都无法以 `FieldBattle.xxx` 读取战场状态——`window.FieldBattle` 从未被赋值，`window.FieldBattle`/裸标识符 `FieldBattle` 在页面外层脚本里均为 `ReferenceError`（此前误以为"裸标识符可行、只是不便"，实测校验后确认其实完全不可达）。已比照 `Skill` 的写法补上 `window.FieldBattle = FieldBattle;` 导出。

**验证**：test21 新增/重写「边境战 A（亲历）」「边境战 B（观战）」两个子场景共 6 项断言，覆盖弹窗文案改为"野战演武"、主角强制编入我方阵容、走完三阵斗将进入挥军破阵、点【开始总攻】后加速推进直到边境战报、观战模式全自动推演（无任何手动确认）且不卡死、城池按胜负易主；21 项全过。调试过程中先后修正两处测试脚本自身问题（均非产品缺陷）：一是最初误以为 `FieldBattle` 裸标识符可从 `page.evaluate()` 访问，实测才发现从未导出，遂直接在产品代码里补上 `window.FieldBattle` 导出（见上）；二是"观战"分支最初断言点击开战后应立即出现 `#screen-field.active`，但全自动推演一开场就直接跳进第一场斗将子画面 `#screen-battle`（不一定先落在 `#screen-field`），改为两个画面出现其一即视为进场、另加 `FieldBattle.external.auto` 状态断言替代原先依赖 DOM 时序的判断，问题消失。回归 test22 14 项、test23 10 项、test24 62 项全过，未改动共用单挑/组队大战引擎其余调用方的行为。

## 四十二、名声/评分/自建/单挑细节等十三项调整（第四十轮）

按用户十三点反馈实施，改动横跨 `js/app.js`、`js/engine.js`、`data/generals.js`、`css/style.css`：

**① 名声上限 10000 + 任务名声减半**：`Campaign.FAME_MAX` 由 9999 改为 10000；`Campaign.addFame(n)` 改为 `Math.min(FAME_MAX, fame + Math.round(n/2))`——单一入账函数是全部名声来源（历练/切磋/设施挑战/悬赏/刺杀/劫牢/宿敌/武道会……）的唯一汇入口，在此处统一减半即可覆盖所有调用方，无需逐处改动数值、也不会有遗漏。各阶名声门槛（`FAME_TIERS`）数值不变，减半后整体升阶节奏相应放缓约一倍。

**② 一键拜访耗 1 点行动力**：`oneClickVisit()` 前置 `if (m.ap<=0) return`（提示"今日行动力已耗尽"），执行后 `m.ap--`；`#map-visit-all` 按钮文案追加"（耗 1⚡）"并在行动力耗尽时 `disabled`。武将详情弹窗内的单人拜访仍保持免费，仅批量的"一键"版本收费。

**③ 自建武将可指定将魂来源**：`Skill.of(g)` 新增最高优先级判定 `if (g.skillOverride) return g.skillOverride`；`RPG.heroGeneral()` 若 `c.skillGeneralId` 有值，取 `Skill.of(DB.get(c.skillGeneralId))` 挂到 `g.skillOverride` 上，使自建角色可"借用"武将库任意一将的将魂技能（含名将专属技能），不再局限于按自身六维最突出一项自动归派门派技能（未指定时仍走原逻辑，向后兼容旧存档）。新增共用组件 `openSkillGenPicker(curId, onPick)`（含搜索的武将网格弹窗），接入开局向导 `Onboard.customFormHtml()` 与旧版创建入口 `RPG.renderCreate()` 的自创武将表单，均新增「将魂」行按钮。

**④ 武将总评分改为纯六维总和**：`ratingScore(g)` 由"六维之和 + 单项达 A 及以上的突出加成"简化为直接 `return sumStats(g)`，`warriorRating` 随之等价于六维均值评级；两处原展示"六维X+突出Y"分解的界面（详情弹窗、角色资料卡）相应精简为单一评分数字（因突出加成恒为 0，分解展示已无信息量）。

**⑤ 修正 8 个武将姓名中的错误"职务·姓名"拼接前缀**：`data/generals.js` 中如"刘表大将·黄祖"这类姓名字段被误写成"职务/来源·真实姓名"的拼接形式，直接显示会很怪异；逐条核实史实后改为姓名单独一项（职务信息已在各自 `title` 字段中另有体现，不丢失信息）：颜文配将·吕旷→吕旷、刘表大将·黄祖→黄祖、吕布部将·张辽前·宋宪→宋宪、可児·加藤嘉明→加藤嘉明、雪斎·朝比奈泰朝→朝比奈泰朝、服部·渡边守纲→渡边守纲、长曾我部·盛亲衆→长曾我部盛亲（含去除误带的"衆"字）、津雲·甲斐姬→甲斐姬。

**⑥ 武将世界杯改为每月 15 日举行**：`isTournamentDay(day)` 由"每季度第二月（2/5/8/11 月）第 1 天"改为 `calYMD(day).dom === 15`，即每月固定 15 日触发，相应弹窗文案"本季武将大会"改"本月武将大会"。

**⑦ 自建武将初始六维上限 60、加点上限维持 30**：`RPG.rollStats()` 的基线随机区间由 `randInt(45,80)` 收窄为 `randInt(30,60)`（单项初始最高不超过 60），可分配加点区间 `randInt(18,30)` 本就以 30 封顶，维持不变。

**⑧ 蓄力成功率改依武力/智力较高者**：`schemeSuccess(self, foe, scheme)` 原先无论何种计策一律用双方智力差算成功率；蓄力（`charge`）改用 `Math.max(己方武力,己方智力) - Math.max(对方武力,对方智力)` 之差，武将偏武偏智皆有机会蓄力得手，不再是智力系专属技巧。

**⑨ 平衡蓄力暴发在猛攻/谋攻上的伤害加成**：`computeDamage()` 的蓄力暴发倍率原先无论后续用何招式一律 ×2.0；谋攻本就"无视格挡、必定全额命中"，叠加同等暴发倍率会显著强于"可被格挡卸力"的猛攻/普攻。改为按后续招式区分：`atkTactic==="strategy"` 时暴发倍率降为 ×1.7，猛攻/普攻维持 ×2.0，用不对等的暴发倍率去补偿谋攻天生的"不可防御"优势，使两条爆发路线的期望收益更接近。

**⑩ 单挑疗伤改依魅力（成功率）与政治（回复量）**：`schemeSuccess` 中 `heal` 的成功率差值由双方智力差改为双方**魅力**差；`applyScheme` 里 heal 分支的回复量公式由 `zhi * (0.26+rand*0.2)` 改为 `zheng * (0.26+rand*0.2)`，即成败取决于己方魅力（风范气度令人信服）、回复效果取决于己方政治（统御调度气血得法），两项数值语义拆分更贴合"疗伤"这一行为的直觉。`TACTICS.heal`/`TACTICS.charge` 的说明文案同步更新。

**⑪ 单挑界面姓名下方"总"字改为该武将将魂名**：`renderFighter()` 中 `.ftotal .ft-lbl` 的固定文字"总"改为 `Skill.of(g).n`（该武将实际将魂技能名，如"陷阵""武圣"），并加 `title` 悬浮提示完整技能描述；CSS `.ft-lbl` 字号由 13px 降至 11px 并加 `overflow:hidden;text-overflow:ellipsis`，容纳较长的技能名（如"虎退治·熊本坚城"）而不撑破卡片。

**⑫ 单挑界面武将姓名过长自动缩字号**：`renderFighter()` 按 `g.name.length` 分级设置 `.fname` 的行内 `font-size`（≥7 字 10px、≥5 字 12px、其余维持原 16px），确保单行显示；CSS `.fighter .fname` 同步由允许折行（`word-break:break-word`）改为强制单行 + 省略号兜底（`white-space:nowrap; overflow:hidden; text-overflow:ellipsis`），双重保险下不再出现姓名折行撑高卡片或被边界截断的情况。

**⑬ 边境大战改为自选出战 + 金币日产出倍数奖惩**：原先"是否亲历"由主角是否恰好站在交战两城之一自动判定（`heroForced`），队伍随之被强制拉上阵；现改为 `openBorderWarPicker` 弹出可勾选的出战名单（含主角本人，`.bw-pick` 按钮式多选，最多 10 员），玩家自主决定谁出战、是否亲自带兵，不再受限于地图上的站位。金币结算同步改造：新增 `Estate.cityDailyGold(m, cityId)`（统一估算城池当前金币日产出，兼容"己方已置办／敌营经营中／尚未置办"三态，与既有 `rowData` 展示逻辑口径一致）与 `Bond.loseGold(n)`（强制扣减、封底 0，供落败赔付使用，区别于余额不足即失败的 `spend`）；原固定 1000 金犒赏改为 `BORDER_WAR_GOLD_DAYS(30) × 所夺/被夺城池当日估算日产出`，己方获胜得此金额、己方落败则同额倒扣，胜负皆与实际城池经济价值挂钩而非固定数额。

**验证**：test21 重写"边境战 A/B"两场景以适配新的自选出战弹窗（勾选主角本人 `.bw-pick[data-id="-1"]` 再开战 / 不勾选任何人直接开战），并更新弹窗文案断言（原断言"野战演武"字样已被自选出战说明取代，改为断言"自选出战"与"日产出"字样）；test24 未直接触及新增的 `.ftotal`/`.fname` 单挑卡片改动（该套件聚焦野战演武 FieldBattle 画面），作为独立回归确认无副作用。engine.js 侧的疗伤/蓄力/暴发倍率改动未变更任何函数签名，仅改判定用的数值来源，回归风险集中在数值结果是否仍在既有护栏区间内。

## 四十三、边境战出战范围收窄 + 世界杯按钮对换 + 组队站位随机 + 跨城宝物贸易（第四十一轮）

**① 边境大战出战范围收窄为「主角 + 团队成员」，主角未出战不担金币盈亏**：上一轮（第四十轮）把边境战出战名单改成了"己方全体已现身武将均可勾选"，本轮进一步收紧——`renderBorderWarPicker` 的候选列表由 `DB.list.filter(...appeared...)` 改为 `Bond.teamGenerals().filter(g=>g.side===heroSide)`（即已招募入队的队友），只有主角本人与队友的出战与否需要玩家亲自勾选决定，其余"仅仅现身过、但未收编入队"的武将与玩家的选择无关，一律照旧由候选池随机抽点补满出战名额。`resolveBorderWar` 相应新增 `teamIds` 集合，未被勾选的队友会从随机补位候选池（`rest`）中整体剔除——不会像普通武将那样仍有概率被随机抽中，"选了才上、不选不上"对队友是硬约束（并加了极端保底：若本方已现身武将几乎全是未勾选的队友、剔除后凑不出兵力，则放宽限制把他们纳回候选池，确保战事总能照常开打）。金币结算同步收紧：原先"主角未上阵、此战胜负仍会左右己方钱袋"的逻辑被取消，`resolveBorderWar` 的 `onDone` 回调中 `!heroIn` 分支不再调用 `Bond.addGold`/`Bond.loseGold`，只展示"本场未见你的身影，前线战报照常传回，胜负不动你的钱袋"——城池归属照常易主，但金币犒赏/赔付这一层风险与收益，只在主角亲自勾选出战时才会发生。

**② 武将世界杯报名/不报名按钮对换位置**：`checkTournament()` 弹窗中 `.btns` 内两个按钮的 DOM 顺序互换——"不参加，静观其变"（`#tn-skip`，灰色次要按钮）移到前面，"报名参加"（`#tn-join`，红色主按钮）移到后面；两按钮的 id、点击逻辑均未改动，纯粹是视觉呈现顺序的调整。

**③ 组队大战站位改为随机，不再固定自选武将排头位**：`TeamBattle.begin(picks, side, opts)` 组装完 `mine`（我方出战名单，含所有以 `[hero, ...mates, ...fill]` 形式传入、原本主角/队友恒排最前的调用方）后，新增一行 `shuffle(mine)`，在正式生成战斗单位前把整个出战顺序打乱。这是该函数唯一的组装出口，覆盖了组队大战自由玩法、角色扮演组队大战、组队遭遇战、国战攻城/守城等全部调用点，无需逐处修改各调用方的传参顺序。

**④ 新增跨城宝物贸易体系**：此前"集市"已有按城市浮动的行情系数 `cityPriceFactor(cityId)`（对马黑市固定 0.8 折，其余城市按城名哈希稳定落在 0.90～1.20），但只用于**买入**定价，出售宝物走的是与城市无关、恒定五折（`Armory.SELL_FACTOR=0.5`）的仓库"出售"渠道，行情差异无法变现。本轮新增 `Armory.tradeSell(uid, cityId)`（行商贩卖）：按**当前所在城市**的真实行情结算售价 `shopPrice(rarity) × cityPriceFactor(cityId) × TRADE_FACTOR(0.85)`，且刻意不受集市折扣事件影响（折扣只降买价，不压行商收购价，否则黑市宿营期间贩卖反而吃亏、有悖直觉）。设计上做了两层平衡：其一，同城买卖必然亏本——买入价是 `base×factor`、贩卖价是 `base×factor×0.85`，0.85 系数在任意 factor 下都保证卖出价低于买入价，堵死了原地打转套利的漏洞；其二，唯有实际"异地低价买入、跑一趟高价城贩卖"才能获利，且移动到相邻城池要消耗 1 点行动力（既有的地图移动机制），行动力这一每日限量资源天然限制了套利频率，无需另设额度/冷却等新机制。理论最大单笔差价：以全图 factor 区间 [0.90, 1.20] 计算，买最低卖最高的极限情形下毛利率约 13%（`1.20×0.85/0.90 - 1 ≈ 13.3%`），扣去往返行动力与货摊数量限制后属于温和、有上限的增值空间。UI 上在"集市"弹窗（`MapUI.openMarket()`）新增「🚢 贩卖随身宝物」区块，与原有的购买货摊列表并排展示，列出玩家已鉴定且未装备的宝物及其在本城的贩卖估价；原有仓库内固定五折的"出售"按钮保留不变，作为无需赶路的快速变现选项与行商贩卖并存。

**验证**：`node --check js/app.js` 语法校验通过。用 Playwright 驱动完整开局向导（自创角色→扮演关羽→巅峰模式→史实分布→确认开局）跑通至天下地图，注入金币后进入洛阳集市：购买一件传说级宝物（书籍类，基准价 10000，本城行情 101% → 买价 10100 与页面显示一致），随即在"贩卖随身宝物"区块看到该宝物估价 8585（= 10000×1.01×0.85，与公式精确吻合），点击贩卖后金币到账、宝物从背包移除、同时回流至全局商店摊位池，UI 区块正确刷新为"身上暂无可贩卖的宝物"空态，全程无控制台报错。边境战与组队大战的改动为纯逻辑/顺序调整（无新增交互面），经代码复核确认无遗留的固定索引、越界或空数组访问风险。

**追加调整（同轮）：集市改为「买入／卖出」双页签，价格从高到低排序，弹窗尽量占满可视高度**。`openMarket(tab)` 新增 `this._marketTab` 状态记忆当前页签（默认"买入"），页签切换与购买/贩卖后的重渲染均保留当前所在页签，不会来回跳转；买入货摊按 `Armory.shopPrice(rarity)` 降序排列，卖出列表按 `Armory.tradeSellPrice(item, cityId)` 降序排列。新增 `.market-card{max-height:94vh}` 与 `.mkt-list{max-height:66vh}`（原两份 `.buff-list` 堆叠展示、默认仅 48vh，现拆分为页签后单份列表可用高度明显放宽），配合新增的 `.mkt-tabs`/`.mkt-tab` 页签样式（浅色卡片配色，与深色背景的 `.side-tabs` 区分，避免视觉突兀）。Playwright 复测：买入页签四件货摊价格降序 `[10100,1010,1010,1010]`（本次随机种子），购入两件后切至卖出页签，两件按估价降序 `[5151,2576]`，页签状态在购买/贩卖后的重渲染中保持不变，`getComputedStyle` 确认 `.market-card`/`.mkt-list` 的高度限制已生效，全程无控制台报错。

**修复：贩卖/购买后集市背后的城池面板金币数未即时刷新**。金币本身在 `Bond.data`/localStorage 中始终正确入账（`Armory.tradeSell` 内 `Bond.addGold` 无误），但集市悬浮层的 `openMarket()` 只重绘悬浮层自身（`openOverlay(...)` 另起一层 DOM），并不触碰其背后 `MapUI.render()` 所管理的 `#map-content`（城池面板 `cityPanelHtml()` 里 `💰<b>${Bond.gold()}</b>` 这行）——此前只有点击「离开集市」关闭悬浮层时才会调用 `this.render()`，导致悬浮层仍打开、连续买卖多笔时，背后城池面板显示的金币数停留在打开集市那一刻的旧值，容易被误判为"钱没到账"。修复：`.market-buy`/`.market-sell` 的点击回调中，在 `this.openMarket()` 之前各补一行 `this.render()`，让悬浮层背后的城池面板与悬浮层自身同步刷新。Playwright 复测：买入、卖出后，悬浮层仍打开的状态下直接读取城池面板 `.mc-head-stats` 文本，金币数与悬浮层内 `.wdesc` 显示的余额一致同步更新。

**追加：卖出页新增「一键卖出非传奇宝物」+ 传奇宝物贩卖二次确认**。卖出页签顶部新增批量按钮，标注件数与合计估价（如"一键卖出非传奇宝物（2 件 · 约 6010 金）"），点击后先 `confirm()` 弹窗二次确认再逐件调用 `Armory.tradeSell`；该按钮**只统计非传奇宝物**（`tradable.filter(item => item.rarity !== "legend")`），传奇级宝物既不计入批量目标、也不会被一键卖出误伤。单件贩卖按钮（`.market-sell`）新增判断：若该件 `rarity === "legend"`，点击时先弹出 `confirm()`（"「XX」是传奇级宝物，确定要贩卖换钱吗？此举无法撤销。"），取消则本次点击不生效、宝物原样保留；其余稀有度维持原先的单击即卖，不受影响。两处确认弹窗均复用浏览器原生 `confirm()`，与仓库页签既有的拆解/出售二次确认风格一致。**验证**：Playwright 用 `page.on('dialog', ...)` 接管确认框，注入 2 件非传奇 + 1 件传奇的测试宝物，依次验证：批量卖出点「取消」三件原样保留、点「确定」后仅两件非传奇被卖出且金币到账、传奇件保留；再对剩余传奇件单独贩卖，点「取消」宝物原样保留，点「确定」后宝物卖出、金币按 `tradeSellPrice` 精确到账；全程无控制台报错。

## 四十四、城池兵力系统（军事一期，第四十二轮）

按用户提议的两项军事玩法扩展、经方案讨论确认后实施的**第一轮**（第二轮为「野战→攻城」两段式，待后续交付）。核心目标：边境大战的出阵兵力不再单纯由武将统帅推算，而是从交战双方各自所在城池的驻军中调拨——发达、城墙厚实的边城才能撑起满编大军，弱小边城捉襟见肘，城池经营第一次和军事直接挂钩。

**新增 `Garrison` 模块（城池驻军）**：每座非海路城池（不分敌我）各自维护一支驻军，数据存战役层 `m.troops`（城→驻军数）。
- **上限**：`30,000 + 繁荣度×12,000 + 城墙等级×8,000`（1★无墙 42,000 ～ 5★墙3级 114,000）。
- **每日回复**：`300 + 繁荣度×250`，宿营时对全图城池（不分敌我）统一结算。
- **募兵**：每员 0.5 金，一键花钱尽量补满至上限（金币不足则按现有金币折算尽力募集），不设次数/行动力限制——补满一城所需总花费恒定为 `(上限-现存)×0.5` 金，分几次点击花的钱都一样，无套利空间。
- 城池面板新增「🚩 驻军」按钮（己方非海路城池可见），展示现有/上限并可一键募兵；「全部城市」总览表新增可排序的「驻军」列，城市详情弹窗同步展示。

**城墙效果追加**：`BUILD_TYPES.wall` 的守备加成（六维 +2/+4/+6）保留不变，另加成本城驻军上限（+8,000/+16,000/+24,000）。**注**：与用户讨论时谈到的"移除城墙旧效果、改为贡献城防值"是第二轮（攻城战）引入"城防值"概念后才有意义的改动——本轮尚无攻城战、无城防值可言，故城墙旧效果原样保留，仅追加驻军上限这一新收益；第二轮攻城战上线时再将旧效果替换为对城防值的贡献。

**敌境自营（AIDev）追加征兵**：原「繁荣 45% / 城建 30% / 产业 25%」三选一的宿营发展动作，调整为「繁荣 40% / 城建 25% / 产业 20% / 征兵 15%」四选一，敌方城池同样会随时间自行募兵（免费，一次性补充上限的 15%），维持敌我此消彼长的节奏。

**边境大战接入城池驻军**：
- `FieldBattle.beginExternal` 新增 `opts.troopScale`（`{mine, foe}`）：按「实际出阵兵力 ÷ 按统帅推算的自然兵力」算出缩放系数（`myScale`/`foeScale`），`_setupCommon` 与 `startClash` 的兵力初始化（含五线各线的初始兵力）统一乘以此系数——**兵力只影响战线的"抗打耐久"，不影响武将六维决定的攻防强度**：兵少的一方战线撑不了几回合就会告破，但每一击的力道不变，這让"兵力"和"武将质量"成为两个独立、都要经营的维度。不传 `troopScale` 时维持原有的纯统帅推算（如小游戏自由试玩），不影响其他玩法。
- 边境战弹窗新增**出阵比例滑杆**（10%~100%，步进 5%，默认 60%）：拖动即时刷新"出阵/留守/本城驻军"文字提示（只更新文字节点、不整体重渲染，避免打断拖拽手感）；己方按滑杆比例从**本方所在边境城池**的驻军中调拨，敌方（非玩家操控）固定调拨 7 成（`Garrison.AI_COMMIT`），松手后的最终比例随「开战」按钮一并读取。调拨的兵力在战斗开始前立即从原城池扣除。
- `FieldBattle.finishExternal` 的结果对象新增 `myTroopsLeft`/`foeTroopsLeft`（战后双方余兵，取整）。`RPG.applyBorderWarOutcome` 新增 `troops` 参数（`{winner, loser}`）：**胜方幸存兵力就地成为新占城池的驻军**（`Garrison.set`）；**败方此役出征在外、侥幸幸存的兵力退往己方相邻城池收编**（`Garrison.add`，adjCities 过滤同阵营，无路可退则损失）；**败方原留守本城、未随军出征的兵力则随城池一并陷落，不设退路**——这一设计使出阵比例形成真实的两难：出得多，赢面更大，但家底也空得更多；出得少，家底更安全，可一旦城池仍然易主（本轮尚无法靠留守兵力守城，第二轮攻城战上线后留守才有直接的防御作用），家底照样归零，仅仅是暂时保住而已，为第二轮的攻城战埋下伏笔。

**验证**：`node --check js/app.js` 语法校验通过。分三层验证：①**核心缩放机制**——利用已导出到 `window` 的 `FieldBattle`，直接调用 `beginExternal(mine, foes, 'cn', {troopScale:{mine:1000, foe:80000}})`（6 名史实武将构造的迷你阵容），确认 `myTroops0=1000`／`foeTroops0=80000` 与缩放系数精确匹配自然兵力之比，五线兵力之和精确匹配总兵力，全自动推演至终局：`playerWon:false`／`myTroopsLeft:0`／`foeTroopsLeft:79207`，符合"千人对八万人几乎必败且己方几乎全灭"的预期。②**驻军 UI**——完整开局后验证城池面板「驻军」按钮/弹窗展示正确（现有/上限/构成拆解/每日回复），人为改动存档余额后验证募兵按钮精确按 0.5 金/兵扣款、金额与兵力增量吻合、面板与"全部城市"总览表同步刷新；总览表新列在多座城市间取值与其繁荣度推算的上限一致。③**边境大战端到端**——利用建业/对马岛（对马岛开局即战国属地、与建业/徐州相邻，天然构成边境）拨快存档天数触发月末边境战，验证出阵比例滑杆文案随拖动即时更新且数值正确，选择不亲历（全自动推演）开战后：本方城池按滑杆比例扣兵、对方按七成扣兵，战罢己方落败、建业易主，新占城池驻军精确等于胜方幸存兵力，己方本城原有留守兵力随城陷落清零，全程无控制台报错。

## 四十五、野战→攻城两段式（军事二期，第四十三轮）

军事扩展的**第二轮**（承接第四十二轮的城池兵力系统），把边境大战由"一场野战定胜负"改为"野战定攻方、攻城定胜负"的两段式。

**流程重构**：`resolveBorderWar` 的野战 `onDone` 不再直接调用 `applyBorderWarOutcome` 夺城，而是把野战胜方判定为"攻方"，交给新增的 `resolveSiege(m, edge, fieldWinnerSide, fieldRes, heroFought, heroIn)` 继续处理：
- **攻方**＝野战幸存之军原样杀奔城下（`fieldRes.mySurvivors`/`foeSurvivors` 与对应的 `myTroopsLeft`/`foeTroopsLeft`，视胜方是哪一边取用），不再重新点选阵容。
- **守方**＝当前驻守目标城池、已现身的敌方武将（`m.assign[g.id]===targetCity`），加委任守将（`Guard`，若有），再加**该城尚未出征的留守驻军**（`Garrison.get(m, targetCity)`）——第四十二轮里"留守（1-出阵比例）的兵力此轮尚无实际作用"这句话，到这里终于兑现：出阵比例滑杆的留守部分，正是本轮攻城战里守方唯一的本钱。
- **空城直接开门**：若目标城一兵一将俱无（`defenderPool.length===0`，对马岛这类从不分配武将驻守的海路中转站必属此类），比照 `Conquest` 的既有先例直接开城，不必再打一场。
- 否则复用**同一套 `FieldBattle` 引擎**再打一场——五线斗将破阵的流程、UI、军令、将魂技能全部原样沿用，只是这次的双方换成了"攻城之军 vs 守城之军"，通过 `troopScale` 把双方战后/留守的真实兵力代入。选择复用而非另起炉灶，是权衡"三条战线+城防值"这类更贴合"攻城"主题的独立系统 vs 尽量降低本轮实现与回归风险后的取舍——已被反复验证的引擎直接抗下第二场战斗，不必再造一遍轮子。

**城墙旧效果正式移交**：第四十二轮暂缓的约定在本轮兑现——野战阶段不再叠加城墙的六维加成（攻出家门用不上自家城墙），改为**专属守城战**：只有当己方城池被围攻时，守军才会获得城墙等级对应的六维加成（`Buildings.lv(m, targetCity, "wall") * 2`），与守将 +3 一并叠加。城墙的驻军上限加成（第四十二轮新增）不受影响，两项收益并存。

**结算规则**（分歧点已按约定拍板）：
- 主角只有在**野战亲历且己方获胜、本人幸存**（`heroFought = heroIn && heroWonField && 幸存`）时才随军继续攻城，这是唯一能让金币/名声与你的钱袋挂钩的路径；若己方野战即落败，或压根没有勾选自己，胜负照旧与钱袋无涉——但战报文案按"是否亲历过野战"细分为两种措辞（亲历后落败 vs 压根没上场），不再一概说"未见你的身影"。
- **攻克**：与此前一致，得所夺城池金币日产出 **30 倍**犒赏、名声 +20。
- **攻城未克**（守方顶住了）：按讨论时拍板的方案，给一份**约合十天产出**（30 倍的三分之一）的"掳掠慰劳金"、名声 +10——败仗也有意义，不至于白跑一趟；城池本身归属不变，但双方此役折损（含野战+攻城两场战斗的合计消耗）照实结算：`applySiegeRepelled` 让守方战后余兵就地驻守（城保住了，兵折损了不少）、攻方战后余兵撤回己方出征那座城池（撤退也非全须全尾）。
- 经验按野战+攻城两场战斗的合计歼敌数结算，`Bond.loseGold`（原用于野战落败赔付）随旧规则一并移除——**新体系下不再有任何"赔付"路径，落败最坏情况就是拿不到犒赏，不会倒扣**，这本身也是与用户讨论时确认的"攻城失败给补偿而非罚款"精神的延伸。

## 四十六、势力系统：势力/忠诚招揽/玩家身份线/双结局（第四十四轮）

一次性交付的三期方案，在既有「国别」之下新增可征伐、可投效的「势力」层，覆盖数据表、忠诚招揽、玩家身份线、双结局四大板块。

**势力数据表**：`FACTIONS` 静态表每国 10 家（cn：蜀汉/张鲁/西凉/曹魏/晋/袁绍/袁术/吕布/荆州/东吴；jp：织田/京都/丰臣/武田/德川/北条/上杉/伊达/毛利/岛津），共覆盖 40 座陆地城池；另设 `tsushima_jp`（领对马岛）/`tsushima_cn`（不领城池）两个哑势力，使海路中转站复用与陆地城池完全相同的 `m.cityFaction` 归属机制。`m.cityFaction`（城池→势力 id）取代原 `m.cityOwner`（城池→国别）；`cityFactionId(m,cid)` 为归属查询唯一入口，`cityOwnerSide(m,cid)`（取该势力的 `side`）与 `isMyCity(m,cid)`（是否等于 `m.playerFaction`）分别服务国别级/势力级两种既有判断场景——**产业查封、驿站直达、守将委任候选池等"经济活动"沿用国别级判断不变**（避免在野玩家被排除在本国经济之外），**守将委任目标城池、驻军调度改为势力级**（`Guard.appoint`/`guardBtnHtml`/`garrisonBtnHtml`/`openGuard`/`openGarrison` 五处判断条件由 `cityOwnerSide(m,cid)!==RPG.char.side` 改为 `!isMyCity(m,cid)`）。`borderEdges(m)` 改为按势力边界筛选 `ROADS`，两国内部由此天然具备"内战"边界。

**武将效忠与忠诚度**：`initGeneralFactions()` 对全部 400 名武将执行确定性分配——沿用 `buildCityAssignment` 史实分支同一套姓名哈希公式定出所在城池，再取 `CITY_FACTION_INIT[城池]` 为初始势力，另按独立哈希 `hashStr(name+"|ronin")%100<20` 判定约 **2 成**武将开局即为**在野**（`generalFaction=null`，无忠诚概念）；有势力者忠诚度初始 `50+hashStr(name+"|loyalty")%31`（50~80 区间）。

- **忠诚月度浮动**（`Loyalty.monthlyTick`，随月末边境战一并结算）：`delta = (该势力当前占城数 − 该势力初始占城数)×0.8 + 随机(-4~4)`；忠诚跌破 **15** 时 **12%** 概率触发叛逃——**60%** 概率投奔本国另一家仍有城池的势力（忠诚重置为 50）、**40%** 概率挂印归隐彻底转为在野。
- **招揽**（`Loyalty.persuade`，武将详情页对非本势力武将可用）：花费固定 **200 金**（不足则提示，友谊不受影响），成功率 `chance = clamp(0.15 + 友谊/300×0.35 + (100−忠诚)/100×0.35 + 名声阶梯×0.03, 0.05, 0.85)`；成功即改写 `generalFaction` 为玩家当前势力（在野玩家招揽成功则目标本人也转为在野，`generalFaction=null`）并把忠诚重置为 **55**（转在野则忠诚清零），失败仅扣费、不影响友谊，可反复尝试。

**边境大战势力化**：`checkBorderWar` 候选边优先级——涉及 `m.playerFaction` 的边境 > 涉及 `RPG.char.side`（本国，含既有的建业/徐州/对马岛跨海边境）的边境 > 任意边境。`bwEligibleSupport(m,edge)` 限定玩家个人声援范围**严格不超出本国**——若玩家势力恰是交战方之一则强制自动代入（无从选择）；否则只在两个交战势力中挑出**国别等于 `RPG.char.side`** 的一方或两方供玩家选择，跨国内战的对方永远不可声援。候选支援方 >1 时新增前置的"声援哪一方 / 不介入静观其变"三选一（`this._bwSupport`），选定/不介入后才进入既有的出战名单+出阵比例流程；`heroFaction` 为 `null`（无从涉身或选择不介入）时以己方常态出阵比例（60%）自动推演，不打断游历节奏。全链路（`resolveBorderWar`/`resolveSiege`/`applyBorderWarOutcome`/`finalizeBorderWar`）的兵力池筛选、守将去留判断、驻军接管对象均由"国别匹配"改为"势力匹配"；`guardFirst` 简化为守将无条件优先编入队列（不再校验势力归属——"既已受托驻守此城，此役便与本城共存亡"）。

- **临阵倒戈**（`battleDefect`，开战前一次性重编队列，非战斗中途切换）：双方候选武将池中忠诚 **< 20** 者，各有 **15%** 概率倒戈至对方队列（守将与已被玩家亲自点选的武将不参与判定）。

**玩家身份线**（`PlayerRank` 模块）：开局向导新增第③步"选开局身份"——**在野浪人**（不效力任何势力）或**投效某势力**（从本国 10 家势力中择一，`m.playerFaction=势力id, m.playerRank=1, m.playerMerit=0`）。

- **官职五阶**（`RANKS`，按累计功勋 `m.playerMerit` 晋升）：在野(0) / 客卿(0) / 偏将(150) / 重臣(400) / 城主(800)。
- **功勋来源**：完成悬赏 `+ 悬赏名声奖励×2`（`completeBountyReward`）；随军攻城得胜 `+80`（攻克）/`+30`（未克，仅在主角亲历攻城战、`heroFought` 为真时结算，与既有金币/名声结算同一判断分支）。
- **俸禄**（`STIPEND_BY_RANK = [0,0,10,30,60]`，按官职阶梯即在野/客卿/偏将/重臣/城主）：官职达"偏将"以上，每游戏日在地图「🏯 身份」面板（`MapUI.openIdentity`）限领一次。
- **自立门户**（`declareIndependence`，需官职达"城主"、`m.fame≥1000`、身处本势力城池方可）：`m.playerFaction` 改写为特殊 id `"_player_"`（`factionDef` 对此特判，实时读取 `m.playerOwnFaction={n,lord,side}` 而非写入静态 `FACTIONS`，避免为动态创建的势力另开一套持久化通道），当前城池 `m.cityFaction[当前城]="_player_"`；代价 `Campaign.addFame(-round(fame×0.15))`（约 15% 名声），此后不可撤销、也不可再投效任何势力（自立即为终局身份）。旧势力不会永远为敌——`m.exLordUntil[旧势力id]=m.day+60`，60 天冷却期满后可重新投效旧主。
- 在野玩家也可随时通过身份面板主动 `PlayerRank.join` 投效麾下任意本国仍有城池、且未处于 `exLordHostile` 冷却期内的势力。

**双结局**（`checkUnifyEnding`，随玩家势力随军攻城得手后判定，与既有"天下无双"并行互不冲突）：

- 若本国（`RPG.char.side`）**全部**非海路城池均已收归玩家势力（`m.playerFaction`，含自立门户的 `"_player_"`），触发「（中原/日本）一统」庆典，`m.endingHomeland` 标记防重复。
- 若进而**两国四十城**（不含对马岛）尽归玩家势力，触发更高阶的「跨海一统」庆典，`m.endingCrossSea` 标记防重复；判定顺序先查跨海条件（若成立同时补写 `endingHomeland`），后查本国条件，避免一次夺城同时满足两个条件时漏发高阶庆典。
- 两个结局触发后均不影响游戏继续进行，可与"天下无双"在同一局内先后达成。

**天下战报**（`WorldWar` 模块，`camp()` 中每次宿营无条件执行，与玩家势力无关的静默推演）：每次宿营独立判定至多 **2** 场——候选边境须两端势力均**不是**玩家势力、且两端各自都有已现身武将；随机选中一条后，双方战力评分 `score = Σ已现身武将评分 + 该城驻军/50 + 随机(-200~200)`，评分高者获胜并吞并对方该城，败方城池若被吞并殆尽则播报"就此覆灭"，否则"退守余部"；胜方留 30% 驻军接管新占之城。此模拟纯文本播报、不触发任何战斗动画，只负责让天下版图在玩家视线之外持续演化。

**老档迁移**（`Campaign.ensureMap()`）：检测到缺少 `m.cityFaction` 时，先按城池原生国别对应的静态势力批量回填，再检查旧档 `m.cityOwner`——若某城旧记录的国别与其原生国别不同（说明曾被异国占领），改投占领国**静态表中第一家仍有城池的势力**兜底（无法精确还原具体占领者，风味折衷）；`generalFaction`/`loyalty`/`playerFaction`/`playerRank`/`playerMerit`/`playerOwnFaction`/`exLordUntil`/`stipendDay`/`endingHomeland`/`endingCrossSea` 等新字段缺失时按各自初始值补齐，不清空旧档其余数据。

**验证**：`node --check js/app.js` 全程通过。Playwright 端到端覆盖：开局向导第③步投效/在野两条路径（含势力列表按国别正确过滤）→ 地图身份状态行（`🚩 客卿·蜀汉` 等）与 `openIdentity` 面板（官职/功勋进度条、俸禄按当前官职正确取值且当日限领一次）→ 自立门户全流程（城池校验拒绝非本势力城池、通过后名声按 15% 精确扣减、`playerOwnFaction`/`exLordUntil` 正确写入、原城池 `cityFaction` 翻转为 `"_player_"`）→ 招揽系统（多次尝试直至成功翻转目标 `generalFaction` 为玩家当前势力）→ 势力化边境大战（借白帝城/江陵天然边境验证：自动识别涉及玩家势力的边境并强制代入声援方、野战→攻城两段全自动推演、城池归属正确翻转、未亲历时功勋不结算符合"仅亲历攻城战才计功勋"的设计）→ 老档迁移（构造缺失全部新字段+含异国占领记录的旧格式存档，验证按预期规则回填、原生归属城池不受扰动）——全程无控制台报错。

**验证**：`node --check js/app.js` 语法校验通过。用与第四十二轮相同的建业/徐州/对马岛天然边境场景，连续三次拨快存档天数触发月末边境战、选择不亲历（全自动）：三次均观察到完整的"野战→攻城战一触即发→（若目标城非对马岛）真实攻城战→最终战报"链路，控制台日志精确捕捉到 `攻城战一触即发` 提示，战报文案随攻方阵营与目标城池正确变化（"战国一举攻克【徐州】"/"战国一举攻克【建业】"），战后驻军数值逐次印证：攻方本城的留守储备全程不受后续战斗影响、被攻陷城池的驻军精确等于两场战斗后攻方的最终幸存兵力、未涉及的第三座城维持满编不变；全程无控制台报错。因随机对战胶着方向不可控，"攻城未被攻克（守方顶住）"与"主角亲身参与攻城"两条分支未能在自动化测试中直接复现结果，但复用同一套已在城池兵力系统与本轮"野战即时切换到攻城"两处都被直接验证过的 `FieldBattle` 引擎与 `troopScale` 机制，经代码复核确认这两条分支的数据流转与已验证路径完全对称、无额外风险点。

## 四十七、势力系统优化：配色/主公/忠诚软上限/势力AI/封赏/身份线（第四十五轮）

承接第四十四轮的势力系统，按十一项反馈做的全面优化。核心是把「势力」从一层归属标记，做成一套自己会运转的天下——各家诸侯每日按自己的国力行动、彼此攻伐算计，玩家只是其中一员。

### 一、势力配色（双通道编码）

`FACTIONS` 新增 `color` 字段。**色温分国别、色相分势力**：中原十家一律暖色（色相约 0~60° 与 320~350°），战国十家一律冷色（约 150~280°）。城池徽章底色/边框取势力色（`--fac` 行内注入），左缘另留 2px 国别竖条（中 `#ff6b6b` / 日 `#7a90ff` / 海路灰）作双保险。`cur`/`adj`/`far`/`mine` 四态一律改用 `box-shadow`、`opacity` 表达，不再覆写 `border-color` 以免与势力色冲突。底色用 `linear-gradient(rgba(0,0,0,.3), rgba(0,0,0,.62)) + var(--fac)` 压暗——刻意不用 `color-mix()`，部分旧版移动端浏览器尚不支持。地图左下角「🎨 势力」可展开色卡图例（`column-reverse` 向上生长，避免被 `.map-svg-box` 的 `overflow:hidden` 裁掉）。

### 二、主公

`FACTIONS.lord` 按**姓名**解析为真实武将（与威名榜八大高手同一先例，兼容图鉴改名/重建默认库），结果按 fid 缓存并在 `DB.save()` 这一唯一落盘出口统一失效。

- **主公一律强制归入自家势力**：势力归属本由所在城池推得，而 `CITY_HINTS` 给个别主公指的城恰属别家——初版因此出现过「袁术效力于吕布」。`initGeneralFactions` 现对主公特判，`seatLordsInOwnCities` 另将其迁回自家首城。
- **主公不可招揽**（UI 置灰 + `Loyalty.persuade` 内兜底），忠诚恒为 100。要其人，唯灭其势力。
- 城池面板、全部城市表（新增可排序的「势力」「主公」列）、城市详情、全武将表（👑 标记）、武将详情均展示。

### 三、忠诚软上限（本轮平衡设计的核心）

忠诚不再无止境爬升，而是逐月**向一个由待遇决定的软上限收敛**：

```
softCap = 55
  + 官职（军师/都督/掾属 +20/+15/+10）
  + 封地 +15
  + 结义 +10
  + 友谊占比 × 10
  + (主公魅力 − 60) ÷ 4          // 刘备的部下天生难挖，董卓的部下人心涣散
  + clamp((当前城池数 − 初始城池数) × 2, −15, +15)
  上限 100

月度：loyalty += (softCap − loyalty) × 0.25 + 私交修正 + 随机(−4~4)
```

- **势力盛衰并入软上限本身**，而非另作一份逐月无界叠加的增量——初版那样写会让连年开疆的势力部众忠诚一路飘到 100，软上限形同虚设（实测未受封赏者 90+ 占比 14%，改后降至 10%，而受封者 90+ 占比 70%，层次分明）。
- **席位有限**是「不会人人轻易到 100」的机制保障：官位数 = ⌊城池数/2⌋+1，封地一城一人，结义全势力至多 3 人。全势力至多供养十余名死忠，其余始终是可撬动的软肋。
- **私交联动（方案 B）**：同势力同僚 `+= (友谊占比 − 0.3) × 8`；他家武将 `−= (友谊占比 − 0.3) × 6`。后者与招揽公式（友谊高 + 忠诚低 = 易得手）串成完整策略链：先结交 → 等其离心 → 一举招揽。友谊入账口 `Bond.addF` 挂钩 `Loyalty.onBondGain`，同僚交情见长时顺带 +1~2 忠诚。
- 忠诚五档配色：赤诚 ≥80 / 忠心 ≥60 / 平常 ≥40 / 离心 ≥20 / 异心 <20。
- **在野比例校正**：`CITY_HINTS` 实覆盖 350/400 名武将（远超旧注释所称的 ~180），旧规则「仅未指定者可在野」只产出 12 人（3%）。改为两档概率——史实有归属者 15%、无归属者 50%，主公恒不在野——实测 21.5%，符合"约两成"的设计意图。

### 四、势力国力三件套与日常行动（`FactionAI`）

收编并取代了旧的 `AIDev`（敌境自营）、`WorldWar`（天下战报）、`AIGear`（敌将武备）三个模块——它们都以「国别」划分敌我，势力系统上线后该前提已不成立。

| 项 | 规则 |
|---|---|
| **威名** | 初始 = 城池数 × 100；夺城 +150 / 失城 −120 / 计谋得手 +30 / 被计谋 −20；**下限 = 城池数 × 30**（防止连番流言把一个坐拥三城的势力中伤到威名归零、军令枯竭，形同活死人——实测出现过） |
| **军令** | 每日回复 `1 + ⌊威名/400⌋`，池上限 `min(8, 3 + ⌊威名/300⌋)` |
| **金库** | 日进 = 所辖各城日产出之和；征兵/营建/购置武备均从中支出 |

**动作表**（耗军令 / 质量维度 / 权重）：营建 1/政治/20、征兵 1/统帅/20、通商 1/政治/11、购置武备 1/政治/9、封赏 2/魅力/10、招揽 2/魅力/11、计谋 2/智力/11、出征 3/武力/20。

**质量 = 该势力麾下已现身武将中该维度前五名的均值**（`FactionTop5`，按 (day, fid, dim) 缓存，避免 400 人名录被反复全表扫描）。政治高则营建快、统帅高则募兵多、智力高则计谋准、魅力高则挖人狠——「麾下有什么人」第一次真正决定了「这家势力擅长做什么」。

**关键调参：抽签须从完整动作表抽，而非只在付得起的动作里抽。** 初版只抽付得起的，导致 1 道军令的杂事总把预算耗光、3 道军令的出征几乎永无机会——200 天全天下仅发动 48 次出征（0.24 次/天），版图几乎纹丝不动。改为抽中付不起就**攒着留待来日**后，升至 132 次（0.66 次/天）。

**攻守公式**（经 200/400/600 天推演反复校准）：

```
攻 = 武力top5 × 12 + 出征城驻军/40 + rand(−350, 350)
守 = 统帅top5 × 12 + 目标城驻军/40 + 城墙等级 × 160 + 60 + rand(−350, 350)
```
初版给守方 +260 固定加成且随机项仅 ±150，胜率不足 23%，天下 200 天几乎不变。现守方仍保有城墙与一点固守之利（攻城本就难于野战），但攻方有胜算；随机项放宽到 ±350，使强弱悬殊之外仍有爆冷余地。攻方另会**择弱而噬、挟怨而攻**（按 `己方胜算 + 敌对度×4` 排序挑目标），夺城后 4 天「疲敝」不可再战。

**实测天下走势**（全员现身、玩家不介入）：第 1 天 20 家各 2~3 城 → 200 天 18~19 家、两强各 6~7 城 → 400 天 14 家、霸主 13 城 → 600 天 10 家、中日各一霸主（16 城 / 10 城）。既有明确的兼并叙事，又不会过早收敛，且始终留有弱小势力供玩家取食。

**计谋七式**（成败与效果强度取决于施谋方与目标方的智力 top5 之差；后四式须智力占优）：

| 计谋 | 门槛 | 效果 |
|---|---|---|
| 离间 | — | 目标 1~3 名武将忠诚 −8~20 |
| 烧粮 | — | 目标城驻军 −10~25% |
| 流言 | — | 目标势力威名 **−5~20** |
| 断道 | +5 | 目标城繁荣 −3，府库损一成 |
| 爆破 | +10 | 目标城城墙 −1 级 |
| 策反 | +15 | 直接策走一名忠诚 <30 的武将（结义者免疫） |
| 诈降 | +15 | 目标势力数日内无力兴兵 |

### 五、封赏体系（`Rewards`）

四类赏赐，**一概不用金币、只耗军令**——金帛买得来兵，买不来人心。玩家与 AI 主公共用同一套规则。

| 赏赐 | 军令 | 限制 | 忠诚上限 |
|---|---|---|---|
| 封官 | 2 | 席位 ⌊城池数/2⌋+1，三等 | +10~20 |
| 封地 | 2 | 一城一人 | +15 |
| 结义 | 3 | 全势力 3 人，需友谊 ≥200 | +10，且**终生免疫策反** |
| 庆功宴 | 1 | — | 一次性全军 +3（不动软上限） |

**举城叛变**：手握封地或身为守将者，若**月初**忠诚 <10 且触发叛离（12%），会把城池一并带走投奔他家。判定刻意取月初值而非收敛后的值——受封者软上限高（+15），每月都被拉回七十上下，若按收敛后判断则封地在手者永无叛变之虞，「封地是双刃剑」便成空话。

### 六、玩家身份线

**官职九阶**（按国别双套命名，阈值共用）：门客/足轻组头 0 → 参军/侍大将 200 → 偏将/物头 600 → 裨将军/番头 1400 → 中郎将/家老 3000 → 重臣/宿老 6000 → 太守/城代 11000 → 城主 20000。初版城主仅需 800，一条 20 层登塔令即可连跳两级，官职毫无分量。

- **功勋**：完成悬赏 = 名声 × 2；随军攻城 120（攻克）/ 40（未克）；**主动出征**夺城 150。
- **俸禄**（按阶）：`[0, 0, 10, 20, 40, 80, 160, 320, 640]`，每日限领一次。
- **主公赐封**：官至第 5 阶（中郎将/家老）以上，有机会获赐一座本势力城池为食邑——让「仕官」本身就是一条有获得感的路，而非仅是通往自立的踏板。
- **诸侯征辟**：在野且名声达第 3 阶时，各家势力的「招揽」动作有 22% 概率转为向你遣使来聘，可应聘出仕（附 500 金见面礼）或婉言谢绝。在野由此从"没人管你"变成"待价而沽"。
- **自立门户**：需官至太守/城代 + 名声 ≥1000 + 身处本势力城池。自立势力名按国别分——中原「某某军」、战国「某某家」（统一叫"家"会让"刘备家"读着像日本战国）。旧日团队全数编入麾下，**根基之城中与你交厚或本已离心的旧部会随你举事**（概率 = 友谊占比×0.7 + 离心度×0.5，封顶 0.9）——否则新主公将光杆一人，城池易主而人心不动，既不合情理也让自立后寸步难行。

### 七、名声 → 势力威名的转轨

自立当主后，「你的名声」与「你这家势力的威名」合二为一。改动收敛于两点（`Campaign.addFame` 是唯一入账口，10 处门槛判定统一改读 `Campaign.effFame`）：

- **入账改道**：自立后 `addFame(n)` 折 `n/2 × 0.1` 计入本家威名。
- **读数改道**：`effFame(m)` 在自立后返回 `威名 × 10`，复用既有的名声九阶阈值表，无需另造一套。
- **一次性折算**：自立瞬间，名声先扣 15% 代价，余下按 1/10 折为起家威名（名声 2500 → 威名 313，含根基城的 100）。
- 体感转折：自立前跑腿悬赏是主线；自立后一条 60 名声的悬赏只折 6 点威名，而**夺一城 +150**——当了主公，唯有开疆拓土才算数。
- 威名九阶：微末之族 → 一乡之豪 → 割据一方 → 名震州郡 → 雄踞数城 → 威压诸侯 → 一方霸主 → 天下枭雄 → 万世基业。

### 八、主动出征 × 月末国战

按「打谁」切割，两套战事各司其职：**月末国战**（每月 30 日）**只打跨国边境**，是中日两国的定期大会战，重新扛起"保留中日两国概念"的立意；**同国势力间的兼并**交由玩家的「⚔️ 出征」（1 行动力 + 3 道本势力军令，目标为接壤的非本势力城池）与 `FactionAI` 的日常行动。两者都复用已验证的野战→攻城两段式。军令天然限制出征频率，无需另设冷却；军令不足时，臣属提示「主公未允出兵」、自立提示「休整数日再图」。

### 九、其余

- **团队并入势力**：抽出统一入口 `Bond.myRoster()`——仕官/在野期返回团队，自立后返回本势力已现身武将（按评分降序）。7 处调用点（2v2 副将、组队、国战、阵营、遭遇战、边境战出战名单与候选池）统一改走此入口，此后新增玩法只要调用它即可自动适配两种身份。招募按钮在自立后改口为「🚩 纳入麾下」，直接改效忠、无名额上限、无替换之说。
- **守将候选池**：改为「**本势力武将**（不含主公）或**友谊满上限**的他家武将」——本势力同僚本就受你调遣，不该还要求友谊满 300。
- **招揽阻力**：成功率加入 `− 目标势力威名/8000 × 0.2`，从如日中天的大家挖人本就该更难。
- **本地武将分组**：按本势力（金边）/他势力（取其势力色为左缘、标注势力名）/在野（灰调虚线）三组罗列。
- **宿营夜报**：改为全量播报、`max-height:90vh` 内部滚动、八类分节（本势力/警讯/战报/密报/人心/动向/修行/快报），空节自动隐去，警讯醒目色。**并修复了一个既有缺陷**：月末那一夜的忠诚浮动等消息原会被随后的大事弹窗直接吞掉（夜报根本来不及出）——现改为夜报先出，「继续 ›」再接续月末国战/武将大会。
- **势力覆灭善后**：旧部尽数转为在野（而非效忠一个已不存在的势力），任天下竞相延揽；若玩家所属势力覆灭，自动重归在野并受一次名声打击（此前是未处理的边界，会让 `isMyCity` 永远为假而卡死）。
- **威名招贤**：每月威名最高的数家有概率获在野豪杰自请来投——威名因而不只是"军令换算器"。
- **敌对度** `m.hostility`：轻量外交层，只影响 AI 挑选出征/施计目标的优先级，使攻伐显出恩怨脉络而非纯随机。

**验证**：`node --check` 四文件全过；Playwright 回归 17 项断言全过（配色注入、图例、归属行、势力一览 20 家、全武将势力/忠诚列、20 主公👑、在野比例 21.5%、分类夜报、金库运转、出征按钮与军令显示等）；专项验证主公归属修正、主公不可招揽、忠诚分层、封赏四式与军令扣减、举城叛变、诸侯征辟、自立转轨（名声 2500 → 威名 313）、团队并入（3 队友 + 5 随附 = 8 麾下）、老档平滑升级（删除全部新字段 + 遗留 `cityOwner` 仍正确回填）、9 个小游戏逐一可进入且无报错；200/400/600 天推演验证兼并节奏。全程无控制台报错。

### 补丁：主公府封赏钮点击无反应（第四十五轮修订）

**现象**：主公府里封官/封地/结义/庆功宴四个按钮点上去毫无反应。

**根因（两层叠加）**：
1. 四个按钮在军令不足时被渲染为 `disabled`（封官/封地需 2 道、结义 3 道、庆功宴 1 道），军令归零时四个同时失效；
2. 而 `.menu-btn` 的**禁用样式只挂在 `.menu-main` / `.map-menu` 两个容器下**，主公府的 `.menu` 位于弹窗内，两个选择器都不匹配——按钮被禁用后依旧是 `opacity:1` + `cursor:pointer`，看上去与可点状态毫无二致。于是玩家看到的就是"一个正常按钮，点了没反应"，且完全无从得知是军令不够、更不知如何补充。

**修复**：
- **不再置灰**：四个按钮改为始终可点，军令不足时点击给出明确说明——`军令不足：封官需 2 道，现有 0 道——宿营一夜可回复 1 道（威名越盛回复越快）`；按钮本身淡化至 62% 不透明度并把耗量标红，作为"现在做不了"的预告。军令判定统一收敛到 `MapUI.LORD_ACT_COST` 一处，不再散落在各分支里。
- **补齐禁用态样式（兜底）**：新增 `.menu .menu-btn:disabled`、`.btn-primary:disabled`、`.btn-ghost:disabled`、`.cup-go:disabled`、`.gift-btn:disabled` 统一禁用样式（45% 不透明 + 灰化 + `not-allowed`）。此举顺带修好了同一类的既有隐患——例如行动力耗尽时的「一键拜访」（`.cup-go`）、今日已领时的「领取俸禄」（`.btn-primary`）此前同样是"看着能点、其实是死的"。

**验证**：军令 4 道时四钮逐一点击均正确进入对应流程（封官→选人、封地→选人→选城、结义→选人、庆功宴→即时结算并提示）；军令 0 道时四钮均给出上述说明而非静默；封官/封地/结义三条完整链路各自正确落库（posts 0→1、fiefs 0→1、sworn 0→1）；回归 10 项断言全过，无控制台报错。

## 四十八、七项体验修复：覆灭善后/俸禄自动/忠诚显示/月末国战/招揽定价/身份卡改版/地图配色（第四十五轮再修订）

### 一、势力覆灭善后：旧部或降或散，不再"消失"

`FactionAI.onExtinct` 原先一律把败亡势力的旧部转为在野，且只有 `FactionAI.war()` 这一条日常出征的路径会调用它——玩家亲手在月末国战/主动出征中灭掉一家势力时，`applyBorderWarOutcome` 完全没有调用这一善后逻辑，导致败方旧部的 `generalFaction` 仍指向一个已无寸土的空壳势力，形同卡死（既不在野可被延揽，也不属于任何还存在的势力）。

- `onExtinct(m, fid, winnerFid)` 改为按 35% 概率让每名旧部就地归降 `winnerFid`（忠诚起于 45，半信半疑）、其余散入民间转为在野；返回一段播报文字交由调用方投递（FactionAI.war 走夜报「人心」分类，`applyBorderWarOutcome` 走 toast）。
- `applyBorderWarOutcome` 补上遗漏的调用：败方城池归零时同样触发覆灭善后，不再只有 AI 出征才会走到这一步。
- 玩家自身势力覆灭时：仕官期覆灭折八成名声，自立期覆灭（basis 已转为势力威名）名声直接归零——两种语境下的惩罚力度分开处理，避免用一套公式在"威名"与"名声"两种量纲间硬套导致数值失真。

### 二、俸禄自动到账

`PlayerRank.claimStipend`（手动+每日限领一次）改为 `dailyStipend(m)`，随 `camp()` 的宿营流程自动结算并计入夜报「本势力」分类，不再需要打开身份面板手动点「领取俸禄」。连带移除了不再需要的 `m.stipendDay` 字段（存档 schema 精简）。

### 三、忠诚度显示修正

- **主公**：`isFactionLord(fid,gid)` 为真时，忠诚一律显示为 `──`（而非此前的满分 100，那会让人误以为主公也是可被拉拢的普通忠诚值）。新增 `loyaltyCell(m,fid,gid)` 统一封装该判断，`showDetail`/全部武将表/势力详情三处call site 全部改用它。
- **配色去绿**：`LOYALTY_TIERS` 原用亮绿（`#5ad66a`/`#9ccc65`）标记高忠诚，但忠诚展示大多落在 `.bond-box`（米黄羊皮纸底），高亮度绿在暖色浅底上对比度极差、几乎读不出来。改用与全站既有 `--cn-gold`/`--cn-red` 呼应的暖色渐变（金→棕→橙→红），在浅色纸面与深色表格两种场景下都保持清晰。

### 四、月末国战消失（回归 bug，非新问题）

根因：checkBorderWar 改为"只打跨国边境"后，唯一符合条件的边——经对马番所中转的建业/徐州↔对马岛——的过滤条件仍要求"两端都须有已现身武将"，而对马番所（`tsushima_jp`/`tsushima_cn`）是纯技术性占位势力，`initGeneralFactions` 的城池池天生不含海路城市，从未也永远不会分配任何真实武将给它。两个条件叠加的结果是唯一可能的候选边永远无法通过校验，月末国战因此从这一轮起彻底消失。

修复：候选边校验对 `DUMMY_FACTIONS`（对马番所）一侧放宽为直接通过，仅要求大陆一侧确有已现身武将，避免打一场双方都没人的空仗，同时恢复了每月一次的正常触发。

### 五、招揽定价随身价浮动

`Loyalty.PERSUADE_COST`（固定 200 金）改为 `persuadeCost(gid) = ratingScore(geared(g)) × 4`——与 `Bond.recruitCost`（评分×10，明面招募入队）同一取值逻辑，打六折体现"招揽走的是忠诚/友谊暗中运作，不像明面招募那么要价十足"。固定 200 金此前对高评分名将而言形同白菜价，与其身价毫无关系（如关羽评分 527，招揽费从 200 → 2108 金）。

### 六、玩家状态卡改版

原「🏯 身份」按钮打开一个大面板才能看到官职/功勋/俸禄/势力信息，现全部移至左侧角色卡常驻展示（`mh-stats` 新增一行：⚡行动力 · 💰金币 · 📜军令；`mh-fame` 追加官职·势力·功勋进度），面板本身按身份三态收窄为「只做需要玩家决策的事」：

- **在野**：`openJoin()` ——响应诸侯征辟 / 择一势力投效。
- **仕官**：`tryDeclareIndependence()` ——自立门户，条件不足时给出具体原因（官职/名声/所在城池）而非把按钮悄悄隐藏。
- **自立**：`openPersonnel()`（原「主公府」重命名为「人事」）——只保留封官/封地/结义/庆功宴四个按钮，概况数字仍留面板内（信息量偏细，角色卡只留威名/官职两条最要紧的）。

### 七、地图配色彻底重做

**根因**：第四十五轮"色温分国别"的设计（中原暖色、战国冷色）恰好与既有的地形底色撞车——中原地图叠了一层暗红色（`.map-land.cn` fill `rgba(193,39,45,.22)`），十家中原势力却又清一色红/橙/褐/品红，颜色和底色同色系，彼此也互相混淆；日本地图叠暗蓝色，十家战国势力又清一色蓝/靛/青，同样的问题在另一侧重演。

**修复**：反其道而行——中原十家专挑红色底色的补色区（避开色相 340°~20° 的红区，改用蓝/绿/青/紫/黄，`hsl(36~324, 58%, 44%)` 均匀取十色）；战国十家专挑蓝色底色的补色区（避开 200°~260° 的蓝区，改用红/橙/黄/绿/紫，`hsl(275~185, 58%, 44%)` 均匀取十色）。国别辨识改由城池徽章左缘 2px 竖条与城池所在的陆地板块（物理位置不会认错）承担，不再指望城池填色去兼顾"报国别"这件事。

另移除相邻城池的绿色勾边（`.map-city.adj`），改为中性白光圈——势力配色已经很挤，绿线只会再添一层混淆；白色不属于任何势力/国别的既有配色体系，在红蓝两块底色上都能保持清晰的"此城可达"信号。

**验证**：`node --check` 全过；专项验证——伊达覆灭后 3 人归降上杉（自然触发，未刻意构造）；俸禄 0→10 金自动到账且计入夜报；刘备详情忠诚正确显示"──"；月末国战连续验证多月正常触发（建业/徐州↔对马岛边）；关羽招揽费用 2108 = 评分 527×4；自立门户条件不足时给出具体原因，达标后「人事」面板正确显示四项封赏；老档迁移 9 项新字段全部补全；全程无控制台报错。

## 四十九、六项体验修复：边境战出阵/角色卡改版/地图勾边/晋势力改名/繁荣曲线/军令回满（第四十六轮）

### 一、月末边境大战"自选武将未参战"

根因是三处连锁 bug，全部由第四十五轮"边境战只打跨国边境"这一改动间接暴露——此后唯一可能的候选边变成大陆↔对马番所，而对马番所是 0 真实武将的哑势力，此前从未真正走到这条路径：

1. `MapUI.resolveBorderWar` 用 `Math.min(poolA.length, poolB.length, 10)` 给两侧出阵人数封顶。对马番所一侧 `poolB.length = 0` 时，这个共同上限被拖到 0，连同另一侧（含玩家亲自点选、已排到阵前的自选武将）也一并被 `slice(0,0)` 砍空——玩家明明勾了自己出战，一开战却"未见你的身影"。改为两侧各自独立封顶（各自最多 10 人，不再取二者较小值），"多人数容错"的 deploy 早已支持两侧人数不等甚至一侧为零，不需要这层人为对齐。
2. 修复 1 之后，`FieldBattle.renderDeploy` 在敌方 0 武将时访问 `this.foes.slice()...[0].name` 崩溃（`Cannot read properties of undefined`）。加长度判断，敌军为空时显示"敌军城中空虚，未见一兵一将——此行形同不战而胜"。
3. `FieldBattle.calcOrders(arr)` 在 `arr` 为空时除以 `arr.length` 得 NaN（敌方军令显示"敌 NaN 道"）。空数组时按下限 2 处理。

三处修复后完整验证一场胜负两种结局的边境战，参战判定与战报文字均恢复正常。

### 二、自选武将区卡片重排

原卡片"头像+三行简介"挤左、"名声/官职/两个按钮"全堆右列，主次不分、行距又窄。`heroCardHtml` 改为按信息类别整体纵向堆叠：① 身份行（头像+姓名+等级评级）② 声望行（名声/威名）③ 官职行（官职·势力·功勋进度）④ 资源行（行动力/金币/军令三项同性质数值合并一行）⑤ 操作行（两个按钮左右并排）。新增 `hero-stack` 修饰类只加在玩家卡片上，`nemesisHtml`（宿敌卡）仍复用原有横向行式布局的基础类，两者互不干扰。

### 三、地图城池勾边精修

- 相邻可达城池不再有任何勾边光圈（原白色光圈也一并移除）——"此城可达"信号改由点选后的操作面板承担，地图上只保留 `z-index` 令其略压在远城之上。
- 所在城池/势力城池的金色勾边由 1.5px 收细至 1px，辉光范围与强度同步收窄，作低调精致的身份标记，不再抢占势力底色的可辨度。

### 四、"晋"势力改名为"司马"

除"晋"以外的所有势力均以主公姓名（或与其身份直接对应的历史称谓）命名，唯独司马懿一系用了"晋"——但"晋"是司马懿身后由其孙司马炎建立的朝代号，司马懿本人终生未曾持有这一称号，与其余势力的命名逻辑不符。改为"司马"，与张鲁/袁绍/袁术/吕布等直接以主公姓名命名的势力保持一致；内部 id 仍为 `jin` 不变，不影响存档兼容。

### 五、城池繁荣度成长曲线放缓

`Prosper` 原先不论星级一律"每积 5 点升 1 星"，玩家操作与各势力 `FactionAI.build` 的日常营建又都会稳定进账，实测不到一年全图城池尽数五星，"繁荣"因此失去了意义。改为按目标星级递增门槛 `STEP_BY_LV = [6, 10, 16, 26]`（分别对应升至 2/3/4/5 星所需累计点数）——初段升星依旧顺畅保留成长感，冲顶两星门槛陡增大幅遏制"全图速满星"。360 天模拟对比：全图 40 城平均星级由（原曲线下）不到一年即可达到的 5 星封顶，降至新曲线下第 360 天平均 3.92 星、仅 5/40 城（12.5%）触顶。

### 六、军令回满 + 非出征动作效果相应减弱

`所在势力很少发动攻击`的根因：`FactionOrders.tickAll` 原按"当日军令 + 每日回复量"缓慢累加，而 `FactionAI.tick` 的贪心抽签会优先把刚回复的一两道军令花在耗 1~2 道的杂事上，军令最低（3 道）的弱小势力因此长年徘徊在 0~2 道之间，永远凑不出出征所需的 3 道。

- `FactionOrders.tickAll` 改为直接回满至当日上限（`cap`），不再是"加 regen"：军令最低的势力当日满额时也恰好够发动一次出征，不必再指望"贪心 AI 手里能存住军令"这种几乎不会发生的事。出征本身仍受 `weary` 冷却节制（胜 4 天/负 2 天），回满不会导致"天天开战"。
- 相应地，出征之外的耗军令动作（征兵/通商/计谋）不再受"军令攒不出手"的天然节制而更趋频密，新增 `FactionAI.NON_WAR_DAMPEN = 0.8` 统一下调这些动作的单次数值效果（征兵人数、通商进账、计谋强度），以总量而非频次去补偿，避免国力/民生/忠诚数值被"回满"变相刷快节奏。
- 360 天模拟对比：全天下出征次数由旧算法的 254 次提升至新算法的 588 次（约 2.3 倍），繁荣度增速则因第五项的曲线调整维持在合理区间，两项改动相互独立验证均通过。

**验证**：`node --check` 全过；Playwright 专项验证——边境战自选武将参战判定修复后完整跑通胜负两种结局；自选武将卡与宿敌卡在多种身份状态（在野/仕官/自立）+ 桌面/移动端两种视口下截图比对，卡片重排与宿敌卡布局互不干扰；地图截图确认相邻城池光圈已移除、所在城池勾边收细；`jin` 势力全部展示位（角色卡/武将详情/势力一览）均正确显示"司马"；360 天离线模拟验证繁荣度与军令/出征频率两项改动的实际效果均达到预期区间；全程无控制台报错。

## 五十、边境战团队随军概率化/出阵比例改AI策略、军令满仓可视化、单挑结果框锁定（第四十七轮）

### 一、边境战团队武将未随军出战

`resolveBorderWar` 中，玩家自选出战名单原先这样拼装：先从 `heroPool`（即 `generalFaction === 本方势力` 的候选池）里筛出被勾选的项，再把主角、这批"筛出项"、随机补位三者拼接成最终出战名单。问题在于团队成员是玩家私交结拜之人，`generalFaction` 未必等于玩家现所效力的这一方——于是玩家勾选的团队成员纵然真实存在，也从未出现在 `heroPool` 里，被这层过滤悄悄清空，只有主角本人（单独用 `RPG.heroGeneral()` 现造，不依赖 `generalFaction`）还能正常上阵。

- 改为直接从 `Bond.myRoster()`（玩家真正的团队名单来源）里找被勾选者，不再依赖 `generalFaction` 是否匹配。
- 同时改按你的建议做了取舍：**主角本人一经勾选必定上阵**；**团队成员纵经勾选，仍按 70%（`TEAM_JOIN_CHANCE`）概率决定当日能否随军**——团队成员本就分驻各处（管产业/守城/自行游历），点选只是"愿意的话请你出战"，不是"传送到场"，未掷中概率的名额仍由候选池随机补位填满，不会因此打不成仗。
- 边境战点将弹窗的说明文案同步更新，明确告知这一概率机制。

### 二、非本势力出阵比例改由 AI 按战力策略决定

原先无论玩家是"亲率本势力出战"还是仅仅"声援同属本国、但并非自己效力的另一家内战"，出阵比例都统一交给玩家手上的那根滑杆决定；而对方（或双方，当玩家全程不介入时）则一律固定抽调 7 成驻军（`Garrison.AI_COMMIT`），不问敌我强弱。

- 新增 `FactionAI.commitRatio(m, fid, foeFid)`：按己方武力评价（top5 武力均值）相对敌方统率评价的优劣、以及双方积怨深浅推算出阵比例（45%~85% 区间），全程无随机项，同等局面归同一个答案。
- `resolveBorderWar` 改为：**只有当此战确系玩家现效力的本势力时**，出阵比例才沿用玩家的滑杆；其余情形（声援他国内战、或玩家全程未介入）双方出阵比例一律走 `commitRatio`。点将弹窗在非本势力亲征时也相应隐藏滑杆，改以文字说明"出阵比例由该势力自行调度"。

### 三、军令"回复满"在实际观感上仍不满

上一轮已将 `FactionOrders.tickAll` 由"加 regen"改为"直接回满至当日上限"，机制本身没错——但 `camp()` 里 `tickAll` 排在 `FactionAI.tick` **之前**，回满的军令当天就被同一次 `camp()` 内的势力日常行动（含玩家所在但非自立的那一方，其军令仍由 AI 代为支配）花掉一部分，玩家实际看到的永远是"回满又被花掉"后的余量，视觉上从未见过"满仓"，因而仍觉得"不对"。

修复：调整 `camp()` 内的执行顺序，改为**先让当日的势力日常行动花用「昨夜已回满、尚未动用」的那一整仓军令，再执行 `tickAll` 把军令重新回满**。这不改变任何数值平衡——每天可供支配的军令数量仍然是满仓的 `cap`，只是把"回满"这一步挪到当日流程的最后，使玩家次日一看永远是满仓状态，同时该势力当天的行动预算与之前完全一致（仍是满编 `cap`，只是取用的是上一晚回满、这一晚才被消费的那份）。

### 四、单挑结束确认框可被误触关闭

经典单挑（含 1v1、双人组队）的胜负结果框 `showResult()`，以及野战演武（团队大战/边境战）的结果框 `FieldBattle.showResult()`，此前调用 `openOverlay()` 时都未传 `{ modal: true }`。而全局遮罩层的关闭逻辑是"点击遮罩背景本身即关闭，除非标记为 modal"——玩家手一抖点到卡片外的半透明背景，整个确认框（连同框内"再战一场/返回"两个按钮）秒退，但游戏流程并未被驱动向前，形同卡死，只能刷新页面重来。

两处均补上 `{ modal: true }`，此后只能通过框内按钮关闭，与边境战点将、声援抉择等一贯的关键确认框保持同一交互约定。

**验证**：`node --check` 全过；Playwright 专项验证——直接调用 `resolveBorderWar` 200 次采样，主角勾选后 200/200 次上阵（100%），两名勾选的团队成员随军概率分别为 67%/69%（目标 70%，样本量下的正常波动）；边境战全流程重新跑通一次胜负结局，无报错；`camp()` 重排序后验证军令在"清空重置→宿营→查看"链路下稳定显示满仓，同时势力当日行动（营建/通商/计谋/出征等）照常正常触发；360 天离线模拟复验，出征次数与繁荣度增速与调整前处于同一量级（632 次出征 / 5 座五星城，均属正常随机波动区间），确认本轮改动未破坏上一轮的平衡校准。

## 五十一、城市繁荣度与城建体系重新设计（第四十八轮）

本轮先出方案经用户确认三处关键取舍（新建筑命名「钱庄」、设施挑战移出繁荣度加成、星级不扩容仍封顶 5 星），再行实施。

### 一、城建扩容：6 种建筑、每城全开、封顶 10 级

`cityBuildOptions` 原按城市哈希"5 选 3"限制可建种类，改为**每城可建全部类型**，不再抽样。`Buildings.MAX_LV` 由 3 提升到 **10**；原先硬编码的 3 档效果/花费数组无法覆盖 10 级，全部改写为按等级生成的公式：

- `BUILD_TYPES[type].eff` 由静态数组改为 `eff(lv)` 函数，现开/次级预览均按需调用。
- 花费表 `Buildings.COSTS` 扩至 10 级（500→800→1200→1800→2600→3600→4800→6200→7800→9600 金，材料 0→1→1→2→2→3→3→4→4→5 份），10 级约合 1 级的近 20 倍。
- 医馆回魂丹价改指数衰减公式 `hospitalRevivePrice(lv)=max(20, round(100×0.85^lv))`（10 级封底 20 金）；驿站折扣改 `postDiscount(lv)`（1 级只开通、之后每级再降 9%，10 级封底剩 19% 原价，避免线性衰减降到负数）；书院/城墙/演武场维持原"每级固定增量"公式，单纯把上限从 3 延伸到 10。

### 二、新增建筑「钱庄」：势力金库收入与自选武将私产彻底分账

**根因**：`FactionGold.income`（势力金库日进）此前直接复用 `Estate.cityDailyGold`——即"自选武将的天下名楼等私人产业投资收益"，两套本该独立的账目混成了一个数字；且 `Estate.npcEstate`（敌营产业）从未被任何代码填充过，导致玩家未亲自置业的城池对国库贡献长期为零。

新增 🏦**钱庄**建筑，`Buildings.bankIncome(lv) = 150（底税）+ 120×等级`，`FactionGold.income` 改为汇总辖下各城钱庄贡献（按城市繁荣倍率加成）。此后**势力金库＝钱庄税收，自选武将产业＝你自己的钱袋**，两条线不再互相牵连；未建钱庄的城池也有底税兜底，不会再出现分文进账的僵尸城池。

### 三、繁荣度来源审计：移出战斗成就，只认市政建设

原繁荣点来源混杂"设施挑战获胜 +1""完成悬赏 +1""置业 +2""夺城 +2"等，语义不一。本轮按确认结果全面清理：

- **移除**：完成悬赏 +1（`completeBountyReward`）、设施挑战获胜/全清（阵营大战/组队大战/国战/车轮战，共 5 处）——均属战斗成就，与市政建设无关。
- **城建捐修改按花费换算**：`Prosper.ptsFromCost(gold) = max(1, round(gold/400))`，越贵的等级贡献越大，不再是"每级固定 +2"；`FactionAI.build` 的 AI 自建路径同一口径。
- **其余来源按新量级重新赋值**：置业 +2→**+8**、产业扩建 +1→**+10**、接管敌营产业 +2→**+8**、边境战夺城 +2→**+12**、「断道」计谋破坏 -3→**-15**、`FactionAI.build` 抽象营建（无对应具体建筑时的兜底）1/2→**4/8**。
- **门槛重算**：`STEP_BY_LV` 由 `[6,10,16,26]`（总计 58）改为 `[40,90,170,300]`（总计 **600**）——六种城建各自封顶投入约 97 点，六种合计约 580 点，与 600 点的 5 星总门槛大致相当，"把六种城建都修到顶"约等价于摸到 5 星，产业/夺城等其余来源是加速捷径而非必经之路。

### 四、验证

`node --check` 全过；Playwright 专项验证——单城市连续捐修 6 种建筑各至 10 级全部成功（`cityBuildOptions` 不再受哈希限制），封顶后繁荣度恰好升至 5 星；钱庄 10 级日进贡献 1350 金，`FactionGold.income` 正确按势力实际辖城汇总（与其他势力城池的建设水平互不干扰）；城建面板截图确认 6 种建筑效果文案、次级预览、花费展示均正确渲染；「全部城市」总览表繁荣星级/日进列渲染正常；360 天离线模拟——纯 AI 被动经营下城市平均星级降至 2.90（仅 1/40 城达 5 星），较扩容前更保守，出征次数 704 次与前一轮同量级；另跑 360 天势力金库模拟，各势力金库均值随时间稳步增长（day90 均值 2755 → day360 均值 13228）且无一破产卡死，确认新旧收入口径切换后经济系统仍健康运转。

## 五十二、六项体验与数值修复：城建/势力数据展示、结果弹窗全面排查、驻军上限校正（第四十九轮）

### 一、全部城市列表增设「城建」列

`AllCityUI.rowData` 新增 `buildTxt`——按 `cityBuildOptions` 汇总本城全部 6 种城建的「图标+级数」（如 🏥3 📚0 🏇0 🏯2 ⚔️1 🏦4），插入表格「繁荣」列之后，海路中转站按现有规则显示「—」。

### 二、城建弹窗按钮风格统一

原「兴建/升级」按钮是塞进 `.wdesc` 描述行右侧的迷你 `btn-primary`（12px 字号、手动内联样式），与游戏里其余候选列表（委任掌柜、委任守将、驿站目的地、封赏对象等一律用整行可点的 `menu-btn`：图标+标题+小字说明）观感割裂。改为同一套 `menu-btn` 列表（新增 `.bld-list { grid-template-columns: 1fr }`，做法与既有的 `.post-dest-list` 单列覆盖完全一致），已至顶级/城池被占时按钮原生 `disabled`（`.menu .menu-btn:disabled` 早已有对应的变灰样式）。

### 三、结果/决策弹窗背景误触关闭——全面排查

根因排查发现，此前只修复过"边境战出阵""单挑结束"两处，但同一个 bug（`openOverlay` 未传 `{ modal: true }`，背景遮罩一点即关）广泛散落在全部 67 处 `openOverlay` 调用中的至少 20 处，包括：车轮战/百人斩/阵营大战/组队大战/国战/世界杯各类结果与结局画面、百人斩天赐机缘三选一、强人劫掠事件、遭遇战战报、以及**宿营夜报**和**武将大会报名/战报**——后两者最关键：它们是 `camp()` 夜报流程 `followUp()` 链条的一环，背景误触关闭后会连累导致地图既不重渲染、月末国战等后续事件也不会触发，形同卡死，比单个战斗结果框更隐蔽也更严重。

逐一排查全部 20 处后统一补上 `{ modal: true }`；对已有明确"取消/返回/离开"按钮、背景误触等价于点击取消且无副作用遗留的弹窗（如团队替换选人、副将挑选、切磋对手挑选、宝物选择等 10 余处）保持不变，不做多余改动。

### 四、全部势力列表与详情增设「日进」

`AllFacUI.rowData` 新增 `dailyIncome: FactionGold.income(m, fid)`，表格在「金库」列后插入「日进」列；势力详情弹窗同步补上「💰 金库：X（日进 Y）」。

### 五、核验势力征兵金库耗费——确认无误

核对 `FactionAI.recruit`：征募人数 `n` 已按 `Math.min(room, ...)` 卡在城池驻军尚余空位内，花费 `cost = n × Garrison.RECRUIT_GOLD_PER`（0.5 金/兵）与玩家自己的一键募兵用的是同一常量、同一单价，且正确从发起势力自己的 `FactionGold` 扣款（而非误扣玩家个人金币或别家金库）。结论：**耗费逻辑本身没有问题**，未作改动。

### 六、城墙驻军加成下调至 1,000/级

`Garrison.WALL_CAP_STEP` 由 8,000 降为 **1,000**：城池驻军上限 = 基础 30,000 + 繁荣度×12,000（5 星 +60,000）+ 城墙等级×1,000（10 级 +10,000），封顶恰为 **100,000**（原先 8,000/级，城墙扩容到 10 级后会把封顶推到 170,000，远超预期）。相应更新 `BUILD_TYPES.wall.eff` 与 README 文档同步改用新数值。

**验证**：`node --check` 全过；Playwright 验证——满繁荣满城墙的城池 `Garrison.cap` 精确等于 100,000；「历练单挑」结果框实测背景点击不再消失、真正点击框内按钮仍正常关闭并继续流程；全部城市/全部势力两张总览表与详情弹窗截图确认新增列正确渲染；360 天模拟确认出征频率与繁荣度增速未受本轮改动影响，与上一轮同量级。

## 五十三、主公七件事：计谋入口、卡片重排、城建行内布局、庆功宴限次、官位展示、金库合一、名声并存（第五十轮）

### 一、主公专属计谋入口

`FactionAI.plot(m, fid, q, push)` 原是 AI 势力专属的自动施计（随机挑目标、随机挑计谋种类），玩家自立为主后既不参与 AI 的每日行动循环（`FactionAI.tick` 明确跳过 `_player_`），也没有任何手动施计的入口——七式计谋（离间/烧粮/流言/断道/爆破/策反/诈降）对玩家形同虚设。

- 重构：从 `plot()` 中拆出结算核心 `resolvePlot(m, fid, foe, pick, plotKey, q, push)`，负责成败判定与七式效果，AI 的 `plot()` 与玩家的新入口共用同一套结算，不分裂成两套平行逻辑。
- 新增 `MapUI.openPlot()` / `openPlotPick()`：玩家先从接壤的敌对势力中选一家下手（按敌对度排序，标注智力差与可用计谋数），再从可用的计谋式中择一（智力差不足的式子照常展示、标灰并注明门槛），确认后耗 2 道军令结算，与 AI 同一套胜负公式和效果强度。

### 二、自选武将卡片再梳理

- 去掉「🎭 角色详情」按钮，头像本身即入口（`#mh-avatar-btn`，点击直接打开角色详情）。
- 身份行（头像+姓名）右侧新增纵向按钮列 `.mh-side-btns`：在野/仕官仍是单个按钮（投效/自立门户），自立后变为两个——**人事**、**计谋**（对应第一项新入口）。宽屏媒体查询里原先"所有 `.cup-go` 拉伸满宽"的规则单独排除 `.mh-side-btns`，避免在平板宽度下把两个小按钮拉成两条整行。

### 三、城建弹窗行内布局精修

原布局把"现状 · 下一级预览 · 花费"全塞进一个 `<small>` 标签，字数一长就被行高裁切、下半截完全看不见。改为两行结构：标题行（图标+名称+**放大字号的等级数字**+★☆星级+同行右侧的当前效果，一行不换行，效果文字过长时省略号截断而非挤压换行）；标题行下方另起一行，专门、完整地放"升级后效果 + 花费"与操作按钮，不再共享空间。

### 四、庆功宴改为按次数使用

原「庆功宴」只耗 1 道军令即可无限次使用，与其设计初衷（战后犒赏三军）不符。新增 `m.feastCharges` 计数：

- **边境大战/攻城得胜**（`applyBorderWarOutcome` 判定 `winnerFaction === m.playerFaction` 时）+1 次；
- **武将世界杯**夺冠或"决赛止步"（屈居亚军）各 +1 次；
- **武将大会**（每月 15 日）冠军或亚军是玩家本人时 +1 次。

`人事` 面板的庆功宴按钮显示剩余次数，次数为 0 时按钮标灰、点击提示"尚无庆功由头"；每次使用扣 1 道军令 + 1 次庆功次数，两者缺一不可。

### 五、官位展示与自立门户旧职清理

**根因**：`declareIndependence` 把团队成员并入新势力时（`joined.forEach`）只改了 `generalFaction`，未调用 `Loyalty.stripRewards` 清除其在原势力挂着的官位/封地/结义——而 `Rewards.postsHeldBy(m, fid)` 判定"某人是否持有本势力官位"只看 `generalFaction` 当前是否等于 `fid`，不检查这个官位到底是谁封的。团队成员带着旧官位空降新势力，白占玩家新势力的官位/封地席位（同一批人在"随军出征时正常倒戈"的 `followers` 分支里其实早就有这行清理，唯独"团队直接并入"这条路径漏掉了）。

- `declareIndependence` 的 `joined.forEach` 补上 `Loyalty.stripRewards(m, g.id)`，与 `followers` 分支保持一致。
- `AllGenUI`（全部武将列表）在"势力"列后新增"官位"列；`showDetail`（武将详情弹窗）的效力势力行同步追加"官位 ×××"。
- `人事 → 封官` 弹窗新增"本势力官位一览"，列出现任者姓名与官位，不必凭记忆猜谁还没有官身。

### 六、势力金库与自选武将金币合一

**根因**：自立门户后，`FactionGold`（势力金库）与 `Bond.gold()`（自选武将私人钱包）本该是同一笔钱——势力"钱庄"收上来的税就是主公的家底，募兵/城建也历来直接花 `Bond.gold()`——但两本账各记各的，`FactionGold["_player_"]` 只由 `tickAll` 的日进单向累加，从无一处会从中支出（`FactionAI.tick` 跳过 `_player_`，玩家自己的花销也不走这本账），形同一本谁都不看的死账，`全部势力` 列表里显示的"金库"数字与玩家实际能花的钱完全脱节。

`FactionGold.get/set/add/spend` 对 `fid === "_player_"` 统一转发到 `Bond`：势力金库日进直接进腰包，`人事`/`全部势力`/详情弹窗等一切读取点自动打通，无需逐处修改。`declareIndependence` 的开国启动资金也相应从 `set(600)`（会清空玩家原有存款）改为 `add(600)`（在原有存款上追加）。

### 七、个人名声与势力威名并存

**根因**：`Campaign.addFame(n)` 自立后原样把名声"改道"计入威名（`FactionFame.add`），个人 `m.fame` 自此完全冻结、不再更新，导致角色卡片里干脆不显示这项数字——用户误以为"没了名声，行动力还怎么涨"（实际行动力上限走的是 `effFame` 换算的威名，机制本身没坏，但个人名声这个数字确实从此销声匿迹）。

`addFame` 改为：自立后 `m.fame` 依旧按原公式继续累加（不再冻结），`FactionFame` 额外入账——两条线并行不悖。角色卡片声望行相应改为「🏯 威名（数值） · ⭐ 名声（数值）」两项并列展示。

**验证**：`node --check` 全过；Playwright 专项验证——通过真实 `declareIndependence()` 流程（而非直接改字段）验证团队旧官位清理、开国资金追加、金库与个人金币互通（`FactionGold.add/get/spend` 与 `Bond.gold()` 结果一致）；`openPlot`→`openPlotPick` 完整点选一次计谋并验证军令正确扣除；庆功宴 0 次时按钮标灰且点击无效，授予 1 次后可用且用后归零；全部武将列表与详情弹窗官位列正确显示；城建弹窗截图确认新行内布局无截断；360 天模拟与边境战全流程回归均无异常。

## 五十四、六项界面可读性与信息密度修复：城建配色、身份卡重排、官位配色、总览表冻结列（第五十一轮）

### 一、城建弹窗对比度与紧凑布局

**根因**：每条设施行原用深色半透明叠层（`background: rgba(0,0,0,.28)`）铺在整个弹窗的浅色纸质底（`.result-card` 用 `var(--paper)` 米黄底）之上，叠出来的实际背景是一种浅灰棕色，而设施名用的却是浅色 `var(--paper)` 文字——浅底配浅字，对比度不足。参照弹窗里同样风格的铁匠铺（`.smith-forge`/`.buff-btn`）与集市货摊（`.item-card`）：它们统一走"浅色渐变底 + 深色/暗金文字"，对比度天然达标。

`.bld-row` 改为与 `.buff-btn` 同款浅色渐变底（`linear-gradient(135deg, #fff8ea, #f0debb)`）配 2px 描边；设施名 `.bld-name` 改为深红加粗、字号从 14px 提到 18px（与等级数字同大）；等级数字、星级、效果说明文字统一换成深棕/暗金等在浅底上可读的颜色。同时收紧各行内边距（10px→5px）、行间距（8px→4px）、说明文字行距，并给本弹窗专属的描述行新增 `.bld-desc` 类（外边距、字号进一步收紧），6 项设施在常见屏幕高度下一屏放完、无需再滚动。

### 二、用户信息卡片改为左右两栏 + 底部并排大按钮

原布局把身份行、声望行、资源行纵向堆成一列，头像旁只挂了两个纵向小按钮，信息密度低且"人事/计谋"按钮存在感弱。改为：左栏保持头像+姓名+等级不变，其下新增身份行（在野/仕官/当主）；右栏纵向堆叠三行——「⭐ 名声 · ⚡ 行动力」「🏯 威名 · 📜 军令」（仅自立当主时才有威名一说，仕官状态下这行退化为单独的军令）「💰 金币（日增约 X）」；两栏下方贯通一行大按钮（自立后为人事/计谋各半，在野/仕官为单个满宽按钮）。

新增 `MapUI.dailyGoldGain(m)`：自立当主时累加 `FactionGold.income(m, "_player_")`（势力钱庄税收，因与私人钱包合一已直接体现在金币数字里，此处单独算出来只为展示"日增"这一估算值）；再遍历名下产业，凡「已委掌柜」（`est.manager != null` 且未被查封）的一律累加 `Estate.dailyRate`——掌柜代收不问身份，仕官/在野状态下置办的产业一样计入。

### 三、武将详情：官位配色与「本势力」文案精简

`showDetail` 的效力势力行原把官位名与"本势力"字样都硬编码成 `var(--cn-gold)`（浅金色），而这一行所在的 `.bond-box` 是浅色半透明底，浅金字在浅底上同样看不清。去掉两处内联颜色，官位名交还给 `.bond-line b` 的默认深红配色（该选择器早已存在，与友谊行的加粗数字同款，天然可读）；「· 本势力」三字直接删除——是否本势力，武将卡片顶部的头像框配色与国别点已经足够说明，无需再赘述文字。

### 四、庆功宴按钮不可用时说明文字改回常规配色

`.menu-btn.low small` 原把说明文字强制改成 `var(--cn-red)`，而按钮本身是深红渐变底，红字落在深红底上同样难以辨认。删掉这条颜色覆盖规则，不可用时的说明文字回退到 `.menu-btn small` 的默认暖白色（`#f0dcae`），可用性状态仍靠按钮整体的 `opacity: .62` 变灰来提示，不必再靠一段几乎隐形的红字。

### 五、全部城市/全部武将/全部势力：首列冻结

三张总览表列数普遍偏多（全部武将一张就有 16 列），横向滚动后最左侧的城市名/武将姓名/势力名会滚出视野，看右侧数值时对不上是哪一行。新增通用规则：`.db-table th:first-child` / `td:first-child` 一律 `position: sticky; left: 0`，配深色不透明底（与页面底色同源的渐变）避免滚动时露出下层内容，并加一道右侧投影与后续列区分开；`row-mine`（玩家所在城/武将/势力）行的首列额外提供加深版渐变，保持高亮效果在冻结列上依旧可辨。三张表首列所用的实际单元格类名不完全一致（`AllGenUI`/`AllCityUI` 用 `dt-name`，`AllFacUI` 用 `allgen-city`），故规则统一按"首个单元格"匹配而非按类名，一次覆盖三处。

### 六、全部武将一览表新增「将魂」「携带宝物」两列

`AllGenUI` 原表只展示六维数值与友谊/官位/忠诚等状态数据，看不到武将的将魂流派与身上宝物——查阅这两项此前只能逐个点开详情弹窗。新增两列：「将魂」直接复用 `Skill.tag(hg)`（与武将图鉴编辑页同款展示，含名将专属高亮），置于「评级」之后；「携带宝物」遍历 `Armory.itemsOf(g.id)`，按稀有度配色显示每件宝物的图标+品级简称（普/精/稀/传），并把全名与品级放入 `title` 悬浮提示，未佩戴任何宝物时显示"—"。

**验证**：`node --check` 全过；Playwright 专项验证——通过真实 `declareIndependence()` 流程搭建自立当主场景，截图核对城建弹窗单屏不再需要滚动（`scrollHeight === clientHeight`）、身份卡左右两栏与底部并排大按钮的实际渲染、庆功宴按钮 0 次庆功时说明文字计算色值确认非红色、武将详情效力势力行确认官位不再内联金色且无"本势力"字样；三张总览表分别核对首列 `getComputedStyle(...).position === "sticky"`；全部武将表表头确认新增「将魂」「携带宝物」两列且样例行正确渲染武将技能标签与宝物品级颜色。

## 五十五、计谋自由化、资源行重排、以及两处出征相关的真实回归修复（第五十二轮）

本轮前两项是体验调整，后三项是**根因排查后确认的真实逻辑缺陷**——其中「出阵阵型残缺」与「攻城武将不可操控」两处，追根溯源竟出自同一段代码：`declareIndependence`（自立门户）把「团队」概念并入「本势力麾下」后，两处仍在读取早已作废的旧字段，属于历史重构遗留的接口不一致，不是这次新写出来的 bug。

### 一、计谋不再受智力门槛限制

**根因**：`openPlotPick` 原先对七式计谋逐一判定 `adv >= p.adv`（智力差需达到该式门槛才可点选），门槛外的三式（爆破/策反/诈降）在我方智力低于敌方时直接置灰不可选。但 `resolvePlot` 的成算与效果强度公式本就对负值智力差有兜底（`chance` 封底 15%、`strength` 的智力加成用 `Math.max(0, adv)` 卡零），机制上完全没有"智力差为负就不能用"的必要性——门槛只是 UI 层多加的一道限制，反而把"知己知彼，虽处劣势也要放手一搏"的主公亲政体验堵死了。

去掉 `can`/`disabled` 的门槛判定，七式一律可选，按钮说明文字统一显示"约 X% 成算"（不再有"智力差需 ≥X"的拦阻提示）；`openPlot` 的势力列表页同步把"可用 X/7 式"改为"约 X% 基础成算"。**注意**：此项改动只影响玩家亲自施计的入口（`MapUI.openPlotPick`），`FactionAI.plot`（AI 势力的自动施法）仍保留原有门槛判定，AI 难度/行为不受影响。

### 二、行动力与军令上移至操作按钮正上方，字号加大

原布局把行动力并入"名声"一行、军令并入"威名"一行，混在声望/资源信息里不够醒目，而这两项恰是每天都要精打细算着花的硬通货，理应比名声/金库更抓眼球。改为独立一行 `.mh-stat-line`（17px、加粗），紧贴在人事/计谋操作大按钮的正上方——离行动力/军令最近的就是要花掉它们的按钮，动线上更顺。

### 三、出阵阵型残缺（真实 bug）：`resolveBorderWar` 把"麾下全军"错认成"未点选的团队成员"而集体拒战

**现象**：自立当主后发起出征/迎战边境战，无论麾下有多少已现身武将，出阵阵型十有八九只有前锋、左翼两条线有人（甚至只有主角一人），中军/右翼/后路形同虚设。

**根因**：`resolveBorderWar` 里"自选出战"的排除逻辑写着——`未勾选的团队成员一概不派，其余（非团队）已现身武将由候选池随机补满`——这条规则是为仕官/在野期设计的：那时 `Bond.myRoster()` 只返回玩家亲自招募的一小撮"团队"，与"其余武将"泾渭分明，理应让玩家对亲信团队有勾选去留的自主权，其余大路武将自动随机凑数。但自立当主后，`Bond.myRoster()` 的语义已经改写为"本势力麾下全部已现身武将"（见 `myRoster` 注释：团队并入势力，不再单列名单）——`resolveBorderWar` 里的 `teamIds`（用来判定"谁是团队"）却还在原样调用这个函数，于是当主之后麾下几乎人人都被当成"未点选的团队成员"一并剔除，候选池随机补位的兜底名额 `rest` 因此永远是空的，出征十有八九只剩你亲自勾选的寥寥数人上阵。

修复：新增 `isLordWar = heroFaction === "_player_"` 判定，当主亲征时 `teamIds` 直接置空——当主麾下诸将本就直接听你号令，不需要像仕官期的私交团队那样逐一点选才肯到场，理应任由候选池随机补满五线十将。Playwright 用 16 名已现身武将实测：修复前出阵仅 1 人（只有手动勾选的主角本人），修复后正确铺满五线共 10 员。

### 四、攻城/野战武将不可操控（真实 bug）：`controllable()` 死认早已清空的 `Bond.data.team`

**现象**：自立当主后每逢阵前斗将，无论点选谁出阵应战（含主角本人），一律被判定"自动接战"，玩家全程只能旁观，找不到任何手动操作单挑的入口。

**根因**：`FieldBattle.controllable(g)` 判断某将能否被玩家亲自操控斗将，写死只认 `Bond.data.team.includes(g.id)`。但 `declareIndependence`（自立）明确会执行 `Bond.data.team = []` 且此后永不再填（团队已并入势力，见上一条），也从未把 `-1`（主角自己）纳入过这个数组——这意味着自立之后 `controllable()` 对任何人都恒为 `false`，阵前斗将从此全面沦为自动接战，玩家亲自出征却连自己的爱将、甚至自己的分身都指挥不动。

修复 `controllable(g)`：① 主角本人（`g.id === -1`）任何时候都可操控——不该要求玩家把自己也"招募进团队"才能打自己的仗；② 当主状态下改按 `generalFaction[g.id] === "_player_"`（本势力麾下）判定，而非早已清空的 `Bond.data.team`；③ 仕官/在野期仍按原逻辑走 `Bond.data.team`，不受影响。Playwright 实测：修复前主角与麾下武将 `controllable()` 均为 `false`，修复后主角恒真、10 员麾下武将全部返回 `true`。

### 五、驻军募兵改为滑杆自选数量

原「募兵」是一键尽量补满至上限的单一按钮，金币富余时点一下就把整城兵额瞬间拉满、钱包同时清空，无法按需分批调度金库。改为与出阵比例滑杆同一套交互：新增 `Garrison.recruitN(m, cityId, n)`（按指定数量募兵，超出"兵额尚余"或"金币买得起"两者中较小者的部分自动截断），驻军面板换成滑杆（默认拉到当前买得起且兵额也容得下的最大值，可自由下调），实时显示"募兵 N 人・耗 X 金・募后驻军 Y/上限"。原 `Garrison.recruit`（一键补满）函数保留未删，供其余潜在调用方延用。

**验证**：`node --check` 全过；Playwright 专项验证——`openPlotPick` 七式按钮均不带 `disabled`；`resolveBorderWar` 用 16 名麾下武将实测出阵人数从 1 人修复至 10 人、五线阵型正确铺满；`FieldBattle.controllable()` 对主角与麾下武将均正确返回 `true`；驻军面板滑杆拖至半量后点击募兵，实际到账数量与金币扣除精确匹配滑杆显示值（募得 33,000 兵、耗 16,500 金，与拖拽时的实时标签完全一致）；身份卡截图确认行动力/军令新行渲染在人事/计谋按钮正上方且字号加大。

## 五十六、宝物库精简、招揽去重、以及一次「查无实据」的繁荣度复核（第五十三轮）

### 一、宝物库仓库去除「出售」按钮

未装备宝物原有「拆解」「出售」两个按钮并排，出售按售价五折回收金币且宝物回流集市。去掉出售入口，只保留拆解——连带删去只此一处调用的 `Armory.sellItem`/`SELL_FACTOR` 与 `.ic-sell` 样式，避免留一个再无引用的死函数。**注意**：城市集市里按当地行情结算的「行商贩卖」（`Armory.tradeSell`）是完全独立的另一功能，不在本次改动范围内，原样保留。

### 二、仓库增加按类别／等级／装备状态筛选

新增 `ArmoryUI.renderStock` 顶部的三个下拉筛选：类别（兵器/坐骑/书籍/服饰/奇珍）、等级（普通/精良/稀有/传说）、装备状态（全部/已装备/未装备）。**未鉴定的「神秘宝物」不受类别与等级筛选影响，一律放行**——它的真实类别/等级本就还没向玩家揭示，若被筛选条件悄悄吃掉或露出，等于用"筛选后还在不在"反向泄露了不该提前知道的信息；装备状态筛选不受此限，未鉴定宝物必然无法装备，天然归入"未装备"一档。

### 三、自立当主后，「纳入麾下」与「招揽」不再同时出现

**根因**：自立当主后，对某位友谊满 300 的其他武将，武将详情弹窗会同时提供两个按钮——友谊区的「纳入麾下」（`bond-recruit`，友谊满即可用固定金额直接改其效忠，必然成功）与效力势力区的「招揽」（`bond-persuade`，按成算掏钱赌一把，随时可用但会失败）。两者最终效果完全相同（都是把 `generalFaction[gid]` 改成 `"_player_"`），前者却是必成的白拿捷径，把后者精心设计的"友谊/忠诚/名声共同决定成算，会失败"这套风险机制彻底架空——只要肯攒到满友谊，永远走前者就好，「招揽」形同虚设。

去掉当主状态下「纳入麾下」按钮的渲染与对应处理逻辑，招人一律走「招揽」一条路（其成算本就会随友谊满格顶到较高值，只是不再保证 100%）。**仕官/在野期的「招募入队」不受影响**——那招的是与效忠对象无关的私人团队，跟"改换门庭"是两回事，不存在重复。

### 四、城市繁荣度星级核验：核实为设计如此，非计算错误

**排查结论**：用户反馈"城建 6 项设施 2 项 10 级、4 项 6 级仍未到 5 星繁荣，怀疑算法不统一"。经 Playwright 用真实 `Buildings.build`（与玩家点击按钮完全同一代码路径）在一座初始 3 星的城池上实测：2 项修满 10 级 + 4 项修至 6 级，最终城池停在 **4 星**、朝 5 星方向已积 136/300 点——与手算完全吻合，并非算法在不同界面各算一套。

繁荣度门槛设计的原意是"六项城建全部封顶（约 594 点）大致才够摸到 5 星"（见 `Prosper.STEP_BY_LV` 注释），2 项封顶 + 4 项六成进度只攒下 306 点，仅完成六项全满所需投入的一半略多，尚未封顶的四项城建（6→10 级）还能贡献不少剩余点数——继续修下去或辅以产业投资/攻城掠地等捷径即可达标。**结论：非 bug，未改动任何代码**，如觉得当前门槛偏高、想让 5 星更易达成，这是一处可以另行讨论的数值平衡问题。

**验证**：`node --check` 全过；Playwright 专项验证——仓库页塞入不同类别/等级/装备状态的宝物，确认已装备宝物不再显示任何按钮、未装备宝物只剩「拆解」、三项筛选逐一验证生效（类别=兵器时只剩 1 件，装备状态=已装备时只剩 1 件）；对一名友谊满格的在野武将打开详情，确认 `#bond-recruit` 不存在、`#bond-persuade` 存在（约 82% 成算）；繁荣度用真实建筑升级函数复算一遍，数值与手工推算的 136/300 完全一致。

## 五十七、野战演武「挥军破阵」全景战场画布（第五十四轮）

### 背景

野战演武原分三段：排兵布阵（DOM 九宫格文字卡片）→ 阵前斗将（直接复用单挑的像素画布，早已有画面感）→ 挥军破阵（五条战线纯文字：姓名 + 士气数字 + 一根红蓝色条 + 💥 + 滚动战报），三段里唯独「挥军破阵」还是文字堆砌，与其余两段的画面感明显割裂。本轮针对性地给挥军破阵补上一整块全景战场画布，并顺带给排兵布阵加一块阵型预览图。

### 一、FieldFX 全景战场画布：五线连成一整幅战场

新增独立渲染模块 `FieldFX`，沿用 `Duel`（单挑）模块的像素堆叠美术语言（`ctx.fillRect` 方块拼装、`imageSmoothingEnabled=false`），但服务于"多线拉锯"而非"两人对决"：

- **五道横带**：按 前锋/左翼/右翼/中军/后路 纵向排开，每道带子独立铺一层地面（按地形——平原草纹/山道岩块/河畔水波——微调点缀与色调），带首居中标出战线名。
- **双方兵列**：每带内按当前兵力用平方根压缩映射成 1~8 个"小兵方块"（含一杆枪的像素形），双方分据带子两端隔着交锋点对推，交锋点位置即战线兵力占比（原 `laneW()`，此前只用来算一根 CSS 色条宽度，现改画进 canvas）；每侧另画一枚缩小的骑将剪影押阵在最前排，稍具"主将带兵"的辨识度。
- **交锋火花**：本模块自行逐帧比对上一帧与本帧的 `myTr`/`foeTr`，一旦发现降了就在交锋点炸一下火花——不需要战斗结算代码到处插一句"通知一下画面"，解耦得更彻底。
- **战线告破**：败方兵列淡出 + 向己方后方位移，带内浮现「突破」/「失守」字样，与原有的 DOM 状态标签相呼应但不重复。
- **两端军旗**：整个画布左右两端各插一杆己方/敌方军旗（旗面按刻风中摆动），标出这是"谁在打谁"。

信息条同步瘦身：`laneHtml()` 去掉原来的红蓝色条 `.fb-push`/`.fb-mass`/`.fb-clashpt`（推挤视觉已交给画布），只保留姓名·士气·战线名一行 + 双方兵力数字一行，供需要精确读数时查看；点击战线下军令的命中区不变。

### 二、军令与将魂发动的画面反馈

`useOrder()` 的擂鼓/鸣金/火攻/计谋/奇袭五种军令、`fireWarSkill()` 的将魂主动技能发动、`tick()` 里敌军 AI 的擂鼓/施计，各自在触发的瞬间调用 `FieldFX.burst(laneK, kind)` 往该战线追加一段限时特效（金环扩散/盾墙浮现/火苗窜动/烟雾腾起/快马掠过/白光爆闪，均在 0.4~0.9 秒内自然衰减），不越俎代庖参与任何数值判定。

### 三、排兵布阵阶段的阵型预览画布

`renderDeploy()` 新增一块小画布：五条战线各一道迷你条形图，按该线双方已排武将的（武力+统帅）合计画一条左右分推的红蓝条，直观预览"照此阵容开打，各线大致谁占优"——每次切换阵形或对调武将站位时随 `renderDeploy()` 重绘一次，不需要独立动画循环。

### 四、生命周期与自动推演

两块画布固定挂在 `index.html` 的 `#screen-field` 内（`#fb-deploy-canvas-wrap`/`#fb-canvas-wrap`），不随 `#fb-content` 的 `innerHTML` 重写而销毁重建——`useOrder()` 触发的特效因此不会被同一帧里紧接着的信息条重渲染清空。`startClash()` 调用 `FieldFX.mount()` 独立起一个 `requestAnimationFrame` 循环持续读取 `FieldBattle.lanes` 作画，与驱动战斗数值推进的 `setInterval` 完全解耦；`abort()`/`finish()`/离开 `#screen-field` 时统一 `stop()`，不留悬空的动画循环。**主角未被抽中亲历、全自动推演的战斗同样会挂载并播放这块画布**（`startClash()` 在判断是否 `auto` 之前就已挂载），不再是"旁观即瞬间出结果"。

### 五、一处 flex 布局踩坑记录

两块画布的容器最初只写了 `width` 与靠 canvas 自身宽高比反推的高度，结果实测整块塌缩成 2px 高的一条缝——根因是 `#screen-field` 作为 `flex-direction: column` 且 `overflow-y: auto` 的容器，其子元素默认 `flex-shrink: 1` 且 `min-height: auto` 在有 `overflow` 的容器内会解析为 0（而非内容的最小尺寸），当同级的 `#fb-content`（塞满武将卡片、大量撑开高度）挤占了几乎全部可用空间时，"内容单薄"的画布 wrap 就被直接压缩到 0。修复：容器改为显式 `height`（与 JS 里的逻辑缓冲高度一一对应）+ `flex: none`（不参与收缩），canvas 本身 `width/height: 100%` 直接填满，不再依赖画布固有宽高比反推布局。

**验证**：`node --check` 全过；Playwright 专项验证——排兵布阵截图确认预览画布五道色条正确渲染；挥军破阵截图确认五线画布（兵列/骑将/地形/军旗）与信息条数字一致同步；点击「擂鼓」等军令后截图确认对应特效（金环/火焰/快马等）出现在目标战线；直接调用 `FieldBattle.beginExternal({observe:true})` 模拟"主角未出战全自动推演"场景，等待其推进到挥军破阵阶段后取两帧 `canvas.toDataURL()` 对比确认画面确实在动；弱旅对阵强敌打到全灭，截图确认告破战线正确显示"失守"字样与兵列淡出；battle 结束后核实 `FieldFX.raf` 归零、无遗留动画循环、控制台无报错。

## 五十八、边境战选将体验、对马岛驻军死局、AI 从不主动犯境——三处真实缺陷修复（第五十五轮）

### 一、边境战出阵选将面板：滚动重置与无处搜索

**现象**：自立当主后麾下武将动辄数十人，边境战/出征弹出的选将列表每点选一人，整个弹窗就整体重渲染一次，滚动条随之弹回顶部——选到列表后段的武将，等于每选一人就要重新滚一遍，人越多越痛苦；列表本身虽已按评分降序排列，但面对几十号人光靠滚动找不到想找的具体某位。

**根因**：`renderBorderWarPicker` 里 `.bw-pick` 的点击回调直接调用 `this.renderBorderWarPicker(m, edge)` 整体重渲染弹窗——`openOverlay` 是整段替换 `innerHTML`，`.buff-list` 的滚动位置自然归零。

修复：点选武将不再整体重渲染，只切换被点按钮自身的 `active` 高亮与顶部"已选 N 员"计数字样，滚动位置纹丝不动；新增一个按姓名过滤的搜索框（`#bw-search`），人数一多不必硬滚，直接打字定位，输入清空即恢复完整列表。列表数据与渲染函数拆成 `entries`/`pickBtnHtml` 复用，搜索时只重绘 `.buff-list` 内部，不动弹窗其余部分与滑杆状态。

### 二、对马岛驻军死局：一年多无法跨海推进

**现象**：中日双方无论谁攻下对马岛，占岛之后都无法继续借道攻略对岸——游戏进行一年多，对马岛始终是双方谁也过不去的死胡同。

**根因**：`Garrison.tickAll(m)`（宿营时全图城池驻军回复）显式排除了 `side === "sea"` 的城池，对马岛因而**只出不进**——首次读取时按满编初始化，但凡被攻占后用其驻军杀奔对岸一次，驻军归零后永不回补，且 UI 层的驻军按钮（`garrisonBtnHtml`）、募兵面板（`openGarrison`）、出征目标（`sortieTargets`）三处又都额外把"海路中转站"排除在外，玩家占了对马岛之后连自主募兵找补的入口都没有——四道口子叠在一起，对马岛占领后立刻变成一座既无法回血、也无法主动补给、更无法作为出征跳板的死岛，中日双方就此在这座桥头堡前僵持不下。

修复：`Garrison.tickAll` 改为全图城池一视同仁回复驻军（对马岛虽无法筑城墙，但驻军回补理应与其余城池同等对待）；`garrisonBtnHtml`/`openGarrison`/`sortieTargets` 三处一并去掉"海路中转站不可用"的限制——占领对马岛后同样可以募兵回补、可以在此发起出征，真正成为可用的跨海桥头堡。全部城市总览表与城池详情弹窗此前对海路中转站隐藏兵力数字（显示"—"），一并改为如实显示。

### 三、AI 从不主动进攻玩家现效力的势力

**现象**：自立当主（或仕官投效）之后，接壤的 AI 诸侯无论多么兵强马壮、宿怨多深，从未主动打过玩家一次——玩家可以毫无后顾之忧地一路吞并四邻，边境战原本"有攻有守"的张力单方面消失，只剩玩家单方面出征。

**根因**：`FactionAI.war()`（诸侯日常内耗、即时结算、无 UI 的自动战斗）里显式把 `m.playerFaction` 从可攻目标中剔除（`f.foe !== m.playerFaction`）——这本是合理的兜底，避免玩家的城池在毫无界面、毫无还手余地的情况下被静默攻陷；但月末国战只覆盖**跨国**边境，同国接壤的 AI 邻居从此对玩家形同透明，游戏里再没有任何机制会让 AI 主动打玩家一下。

修复：不改动 `war()` 本身的即时结算逻辑（避免打乱已校准过的 AI 对 AI 内耗节奏），另加一条专打"接壤玩家势力"这一缺口的每日小概率检查 `MapUI.checkFactionRaid`（触发概率 10%，接入宿营 `followUp` 流程，紧随月末国战之后、夜袭之前）：按"敌方攻我方之利"（`warStrength(敌方) - defStrength(我方)` + 敌对度）从接壤真实势力中择一犯境，敌方毫无胜算（评分 < -400）时不会以卵击石、刚交手过的势力（`weary`）当天也不会重复来犯；一旦触发即走与玩家主动出征完全相同的正式流程——`openBorderWarPicker` 弹出选将/出阵比例滑杆，玩家可亲自应战，胜负同样影响城池归属，不再是"AI 永远不敢先动手"的单向游戏。

**验证**：`node --check` 全过；Playwright 专项验证——选将面板塞入 20 名麾下武将模拟长列表，滚动到底部后点选武将，`scrollTop` 点选前后完全一致（1272 = 1272），搜索"关"字精确过滤出「关羽」一人；对一场真实边境战（成都 vs 汉中）通过 `checkFactionRaid` 直接触发 AI 犯境，确认弹出的选将面板 `isOwnFactionWar` 判定正确（含出阵比例滑杆）、toast 提示"张鲁举兵来犯！"；对马岛驻军按钮/募兵面板/出征入口三处逐一核实不再因"海路中转站"被拦截。

## 五十九、野战演武·棋盘对垒（GridBattle：方格回合制取代旧版主入口，第五十六轮）

旧版「野战演武」（五线实时推兵，`FieldBattle`）整体改为回合制方格棋盘（`GridBattle`），作为小游戏合集「野战演武」的新主入口；旧版原样保留，改名「野战演武·经典版」另开一个菜单入口，且边境战事等角色扮演内部调用（`FieldBattle.beginExternal`）完全不受影响、未作任何改动。以下公式均只适用于 `GridBattle`。

### 一、棋盘与单位

- 12 列 × 9 行方格棋盘；我方部署区为底部 2 行，敌方为顶部 2 行；三种地形模板（平原/山道/河畔）随机生成，中线纵向打通一条道路，双方各设一处大营（底/顶行中点）。
- 地形：平地（移动消耗 1，无加成）、丘陵（消耗 2，守方防御 ×1.25）、河道（消耗 3，站立其上者本回合不可发起攻击——"半渡而击"）、道路（消耗 0.5）、大营（消耗 1，守方防御 ×1.4，回合末驻守其上者回复 8% 体力）。
- 单位属性（一将一格）：`兵力(HP) = max(100, round(统帅×100))`，`攻击 = 0.65×武力+0.35×统帅`，`防御 = 0.65×统帅+0.35×武力`，`移动力 = clamp(3,5,round(统帅/28))`。持「volley」将魂者获远程 2 格且不受反击，其余近战 1 格。

### 二、阵形（第五十七轮起改为逐将独立，见第六十节）

- 阵形不再是"全军统一选一种"，而是**每员武将各自持有**一种阵形：锥形/鹤翼令该部前 3 回合攻击 ×1.2，方圆令该部前 3 回合防御 ×1.25，雁行令该部前 3 回合每回合对身周 2 格内敌方各自蚀其士气 -1.5。我方默认全给「锥形」，部署页可逐将点选切换；敌方随机各配一种。
- 阵形相克（锥克方圆、方圆克雁行、雁行克鹤翼、鹤翼克锥形）：加成期内，攻方阵形克制守方阵形则再 ×1.1（比较双方各自的阵形，而非"全队统一阵形"）。

### 三、战斗结算

```
折损 = 220 × (攻方攻击×被动技能攻击乘区×阵形攻击乘区×阵形相克乘区×攻方全队士气乘区)
           ÷ max(1, 守方防御×被动技能防御乘区×地形防御乘区×阵形防御乘区)
           × random(0.85, 1.15)
全队士气乘区 = 0.7 + 士气/100×0.6
```
守方选择「待命」（原地不动结束行动）再享 ×0.85 减伤（即 +15% 防）。命中且守方未阵亡、攻方非远程、守方未处半渡地形时触发反击，反击折损按上式互换攻守方角色再 ×0.5 计算。单位兵力归零即溃退出局：溃退方全队士气 -10，另一方 +6。

### 四、被动将魂（网格化，仅覆盖战场侧既有类型）

`school-tong`/`adamant`/`tiger` 提供防御乘区 ×1.1/×1.2/×1.15；`sanada` 提供攻防各 ×1.08。其余单挑侧专属被动（如护体、二天一流等）本期未接入网格战斗，留待后续分期评估是否需要网格化改写。

### 五、军令（斥候/擂鼓/火攻）

`军令道数 = clamp(2, 6, round((存活武将智力均值-30)/12))`，每回合开始按当前存活武将重新核算（折损越重、调度越吃紧），deploy 阶段也可动用。斥候（1 令）：揭晓敌军阵形。擂鼓（1 令）：全队士气 +8。火攻（1 令）：须目标所在地形为丘陵，或军中有「firemaster」被动持有者（不限地形），命中造成 400~800（后者翻倍）兵力折损；敌方 AI 同享一套军令，40% 概率/回合尝试擂鼭或火攻。

### 六、主动将魂 →「战法」按钮

冷却 `clamp(2, 4, round((14-(统帅-40)/7)/3))` 回合，冷却完毕后由持有者所在回合内主动点击发动（占用本回合行动）；`volley` 已落实为远程被动，不再重复出现战法钮。效果：`school-wu` 对相邻敌方额外造成 1.6× 攻击结算的伤害；`school-ti`/`school-zheng` 自部回复 15~18% 兵力上限；`school-mei` 全队士气 +8；`awe`/`discord`/`roar` 敌方全队士气 -10；`infiltrate` 对随机一路敌军直接造成 700~1200 折损；`dualblade` 对相邻目标额外发起一次攻击结算；`tempo` 令一名已行动的相邻友军重获本回合行动。AI 一方冷却完毕后 40% 概率优先使用战法而非移动攻击。

### 七、单挑（原「挑发」改名，第五十七轮起）

我方任意未行动单位可对相邻敌方单位发起「单挑」，是否应战取决于双方魅力/智力差：`P(应战) = clamp(0.25, 0.85, 0.5 + (发起方魅力-应战方智力)/200)`。应战则复用既有单挑引擎真实打一场（`startTeamDuel`）：败方兵力折半（`hp = max(1, round(hp×0.5))`）、败方士气 -20（`dropMorale`，跌破 0 会直接触发该部溃退），额外 12% 概率使败方这一部当场溃散退场。第五十七轮起该 12% 结局只影响败者本人这一部，不再是"全军瞬间崩溃"（见下节九）。此机制专属玩家一方主动发起，AI 本期不会主动求战。

### 八、胜负判定

一方全部武将皆已溃退（兵力或士气归零）才判负；30 回合未分胜负则按双方存兵总量多寡定胜负。夺营（占领对方大营）本期不作为胜负条件（留待后续攻城玩法评估是否启用）。

### 九、士气改为按单位独立 + 夹击 + 暴击 + 攻击演出（第五十七轮，用户实测反馈八项）

- **士气不再是全队共享一个数值**：每员武将各自维护 `morale`（0~100，初值 50）。攻方发挥取决于攻方自身士气（`0.7+己方士气/100×0.6`），不再看"全队"这个抽象值；擂鼓/军歌类战法/军令的"全队 +8"效果改为对每员存活武将各自 `+8`。溃退（兵力或士气归零）时，同袍士气各 -5、敌军各 +3（`routUnit`），由个体的溃败连锁牵动局部而非旧版"一个开关"式的整队瞬间崩溃——这也是本轮修复的核心 bug：此前一场单挑落败若命中 12% 概率，会把敌方"全队"这个共享数值直接清零，导致对方明明大部分武将尚在，战斗却已判定为「野战大捷」；per-unit 士气模型下，同一 12% 结局只会波及败者这一部。
- **夹击**（第五十七轮起改为玩家明确抉择，见第六十节）：攻方发起攻击时，若守方相邻格（除攻方本身外）另有攻方同袍存活，每员额外 +25% 伤害，封顶两员 +50%。
- **暴击**：由攻方「魅力」决定，`P(暴击) = min(0.6, 魅力/280)`，命中则伤害 ×1.7，与既有单挑引擎同一套公式；暴击时额外播放全屏微闪特效与专属音效（`AudioSystem.sfx.crit`）。
- **攻击演出与音效**：`resolveAttack` 改为异步——挥砍前倾动画（`fg-atk-anim`）→受击闪红+微震（`fg-hit-flash`）→伤害数字浮现于目标格（`fg-dmgnum`，暴击更大更金），全程配 8-bit 音效（`swing`/`hit`/`crit`）；反击、将魂战法（`school-wu`/`infiltrate`/`dualblade`）、火攻军令同样配上相应音效。演出期间以 `this.busy` 锁定棋盘交互，防止动画播放中重复点击造成的并发操作。
- **姓名完整显示**：棋子由圆形单字头像改为方形徽标，内嵌完整武将姓名（超长则省略号兜底），不再只显示姓氏首字。
- **兵力 / 士气两值随身**：单位徽标下方常驻两条细条（兵力/士气），选中后的行动条同样显示"兵 X/Y　气 Z"（原「体力」措辞统一改为「兵力」，呼应武将带的是部众而非血肉之躯）。
- **手机竖屏紧凑化**：棋盘按视口高度（`width:min(100%,54vh)`）反推尺寸而非纯按宽度撑满，banner/军令条/日志的内边距普遍收窄一档，移除原先占用大量纵向空间的整队士气进度条（改为 banner 内一行"均气 我X·敌Y"），减少一局对战中的上下滚动。

**验证**：`node --check` 三文件全过；Playwright 覆盖姓名完整显示、术语改字（兵力/单挑）、夹击伤害对比（12 样本均值随夹击者数量提升）、暴击命中率采样（魅力拉满后 40 次采样命中约六成，符合公式）、攻击全流程动画类名/伤害数字节点/音效调用计数、单挑全链路（含 12% 溃散只影响败者一部、战斗未结束、`busy` 锁正确复位）、AI 多回合稳定收敛压测（12 组地形/阵形组合各跑 26~31 回合，其中 3 组耗尽被动测试方全部单位触发正常判负，全程无 NaN/崩溃）；另行验证旧版「野战演武·经典版」与本轮改动完全隔离、不受影响。

## 六十、野战演武·棋盘对垒八项体验反馈（第五十八轮）

### 一、单独攻击 / 夹击改为玩家明确抉择

选中武将进入行动阶段后，`actionMode` 由「攻击/单挑」两态扩为「单独攻击/夹击/单挑」三态；只有当前高亮的敌方目标至少有一名**未行动**的己方同袍已贴在其相邻格时，才会出现「夹击」选项（`eligibleFlankers`）。选「单独攻击」只有选中的这一部出手，夹击加成不生效（即便几何上确有同袍贴身）；选「夹击」则调用 `resolveCoordinatedAttack`——把选中武将与全部就位的同袍依次拉入这次围攻，各自独立结算伤害与反击、各自播一遍完整的挥砍/受击/伤害数字演出、各自标记为已行动，真正在画面上看得见"数人一起动手"。夹击加成按"本次围攻中其余存活参与者数量"实时计算（封顶两员 +50%），不再是旧版单纯看几何位置的自动加成。

### 二、兵力条与士气条粗细统一

`.fg-hpbar`/`.fg-morbar` 本就共用同一条 CSS 规则（`height:2.5px`），二者在样式表层面不可能出现粗细差异；本轮连带棋子改版一并复核，未发现结构性偏差。

### 三、顶部总体兵力与均士气对比

战斗页 banner 新增一行 `兵力 我{全队现存兵力总和}·敌{…}　均气 我{avgMorale}·敌{avgMorale}`，与原有的单位选中提示分层展示——顶部看大局，选中某部看该部明细。

### 四、行动菜单浮于武将周围

原固定贴底的操作条改为 `.fg-radial`——按选中武将所在格的行列换算百分比坐标，绝对定位浮现在该棋子正下方（贴近棋盘下边缘时改浮在上方），水平方向按列位置自动贴左/居中/贴右避免出界。菜单内为单独攻击/夹击/单挑/战法/待命五枚小图标钮，附一行"该部兵力/士气"提示。

### 五、AI 移动改为逐格挪动

新增 `findPath()`（复用移动力 BFS 的来路记录 `_bfsReach`）与 `animateMove()`：AI 单位不再瞬移到目的地，而是沿实际路径逐格移动，每格之间短暂停顿（110ms）并重绘一次，玩家看得清楚它是"走"过去的、走的是哪条路。

### 六、地形纹理与棋子阵形图标

各地形底色叠加渐变纹样与半透明地形图标（丘陵为山丘状高光、河道为水波纹、道路为原木纹理、大营为暖光晕）；空格上同样浮现该格地形图标（不放置单位时也能看出地貌）。棋子左上角新增阵形徽标（与右上角技能徽标对称），部署阶段可点击切换该武将的阵形（见下条）。

### 七、阵形改为逐将独立（详见第二节改写）

不再有"我方阵形/敌方阵形"这两个全局选择，`FieldBattle`（经典版）不受影响，`GridBattle` 每个单位各自持有 `u.form`；「斥候探阵」军令因此失去意义，本轮一并移除，军令收窄为擂鼓/火攻两式。

### 八、折叠屏/宽屏战场放大

`.fg-board-wrap` 尺寸公式由单纯"按视口高度封顶"（`min(100%,54vh)`）改为 `min(100%, 720px, 82vh)`——窄屏手机上仍主要受 100% 宽度约束（不变），折叠屏展开等宽屏设备上会显著放大棋盘（受 720px 绝对上限与 82vh 高度上限中较小者约束，而非继续被窄屏取向的高度公式束缚）。

**验证**：`node --check`/CSS 大括号配平复核全过；Playwright 端到端覆盖夹击的"单独攻击 ally 不参战 vs 夹击 ally 一并出手"对照（含 `ally.acted` 状态断言）、AI 单次行动内 `renderBattle` 多次调用与逐格坐标采样（确认非瞬移）、阵形徽标点击切换、地形图标渲染计数、390px 与 900px 两种视口下的棋盘实际渲染尺寸（截图人工核验）；回归确认旧版「野战演武·经典版」与边境战事内部调用不受影响。

## 六十一、野战演武·棋盘对垒八项体验反馈二：VS 兵力条、战报重排、环形菜单、双指缩放（第五十九轮）

### 一、顶部兵力对比改为 VS 色条

新增 `.fg-vsbar`：左右两条各占轨道一半、分别向中线对齐（`justify-content:flex-end`/`flex-start`），宽度按 `现存兵力/满编兵力总和` 的百分比填充。因两条都锚定在中线一侧，兵力越少，条的外沿就越往中间方向收缩，此消彼长一眼可辨。原来的纯文字兵力对比移入下方新增的 `.fg-matchup` 一行，同排列出双方存活武将数、均士气、军令余量，「基本对阵情况」不再局限于兵力一项。

### 二、宽屏/折叠屏铺满不留白

`renderBattle()` 输出改用 `.fg-layout`（CSS Grid + `grid-template-areas`）：手机竖屏单列纵向堆叠（对比条→对阵情况→战报→棋盘→军令一行），`@media (min-width:700px)` 起改双栏——棋盘占左侧全部宽度（`.fg-board-wrap` 撤掉宽度上限，跟随列宽撑满），战报独立挪到右侧一栏（同时放宽 `max-height` 到 `min(70vh,640px)`），不再像旧版那样棋盘固定卡在 720px 而右侧大片留白。

### 三、出手即隐藏行动菜单

`doTargetAction`/`challenge`/`useActiveSkill` 三处认领动作后，第一步先把 `selectedUnit`/`selPhase` 清空并 `renderBattle()`，再 `await` 后续的攻击/夹击/单挑动画——菜单先于演出消失，不再挡在攻击特效前面。

### 四、行动菜单按钮放大、环形排布

按钮从 30px/15px 放大到 40px/20px，且从"下方堆叠一列"改为以选中武将棋子为圆心、以 48px 半径均匀环绕一圈（`transform:translate(calc(-50% + Rcosθ), calc(-50% + Rsinθ))` 逐一定位，从正上方顺时针排开）。因棋盘边缘处环形可能溢出可视区，`renderBattle()` 渲染完成后会实测 `.fg-radial` 全部子元素（信息条/按钮/提示）的并集边界，与 `#fg-board-wrap` 的可视范围比对，超出部分原地平移拉回——即便武将站在棋盘四角，菜单仍完整可见。

### 五、战报挪至对比条与棋盘之间、新消息在下

`log()` 由 `unshift`+`pop`（旧的顶插、超限砍尾）改为 `push`+`shift`（新的底插、超限砍头），每次写入后 `el.scrollTop = el.scrollHeight` 自动滚到底部；`renderBattle()` 整体重绘时同样在渲染后补一次滚底，避免整局重绘瞬间战报又回到顶部。日志区域挪到 `.fg-layout` 的 `log` 网格区（对比条/对阵情况之下、棋盘之上），字体由 11px 增至 12.5px（宽屏栏进一步到 13px）。

### 六、点击空白处取消当前选择

`onCellClick` 未命中任何有效目标（移动阶段点了不可达空格、行动阶段点了非目标区域）时不再原地不动，而是清空选择、重新渲染，方便随手换另一员武将行动；棋盘/菜单以外的区域（顶部对比条、对阵情况、战报）另由 `renderBattle()` 结尾挂的 `root.onclick`（经 `closest(".fg-cell")`/`closest(".fg-radial")` 排除棋盘与菜单自身）统一处理点击外部取消，二者共同覆盖"点其他地方=退出当前选择"。

### 七、军令栏收窄为一行、靠右加大

底部 `.fb-orders` 拆掉了并入 `.fg-matchup` 的"军令 X·敌令 Y"文字，只留擂鼓/火攻/结束回合三枚按钮，`.fg-orders-row` 固定单行不换行、`justify-content:flex-end` 靠右，按钮内边距与字号同步放大（5px/12px → 10px/15px），更方便单手右手拇指点按。

### 八、棋盘双指捏合缩放/拖动

新增模块级 `GridZoom`（与地图页 `MapZoom` 同构但独立命名）与 `applyGridZoom`/`clampGridZoomState`/`bindGridZoom` 三个方法，照搬地图页"不使用 `setPointerCapture`，改在 `document` 上临时挂 `pointermove`/`pointerup`"的既有手法，缩放范围 1~3 倍；棋盘格与浮动行动菜单一并包进 `.fg-zoom-layer`，缩放/平移时菜单随棋盘同步移动、始终贴在武将棋子旁。`.fg-board-wrap` 加 `overflow:hidden` 作为可视裁切窗口，右下角另设 ＋/－ 按钮供无触屏设备使用；`GridBattle.open()` 时重置缩放状态，避免上一局的缩放残留到下一局。

**验证**：`node --check`/CSS 大括号配平复核全过；Playwright 端到端覆盖 VS 条双侧宽度百分比读取、对阵情况文本、战报连续写入三条后 DOM 顺序核验（旧在上新在下）、真实点击选将→再点开进入行动阶段→环形菜单出现→点击攻击目标后 50ms 内菜单已消失（`this.busy` 生效期间）、点击棋盘/菜单以外区域使选择清空、模拟双指 `PointerEvent` 捏合后 `.fg-zoom-layer` transform 中 `scale` 确有变化、390px 手机与 900px 宽屏两种视口下 `.fg-layout` 网格实际渲染结构截图核验（宽屏下棋盘列宽 594px 撑满、战报独立成右栏）；另跑 300 回合上限的 AI 对战压力测试确认新增测量/挪窗逻辑不引入异常或死循环，回归确认旧版「野战演武·经典版」与边境战事内部调用不受影响。

## 六十二、野战演武·棋盘对垒十一项体验反馈：火攻演出、地图铺满、逐次刷新、终局战报等（第六十轮）

### 一、火攻燃烧演出

新增 `playFireFx(target, dmg)`：目标格橙红闪动 + 火苗图标升起消散，棋盘整体叠一层短促暖色闪光，命中瞬间即同步该部兵力条与顶部总量（见第五条）。`tryFireAttack` 与敌方 AI 军令分支中的火攻均改为 `async` 并 `await` 这段演出，与近战攻击的挥砍/受击一套节奏。

### 二、兵力条/士气条宽度核验

实测同一棋子上 `.fg-hpbar`/`.fg-morbar` 两条轨道的渲染宽度像素级一致（同一条 CSS 规则、同为 flex 子项），并非样式错位——用户观察到的"宽窄不一"其实是两条各自反映兵力/士气两个独立数值，数值不同、填充长度自然不同。为降低这种"看起来像 bug"的误读，士气条改用独立的蓝紫色阶 `moraleColor()`（不再复用兵力的绿黄红），一眼就能分清这是两码事。

### 三、点击对比栏展开逐将详情

`.fg-matchup` 一行末尾新增「▾ 查看详情」，点击后 `matchupOpen` 置真，下方展开 `.fg-matchup-detail`——双栏（我方/敌方）列出每员在场武将的兵力/士气/攻/防，阵亡者灰显删除线；再点一次收起。`vsbarHtml()`/`matchupAreaHtml()`/`matchupDetailHtml()` 拆成独立方法，供 `syncTopBars()` 局部刷新复用，不必靠整块 `renderBattle()` 重绘。

### 四、地形图标放大

`.fg-terrain-icon` 字号由 62% 提到 92%、透明度由 .4 提到 .55，空格上的地形图标视觉上更接近撑满整个格子。

### 五、攻击命中即时刷新兵力/士气/总量

新增 `syncUnitDom(u)`（直接改该棋子兵力条/士气条的行内宽度与颜色、更新 tooltip）与 `syncTopBars()`（重绘顶部 VS 条与对阵情况两个小节点）；`playAttackFx` 在受击闪光/伤害数字出现的**同一瞬间**调用二者，而不是等一整套攻击动画（含反击、含夹击的每一次出手）全部播完才靠外层的整块重绘去更新——夹击时能看见每一次命中都让血条应声而落，不再是打完一串才一次性跳变。`dropMorale`/`gainMorale` 也顺带补上同步，覆盖军令/战法/溃退连锁等所有会改动士气的场景；`useActiveSkill` 里各条造成伤害/治疗的分支同样补齐。

### 六、战场地图自适应铺满剩余空间

`renderBattle()`/`renderDeploy()` 共用的 `#fg-content` 改为 `display:flex;flex-direction:column;flex:1`，`.fg-layout` 同样改 flex 列而非固定像素/vh 上限的 CSS Grid；除顶部对比条、对阵情况（含展开详情）、战报、底部军令这几处按内容自适应大小的区域外，`.fg-area-board`（进而 `.fg-board-wrap`）用 `flex:1 1 auto` 吃掉全部剩余空间——不论手机竖屏、折叠屏展开、桌面宽屏，同一套规则统一处理，不再需要针对某类设备猜一个固定上限。

### 七、终局战报丰富化

`finish(won, reason)` 补上此役头功（`kills`/`dmgDealt` 由 `resolveAttack`/`useActiveSkill`/火攻等全程逐次累加后综合评出，取斩获数优先、伤敌量次之）、双方折损武将点名（而非只报数量）、我方留存主力（按剩余兵力前三，附兵力/士气）。

### 八、排兵布阵按钮遮挡与位置调整排查

排查确认"武将位置调不动"并非逻辑 bug（真实点击链路本就能正确改站位），根子在另一处：`.fg-board-wrap` 在上一轮为战斗棋盘接入双指缩放时统一加了 `touch-action:none`，而排兵布阵页共用同一个类却从未绑定缩放手势——`touch-action:none` 在触屏设备上会连带把该区域的默认滚动/滑动手势也封死，若布阵页内容偏高，用手指在棋盘区域上划就滑不动，摸不到底部按钮，才会被误以为"点不动棋子"。解法：新增 `.zoomable` 修饰符，只在真正接了 `bindGridZoom` 的战斗棋盘上生效，排兵布阵页的棋盘保留可正常滚动的默认行为；叠加第六条的 flex 铺满布局后，排兵布阵页在绝大多数视口下已经不需要滚动就能看到按钮。「擂鼓·两军对圆」改名「全军出击」。

### 九、战报挪到棋盘正上方、满宽 3~5 行

`.fg-area-log` 在 flex 列中排在对阵情况之后、棋盘之前，宽度随容器撑满；`.fg-log-compact` 高度按 3~5 行文字估算（84px），超出部分内部滚动。战报本身"新消息在下+自动滚底"沿用上一轮已落地的机制。

### 十、棋盘缩放边界收紧 + 去除 ＋/－ 按钮

`clampGridZoomState` 去掉旧版留的一截缓冲余量，缩放 1 倍时可平移范围钳死为 0、缩放 N 倍时平移范围精确到"内容边缘贴齐可视区边缘"为止，拖不出可视范围。右下角 ＋/－ 按钮整个移除，缩放只靠双指捏合（触屏）与滚轮（有鼠标的设备仍保留 `onwheel` 作为兜底）。
过程中揪出两处真实缺陷并修复：
① 原实现里单指按下即无条件进入"拖拽追踪"（挂 `document` 级 `pointermove`/`pointerup`），未放大（scale=1）时因为钳制已把可平移量锁死为 0、拖拽毫无视觉效果，但追踪逻辑仍在运行——若合成/触屏输入在按下与抬起之间有哪怕几像素的抖动，就会被判定成"刚拖拽过"（`_justDragged`），导致紧接着的 `click` 事件被自身挂的捕获阶段监听器拦截、点选目标偶发失灵。修复为：仅当 `scale>1` 时才把单指按下计入"拖拽"，scale=1 时单指按下形同虚设，点击畅通无阻。
② 早期修复第①点时曾一并把"scale=1 就完全不挂 move/up 监听"，结果导致这根手指的 `pointerup` 永远等不到清理，长期残留在内部的 `pointers` 追踪表里，污染后续双指捏合的手指计数判定（表里始终多出一个幽灵指针，`size===2` 的判断条件永远凑不齐）。改为监听照常挂上摘下（`pointerup` 收尾正常），只是不把 `dragging` 置真——既堵住了①的漏洞，也不再产生这条新缺陷。

### 十一、行动菜单改为"选定行动类型后才隐藏"

`selectUnit`/`_setupCommon` 不再预设 `actionMode="attack"` 默认值（改为 `null`）；进入行动阶段后环形菜单完整显示（单独攻击/夹击/单挑/战法/待命），此时棋盘上**不**高亮任何目标（`onCellClick`/`enemySet` 计算均以 `this.actionMode` 是否已选定为前提）。玩家点选其中一枚行动类型按钮后，菜单立即收起、只留一枚 `pointer-events:none` 的小标签提示"正在做什么、请点目标"，此时目标才会高亮、才能点选执行——菜单按钮不再挡在候选目标身上。

**验证**：`node --check`/CSS 大括号配平复核全过；Playwright 覆盖：火攻动画节点与音效、兵力/士气条轨道像素级宽度核验（20 员武将样本）、点击对比栏展开/收起详情（20 行明细渲染核验）、`actionMode` 初始为 `null`/无预高亮按钮/无预高亮目标、盲点敌方（未选行动类型）不触发攻击且退回未选中态、选定攻击类型后环形按钮消失+标签出现+目标高亮、真实点击目标后命中瞬间（200ms 内，动画尚未播完）兵力条宽度已与最新 `hp/hpMax` 吻合、`finish()` 结果弹窗含头功/折损点名/留存主力、390×780 手机与 900×460"宽而矮"视口（模拟折叠屏展开后高度受限的极端场景）下开战/全军出击按钮均完整可见且可点、缩放邊界拖拽超限后 transform 精确停在边缘、300 回合上限 AI 压力测试三连跑无异常；另发现并修复本节第八/十条所述的两处真实缺陷。回归确认旧版「野战演武·经典版」与边境战事内部调用不受影响。

## 六十三、野战演武·棋盘对垒十三项体验反馈：打满全场、军令累积、大本营围城等（第六十一轮）

### 一、取消 30 回合上限

`endTurnCycle()` 删去 `turnN > 30` 的强制兜底判定，battle 只在「一方全军溃退」或「大本营被围满 3 回合」（见第五条）时结束，鏖战多久由局势自然决定。

### 二、军令改为统帅+智力换算、累积上限 10 道

`calcOrders(arr)` 由单纯智力均值改为「统帅+智力」均值折算，公式区间不变（约 2~6 道/回合）；语义也从"每回合初始重置"改为"每回合恢复量"——`endTurnCycle` 里不再整段覆盖 `myOrders`/`foeOrders`，而是 `Math.min(ORDERS_CAP, 当前余量 + 本回合恢复量)` 累加，新增 `ORDERS_CAP=10` 硬上限。折损越重、统帅智力均值被拖累，恢复越慢；但不用完不会清零，允许攒钱打一波大的。

### 三、军令种类扩充（提案，待用户确认后再实施）

本轮仅给出建议方案，未动代码：候选新增「医疗营救」（选定一员己方部众小幅回复兵力，类似战法但不占该部行动、走军令预算）、「疑兵/佯攻」（令一员敌方部众下回合防御力临时下调）、「劫粮」（消耗军令直接扣减敌方本回合可用军令）三选一或组合，具体以用户回复为准。

### 四、结束回合自动待命

`endMyTurn()` 里对所有尚未行动的我方部众批量补一次「待命」：未挪动过的同样吃满 +15% 防御加成（`standDef=true`），逻辑与手动点「待命」完全一致，不必逐个补点。

### 五、占领大本营 3 回合判定胜负

新增 `myCampSiege`/`foeCampSiege` 两个计数器，`endTurnCycle()` 每回合末检查敌方是否仍站在我方大营（反之亦然）：仍站着则计数 +1，换防（占领者撤走）立即清零重算，不是"历史上出现过 3 次"而是"连续占领 3 回合"。满 3 回合直接 `finish()` 判负/判胜。棋盘上大本营格新增 `.fg-siege-ring` 光圈（`boardCellsHtml` 里不论格上有没有棋子都画），进度越高圈越粗越亮、伴随脉冲动画；顶部提示行同步文字播报"已被围 N/3 回合"。AI（`aiActUnit`）在没有当回合可执行的攻击时新增大本营策略：己方大营已被围困或有敌军逼近 2 格内时，优先折返回防；否则约三成概率主动改道扑向玩家大本营伺机围城，不再无脑一味冲着最近的敌军去。

### 六、排兵布阵界面框选选中武将与可布阵区域

`renderDeploy()` 复用 `boardCellsHtml` 既有的 `.fg-selected`/`.fg-reach` 高亮机制（新增 `opts.selectedUnit` 入参，与战斗阶段的 `this.selectedUnit` 高亮共用同一段判断），选中武将后金框标出该棋子本身、蓝框标出后两排全部空格；底部提示文字同步显示"已选中 XXX，点蓝框空格调整站位"。

### 七、顶部对比栏改半透明浮层，不再挤占棋盘

原来点「查看详情」在棋盘上方内联展开的做法整个替换掉：新增 `showMatchupOverlay()`，点兵力条本体（`#fg-vsbar-open`）直接复用全局 `#overlay` 遮罩弹出逐将详情，不影响棋盘区域的高度/布局；浮层内容区自身也挂了点击关闭（`$("#overlay-content").onclick = () => closeOverlay()`），叠加遮罩本就有的"非 modal 点遮罩关闭"，满足"点任意处关闭"。

### 八、默认棋盘改为 9 行 13 列

`COLS: 12` 改 `COLS: 13`（`ROWS` 维持 9）。中军主干道、双方大本营均取列中点 `Math.floor(COLS/2)`，13 的中点仍是整数列，改动后地形生成/寻路/部署站位无需额外适配。

### 九、战报区域锁定固定高度

`.fg-log-compact` 由 `max-height` 改 `height`（外加 `max-height` 兜底）锁定 70px（约 3 行文字），不论战报有 0 条还是 30 条都不再撑高/收缩，杜绝棋盘因日志多寡而上下跳动。

### 十、火攻模式高亮可选目标

`renderBattle()` 计算 `enemySet` 时新增分支：`orderMode==="fire"` 时，将当前满足火攻条件（站在山道，或我方有精通天时者时不限地形）的敌方单位一并计入高亮集合，与既有的攻击目标高亮复用同一套红框视觉，玩家不必凭空猜哪些目标能烧。

### 十一、夹击伤害改固定倍率

`computeCombat` 的夹击加成由"每多一人 +25%、封顶 2 人"的线性公式改为按参战总人数（含发起者本人）查表：2 人 133%、3 人 167%、4 人及以上封顶 200%，与用户指定的数值直接对齐。

### 十二、回合信息移至顶部标题栏、居中

新增 `syncTopbarTitle()`：战斗阶段把 `#screen-fieldgrid .topbar h2` 的文字动态换成"🟢/🔴 我方/敌方回合 · 第 N 回合"，离开战斗（部署阶段/返回菜单）时换回"野战演武"；标题行本就 `flex:1`，改窄字距、降字号后居中不截断。原来挤在对比区第一行的这段文字随之移除。

### 十三、存活/均气/军令并入兵力条同行

`vsbarHtml()` 的每一侧从"军名+数字"两行扩为三行，新增一行"存 N/10・气 N・令 N"小字；原本单独占一行的 `.fg-matchup` 整个撤掉，连带的展开/收起交互也一并被第七条的浮层方案取代。`.fg-area-matchup` 现在只保留阵形加成/大本营围城/火攻选目标这类少量、间歇性出现的提示文字，平时高度为 0。

**验证**：`node --check`/CSS 大括号配平复核全过；Playwright 覆盖：9×13 棋盘尺寸读数、部署页选中武将/可落子格高亮、战斗页顶部标题栏文字与居中、VS 条内嵌存活/均气/军令文本、旧版单独对比栏元素确认已移除、点兵力条弹出浮层且不影响棋盘区域尺寸、点浮层内容关闭、日志连写 10 条前后高度不变、军令累积公式与硬顶 10 道核验、夹击 2/3/4 人固定倍率 1.33/1.67/2.00 数值核验（钉死随机数验证纯倍率）、结束回合批量待命核验、大本营连续围城 3 回合触发胜负与光圈可见性、AI 大本营策略代码路径核验；8 回合真实攻防的对战压力测试（双方均主动出手，不再是单纯待命）全部在 24~29 回合内正常分出胜负、军令余量全程未超硬顶、无 NaN/控制台报错；回归确认旧版「野战演武·经典版」与边境战事内部调用不受影响。

## 六十四、野战演武·棋盘对垒八项体验反馈：详情浮层重排、移动 bug 修复、医疗疑兵、火攻遮罩、VS 条旗帜化等（第六十二轮）

### 一、两军详情浮层字体放大、间距统一、新增将魂名列

`matchupDetailInner()` 由「六列挤一行」的 CSS Grid 改成「每将两行」：第一行姓名+阵形图标+将魂名（三者都要完整显示、不截断），第二行兵力/士气/攻防三项数值用 flex 统一 `gap`，兵力↔士气与士气↔攻防的间距天然一致。原六列单行方案在放大字号后，将魂名等不定长字段被 CSS Grid 的隐式 `min-width:auto` 挤没——`overflow:hidden` 的列在轨道尺寸计算阶段会被判定为 `min-width:0` 让出全部空间，而没设 `overflow:hidden` 的兵力/攻防等列反而按未截断的原始文本宽度抢占轨道，导致姓名/将魂名被压缩到不足一个字宽，这是本轮排查中发现的真实渲染缺陷，改两行布局后从根上绕开。整体字号 13px→14.5px，标题提示 10.5px→11px。

### 二、修复移动后仍可再移动的 bug

`selectUnit(u)` 原本无条件把 `selPhase` 设为 `"move"`，武将挪动一步后点空处取消选中、再点回该武将会重新进入移动阶段，可无限挪位。改为 `this.selPhase = u.moved ? "act" : "move"`，已移动过的部众重选后直接进入行动阶段，堵死无限位移。

### 三、军令新增「医疗营救」「疑兵佯攻」两种

`orderMode` 状态机扩为 `null | "fire" | "heal" | "feint"`。医疗营救（`tryHealOrder`）点选一员己方部众，回复其兵力上限 14%；疑兵佯攻（`tryFeintOrder`）点选一员敌方部众，令其防御力 -20%、持续 2 回合（`feintTurns`/`feintMul`，随 `endTurnCycle` 逐回合递减，`computeCombat` 的防御分母乘区里生效）。两者结构均照搬既有 `tryFireAttack` 的 `busy` 锁+扣军令+清 `orderMode`+播动画流程，AI（`runFoeTurn`）的军令分支同步扩为四选一随机分派。

### 四、火攻模式改为全屏半透明遮罩、提示挪至按钮上方

`renderBattle()` 新增 `dimSet` 计算：军令选目标期间（火攻/疑兵/医疗），非可选目标的全部棋盘格叠加 `.fg-dimmed` 半透明遮罩（`::before` 伪元素，`z-index:4`、`pointer-events:none`，不影响原有点击判定），只留可选目标本体清晰可辨，不必再逐格辨认哪里能烧。原本挤占棋盘上方战场空间的提示语整段移除，改为军令按钮正上方一行小字 `orderModeHintHtml()`，平时不选军令时不占位。

### 五、火攻新增燃烧音效

`audio.js` 的 `SFX` 新增 `burn()`：两路 `noise()`（带通+高通）叠加一枚音高下滑的锯齿波振荡器模拟"火起噼啪+气浪"的听感，`playFireFx` 里把原先复用的 `.hit()` 换成专属的 `.burn()`。

### 六、敌方回合内已移动/未移动武将颜色区分（真实缺陷修复）

排查后发现并非视觉规则本身有问题——`.fg-unit.acted` 的灰化样式一直都在——而是 `foeUnits[].acted` 从未在任何地方被重置，只有 `myUnits` 会在 `startBattle()`/`endTurnCycle()` 里清零，导致敌方棋子从第二回合起永久顶着"已行动"的灰化外观，玩家根本分不清谁动过谁没动。修复：`startBattle()`/`endTurnCycle()` 里补上对称的 `this.foeUnits.forEach(u => { u.acted = false; })`，与 `myUnits` 同一时机重置。

### 七、战报区域高度精确锁定 3 行

实测 `.fg-log-compact` 在 12.5px 字号、1.5 行高、6px 上下内边距下的精确 3 行高度为 `18.75×3+12=68.25px`（此前沿用的 70px 系估算值，偏差 1.75px），改为精确的 `68.25px`，一开始即锁死、后续不再变动。

### 八、双方简介区域重排：军名贴数字、存活均气加大字号、军令改画金旗

`vsbarHtml()` 每侧由"军名+数字"单行改三段：第一行"我军/敌军"紧贴兵力数字外侧（我军居左、敌军居右）；第二行"N 存活・N 均气"字号放大（13.5px、金色加粗）；第三行军令数不再用数字，改画竿+三角旗的纯 CSS 小旗子（`.fg-flag-icon`），军令余量即旗子数量，多了自动换行不挤爆两侧。

**验证**：`node --check`/CSS 大括号配平复核全过；Playwright 覆盖：详情浮层实测姓名/将魂名列宽（放大字号后不再被压缩至 1 字符内，如"槍の又左""十胜十败"等长将魂名完整可见）；选中武将→移动→取消选中→重选→`selPhase` 断言为 `"act"`（bug 复现用例反向验证已修复）；`tryHealOrder`/`tryFeintOrder` 端到端调用核验兵力回复量、`feintTurns`/`feintMul` 生效、军令各自正确扣减 1、`endTurnCycle` 后 `feintTurns` 正确递减；火攻模式下 `.fg-dimmed`/`.fg-order-hint` 均正确出现在 DOM；`AudioSystem.sfx.burn()` 可正常调用无报错；`foeUnits[].acted` 在 `endTurnCycle()` 前后核验从"全部为 true"正确重置为"全部为 false"；`#fg-log` 实测高度精确等于 3 行文字高度（68.25px），连写 6 行战报后仅显示最新 3 行、无裁切也无多余留白；VS 条截图核验"我军/敌军"文字位置、`.fg-flag-icon` 旗帜数量与军令余量一致。回归确认旧版「野战演武·经典版」与边境战事内部调用不受影响。

## 六十五、野战演武·棋盘对垒八项体验反馈：详情浮层免滚动、军令悬浮提示、战报单行截断、VS条纵向三行、AI夹击与积极用令、百回合封顶（第六十三轮）

### 一、详情浮层放大，内容不折行不滚动

`.fg-md-col` 去掉原有的 `max-height:50vh; overflow-y:auto`，浮层容器 `.fg-matchup-overlay` 改为 `width:96vw`（`max-width:640px`）尽量撑满视口，10v10 满编场景下实测两栏内容整段完整显示、不再需要内部滚动条；外层 `.overlay` 补一道 `overflow-y:auto` 兜底，真遇到极端小屏、内容仍超出视口高度时，由整页滚动兜住，不会卡在够不到的地方。

### 二、军令选目标提示改为悬浮标签，不再挤压战场

`orderModeHintHtml()` 的提示文案不再塞进 `.fg-area-orders` 这个参与 flex 布局高度分配的独立行，而是改成新的 `.fg-order-float`——与武将行动菜单收起后的 `.fg-radial-tag` 同一视觉语言（深底金边小药丸），绝对定位钉在 `.fg-board-wrap` 内、`.fg-zoom-layer` 之外（不随棋盘缩放/拖拽跑位），悬浮在棋盘正上方居中。提示文字有没有、多长，都不会牵动战场区域的高度或位置。

### 三、战报区域精确 3 行、居中、逐条单行截断

排查发现此前"高度总是调不对"的真正原因不在盒子高度的像素数学（`68.25px` 本身量得没错），而是长战报消息在容器内会自己折成 2 行，导致"箱高 3 行"与"看起来显示几条"对不上、时多时少。修复：给每条战报（`#fg-log > div`）加 `white-space:nowrap; overflow:hidden; text-overflow:ellipsis`，强制单行 + 超长省略号截断，这样"箱高 3 行"与"稳定显示最新 3 条"才能永远对得上；同时 `.fg-log-compact` 加 `text-align:center` 让每行文字居中显示。

### 四、双方简报区域改回真正的纵向三行

此前的实现是"两侧栏夹中轴"的三栏布局，字面上不是用户要的"三行"。本轮改为货真价实的纵向堆叠：第一行 `.fg-vsbar-row1` 军名紧贴左右外沿、兵力数字用 `justify-content:space-between` 各自朝中线一侧靠拢；第二行仍是从中线向两侧撑开的兵力色条；第三行 `.fg-vsbar-row3` 把"武将数（不再写"存活"二字，改成"武将 N"）+ 均气"放在最外侧，中间 `.fg-vsbar-flagzone` 对齐色条中轴处摆双方军令小旗——我方旗组 `flex-direction:row-reverse` 贴中线向左展开，敌方旗组正常方向贴中线向右展开，旗子本身放大一档（8×9px → 10×11px），并用 `transform:scaleX(-1)` 把敌方旗子水平镜像，两组隔中线相对而立、方向相反。

### 五、每回合军令恢复量减半

拆出独立的 `calcOrderRegen(arr)`，在原 `calcOrders(arr)`（统帅+智力换算的基准值，仍用于开战起始军令池）基础上再打五折（向下取整、至少 1 道），`endTurnCycle` 里的每回合恢复改用这个新函数；起始军令池数量不变，只有"回合恢复"这一项减半，军令消耗更有分量，不再是一场仗打下来根本花不完。

### 六、AI 军令使用更积极、更有策略

新增 `aiUseOrder()`，把原来"四选一纯掷骰子"改成按局势优先级挑招：己方有部众兵力跌破 55% 时优先医疗营救（85% 概率）；否则若有可火攻目标则优先火攻（65% 概率，山道地形或军中有精通天时者时可选，命中后仍会正确触发 `routUnit`/`checkBattleEnd`）；否则己方均气低于 35 时擂鼓提振；否则挑我方攻击力最高、尚未被疑兵过的一部实施疑兵佯攻削弱其防御（55% 概率）；以上都不满足则兜底擂鼓。`runFoeTurn()` 相应把原来"至多用一次、40% 概率触发"的军令判定改成循环，每回合最多连用两道（首次 75% 概率、第二次 40% 概率），配合第五条的恢复量减半，整体军令的"使用频率"与"消耗速度"都更贴近真实对局节奏。

### 七、AI 好好利用夹击

`aiActUnit()` 原来选定攻击目标后一律 `resolveAttack(u, target)` 单打独斗，从不夹击。本轮在最终出手前调用既有的 `eligibleFlankers(u, target)`——只要目标周围还有其他未行动的敌方（AI 一侧）同袍已就位，就直接改用 `resolveCoordinatedAttack([u, ...joiners], target)` 合力围攻，拿到与玩家手动夹击完全同一套 133%/167%/200% 固定倍率加成，不再是"每次都只有当前这一部单独出手"。

### 八、封顶 100 回合，满百回合以存兵多寡定胜负

十六期取消的 30 回合硬顶不再无限期地"打到分出胜负为止"，本轮改为 100 回合软顶：`endTurnCycle()` 里在常规胜负判定（全军溃退/大营被围满 3 回合）之后、`turnN` 自增之前，检查若已达到第 100 回合，直接按当前双方存兵总量（`totalHp`）多寡判胜负，兵力恰好相同时以双方存活单位的总士气作为次级判据，避免鏖战无止境地拖下去。

**验证**：`node --check`/CSS 大括号配平复核全过；Playwright 覆盖：10v10 满编详情浮层截图核验两栏均无滚动条、姓名/将魂名/数值均完整不截断；军令提示悬浮期间棋盘区域 `getBoundingClientRect().height` 前后不变、`.fg-order-float` 确实挂在 `#fg-board-wrap` 下且不在 `.fg-zoom-layer` 内；连写 6 条长战报后 `#fg-log` 高度仍精确为 `68.25px`、逐条 `getBoundingClientRect().height` 均 ≤ 20px（单行未折行）、`text-align:center` 生效；VS 条截图核验第一行军名贴外沿数字贴中线、第三行"武将N·气N"与镜像旗组布局；`calcOrderRegen`/`calcOrders` 数值核验减半关系（`Math.floor(base/2)`，至少 1）；`aiUseOrder()` 在人为制造"我方有重伤部众"场景下断言优先选择医疗营救；人为摆位令两个敌方单位同时贴身一个目标后调用 `aiActUnit`，核验 `eligibleFlankers` 返回非空、且两个敌方单位的 `acted` 均被置为 `true`（确认走的是合力围攻而非单打独斗）；人为设定 `turnN=100` 且双方兵力悬殊后调用 `endTurnCycle()`，核验 `finish()` 被正确调用且 `won` 与兵力多的一方一致；15 回合 AI-vs-AI 连续推进压力测试全程无 `NaN`、无控制台报错、军令池正确封顶于 10。回归确认旧版「野战演武·经典版」与边境战事内部调用不受影响。

## 六十六、野战演武四项体验反馈：详情浮层兵力归位、VS条留白、精简战报区域、正式接入边境战与攻城战（第六十四轮）

### 一、详情浮层兵力数据移到姓名旁、行中间

`matchupDetailInner()` 的两行布局重新分配：第一行姓名+兵力+阵形图标（`justify-content:space-between` 三栏分布，兵力自然落在姓名与阵形图标之间、行的中段），不再垫到姓名下面单独一行；第二行将魂名+士气+攻防。姓名一眼就能对上自己的兵力数字，不必再往下多看一行。

### 二、VS条数字/旗帜间距加大、旗子再放大一档

`.fg-vsbar-row1`（两侧军名+兵力数字所在行）的 `gap` 由 6px 提到 24px——这个 gap 正是两侧兵力数字之间唯一的间距来源（军名各自贴外沿、数字各自贴内沿），加大后数字不再紧贴中线；`.fg-vsbar-flagzone` 新增 `gap:16px`，让我方/敌方两组军令旗不再在中轴处直接相碰；旗子本身 10×11px 再放大到 12×13px。

### 三、移除阵形相克加成倒计时提示，进一步压缩战报区域

`hintLineHtml()` 去掉 `formTurnsLeft` 那一条文案——阵形相克这个玩法本身留待以后再打磨，眼下的提示语几乎每局开局都要显示满 3 回合，长期占着战报区域上方一整行。摘掉之后，`.fg-area-matchup` 在没有大本营围城预警时会像设计之初那样自动收缩为 0 高度，省下的空间实打实地让给战场地图；大本营围城预警（进攻/防守两条）保留。

### 四、GridBattle 正式接入，取代 FieldBattle 成为边境战事/攻城战的战斗界面

这是本轮工作量最大的一项：此前"野战演武·棋盘对垒"仅作小游戏自由试玩入口，边境战事与随后的攻城战仍固定走旧版「野战演武·经典版」（`FieldBattle`）的五线推兵引擎——`GridBattle` 缺一套与 `FieldBattle.beginExternal` 对等的委外接口。本轮补齐：

- 新增 `GridBattle.beginExternal(myRoster, foeRoster, heroSide, opts)`，签名/`opts`（`rpg`/`observe`/`troopScale`/`onDone`）与旧版完全对齐，`open()` 也补上 `rpg`/`external`/`troopScale` 的重置，避免残留上一场委外战场的状态。
- 城池驻军兵力覆盖：`_setupCommon()` 按 `troopScale.mine/foe` 与「统帅推算的自然兵力」之比换算缩放系数，`makeUnit(g, side, form, scale)` 新增 `scale` 参数按此系数缩放每部兵力上限，未传时维持原样（小游戏自由试玩不受影响）。
- 全自动推演（`observe:true`，主角未被抽中亲历）：新增 `autoPlayMyTurn()`/`aiUseOrderMy()`，结构上与既有的 `runFoeTurn()`/`aiUseOrder()` 完全对称、只是主体换成我方；`aiActUnit(u)` 原本写死"我方=玩家、大本营=固定 my/foeCamp"的判断全部改用 `u.side` 动态判定敌我与己方大营，使其能双向驱动任意一侧——`endTurnCycle()`/`_setupCommon()` 首尾相扣，跑通"开战即全自动、直到分出胜负"的无人值守闭环，全程无需一次点击。
- `finish()` 新增 `if (this.external) { this.finishExternal(won, reason); return; }` 分支，`finishExternal()` 比照旧版 `FieldBattle.finishExternal` 的历战成长（`Growth.battle`）逻辑，折算为调用方约定的结果对象（`playerWon`/`mySurvivors`/`foeSurvivors`/`kills`/`myTroopsLeft`/`foeTroopsLeft`，`mySurvivors`/`foeSurvivors` 就是原样的武将对象引用）后回调 `onDone`，不再弹野战演武自身的战果卡。
- 退出键补上 `GridBattle.rpg` 判断——委外流程发起时退出应回到天下地图（`goHome()`），不再固定直接回首页，与旧版 `FieldBattle` 同一套规则。
- `MapUI.resolveBorderWar`/`resolveSiege` 两处 `FieldBattle.beginExternal(...)` 全部改为 `GridBattle.beginExternal(...)`，调用方式与参数不变——「野战演武·经典版」本身原样保留、继续作为小游戏合集的独立试玩入口，只是不再是边境战事/攻城战内部实际调用的引擎。

**验证**：`node --check`/CSS 大括号配平复核全过；Playwright 覆盖：`beginExternal` 主角亲历路径（`observe:false`）——调用后 `phase` 停在 `"deploy"`（等待手动排兵）、`rpg`/`external` 标记正确、`#screen-fieldgrid` 正确激活；`troopScale` 缩放核验——传入 `{mine:30000, foe:20000}` 后 `myUnits`/`foeUnits` 的 `hpMax` 总和精确落在目标值附近（≤50 误差，来自四舍五入累积）；全自动推演（`observe:true`）端到端跑通一整场真实战斗（5v5，`setTimeout` 延迟压缩到 ≤2ms 加速回放但不改变任何判定逻辑），全程无一次点击、约 15.5 秒真实耗时内跑完并正确触发 `onDone`，返回的结果对象字段/类型/数值全部符合约定契约（`mySurvivors` 为完整武将对象数组、`kills` 与 `foeSurvivors` 长度吻合、`myTroopsLeft`/`foeTroopsLeft` 与终局兵力吻合）；15 回合 AI-vs-AI 压力测试确认 `aiActUnit` 泛化重构后双方行为不受影响、无 `NaN`/控制台报错。回归确认旧版「野战演武·经典版」小游戏自由试玩入口（`FieldBattle.open()`）不受影响，仍按原引擎运行。

## 六十七、修复野战定时器陈旧竞态：打完仗回天下游历后误触其他面板会莫名跳出（第六十五轮）

用户反馈：正式接入边境战/攻城战之后，打完一局野战演武、回到天下游历界面，再操作城建、委任掌柜、集市买卖、计谋、出征等任意一处面板，点了按钮却直接跳出到天下游历——但重开页面后问题消失、且用干净的自动化测试环境怎么都复现不出来（每次都是全新页面加载）。这个"必须先打过一局仗、且不能重开页面"的线索是找到根因的关键。

**根因**：`endMyTurn()`（结束我方回合后 400ms 排定 `runFoeTurn`）、`_setupCommon()`/`endTurnCycle()`（委外战场全自动推演时排定下一轮 `autoPlayMyTurn`）三处 `setTimeout` 续跑逻辑，写法都是 `setTimeout(() => this.xxx(), 400)`——真正的世代（`gen`）校验写在被调用函数**内部**（`const myGen = this.gen;` 再逐步比对），也就是说这个校验值是定时器**触发那一刻**才读取的"当下"的 `this.gen`，而不是排定这个定时器**那一刻**的 `this.gen`。这样的校验等于自己和自己比，永远相等，起不到任何拦截作用。

真实后果：玩家如果在这 400ms 窗口内中途退出战斗（点返回键触发 `abort()`，`gen` 自增）——不论是退出小游戏自由试玩的"野战演武"，还是（更常见地）委外战场自动推演过程中提前离开——这个陈旧定时器到点后仍会照常触发，在玩家已经回到天下地图、正操作着某个完全无关的面板时，凭空跑出一段幽灵般的敌方回合甚至整场战斗的收尾逻辑，最终调用 `finish()` 弹出野战演武自己的结算浮层（`{modal:true}`），叠在玩家当前面板之上。玩家看着像是自己点的按钮，实际点中的是这个不请自来的浮层里"返回菜单"一类的按钮，于是被带回天下游历——這正是"点哪个面板的按钮都会跳出"这个现象的成因：不是哪个面板本身坏了，而是一个游离的战斗浮层不定时凭空出现，恰好接住了玩家下一次点击。

**修复**：新增 `scheduleIfCurrent(fn, ms)`，在**排定定时器的那一刻**就把 `gen` 钉死（`const gen = this.gen`），定时器触发时先比对这个当时钉死的值，不匹配则直接跳过，不再执行 `fn`。三处 `setTimeout` 全部换成这个包装：`endMyTurn()` 排定 `runFoeTurn`、`_setupCommon()` 排定首轮 `autoPlayMyTurn`、`endTurnCycle()` 排定下一轮 `autoPlayMyTurn`。

**验证**：`node --check` 复核通过；Playwright 直接复现该竞态——排定一个 `scheduleIfCurrent` 定时器后立刻自增 `gen`（模拟中途退出），等过设定延迟后断言回调确未执行，另建一组不自增 `gen` 的对照组断言回调确有执行；进一步模拟真实场景：开局→结束我方回合（排定陈旧 `runFoeTurn`）→立即 `abort()`→清掉浮层→等过 700ms，断言全程无浮层弹出、敌方兵力全程未变（陈旧的 `runFoeTurn` 确未跑起来）；同样手法核验委外战场全自动推演链路（`beginExternal(observe:true)` 排定首轮 `autoPlayMyTurn` 后立即 `abort()`）同样被正确拦下、`onDone` 未被误触发。回归确认修复后正常流程（无中途退出）不受影响：完整 5v5 全自动推演端到端跑通、15 回合 AI 对战压力测试、真实"出征→野战→攻城→边境战报"全流程测试，均一切正常、无报错。

## 六十八、野战演武大屏字体缩放与AI单挑/军令概率调优（第六十六轮）

### 一、格子随屏幕放大后，格内文字/徽标同步放大

排查发现根因在 `.fg-name` 用 `clamp(6px, 1.6vw, 9px)` 按视口宽度估算字号，上限硬顶死在 9px——手机上凑巧够用，但 iPad/桌面浏览器这类大屏下格子被 9×13 的行列均分撑到很大时，字号却涨不上去，看起来极不协调；`.fg-skbadge`/`.fg-formbadge`（将魂/阵形徽标）、`.fg-hpbar`/`.fg-morbar`（兵力/士气条）、`.fg-dmgnum`（伤害跳字）、`.fg-terrain-icon`（地形图标）则干脆是完全不随屏幕变化的固定像素值。

改用 CSS **容器查询**（container queries）：`.fg-cell` 声明 `container-type: inline-size`，格内所有这些元素的尺寸全部改写成 `cqw`（格子自身宽度的百分比）配 `clamp()`，直接以"格子实际渲染出来有多宽"为准，不再用视口宽度去间接估算——不论手机竖屏、iPad 横竖屏、折叠屏展开、桌面浏览器缩放窗口，只要格子本身变大，里面的字/徽标/血条/跳字就跟着等比例变大，且不会小于原先手机端的观感、大屏下也有明确上限（如姓名字号最高 20px）不会离谱地过大。

### 二、AI 单挑与军令使用概率调优

**AI 此前从未主动发起单挑**——排查发现 `challenge()`（单挑结算）在全份代码里唯一的调用处是玩家手动点选环形菜单的"单挑"按钮，`aiActUnit()` 只会走普通攻击/夹击，压根没有触碰单挑的分支。本轮在 `aiActUnit()` 里补上：贴身交锋（非远程）且这一击并非稳赢一记击溃时，18% 概率改为挑发单挑而非直接开打，让 AI 也会给战局添些赌一把的变数；已经稳赢的场合仍照常直接攻击，不会把送到嘴边的战果拱手让给赌局。

**军令使用概率**：十八期为了让军令用得起来，把"这回合要不要试着用一道军令"的概率顶到了 75%/40%（一回合最多两次），实战下来近乎每回合都会用，且找不到合适时机时也会硬用擂鼓"图个不浪费"，未必合理。本轮把这层概率降到 55%/20%，同时去掉 `aiUseOrder`/`aiUseOrderMy` 末尾"怎么都要擂个鼓"的兜底——士气本就不低（≥55）时若也没有伤兵可救、可烧的目标、可疑兵的强敌，这次就什么都不做，把军令留给真正用得上的时机，敌我双方（`aiUseOrder`/`aiUseOrderMy`）与全自动推演链路（`autoPlayMyTurn`）三处一并调整。

**验证**：`node --check`/CSS 大括号配平复核全过；Playwright 截图核验手机（390px）/iPad（834px）/桌面（1600px）三档视口下格子宽度从 28px→62px→121px 变化时，姓名字号相应从 6px→11.2px→20px 同步放大、徽标同步放大，手机端观感与改动前一致（回归验证）；60 次重复采样单挑触发率约 20%（目标 18%，符合统计误差范围）；军令兜底逻辑核验——人为清空一切"值得用军令"的条件后调用 `aiUseOrder()`，断言返回 `false` 且军令余量未变（未被浪费性使用），另核验原有的优先级分支（救援伤兵等）不受影响；15 回合 AI-vs-AI 压力测试全程无 `NaN`/控制台报错。

**用户还提出三项需求，因涉及全新数值体系设计，已给出方案待用户确认后再实施，本轮未动代码**：①功勋按参战武将攻城战场排名结算（依赖②）；②野战功勋值体系（歼敌/斩将/计谋成功等按分值累计，终局给出双方排名）；③每月集市行情波动方案；④城池产业收益浮动方案。四项方案详见本轮对话记录。

## 六十九、野战功勋值体系、攻城功勋按排名结算、集市月度行情、产业收益逐日浮动（第六十七轮）

用户对上一轮给出的四份方案全部确认（"以上都同意"），并顺带追加一问：GridBattle 接入边境战/攻城战之后，每月 15 日的武将大会、30 日的边境战事是否仍准时发动。本轮先核验后一问，再依次落地四项方案；核验与落地过程中又顺手揪出一个与本轮改动关系密切的既有竞态 bug，一并修复。

### 〇、核验：月度事件仍准时发动

`isTournamentDay(day)`（`dom===15`）、`isMonthEnd(day)`（`day%30===0`）及 `camp()` 内的调用顺序（`followUp()` 依次判定武将大会→月末国战→势力袭扰→夜袭→宿敌寻衅）本轮均未被触碰。Playwright 直接跑通真实 `camp()` 流程：把 `m.day` 摆到 14/29，各推进一天，断言弹出的浮层标题分别含"武将大会""边境战事"，均正常触发、无报错。结论：未受影响，不需要修复。

### 一、野战功勋值体系（GridBattle 全新战场功勋值）

`makeUnit` 新增 `merit` 字段，`gainMerit(u, n)` 统一入账口。功勋来源与分值：
- 伤敌：每次命中（含反击）按 `伤害/100` 计入，贴身肉搏与远程/军令伤害一视同仁；
- 斩将（对某一部的最后一击，含近战/反击/主动战法/军令火攻各种击溃手段）：+30；
- 计谋成功（主动将魂 `useActiveSkill` 任一分支真正生效，而非"未寻得目标"落空）：+8~15 随机；
- 单挑得胜：胜方 +40；
- 力战存活至终局：+5。

`finalizeMerit()` 在终局统一结算存活加成、把双方所有参战武将按功勋值排出名次，供两处消费：①野战演武小游戏自身 `finish()` 的结算卡片新增"此役功勋榜"列表（`meritRankingHtml`，取代原先只挑单人头功的写法）；②委外战场 `finishExternal()` 把整份榜单塞进 `res.meritRanking`，供边境战/攻城战沿用（见下一节）。CSS 新增 `.merit-board`/`.merit-row` 系列样式，双方用红/蓝配色区分，折损者标注"（折损）"。

### 二、攻城功勋按战场排名结算

`resolveSiege` 把攻城战 `beginExternal` 结束时的 `siegeRes.meritRanking` 一路传给 `finalizeBorderWar`（空城直入无攻城战可打的边路则传 `null`，退回原口价）。原先攻城得胜一律按 `120`（月末国战）/`150`（主动出征）一口价发功勋，现改为：找到主角本人（`id===-1`）在这份榜单里的名次，换算成百分位——前 30% 名次 ×1.3、中间 40% 原价 ×1、垫底 30% ×0.75，`Math.round` 后再交给 `PlayerRank.addMerit`；"你的战果"文案里同步标出"此役功勋第 X/N 名 · 居前加成/居中原价/垫底折算 ×倍率"，边境战报浮层里也贴上完整功勋榜供玩家复盘整场战事谁的表现最突出。未取胜（攻城未克）仍维持原先的 40 一口价，不参与这套折算——毕竟"打赢了排名靠前多得，打输了没得可排"。

### 三、每月初一：集市行情大幅波动

新增 `isMonthStart(day)`（`dom===1`），随 `isMonthEnd` 一并挂进 `camp()` 的每日结算流程（不占用 `followUp()` 的弹窗优先级链，静默生效，宿营夜报里以新闻条目播报哪些城市变了天）。每月初一，每座陆地城市（对马岛黑市固定八折，不参与）各自独立 45% 概率从「📉甩卖（×0.7~0.8）／📈抢购（×1.2~1.35）／🈹商旅云集（×0.85~0.95）」三选一，写入 `m.marketTrend[cityId]`；未中签的城市清空当月事件、退回哈希底价。`cityPriceFactor(cityId)` 在原有城名哈希底价基础上，若当月有生效事件则再乘一层这个系数（读 `Campaign.mapState()`，不改动函数签名，调用方零改动）。集市面板（`openMarket`）、城池面板（`cityPanelHtml`）、全部城市详情（`showCity`）三处都追加"·本月📈抢购"这类标签，让玩家一眼看出这座城这个月不太一样。

### 四、城池产业收益逐日浮动

`Estate.dailyNoiseMul(cityId, day)` 按 `(城市, 天数)` 播种：常规 92% 概率落在 ±15% 区间，另各自独立 4% 概率「🎉丰收」（+35%~60%）、4% 概率「⚠️歉收」（-45%~-30%）。`Estate.accrue()` 原先是"待收天数 × 当前日进"的整段线性结算，现改为逐日累加——掌柜久未收账、一次结算多天时，期间每一天各自的涨跌都各算各的，不会被平均抹平；待收上限仍按未浮动的 `dailyRate × ACCRUE_CAP_DAYS` 封顶，不让浮动把上限也带歪。产业面板（`estateBtnHtml`）在触发丰收/歉收当天额外标"🎉 今日丰收"/"⚠️ 今日歉收"，常规波动不特别提示（避免每天都在刷小数字，noise 但不 noisy）。

**实现过程中的一处返工**：`dailyNoiseMul` 最初直接拿 `hashStr("estate|城市|天数")` 当种子喂给现成的 `seededRand`（沿用 `cityMarketStalls` 的老套路），测试时发现连续 30 天的结果几乎是平滑爬升的一条线，600 天抽样里"丰收/歉收"一次都没触发过。根因：`hashStr` 是逐字符 `h=h*31+c` 的多项式哈希，"...|9" 到"...|10" 这类只在末位递增的字符串，其哈希值往往也只相差一个很小的量；而 `seededRand` 内部的 LCG（`s'=s*1664525+1013904223`）对相邻的种子会给出同样按固定步长缓慢漂移的输出——种子不够"雪崩"，产出自然也谈不上随机。补一道 `mixSeed()`（murmur3 终拌手法：两轮 `异或右移16位 + 常数相乘`）把 hashStr 的输出先打散再喂给 LCG，30 天样本立刻看不出规律，600 天抽样丰收/歉收落在 21/24 次（预期各 24 次附近），符合统计期望。这个雪崩步骤只用在这一处逐日取值的新场景，不改动 `hashStr`/`seededRand` 本身，`cityMarketStalls` 等既有调用方行为不受影响。

### 五、顺手修复：`checkBattleEnd()` 未防重入，胜负判定可能被同一场战斗触发两次

排查功勋测试数据时发现一处异常：同一场自动推演的战斗结束后，存活武将的功勋值比 `onDone` 回调里拿到的结果多了 5 点（恰好是"力战存活"这一档加成的数值）——说明终局结算被跑了两遍。追查到 `checkBattleEnd()` 本身不设防：`aiUseOrder`/`aiUseOrderMy` 的火攻分支命中即可能击溃一整部敌军，分支内部已经调用过一次 `checkBattleEnd()`（并借此触发了 `finish()`），可外层 `runFoeTurn()`/`autoPlayMyTurn()` 的军令循环紧接着又不知情地再调一次——此时胜负结果没变，仍会再喊一次 `finish()`。而 `finish()` 第一次执行时已经把 `this.external` 清空，第二次进来会误判为"这是野战演武小游戏自己在打"，凭空多开一张独立的"野战大捷/兵败如山"结算卡，叠在边境战/攻城战真正的战报浮层之上——这正是本会话此前修过的"陈旧定时器致玩家莫名跳出面板"同一类"回调重入"问题的又一处变体，只是触发路径不同（这次是同步的双重调用，不是异步定时器）。

**修复**：`checkBattleEnd()` 开头加一道 `phase !== "battle"` 的守卫——只要战斗已经进入 `done` 阶段（不论是刚被自己终结，还是被别的路径终结），后续任何重复调用一律直接返回 `false`，不再二次触发 `finish()`。

**验证**：Playwright 直接构造场景复现——`beginExternal` 开局后手动把敌方全灭，连续同步调用两次 `checkBattleEnd()`，修复前 `finish()` 被记录调用两次（第二次 `external:false, phase:"done"`），修复后只调用一次；5v5 全自动推演端到端跑通（35~57 回合不等，无报错），`onDone` 拿到的 `meritRanking` 与战斗结束后直接读取 `myUnits`/`foeUnits` 的即时 `merit` 值完全一致（此前相差 5 点的问题消失）。

**其余验证**：`node --check` 复核通过；每月初一集市行情推演——真实 `camp()` 流程把 `m.day` 推到月初，21/46 座陆地城市（约 45%）各自变更行情，`cityPriceFactor` 与集市/城池/城市详情三处 UI 文案联动核验（118%→147%/89% 等换算准确）；产业逐日浮动——5 天累计的待收金额与逐日手算结果分毫不差、30 天累计正确被待收上限封顶且无 `NaN`；攻城功勋排名结算——合成三档名次（1/10、5/10、10/10）分别核验发放 156/120/90 功勋（对应 ×1.3/×1.0/×0.75），战报文案与功勋榜渲染均正确；15 回合 AI-vs-AI 压力测试全程无 `NaN`/控制台报错。

## 七十、全局字体改黑体+设置面板、野战委托与可操控范围收紧、势力一览跳转、集市一键买入、城市行情列（第六十八轮）

用户本轮提出六项需求：①全局字体从宋体改黑体，右上角新增设置入口，首期放字体选择；②野战军令栏最左侧加「委托」按钮，一键交给 AI 代打；③野战里玩家不该能操控全部己方武将，应收紧到本人/团队成员，身为城主或势力主公时才能统御本势力全部出阵武将；④全部势力一览点城池数/武将数可跳转到全部城市/全部武将并按该势力筛选；⑤集市买入页签加"一键买入所有宝物"；⑥全部城市一览日进列右侧加本地行情列。

### 一、全局字体改黑体 + 设置面板

`:root` 新增 `--app-font` 变量（默认黑体族：`Noto Sans SC`/`PingFang SC`/`Microsoft YaHei`/`Heiti SC`），`html,body` 的 `font-family` 改引用这一个变量，其余组件原本大多是 `font-family: inherit`，无需逐处改。新增 `Settings` 模块（`localStorage` 键 `wujiang_settings_v1`），提供黑体/宋体/楷体/圆体四种字体供选，`Settings.setFont(key)` 写入存档后即时 `document.documentElement.style.setProperty("--app-font", ...)` 生效，下次启动 `Settings.load()` 自动复原。右上角每块 `.audio-btns`（全站 18 处，逐屏幕各自一份）音乐/音效按钮之后新增 `⚙️` 设置按钮，统一走 `[id^="btn-settings"]` 选择器绑定（与既有 `btn-music`/`btn-sfx` 同一套模式），点开是一张列出四种字体、当前选中项高亮的浮层。Google Fonts 链接同步补上 `Noto Sans SC`，与原有 `Noto Serif SC` 并列加载。

### 二、野战「委托」按钮

军令栏最左侧新增 `🤝 委托` 按钮（`this.external.auto` 为真的全自动委外战场本就无需再委托，不显示）。点击后二次确认，置位 `this.delegated = true`，把本回合剩余未行动的部众（含此前一直亲自操控的部众）一并转交 AI（`autoFinishMyTurn()`），随后 `endTurnCycle()` 见到 `this.delegated` 便持续排定 `autoPlayMyTurn()`，无需逐回合再点，直至战斗结束；军令四钮与「结束回合」按钮委托后一律禁用。

### 三、野战可操控范围收紧

`GridBattle.controllable(g)` 原先只在玩家已自立门户（`playerFaction==="_player_"`）时把本势力全军判定为可操控，其余情形只认主角本人与 `Bond.data.team`。本轮补上"城主"（`PlayerRank.RANKS` 最高一阶，仍效力于真实势力、尚未自立）同享统御本势力全军的资格——毕竟已官至一方封疆，理应节制得动本势力出阵的每一部；同时给这套限制加了一道总开关：`this.rpg` 为假（野战演武·自由练习小游戏）时直接放行所有单位，不受身份限制，保留原有的练习/试玩体验，只有委外战场（边境战/攻城战，`rpg:true`）才收紧。落地到实际操作：`selectUnit(u)` 点选前先查 `controllable`，点不可控的部众会弹出提示"已由部将自主接战"；每个我方回合开局与每回合切回我方时，新增 `autoPlayNonControlled()` 自动帮不可控的部众行动完毕，只把真正能指挥的部众留给玩家。

**排查中顺手揪出一处真实死锁**：`challenge()`（单挑触发）原来的 `spectate: !this.controllable(att.g)` 只看攻方"理论上是否可操控"，没考虑"这一步棋此刻究竟是不是玩家亲自点出来的"。委托之后，`autoFinishMyTurn`/`autoPlayMyTurn` 仍会对主角本人这个单位调用 `aiActUnit`（毕竟主角这一部也要继续打），若恰好触发单挑，`controllable(主角)` 恒为真、`spectate` 就被判成假——单挑画面按"玩家亲自出招"打开，进入非观战模式后卡在等一次永远不会来的人工点击「出招」，全场战斗就此永久卡死。同理，委外战场全自动推演（`external.auto`，主角未被抽中亲历）时，若玩家恰是"城主"级别、其随机补位的本势力部众发起单挑，也会撞上同一个坑。修复为 `spectate: this.delegated || (this.external && this.external.auto) || !this.controllable(att.g)`——只要这一步是 AI 代打（已委托／全自动推演／攻方本就不可控，三者居一），单挑一律走观战/自动结算。

**验证**：Playwright 直接构造委外战场（`resolveBorderWar`，主角以普通"偏将"身份随军，另设 2 名团队成员）——核验主角本人 `controllable=true`、随机补位的非团队武将 `controllable=false`；`selectUnit()` 对不可控武将确实拦下、对可控武将放行；点击委托后 `delegated` 置位、军令/结束回合按钮禁用；此前会在某次自动触发的主角单挑上卡死（`turnN` 冻结在 25、`busy` 恒真、`#screen-battle` 停在"阵前单挑"不再前进），改用带追踪的 `challenge()` 复核确认卡死的正是 `spectate:false` 的主角单挑，修复后同一场景一路打到 `myAlive:1 vs foeAlive:1`（回合数过 60）仍在稳定推进，未再复现卡死。

### 四、全部势力一览点数字跳转

`AllFacUI` 的城池数、武将数两格加 `allfac-link` 类与 `data-goto` 标记，点击各自 `stopPropagation` 后跳转 `AllCityUI.open(fid)`/`AllGenUI.open(fid)`（不冒泡到整行原有的"点行看势力详情"）。`AllCityUI`/`AllGenUI` 新增 `filterFid` 状态，`render()` 按其过滤行数据，并在列表上方渲染一枚"只看：某势力〔✕〕"筛选提示条（新增 `#allcity-filter`/`#allgen-filter` 容器与 `.filter-chip` 样式），点 ✕ 清筛选。

### 五、集市一键买入所有宝物

`openMarket` 的"买入"页签新增 `bulkBuyTargets`（本城今日在售、尚未售罄的全部货摊，不分稀有度）与对应的 `🛒 一键买入所有宝物（N 件 · 约 X 金）` 按钮，与既有"一键卖出非传奇宝物"并列展示。点击先二次确认，再核验总价是否够钱（不够则整单作罢，不做"买到哪算哪"的半吊子结算），够钱则逐件走 `Bond.spend`+`Armory.makeItem` 入账。

### 六、全部城市一览增设本地行情列

`AllCityUI.rowData` 补上 `priceFactor`/`priceTxt`（复用 `cityPriceFactor`+`marketTrendSuffix`，海路中转站显示"—"），表头"日进"右侧插入"本地行情"新列，支持点列头按行情高低排序。

**验证**：Playwright 端到端跑通——设置面板切字体后 `body` 计算样式确认生效且写入 `localStorage`；点击某势力"曹魏"的城池数/武将数分别跳转到全部城市（3 座，含筛选提示条）与全部武将（4 员）；全部城市表头含"本地行情"列，样例行正确显示"⚖️公道102%"；集市一键买入 4 件宝物，花费与按钮标注金额一致，购毕全部转为"已售出"。`node --check` 复核全过。

## 七十一、委托可撤销、双方详情实时刷新、AI军令更积极、功勋榜改造战况总览、弹窗宽屏适配、首页对齐、新增字体、集市误跳转回归排查（第六十九轮）

用户本轮提出八项需求：①野战委托要可撤销，再点一次收回指挥权，且去掉点击委托时的二次确认弹窗；②委托战斗中的两军详情对比面板要随战斗实时刷新，不是打开那一刻的静态快照；③AI（含委托后的我方）军令使用要更积极更有策略；④终局功勋榜配色看不清、且与上方已有的折损武将信息重复，要求"好好规划"合并改造成一份完整的战况总览；⑤大量弹窗是竖版手机布局，浏览器/iPad/折叠屏展开态下太窄、没用好宽度，但不要简单粗暴拉满宽度、四周留白；⑥首页"新游戏"图标与下方其他按钮没对齐；⑦设置里的可选字体再加微软雅黑、苹果系统字体两个独立选项；⑧集市卖出等操作仍会出现整个弹窗消失跳回天下游历的回归，此前排查过是野战后一个设置没关，要求再查。

### 一、委托可撤销 + 去确认弹窗

`GridBattle.delegate()` 原先点一次就永久生效（且弹 `confirm()` 二次确认）。改为纯状态切换：`this.delegated` 为真时再点一次即置回假、打印"已收回指挥权"日志并 `renderBattle()`，不再有任何弹窗打断；按钮文案与图标随之切换（`🤝 委托` / `🎮 收回指挥`，`title` 属性同步给出下一步会发生什么的提示），玩家看按钮当前状态就知道点下去是委托还是收回，不需要额外确认这一步。

**排查中顺手揪出一处真实死锁式静默失效**：最初的 `delegate()` 开头有 `if (this.turnSide !== "my" || this.phase !== "battle") return;` 这道守卫——本意是"非我方回合、非战斗中不响应"，但委托之后敌我回合飞快自动切换，玩家很可能恰好在敌方回合点下"收回指挥"，这道守卫会让整次点击被静默吞掉、按钮看着点了却毫无反应，玩家只会以为按钮坏了。修复为：收回指挥权（`this.delegated` 为真的分支）不再受 `turnSide` 限制，只要战斗仍在 `"battle"` 阶段就必定生效；只有"开启委托"这一步才保留"若正巧是我方回合，立即代打完当前回合剩余部众"的逻辑（`this.turnSide === "my"` 才调用 `autoFinishMyTurn()`），若开启时恰逢敌方回合，则乖乖等 `endTurnCycle()` 轮到我方回合时按已有的 `this.delegated` 判断走 `autoPlayMyTurn()`，无需额外代码路径。

**验证**：Playwright 用真实动画节奏（未打时钟补丁）跑通委托战斗——点击委托后立即变为已委托状态、按钮文案变化，且全程未触发任何 `dialog` 事件（确认过去弹的 `confirm()` 已彻底移除）；战斗推进到敌方回合（`turnN:2`）时点击"收回指挥"，`delegated` 立即变回 `false`、按钮文案变回"🤝 委托"——用修复前的版本重跑同一测试，`afterRevoke.delegated` 仍为 `true`（点击确实被吞掉），修复后稳定复现为 `false`。

### 二、委托中两军详情面板实时刷新

`showMatchupOverlay()` 原来只在打开那一刻调用一次 `matchupDetailInner()` 拼好 HTML，此后就是一张静态快照——委托战斗后台仍在不断出手，玩家点开面板看到的兵力/士气很快就与战场实际状况对不上。改为打开时记下 `myGen = this.gen`，另起一个 `setInterval(..., 500)`：每 500ms 检查浮层是否仍处于打开状态（`overlay.classList.contains("show")`）且未切换到别的战斗（`this.gen === myGen`），只要有一项不满足就 `clearInterval` 自行停手，否则用 `wrap.outerHTML = this.matchupDetailInner()` 把两军详情整体重绘为最新数据。沿用了本会话建立的 `gen` 陈旧性防护惯例，确保浮层关闭、或玩家中途已经离开这场战斗后，这个定时器不会残留下去继续无意义地重绘或报错。

**验证**：Playwright 打开委托战斗的两军详情浮层，记录首次渲染的 HTML 快照，等待 2.5 秒（委托 AI 期间真实打了若干回合）后再次读取 HTML，两次快照确认不同（数据已刷新）；随后点击浮层关闭，1.5 秒观察期内无任何报错（确认定时器已正确停止，未残留成陈旧的重绘/报错源）。

### 三、AI 军令使用更积极更有策略

原有 `aiUseOrder`/`aiUseOrderMy` 及其调用点 `runFoeTurn`/`autoPlayMyTurn` 里，"这回合要不要试着用一道军令"固定是最多 2 次尝试、概率 55%/20%，与手头军令余量（`myOrders`/`foeOrders`，上限 `ORDERS_CAP=10`）毫无关系——余量充裕时该出手不出手，眼睁睁看着军令攒到封顶浪费；余量紧张时又可能因为固定 55% 的首轮概率而出得太随意，攒不出关键时刻。新增 `orderAttemptPlan(orders)`：余量 ≥7 时给 3 次尝试机会（概率 75%/45%/20%）、≥4 给 2 次（60%/25%）、≥1 给 1 次（40%），敌我双方共用同一套节奏，令用得的频率随余量走高走低。另外两处策略性调整：①火攻目标改为优先挑"这把火大概率能直接烧溃"的残部斩杀（`fireTargets` 里先筛 `hp<=预估伤害` 再按血量升序取，找不到才退回原来的"烧兵力最厚的那部"逻辑），与近战集火的思路对齐，军令要优先换成实打实的斩获而非平均分摊伤害；②新增兜底分支——前面所有局势判断都没触发、但军令已攒到 `ORDERS_CAP-2` 以上时，主动疑兵佯攻削一削对面最能打的那部，不再让军令白白溢出浪费。

**验证**：Playwright 拦截 `GridBattle.log()` 按关键词统计各类军令实际触发次数，让委托战斗真实跑约 20 秒（15 回合）——医疗 11 次、擂鼓 4 次、疑兵 7 次（其中新增的"军令将满·主动疑兵"兜底分支触发 2 次），全程无报错，确认军令确实比此前更频繁、更有针对性地被用掉，而非在满仓状态下持续闲置。

### 四、功勋榜改造为战况总览（好好规划后的合并方案）

**规划**：原先终局战报的 `.wdesc` 文字块里已经点名列出双方折损武将（`⚰️ 我方折损：甲、乙、丙`）和留存主力（`🛡️ 我方留存主力：丁(兵…气…)`），下方又另起一张 `.merit-board` 功勋榜按名次重复列出所有参战者——同一份"谁死了谁还活着"的信息被拆成两处不同格式各说一遍，且 `.merit-board` 当初是按深色 HUD 配色设计（`rgba(0,0,0,.4)` 底配浅色文字），实际却被嵌进浅色宣纸底的 `.result-card` 里，两套配色一叠加、文字对比度奇低，正是用户反映"看不清楚"的根源。改造方案：只留一张"此役战况总览"板——把原本分散的存亡状态、兵力、士气、功勋这几项数据合并进 `meritRankingHtml()` 产出的同一行内，`.wdesc` 里只保留精简的聚合统计（阵亡总数、头功一人）与叙事文字，不再逐名重复。

**实现**：`meritRankingHtml(ranking, opts)` 改为按行渲染两小行——第一行"名次+姓名+我方/敌方标签（+阵亡时追加'阵亡'标签）"，第二行"兵力现值/上限（阵亡则显示'－'）+士气+功勋值"；`hp` 字段是否存在决定要不要渲染第二行的兵力/士气（委外战场/边境战报调用时传入的仍是 `finalizeMerit()` 精简后的 `{id,name,side,merit,alive}`，没有 `hp` 就只显示功勋一项，向下兼容不炸）。`GridBattle.finish()` 新增 `unitById` 映射，在调用 `meritRankingHtml` 前把每位武将的 `hp/hpMax/morale` 一并合并进排名条目，随后删除原来 `.wdesc` 里的 `fallenMy.join("、")`/`fallenFoe.join("、")`/`topSurvivors` 这几行逐名文字，只留鏖战回合数、双方阵亡总数、头功一句话。CSS 同步重写 `.merit-board`/`.merit-row` 配色——背景改浅色系 `rgba(0,0,0,.05)` 描边、文字改深色（`var(--ink)` 姓名、`#8a6412` 功勋值、`var(--cn-red)`/`#2c4a8a` 分我方/敌方标签色），全部按浅色宣纸底的实际使用场景配色，不再照搬深色 HUD 那一套。

**验证**：Playwright 直接判定终局（人为将部分武将置为阵亡）读取渲染结果——`.merit-row` 恰好 20 行（双方各 10 人，无重复/遗漏）；`.wdesc` 文本正则核验不再包含"折损：""留存主力"字样（确认旧版重复文字块已移除）；随机抽查一条阵亡行渲染为"阵亡"标签、"－/－"占位、功勋 0，且 `text-decoration:line-through`；`getComputedStyle` 读取 `.merit-board` 背景色为 `rgba(0,0,0,0.05)`、姓名文字色为 `rgb(26,20,16)`（即 `var(--ink)`），确认新配色已生效且属深色系（浅底配深字，非此前的浅底配浅字）。

### 五、弹窗大屏幕响应式排版

**规划**：全站约 70 处 `openOverlay()` 弹窗几乎清一色套壳在 `.result-card`/`.detail-card` 之下，两者的宽度公式此前都是 `width: min(420px, 92vw)`——无论屏幕多宽，卡片本身永远锁死在 420px 上限，浏览器/iPad/折叠屏展开态下两侧空出大片背景，卡片内部却还是竖版手机那一套单栏挤在一起。用户特意提醒不要"为撑满宽度而过度留白"，因此没有简单粗暴地把卡片本身拉到接近全屏宽，而是分两处着手：①按视口宽度分两档适度放宽卡片本身可用宽度上限；②对天然是"一长串结构相似条目"的内容（夜报新闻、战况总览榜），额外让它们在宽屏下改用多栏排版真正吃下多出来的宽度，而不是让卡片内部的单栏内容独自被拉得又宽又空。

**实现**：`.result-card`/`.detail-card` 各自新增两档 `@media (min-width: 640px)`/`@media (min-width: 1000px)` 响应式规则（须放在各自的基础规则之后才能在层叠中生效，踩过一次"媒体查询写在基础规则前面反被覆盖回 420px"的坑，后移了 `.detail-card` 的响应式覆写位置），`.result-card` 依次放宽到 `min(560px,88vw)`→`min(620px,60vw)`，内容通常较薄的详情/列表类 `.detail-card` 放宽到 `min(680px,88vw)`→`min(860px,70vw)`；`.merit-board`（战况总览板）在 ≥640px 时改用 `column-count:2` 两栏摆放（`.merit-row` 配 `break-inside:avoid` 避免单行被从中截断），行数较多时不必再上下滚动就能看全双方参战者；宿营夜报 `.nr-body`（本就是若干条长短不一的新闻文字）在 ≥640px 时同样改 `columns:2` 报纸式排版，`.nr-sect` 同样 `break-inside:avoid` 令一整个分类尽量整段落在同一栏内。两处改动都只在弹窗内容本就是"一长串条目"的场景生效，简单的确认/结果类弹窗（几行文字+按钮）则只放宽卡片宽度、不强行拆栏，避免内容单薄的弹窗被拉出大片空白。

**验证**：Playwright 分别在手机（390px）、iPad（1024px）、桌面浏览器（1440px）三档视口下截图核验——战况总览板在手机端维持单栏滚动，iPad/桌面端自动切换为两栏、14 行武将一屏看全无需滚动，卡片宽度随视口适度放宽但未顶到接近全屏；夜报同样核验多栏排版在宽屏下把"本势力/警讯/战报"等分类合理分布到两栏、窄屏保持单栏不受影响，`.nr-item` 总数核验前后一致（确认多栏布局只是视觉重排、不影响内容完整性）。

### 六、首页"新游戏"按钮对齐修复

**排查**：用截图比对确认这不是布局意义上的错位——所有 `.menu-main .menu-btn` 的 DOM 盒模型（`left`/`width`/图标区 `miLeft`/`miWidth`）在各行之间完全一致（像素级相同），文字起始位置也逐行对齐。真正的问题出在 emoji 字形本身：`🆕`（新游戏图标）在系统 emoji 字体里天生渲染成一枚偏瘦小、贴左上角的"NEW"徽章，与 `▶️`/`📜`/`🎁`/`🕹️` 这些视觉上更饱满、居中的圆形/方形图标放在同一竖排里，观感上明显"没坐正"，即便它们所在的容器盒子其实完全对齐。

**修复**：`.menu-btn .mi` 由"只设字号、宽高随字形自适应"改为"固定 30×30 定宽定高 + `display:flex` 居中"，把每一枚 emoji 都摆进同一个视觉框正中央，字形本身的留白差异不再影响整排的对齐观感。该规则是全站所有 `.menu-btn`（含小游戏合集、天下地图等菜单）共用的基础样式，此修复顺带让其余菜单的图标对齐观感也更一致，未见任何负面影响。

**验证**：修复前后截图对比——`🆕` 图标从贴左上角的瘦长徽章变为居中方形图标，与"继续游戏""武将图鉴"等行的图标视觉重量一致；额外截图核验小游戏合集页（2 列网格布局）的图标同样正常居中、未因改动产生位移或裁切。

### 七、新增系统字体选项

`Settings.FONTS` 新增两项独立可选字体：`微软雅黑`（`stack` 以 `"Microsoft YaHei"` 开头，非 Windows 平台按序退回 `Noto Sans SC`/`PingFang SC`）与 `苹方`（`stack` 以 `"PingFang SC"`/`-apple-system` 开头）——此前这两款字体只是"黑体"选项里众多回退字体之一，用户看不到、也选不到具体是哪一款在生效；现在作为独立条目单列出来，选中后 `--app-font` 直接锁定对应 `stack`，效果所见即所得。

**验证**：Playwright 打开设置面板，确认字体列表新增"微软雅黑""苹方"两个按钮；点击"微软雅黑"后读取 `document.documentElement` 的 `--app-font` 计算值确认已切换为对应 `stack` 且已持久化到 `localStorage`。

### 八、集市等弹窗误跳转回归——根因排查与修复

**排查**：用户反映"点集市卖出按钮后整个弹窗消失跳回天下游历"这个问题此前排查过、当时认定是"野战后一个设置没关"，本轮要求再次核验。先按老思路做了一版动态复现测试——模拟"委托战斗中途按返回键离开战场→回到地图→打开集市"这一链路，跑下来虽然确认了 `handleBackAction()` 在委托战斗仍在产生新单挑时确实存在返回键容易被同一场战斗反复拉回 `#screen-battle` 的体验糙点，但并未在这一条路径上直接复现出"集市弹窗消失跳转"的核心症状，说明真正的根因另有其处。转而对全站进行静态审查：`openOverlay(html, opts)` 的 `opts.modal` 若为真则点击遮罩背景不关闭浮层，为假（默认）则点击遮罩背景会静默关闭——用一套能正确处理模板字符串里嵌套反引号/花括号插值的手写平衡括号扫描器（`.map(t=>\`...\`)` 这类调用内部还套一层模板字符串，naive 的非贪婪正则会在这里正则截断到内层的反引号，定位到错误的插入点），逐一核对全站 71 处真实 `openOverlay()` 调用，发现其中 **32 处**（含集市 `openMarket()` 本身）都遗漏了 `{modal:true}`——这意味着这些弹窗只要用户的点击不偏不倚落在了卡片本体之外的遮罩区域（在弹窗普遍偏窄、宽屏下卡片周围本就大片空白的情况下这种误触相当容易发生，与本轮第五项的宽屏留白问题互为因果），就会被静默关闭、露出底下的天下地图——这正是用户反复描述的症状，也是比"某个战斗后忘关的设置"更系统性、更能完整解释现象的根因。

**修复**：为这 32 处 `openOverlay()` 调用逐一补上 `{modal:true}`，只保留"两军阵前对比"（`showMatchupOverlay`，本就设计成点击浮层任意处关闭，非模态是有意为之）这一处例外不动。首次用非贪婪正则批量插入时踩了坑：脚本把插入位置误判到 `.map(t=>\`<button>...</button>\`, {modal:true}).join("")` 这类调用内部（正则被内层模板字符串的反引号截断），造成语法虽合法但插入位置完全错误、真正需要修的 `openOverlay()` 调用反而没被改到；`git checkout` 全量回退后改用手写的上下文状态机（跟踪 `code`/`template`/`interp`/`interp_brace`/`interp_paren` 五种状态）重新定位，逐一核对无误后才应用。

**验证**：独立静态扫描确认全站 71 处真实 `openOverlay()` 调用中，除预期的 1 处例外，其余 70 处均含 `modal:true`；`node --check` 复核通过。Playwright 用 1200×900 宽屏视口打开集市，点击弹窗遮罩区域（`(20,20)`，明显落在卡片本体之外），弹窗确认仍保持打开、标题不变；随后点击真正的关闭按钮，弹窗才正常关闭——修复前的版本重跑同一测试会在点击遮罩后立即关闭并露出天下地图，修复后不再复现。

**验证（本轮全量）**：`node --check` 复核通过；CSS 大括号配平复核通过；上述八项各自的 Playwright 端到端测试均独立通过，全程无 `pageerror`/控制台报错。

## 七十二、战报/集市弹窗被意外关闭回归排查、自选武将蜘蛛图忽大忽小修复、全部城市详情增设驿站直达（第七十轮）

用户本轮反馈三项问题：①野战结束后仍会出现"点战报区域外战报就消失"，集市里切到卖出功能后集市也会关闭——这两个问题此前已排查修复过（见上一节），这次要求再查；②在浏览器和 iPad 里玩时，自选武将角色详情页的蜘蛛图会忽大忽小；③希望全部城市一览里，若点开的城市与当前所在城池间可通驿站，能直接给一个走驿站过去的按钮，费用/行动力与驿站快马一致。

### 一、战报/集市弹窗被意外关闭——这次的真正根因

**排查过程**：先复核上一轮的修复是否仍然有效——静态审计 `openOverlay()` 全站调用，确认集市（`openMarket`）、野战终局战报（`GridBattle.finish`）、边境战报（`resolveBorderWar`）等确实都带着 `modal:true`；又用 Playwright 反复模拟点击遮罩背景、切换集市买卖页签、执行真实卖出动作，在手机/iPad/桌面三档视口下均未能复现"弹窗消失"。既然 `modal:true` 本身确凿无误，说明这次是另一条独立于"点击遮罩关闭"的关闭路径在作祟。

顺着这个思路查到 `window.addEventListener("popstate", ...)`——这是响应手机系统/浏览器返回键（含 Android 物理返回键）的同步逻辑，此前的写法是"只要弹窗开着，一律直接关掉"，完全没有看 `modal:true`。而 `popstate` 事件不只由用户主动点返回键触发：**浏览器里触控板/鼠标的"两指横滑=返回上一页"手势、iPad Safari 的"从屏幕边缘向右滑动=返回"手势，都会触发同一个 `popstate` 事件**——这正是用户反复提到"浏览器和 iPad"的场景特征（手机 PWA 独立安装后没有浏览器手势，不受影响）。用户在宽屏弹窗里想滚动列表、或想点集市的卖出页签时，一次不经意的横向滑动（尤其是触控板误触，或手指在屏幕边缘区域的一次点按）就可能被系统识别成"返回"手势，弹窗因此被这条从未考虑过 `modal` 状态的 `popstate` 处理器无差别关闭——这是一条完全独立于"点击遮罩背景"的关闭路径，也是为什么"背景点击"的 `modal:true` 修复看起来正确、问题却"又出现了"的原因。

**修复**：`popstate` 处理器改为只在 `!overlayModal`（非模态）时才 `closeOverlay()`；模态弹窗（集市、战报等需要点具体按钮才能离开的绝大多数弹窗）遇到这类误触发的返回事件，只补回一条历史记录、不做任何界面变化，逼用户走弹窗自己的按钮离开——与"点击遮罩背景不关闭"的既有 `modal:true` 语义完全对齐，堵上了这第二条此前从未被覆盖到的关闭路径。非模态弹窗（如"两军阵前对比"，本就设计成点哪都能关）的返回键行为不受影响，一如既往地会被关闭。

**验证**：Playwright 用 `history.back()` 模拟一次误触发的返回导航——集市弹窗（模态）核验前后仍保持打开、标题不变；"两军阵前对比"浮层（非模态）核验确实被关闭，行为符合其原本"点哪都能关"的设计；额外核验常规场景（首页→小游戏→物理返回键回首页）与"无弹窗时返回键仍走 `handleBackAction`"两条既有链路均未受影响。

### 二、自选武将蜘蛛图忽大忽小

**排查**：这个蜘蛛图在 `RPG.renderHub()`（自选武将角色详情/主面板）里，外框高度并非固定值，而是用 JS 在渲染那一刻测量一次右侧"评分+加点+六维"区块（`.rpg-side`）的实际高度，再把这个数字写死到蜘蛛图容器（`.rpg-radar`）的行内样式上，让两者视觉对齐。问题在于这次测量只做一次——此后浏览器窗口缩放、iPad 分屏宽度变化、地址栏收起展开引起的视口高度变化，乃至网络字体异步加载完成后的一次文字重排，都会继续改变 `.rpg-side` 的实际高度，但蜘蛛图的高度早已被行内样式锁死、不会跟着更新。宽度仍随 flex 布局走、高度却停留在渲染那一刻的旧值，两者一旦对不上，蜘蛛图的显示比例就会走样——恰好只在"浏览器和 iPad"这类窗口/视口尺寸容易变化的场景下才会被注意到，手机全屏 PWA 里窗口尺寸不会变化，自然不会触发。

**修复**：把"渲染时测量一次"改为用 `ResizeObserver` 持续监视 `.rpg-side` 的尺寸变化，任何原因导致的高度改变都会立即重新同步到蜘蛛图容器上；`renderHub()` 每次重新渲染都会创建新的 DOM 节点，因此在建立新的 `ResizeObserver` 之前先断开上一次挂的那个，避免每次刷新面板都残留一个失效的观察者。

**验证**：Playwright 打开自选武将主面板，记录蜘蛛图与右侧区块的初始高度（一致）；调用 `page.setViewportSize` 把视口宽度从 1024px 收窄到 480px（不重新渲染面板，只改变视口触发 `.rpg-side` 自然重排），等待 `ResizeObserver` 的异步回调后再次读取——蜘蛛图高度自动跟随右侧区块的新高度同步变化（230px→252px），两者始终保持一致，不再需要手动刷新页面才能对齐。

### 三、全部城市详情增设驿站直达按钮

`AllCityUI.showCity(cityId)` 新增判断：若当前所在城池（`m.curCity`）与正在查看的这座城池之间已在 `Buildings.postDests(m)`（驿站可达列表，判定条件与驿站快马面板完全相同——两端均归属本方国别、均建有驿站）名单内，详情弹窗的按钮区多一个"🏇 驿站直达（N 金 · 1⚡）"按钮，直接标出费用。点击后复用既有的 `MapUI.confirmPostTravel(m, dest)` 二次确认弹窗（与驿站快马面板、地图点击远城完全一致的文案与流程），确认后调用同一个 `MapUI.postTravel()` 完成实际的扣费/耗行动力/迁移当前城池；由于这次的触发入口是"全部城市"这个独立只读表格页面（不是地图屏），travel 完成后额外调用 `showScreen("map")` 切回天下地图，让玩家能立即看到自己已置身新城，这一点是唯一区别于原有两条入口（本就已身处地图屏）的地方。当前所在城池本身、以及未与当前城池互通驿站的城市，均不显示这个按钮。

**验证**：Playwright 构造两座己方国别、均建有 3 级驿站的城市，从"全部城市"点开目标城详情——按钮正确显示费用（148 金）；点击后弹出与驿站快马面板完全一致的确认框；确认后城池、行动力（5→4）、金币（-148，与按钮标注一致）均正确变化，且自动切回天下地图。另核验两处不应出现按钮的场景：查看当前所在城池本身、查看未建驿站的城市，按钮均正确缺席。

**其余验证**：`node --check` 复核通过；CSS 大括号配平复核通过。

## 七十三、繁荣度改为纯城建计算、新增人口系统、驻军/征兵全面转为人口驱动（第七十一轮）

用户本轮反馈两项问题：①他势力主动进犯本势力城市时，似乎只弹出一句"敌方来袭"提示就没有下文，怀疑防御战没有真正打起来；②每月集市本地行情波动之下，靠低买高卖宝物赚差价太容易发家致富，导致有钱之后城池征兵毫无阻力，希望在保留"赚差价的爽感"的前提下，给这条财路找到与征兵挂钩的合理约束，而不是简单砍掉行情差。

### 一、"防御战没有下文"排查结果

用 Playwright 直接调用 `checkFactionRaid()`/`checkBorderWar()` 强制触发一次他势力进犯，并全程跟踪 `openBorderWarPicker` 弹窗 → 选择出阵武将 → `resolveBorderWar` 结算 → 实际跳转到 `GridBattle` 棋盘对垒战场的完整链路：从提示 toast 弹出到进入真实可操作的战斗画面，全程没有报错、没有中途卡死，防御战本身的流程是完整且正常工作的。结合第七十轮已经修复过的 `popstate` 弹窗误关闭回归（横滑手势/触控板误触会把模态弹窗强制关掉），推测用户此前遇到的"只有提示没有下文"，本质上是那个已经修复的历史问题——进犯提示弹出后紧接着的选将弹窗被一次误触发的返回手势关掉了，看起来就像"喊了一声就没了"。这次没有再发现新的代码缺陷，结论已回传用户。

### 二、行情套利与征兵门槛——设计过程

这是本轮的主体工作，经过反复来回确认后才定案，中途否掉了两个方向：一是"随身货物负重上限"（挂体力/行动力算负重上限），用户认为不必做；二是给繁荣度本身设计更复杂的分层加成，用户认为没必要——最终采纳的思路是"给征兵这件事本身找一个独立于金钱的硬约束"：**城市人口**。核心逻辑是，行情差可以让玩家迅速攒起一大笔钱，但钱能兑换的士兵数量由人口决定而非钱包厚度，人口消耗后需要时间自然恢复，也会被战争损耗，从而把"暴富"和"暴兵"彻底解耦。

最终拍板的方案由用户逐项拍板确认：
- 人口上限按繁荣度分档，顶级豪华城市（★5）人口上限明确定在 **100 万**；
- 人口存在每日双向流动（当前值与上限的差额乘固定比率），既能自然增长也能在城市被攻打导致上限骤降后自然回落；
- 征兵消耗人口（1:1），且每日还有独立的征兵名额上限（与人口、演武场等级挂钩），钱、人口、名额三者同时卡住征兵速度；
- 驻军上限从"繁荣度+城墙"改为"人口+城墙"驱动，明确定在满人口+满城墙时正好是 **10 万**（此前的老公式在满配置下大致也是这个量级，用于对齐历史平衡感）；
- 繁荣度本身的计算方式借这次机会大幅简化：不再是一个需要到处手动调用 `Prosper.add()` 累加的独立计数器，而是**直接实时求和城市六项城建等级**（医馆/学舍/驿站/城墙/演武场/钱庄，每项 0~10 级，总和 0~60），按区间直接映射到 1~5 星——这样一来城市发展程度只有一份数据源（城建等级），不会再出现"城建修好了但繁荣度没跟上""某个事件加了繁荣度但城建没变"这类脱节。

开工前专门做了一轮安全性复核（用户主动要求），复核中揪出并经用户逐条拍板的问题：人口流动公式要能同时处理"上限突然降低导致当前人口反超上限"的情况（改成双向收敛公式后自动解决，不需要额外分支）；驻军回复速度原先挂在繁荣度上，改用人口驱动以保持内部逻辑一致（用户同意）；AI 势力原本"看政治评分掷骰子、不中就发一笔抽象繁荣度点数"的建城逻辑，在新模型下这笔抽象点数已经没有意义，改成"每次必定真实升一级某项城建、按实际造价扣真实金库"（用户同意）；置业/接管/扩建这几个产业动作原本会顺带给一点繁荣度，在纯城建模型下自然解耦，不再给任何繁荣度加成（用户同意，明确这是预期范围内的收窄）。攻城破城后的人口折损比例也来回调整过一轮：从最初的"打七折到八折（损失 20%~40%）"改为用户最终拍板的"打七到九折（损失 10%~30%）"，即为破城战争损耗定了更温和的基调。

### 三、最终实现：繁荣度 / 人口 / 驻军 / 征兵 四套公式

**繁荣度**（`Prosper`，纯派生值，不再存储）：
```
buildSum = 六项城建等级之和（0~60，每项 0~10 级）
繁荣度：buildSum < 12 → ★1；<24 → ★2；<36 → ★3；<48 → ★4；其余 → ★5
```

**人口**（`Population`，新增的有状态、可消耗可回复的城市数值）：

| 繁荣度 | ★1 | ★2 | ★3 | ★4 | ★5 |
|---|---|---|---|---|---|
| 人口上限 | 30 万 | 45 万 | 60 万 | 80 万 | 100 万 |

每日流动：`人口 += round((上限 − 人口) × 3%)`，同一公式天然覆盖"人口低于上限时增长"与"上限骤降后人口向下收敛"两种情形。

**驻军**（`Garrison`，公式改为人口驱动，满人口+满城墙时精确等于 10 万）：
```
驻军上限 = round(人口 × 9% + 城墙等级 × 1000)
每日回复 = round(300 + 人口 × 0.001)
```

**征兵**（新增每日名额，与钱、兵额、人口三重挂钩，次日清零重算）：
```
每日征兵配额 = round(人口 × 0.5% × (1 + 演武场等级 × 15%))
募兵实际数量 = min(兵额余量, 金币可负担数, 当日剩余配额, 当前人口)，1:1 消耗人口
```

### 四、初始城建预置与攻城战争损耗

新开局不再是所有城市清一色的空白城建。按史实重要程度把陆地城池分成三档，各档给定一个固定的"城建等级总和"，但**每一局具体分配到六项城建上的比例都是随机的**——保证发展档位稳定（同一档次总繁荣度体验一致），又保证"每次开局，同一座城市可能偏重钱庄也可能偏重演武场"，不会千篇一律：

- **史名大城**（洛阳/许昌/成都/建业/京都/大坂/尾张/骏府，共 8 座）：城建总和 24（对应 ★3）
- **普通城市**（其余所有陆地城市）：城建总和 12（对应 ★2）
- **边陲小城**（白帝城/上庸/奥州/肥后/常陆，共 5 座）：城建总和 0（★1，白手起家）

攻城破城后，被攻克的城市统一承受战争损耗（不分玩家/AI势力，谁攻下都一样）：人口按 0.7~0.9 的随机比例打折（即损失 10%~30%）；六项城建各自独立判定，60% 概率损失 1~3 级（不低于 0 级），40% 概率毫发无伤——损耗后的城建等级会立刻让繁荣度自动重新计算，连带把人口上限、驻军上限一并同步降下来，不需要任何额外收尾代码。

### 五、UI 同步与端到端验证

驻军面板（`openGarrison`）、地图城池面板的驻军按钮、"全部城市"总览表与城池详情弹窗，均同步加上了人口存量/上限、当日剩余征兵配额的展示；驻军面板的募兵滑杆上限也补上了"当日剩余配额"这一约束项，此前遗漏这一项会导致滑杆允许拖到超过配额、实际募兵时被静默截断却不说明原因，现已修正为滑杆本身就不会超过真正能募到的数量，且用完配额/人口枯竭时会给出对应的提示文案而非笼统的"金币不足"。

**验证**：Playwright 分别核验了初始城建三档随机分布（史名大城/普通城市/边陲小城的总和分别稳定为 24/12/0，六项城建的具体分配逐局不同）；攻城战争损耗（连续多次模拟攻城，人口保留比例稳定落在 0.7~0.9 区间，城建等级与繁荣度联动下调，归属正确转移）；AI 势力建城行为（金库充裕时必定挑一座有空余城建位的城市真实升级并按 `Buildings.COSTS` 实扣金币，全势力城建都点满后正确返回"未消费"而不再乱扣钱）；驻军面板/全部城市表/城池详情三处 UI 均正确显示人口与配额且无报错。另外专门补跑了一轮 120 天的端到端推演（每 5 天随机制造一次攻城战、每天各势力尝试建城），持续采样全图城市的人口/驻军/上限，未出现 `NaN`、负数，驻军上限也始终没有超过设计死线的 10 万、人口上限没有超过 100 万，城市在被攻破后能观察到繁荣度、人口、驻军同步回落又逐步自然恢复的完整曲线，未发现某座城市"打崩后再也起不来"或数值失控暴涨的情况；此外重新跑了一遍此前已验证过的边境战完整链路（提示 → 选将弹窗 → 结算 → 棋盘对垒画面），确认这次的重构没有影响到防御战的既有链路。

**其余验证**：`node --check` 复核通过；全文检索确认无任何遗留的旧版 `Prosper`/`Garrison` 常量（`BASE_CAP`/`PROSPER_CAP_STEP`/`PTS_PER_GOLD` 等）引用。

## 七十四、宝物库一键拆解、野战按统帅值补位出手、以及一处弹窗被误关闭的真正根因（第七十二轮）

用户本轮反馈三项：①宝物库里逐件点「拆解」太麻烦，希望能一键拆掉所有非传说级、未装备的宝物；②野战里自选武将参战（非主公/城主身份，只能操控自己这一部）时，本部武将总是全场最后一个出手，队友都已经打完仗，导致夹击拉不到人；③野战之后（或还有别的诱因）经常出现点了弹出界面里某处、整个弹窗却直接没了的情况，游戏卡在等一次永远不会来的点击上，只能重启。

### 一、宝物库一键拆解

`Armory` 新增 `dismantleAllNonLegendUnequipped()`：一次性筛出「已鉴定 · 未装备 · 非传说」的全部宝物批量拆解，返还材料；神秘宝物（未鉴宝，拆解入口本就不放行）、已装备的宝物、传说级宝物一律跳过，不参与批量操作——传说级排除在外是有意为之，避免误拆代价过高的稀缺宝物。仓库页签新增「🔨 一键拆解非传说未装备（N）」按钮，点击前二次确认（不可撤销），拆解后 toast 汇总各类材料的实际获得数量。

**验证**：构造普通/传说/已装备/未鉴定四种宝物各一件，调用批量拆解后核验：只有普通未装备那件被拆掉并正确入账材料，其余三件原样保留、材料不变。

### 二、野战按统帅值补位出手——修正"自选武将永远最后出手"

**排查**：野战里"主角亲历但队友是候选池自动补位、不受你直接调度"这种半自动场景（`this.rpg` 为真、未主公/城主身份），原逻辑是 `autoPlayNonControlled()`——每个我方回合一开始，就把所有不可控的队友按数组原始顺序**一次性全部**打完仗，然后才把回合交给玩家操作自己那一部。问题在于 `eligibleFlankers()`（判断谁能加入夹击）要求同袍必须"未行动"（`!acted`），而队友全部提前出手完毕后早已清一色"已行动"，玩家轮到自己出手时，身边能拉来夹击的同袍事实上一个都不剩——夹击这个机制对自选武将玩家形同虚设。

**修复**：新增 `sortedAliveByTong()`，把"本回合尚未行动的存活武将"按统帅值从高到低排成一份快照顺序；`autoPlayNonControlled()` 改造为 `advanceAutoPlay()`——按这份统帅顺序依次自动接战，一旦排到玩家可操控的那部就立即停手，把回合交还给玩家（统帅比玩家高的队友仍会先手，符合"统帅代表用兵反应速度"的设定）；玩家出手完毕后（`afterAction()` 收尾时）再接着调用 `advanceAutoPlay()` 往下推进，让排在玩家之后（统帅比玩家低）的队友继续由 AI 补打完——这样玩家出手那一刻，统帅比自己低的队友仍处于"未行动"，可以正常被拉来夹击。委托全自动（`autoFinishMyTurn`）、全自动推演（`autoPlayMyTurn`）与敌方回合（`runFoeTurn`）的内部行动顺序也一并按统帅值重排，行为更统一。

`advanceAutoPlay()` 本身有三重把关：只在真正的"我方回合、非委托、非全自动推演"场景下才会做事，敌方回合/委托/全自动推演各自另有独立的整队循环，互不干扰；另加一个 `_advancing` 防重入标记——武将的主动战法与单挑（`useActiveSkill`/`challenge`）AI 与玩家共用同一份实现，末尾都会调用 `afterAction()`，若不设防重入，AI 出招时也会稀里糊涂触发一次 `advanceAutoPlay()`，与外层正在跑的循环并发抢同一个武将的行动权，导致双重出手。

**验证**：构造统帅 70 的自选武将 + 统帅 90/50 的两名队友，开战后确认统帅 90 的队友已自动出手、自选武将与统帅 50 的队友仍未行动；模拟自选武将「待命」结束行动后，确认统帅 50 的队友随即被 AI 补打完毕。另跑了委托全自动、自由练习两种场景的回归，均无报错、无卡死、无双重出手迹象。

### 三、弹窗点击某处就整个消失——找到了真正的根因

这是本轮排查的重点，此前已经两次以为修复完毕（背景遮罩点击、返回手势 popstate），但这次是第三条、此前完全没被覆盖到的关闭路径。

**排查**：全局弹窗系统由 `openOverlay()`/`closeOverlay()` 两个函数与共享的 `#overlay-content` 容器节点构成，`modal:true` 的弹窗理应点哪儿都不会被误关。顺着"点弹窗内容区域会关闭"这个线索去找，命中了野战「两军阵前对比」浮层的实现——`showMatchupOverlay()` 里直接写了 `$("#overlay-content").onclick = () => closeOverlay();`，把"点内容任意处关闭"这个处理器**直接挂在了全站共用的 `#overlay-content` 容器节点本身**，而不是挂在这层浮层自己的内容包裹元素上。`openOverlay()` 每次开新弹窗只会 `.innerHTML = html` 替换子节点，从不会去清空容器节点自己身上挂的 `onclick`——这意味着玩家只要在任意一场野战里点过一次顶部兵力条弹出的「两军阵前对比」，这个「点哪儿都关」的处理器就会**永久残留**在这个全局共用的容器上，此后无论切到哪个界面、隔了多久，只要再打开任何一个新弹窗（哪怕是 `modal:true`、正在等待玩家做出关键选择的弹窗，比如边境战选将、驻军募兵），只要点到按钮之外的空白处，就会被这个陈年处理器误当成"点内容关闭"而直接清空——玩家等着按的按钮随弹窗一起消失，游戏卡在一个再也不会到来的点击上，只能重启。这正好解释了用户描述的"野战之后"更容易复现——因为触发点恰恰就在野战界面里。

**复现验证**：临时还原成旧代码逻辑后用 Playwright 复现——打开一次「两军阵前对比」浮层并点击关闭后，紧接着打开一个 `modal:true` 的驻军面板，点击面板空白处，面板确实被误关闭（`stillShownAfterBlankClick: false`），完整复现用户描述的症状。

**修复**：两处双重加固——① `openOverlay()` 每次开新弹窗时，先把 `#overlay-content` 容器自身的 `onclick` 清空，作为兜底防线，堵住这一整类"某个弹窗给共享容器挂了处理器却从不清理"的泄漏；② `showMatchupOverlay()` 本身也改为把"点内容任意处关闭"的处理器挂在浮层自己的包裹元素（`.fg-matchup-overlay`）上，而不是共享容器——这样它会随下一次 `openOverlay()` 替换 `innerHTML` 而自然一并消失，从源头上不再产生这类残留。

**修复后验证**：同一套复现脚本，打开对比浮层关闭后再打开驻军面板、点击空白处，面板正确保持打开（`stillShownAfterBlankClick: true`）；对比浮层自身"点浮层内任意处关闭"的原有体验不受影响。

**其余验证**：`node --check` 复核通过；全文检索确认 `#overlay-content` 上不再有其它类似的裸 `onclick` 赋值。

## 七十五、宝物库一键拆解按钮文字看不清（第七十三轮）

用户反馈上一轮新增的"一键拆解非传说未装备"按钮只看得见锤子表情，后面的文字消失了。

**排查**：文字其实一直都在，只是撞上了颜色对比度问题——`.btn-ghost` 默认给的是 `--ink`（近黑色）字色，这是给「结果卡」那类浅色纸卡背景配的深色墨字；但这个按钮所在的「仓库」页签是深色底（与它上方筛选下拉框统一用浅色字 `--paper` 的道理一样），深色字落在深色底上自然只剩下不受 CSS 颜色影响的锤子表情还看得见，其余文字近乎隐形。

**修复**：`.stock-bulk-row .btn-ghost` 改为浅色字 + 半透明白底 + 金色描边（参照 `.cup-go` 在同类深色页签里的配色思路），不再吃 `.btn-ghost` 默认给浅色纸卡用的深色字。

**验证**：Playwright 截图核验，按钮文字「🔨 一键拆解非传说未装备（N）」在深色背景下清晰可读。

## 七十六、天下地图改为真实地理海岸线（第七十四轮）

用户反馈天下地图目前就是两块纯色色块（勾了个大致轮廓的中国、日本），没有任何地貌可言，希望参考真实地图或其他游戏的做法改进；追问后明确要求要"逼真度高的真实地理"。

### 数据来源与处理流程

地图海岸线数据取自 [Natural Earth](https://www.naturalearthdata.com/) 1:50m 精度的国界矢量（公有领域数据，`nvkelso/natural-earth-vector` 仓库的 `ne_50m_admin_0_countries.geojson`），从中提取中国、日本两国的真实海岸线多边形：

- **中国**：取最大的那一圈主陆地边界（原始 2478 个顶点），裁剪到三国故事主要发生的舞台——巴蜀、关中、中原、荆襄、江南一带（经度 97°~123°E、纬度 21°~40.5°N）；裁剪后西/北/南三边是理论裁切框线（这一带内陆本就不靠海，裁切边界不必是海岸线），东边保留了真实的黄海/东海海岸线，包括山东半岛的凸起、长江口一带的锯齿状海岸都如实保留。
- **日本**：从原始 34 块岛屿多边形里挑出本州、九州、四国三块主岛，另外找到了对马岛这一块独立的小岛（游戏里"对马岛"本就是一座独立城池），四块合并使用；不含北海道与琉球群岛——两地在游戏的战国时代都没有城池分布，画进来只会占地方、拉伸比例。
- 两国**共用同一套投影与比例尺**（等距圆柱投影，按两地平均纬度做经度余弦修正），保证中日之间的相对距离、各自陆地的长宽比例都是真实的，不是各自独立拉伸凑出来的"看着差不多"。
- 用 Douglas-Peucker 算法把每块海岸线简化到一两百个顶点（原始数据几千个顶点对一张手机小地图来说没必要、还拖累渲染），最终中国主陆地约 243 点，本州约 101 点，九州约 57 点，四国约 29 点，对马岛约 10 点——肉眼看轮廓细节基本不受影响，文件体积和渲染开销却小了一个数量级。
- 额外按大致真实流经路线加了长江、黄河两条线（关键点连线，不追求逐水文精度），进一步增加"这是一张真地图"的可信度——黄河在中原一带的北折向南绕行、长江贯穿荆襄江南的走向都清晰可辨。

### 城池坐标改为真实经纬度换算

全部 40 座城池（含"对马岛"）的坐标改用其历史对应今地的真实经纬度（如洛阳、长安=西安、建业=南京、京都、大坂等），套用与海岸线完全相同的一套投影/比例尺计算出画布坐标，写回 `CITIES` 数组——城池点位因此与新海岸线严格对齐，不会出现城池飘在海里或飘出陆地轮廓的情况。原有的城池连线（`ROADS`，按 id 连接，不受坐标变化影响）、点击跳转、势力配色、缩放/拖动/聚焦当前城市等全部功能均直接复用，未改一行相关逻辑。

技术上，城池坐标原本就是靠 `left:x%;top:y%` 的 HTML 定位、与 SVG 各自独立按 x/y 跨度拉伸对齐既有的 `viewBox="0 0 100 100"` + `preserveAspectRatio="none"` 架构（SVG 本就不保真实长宽比，会被拉伸铺满手机屏幕的任意比例容器）；新海岸线与新城池坐标沿用同一套"按 x/y 各自独立归一化到 0~100"的换算，保证无论最终容器是什么形状，海岸线和城池点永远严丝合缝地对在一起，不会出现两者各自拉伸走样、逐渐错位的问题。

**验证**：Playwright 截图核验天下地图默认视图（中国大陆真实海岸线清晰可辨、山东半岛/长江口轮廓可见，长江黄河两条河流可见）；聚焦日本京都的缩放视图（本州海岸线曲折可辨，各令制国城池点位准确落在陆地上，不重叠出海）；点击相邻城池验证地图交互（选中态切换、势力/势力legend、缩放控件）均未受坐标变更影响，无报错。

**其余验证**：`node --check` 复核通过。

## 七十七、天下地图铺设卫星地形栅格底图（第七十五轮）

上一轮把地图的海岸线换成了真实数据，但陆地本身仍是纯色块。用户明确要求"卫星视图"级别的地貌——要能看见山丘、河川、平原这些真实地形起伏，不只是一个描了真实轮廓的色块。

### 方案：Natural Earth 明暗浮雕地形栅格 + 矢量海岸线双层叠加

去 Natural Earth 官方下载了 1:50m 精度的《Natural Earth I with Shaded Relief》全球地形栅格图（`NE1_50M_SR_W`，10800×5400px，公有领域数据），按经纬度换算像素坐标，裁出覆盖中国东部＋日本列岛＋朝鲜半岛＋台湾岛这一整片区域的一块（裁剪范围比矢量海岸线略大，让朝鲜半岛、台湾岛作为背景陪衬一并露出，更有"这是一张真地图"的说服力），转存为 JPEG（82% 质量，约 180KB，控制在移动端可接受的加载体积内），作为新增静态资源 `assets/map/relief.jpg` 随 Service Worker 一并离线缓存。

这块栅格图本身就是按经纬度真实呈现的地形晕渲（mountains 用等高线阴影表现起伏，plains 用平坦浅色区块表现，连海底地形的洋脊也画出来了），不需要额外画山丘图标或做等高线，比自己拼贴装饰性地貌图标真实得多。

**关键技术点——矢量与栅格像素级对齐**：上一轮的矢量投影带了纬度余弦修正（等距圆柱投影的标准做法，为了让形状看起来更"正"），但这一轮栅格图本身是最朴素的等距投影（每像素固定对应 1/30 经纬度），两者若继续各用各的投影公式，海岸线矢量描边和栅格图的实际海岸线会对不齐、错开一截。这一轮把矢量投影统一改成与栅格完全一致的朴素等距投影（不做余弦修正），城池坐标、长江黄河两条河流线也一并按新投影重算——现在无论怎么缩放拖动，金色描边的矢量海岸线都精确压在栅格图的真实海岸线上，城池点位也稳稳落在真实陆地范围内。

铺了栅格底图之后，原本给中日两国区分色块的纯色填充（红/蓝半透明）不再需要靠自己"表意国别"——那样反而会盖住底下的地形纹理——改成极淡的色彩提示（透明度从 0.22/0.26 降到 0.08/0.1）+ 更清晰的金色描边，把山丘、平原、水系这些真实地貌完全交给栅格图自己说话。

**验证**：Playwright 截图核验默认视图（中国大陆地形起伏清晰可见——四川盆地周边山地、华北平原的平坦区块、渤海及黄海海底地形均可辨认，矢量海岸线与栅格图实际海岸线完全重合，无错位）；聚焦日本京都的缩放视图（本州山脉走向、九州四国轮廓与栅格图完全对齐，城池标签在纹理复杂的背景上仍清晰可读）；城池点击跳转、地图缩放拖动、势力图例等既有交互功能核验无回归。

**其余验证**：`node --check` 复核通过；确认 `sw.js` 的离线缓存清单已加入新的地图栅格资源，游戏离线运行时地图底图不会缺失。

## 七十八、天下地图四项体验反馈：拖动越界、放大不够、城池改图标、道路线补描边（第七十六轮）

铺上卫星地形底图后用户提出四点体验反馈：①拖动地图时会露出地图范围之外的空白区域；②放大倍数不够大，日本那片城池密集，挤在一起看不清；③城池标记想改成一座座城池图样（参考其他游戏做法），且按城市规模星级呈现不同图样；④真地图背景纹理复杂，原来的城池连线（虚线）变得很难看清。

### 一、拖动越界

`clampZoomState()` 里平移边界原本是 `(scale-1)*宽/2 + 40`——多留的这 40px 是旧版纯色块地图时代刻意给的"弹性余量"，让拖到头时不会硬邦邦地卡住。换了写实卫星底图后，这松出来的 40px 露出的是地图内容之外、`.map-svg-box` 容器本身的空底色，一眼就能看出"这里没有地图了"，观感比旧版明显更违和。直接去掉这个 `+40`，平移边界严格收在地图缩放后的实际范围内。

**验证**：Playwright 模拟拉到最大缩放后分别往四角狂拖，截图核验画面自始至终被地形栅格（或代表深海的蓝色区域）填满，任何角落都不会露出容器背景。

### 二、放大倍数

最大缩放从 3 倍提到 8 倍，+/− 按钮单次步进从 0.4 提到 0.7（省得倍数上限提高后要多点很多下），「聚焦当前城市」的默认聚焦倍数也从 2.2 倍提到 4.5 倍——日本地区那些历史上不小但地理面积确实比中国裁剪区域小得多的令制国城池，放大后终于能摊开看清彼此，不再挤成一坨。

**验证**：截图核验聚焦京都后的画面，此前挤在一起分不清的近江、越前、加贺、常陆等城市标签之间已有明显间距，各自可读。

### 三、城池改用图标图样，按繁荣度星级分级

原来的城池标记是一块贴着势力色底、写着城名的圆角色块，铺上写实地形栅格之后，这种纯色块显得又扁又假、格格不入。改为一座简化的城池剪影图标（新增 `cityIconSvg()`，城墙+雉堞的基础剪影，`CITY_ICON_PATH` 定义），填色仍用势力本色，按 `Prosper.lv`（城市繁荣度星级，1~5）分五级呈现：

- ★1~2：朴素的小型城寨轮廓，级别越高城墙略宽一分
- ★3 起：城头加一面小旗
- ★5：旗杆更高、旗面更大，一眼可辨"这是一座大城"

图标本身随星级放大（13px～26px），城名标签改为图标下方的小型半透明黑底文字（不再是撑满势力色的大色块），当前所在城池/势力城池的身份标记也从色块勾边改为贴合图标剪影轮廓的金色辉光（`drop-shadow`），更精致也更不遮挡底下的地形纹理。海路中转站（对马岛）无城建可言，按最低星级（★1，最小图标）处理。

**验证**：截图核验默认视图与聚焦视图下，五档星级的城池图标大小/是否挂旗均按预期区分（成都★3 有旗、边陲小城★1 无旗且图标最小），势力配色与"当前/己方/相邻/远方"四态视觉状态未受影响。

### 四、道路连线描边

原本的城池连线是一条低透明度金色虚线（`stroke: rgba(212,175,55,.32)`），在纯色块背景上尚可辨认，压在纹理复杂的卫星地形上就经常被"吃掉"、忽隐忽现。做法是给每条连线先垫一条更粗的深色实线（`.map-road-halo`，纯黑半透明、无虚线），再在其上叠加原有金色虚线并调高不透明度（0.32→0.85）——两条线共用同一套坐标数据、按相同顺序批量生成，不管连线压在山地、平原还是海面上，都有深色描边兜底保证辨识度。

**验证**：截图核验连线在山地纹理密集区域、平原浅色区域上均清晰可辨，不再出现虚线被地形颜色"融掉"看不清的路段。

**其余验证**：`node --check` 复核通过；重新核验城池点击跳转功能未受图标化改造影响。

## 七十九、天下地图四项再反馈：西边裁剪、城池防重叠、双击定点放大、图标固定视觉大小（第七十七轮）

上一轮铺了卫星地形+城池图标后，用户又提出四点：①中国地图成都左边空出一大截没有城市的地方，该截掉；②城池图标现在挤在一起重叠了，图标不能重叠，城名可以放进图标里；③地图要支持双击屏幕后按双击的位置放大；④日本那片还是太小——城池图标不要跟着地图一起放大，放大地图时图标应保持固定视觉大小，城名文字也一样。

### 一、裁掉成都以西的空地

矢量海岸线与栅格底图的西边界统一从东经 97° 收紧到 101°——成都（东经 104.06°）以西留出约 3 个经度（约 300 公里）的余量，不再像上一轮那样空出一大片没有任何城池、纯粹是青藏高原边缘的荒地。栅格底图同步收紧裁剪范围。

### 二、城池防重叠

新增一次基于力导向的松弛处理：给每座城设定一个远小于图标真实像素占用（考虑到城名已并入同一个视觉单元）的"占位半径"，两两若小于这个半径就相互推开，同时用一个不弱的回拉力把每座城钉在其真实经纬度换算出的位置附近，避免松弛跑得太远、糊掉地理真实感。第一版尝试过按图标真实像素大小去做"绝对零重叠"，结果因为日本本州中部真实历史上就有一堆挨得极近的令制国（京都·大坂·近江·美浓·尾张……），要满足全部城两两不挨着，个别城市被迫推出去了半张地图那么远——完全不能要。改用小得多的占位半径后，绝大多数原本完全重叠的城池现在都有了间距，少数历史上确实紧邻的城市仍会贴得比较近（配合下面第③④两项的双击定点放大，玩家可以自行放大到这些区域细看，不必靠打乱地理位置来解决全部拥挤）。

城名不再是图标下方单独的一行文字标签，现在算作图标所在这枚标记的一部分（同一个 `.map-city` 容器，一起参与防重叠计算、一起响应下面的固定视觉大小），视觉上更像"一座带名字的城池"而不是"一个图标+一行浮字"两个各自漂移的元素。

### 三、双击定点放大

新增 `MapUI.zoomAtPoint(box, clientX, clientY, targetScale)`：先按当前的缩放/平移状态反推出双击的这个屏幕像素点对应的地图归一化坐标，换到目标缩放倍数后，再反过来解出让这个坐标仍显示在同一屏幕位置所需的新平移量——双击哪里就以哪里为中心放大，而不是永远从画面正中心放大导致点开的地方跑出视野。识别双击/双次点按沿用触摸端惯用的"两次点按的时间间隔<350ms 且位置偏移<30px 才算一次双击"判定，不依赖浏览器原生 `dblclick`（移动端对合成 dblclick 事件的支持不够一致）。每次双击放大到当前倍数的两倍（封顶 8 倍），与主流地图应用的手感一致，不会一步跳到最大。

### 四、图标/城名固定视觉大小

这是本轮最关键的一处改动，从根本上让"放大地图看局部"这件事变得好用：城池图标本身仍留在会被地图整体 `scale()` 的 `.map-zoom-layer` 容器里（这样它们的锚点才能继续正确跟随地图缩放/平移），但在 `.map-city` 自身的 CSS transform 上叠加一个反向的 `scale(var(--zk))`，`--zk` 由 `applyZoom()` 每次更新缩放状态时按 `1/MapZoom.scale` 实时写到 `.map-svg-box` 上、由所有城池标记继承——两次缩放相乘的净效果就是"标记的锚点位置跟着地图走，但视觉尺寸恒定"，地图放大到 8 倍，图标和城名文字看起来还是跟 1 倍时一样大，不会随着放大变成一坨糊在一起的色块。这样一来，放大地图这件事就从"能看清更大范围的细节，但图标本身也跟着放大、可能反而更占地方"，变成了纯粹的"把原本挤在一起的锚点在屏幕上拉开距离"，对解决城池密集区域的可读性问题更直接有效。

**验证**：Playwright 截图核验裁剪后的中国地图西边界不再留白；城池防重叠后核验此前完全重叠的几处坐标均已有可辨间距；模拟双击地图右侧（日本方向）后核验画面确实以双击位置为中心放大到 2 倍；核验放大前后同一座城池（京都）的图标渲染像素宽度完全相同（19px→19px，未随地图放大而变化）；核验单击城池仍能正常跳转、极限缩放+四角拖拽仍不会露出地图外空白（回归自上一轮）。

**其余验证**：`node --check` 复核通过。

## 八十、城池连线固定视觉宽度、城池图标放大三倍（第七十八轮）

用户又提两点：①城池连线也要跟城池图标一样，放大地图后不跟着一起变粗；②城池图标和文字现在偏小，再放大 3 倍。

### 一、城池连线（及海岸线/河流描边）固定视觉宽度

排查发现 `vector-effect="non-scaling-stroke"` 只能抵消 SVG **自身坐标系内**的缩放（viewBox 换算、SVG 内部 transform），抵消不了 `.map-zoom-layer` 这层——它在 SVG **外部**用 CSS `transform: scale()` 施加整体放大，从 SVG 自己的渲染管线来看什么都没变，等浏览器合成时才把整张已经画好的图按倍数放大，描边粗细自然被一起放大了。用 Playwright 实测验证：放大到 5.2 倍后，连线的渲染宽高确实跟着长到了原来的 5 倍多。

修复思路与上一轮城池图标的 `--zk` 反向缩放同源：把 `stroke-width`、`stroke-dasharray` 都改成 `calc(原始值 * var(--zk, 1))`（`--zk = 1/MapZoom.scale`，已经挂在 `.map-svg-box` 上），放大倍数越高、这些属性值就被 JS 侧同步缩得越小，两次缩放相乘净效果就是线宽、虚线疏密恒定。这次不只改了城池连线（`.map-road`/`.map-road-halo`），海岸线描边（`.map-land`）和长江黄河的河流线（`.map-river`）也顺带一并修了——它们和连线共享同一个"vector-effect 挡不住外层 CSS 缩放"的根因，不修的话地图放大后海岸线也会变得又粗又糊。

**验证**：Playwright 核验放大到 5.2 倍后，连线的 `stroke-width` 计算值从 1.6px 正确缩到约 0.31px（1.6÷5.2），乘回缩放倍数后净视觉宽度与放大前一致；截图核验放大后连线、海岸线、河流线均保持纤细，未出现变粗糊化。

### 二、城池图标/文字放大 3 倍

`CITY_ICON_SIZE` 五档星级对应的图标边长从 `[13,16,19,22,26]` 提到 `[39,48,57,66,78]`（各档恰好 3 倍）；城名字号从 6px（当前所在城 7px）提到 18px（当前所在城 21px），连带阴影/辉光的模糊半径等细节尺寸同步等比放大。由于图标已经是"固定视觉大小、不随地图缩放"（见上一轮），这次放大是直接、永久地把默认显示大小调大，不需要玩家借助缩放才能看清。

**必须提醒的一处取舍**：图标放大 3 倍后，中国腹地、日本本州中部这些历史上城池本就密集的区域，在地图默认（未放大）视图下会比上一轮更容易看到图标彼此靠近甚至轻微遮挡——这是"图标固定尺寸变大"与"城池防重叠松弛的间距还是上一轮按更小图标算出来的"两者叠加的直接结果。没有在这一轮同步放大防重叠间距，是因为上一轮已经验证过：把间距参数跟着图标尺寸等比例放大到能保证零重叠，会在日本中部这类真实历史上极度紧凑的区域引发松弛算法失控式发散（个别城市被推出去大半张地图远，完全不能要）。目前的应对仍是双击/多次点按放大——玩家放大到这些区域后，因为图标不随之变大，紧凑的锚点会被拉开距离，可读性会明显改善。如果放大 3 倍后默认视图的拥挤程度仍然不能接受，需要另外单独调整防重叠参数或城池布局，而不是简单再等比例放大间距。

**其余验证**：`node --check` 复核通过；重新核验单击城池跳转、拖拽平移边界等既有功能未受影响。

## 八十一、地图新增中/日一键缩放按钮、城池图标设备自适应大小（第七十九轮）

用户又提两点：①希望在"显示全景"按钮下方再加两个按钮，分别是中国、日本国旗，点击后把地图缩放到正好把该国全部城池纳入视野；②城池图标在手机上看太大，但在电脑浏览器上看正好，需要按设备自适应调整。

### 一、一键缩放至某国全境

新增 `MapUI.zoomToFitSide(box, side)`：取该国（`cn`/`jp`）全部城池坐标的外包框（四周留一圈余量，避免边缘城池的图标/城名贴边被裁切），按 x、y 两个方向分别算出恰好能把这个外包框整个纳入当前地图框视野所需的缩放倍数，取两者中较小的一个（否则较大的那个方向会超出视野），再用与「聚焦当前城市」相同的换算方式把外包框中心对准画面正中心。地图控件区域（`.map-zoom-ctl`）在"显示全景"按钮下新增两个按钮：🇨🇳（缩放至三国全境）与🇯🇵（缩放至战国全境）。

**验证**：Playwright 分别点击两个新按钮后，逐一核验该国全部城池的图标中心点均落在地图框可视范围内（留了 5px 容差），中国、日本两侧均全部通过；截图核验缩放后的构图美观、各城池间距合理可辨。

### 二、城池图标/城名设备自适应大小

排查确认：`地图整体不设最大宽度上限`（`#app` 无 `max-width`），意味着桌面浏览器窗口越宽，`.map-svg-box` 就跟着铺得越宽；而上一轮固定死的图标像素尺寸（39~78px）不会跟着容器宽度变化——同样的绝对像素值，在桌面这种被拉得很宽的大容器里显得恰到好处，摆到手机这种本就只有 380~420px 宽的窄容器里就顶了很大一块屏幕，两头没法兼顾。

新增 `MapUI.applyResponsiveScale(box)`：以「桌面浏览器下容器宽度约 800px 时当前尺寸看起来刚好」为参照，按 `.map-svg-box` 实际渲染宽度相对这个参照值取比例（`rect.width / 800`），夹在 0.5~1 之间——容器越窄，图标/城名等比缩得越小（窄到手机的 380~420px 时封底到 0.5 倍，即约 19.5~28.5px，比放大 3 倍前的老尺寸还略大但已明显不再"顶屏幕"）；容器越宽最多缩回 1 倍（原始设计尺寸），不会因为超宽显示器而继续放大失控。这个比例通过新增的 CSS 变量 `--rk` 与既有的缩放无关变量 `--zk` 在 `.map-city` 的 `transform: scale(calc(var(--zk,1) * var(--rk,1)))` 里相乘生效，二者各管一段、互不干扰：`--zk` 保证放大地图时图标不跟着变大，`--rk` 保证不同设备/窗口宽度下图标的"基准大小"本身就取得合适。

`--rk` 在地图每次渲染绑定缩放时计算一次，另外全局注册一个仅生效一次的 `window resize` 监听器（每次触发都现查当前 `.map-svg-box` 重新计算，不闭包捕获某一次 `render()` 传入的旧 box 引用），处理桌面浏览器调整窗口宽度这种地图未重新渲染也需要跟着重算的场景，避免每次地图重渲染都新增一个监听器、长时间游玩后每次缩放窗口要白跑一堆指向已失效旧节点的老监听器。

**验证**：Playwright 分别在 380px（窄手机）、420px（常规手机）、800px（平板/小桌面）、1400px（宽桌面）四档视口下核验：手机档 `--rk` 命中下限 0.5、图标渲染宽度 28.5px；平板档 `--rk`≈0.97、图标≈55px；宽桌面档 `--rk`=1、图标=57px（与放大 3 倍前设计的原始尺寸一致）——数值曲线符合预期，手机端图标肉眼观感相比上一版明显收敛、不再顶屏幕，桌面端维持原有观感不变。

**其余验证**：`node --check` 复核通过；重新核验单击城池跳转、双击定点放大（含图标固定视觉大小不受影响）均正常。

## 八十二、缩放至日本留白不对称、城池图标缩放策略重新设计（第八十轮）

用户又提两点：①缩放至战国全境时，对马岛这一侧的留白明显比奥州那一侧宽很多；②城池图标"完全固定视觉大小、不随地图缩放"这个策略本身不合适——地图缩到最小时城池本就挤在一起、图标再小也没用只能堆叠，地图放大到最大时图标却还是那么大一丁点，反而没有放大细看该有的效果，要求重新测算一个"尽量不堆叠、放大后又能凸显"的方案。

### 一、对马岛留白不对称——两次排查才找到真正原因

**第一次排查（找错方向）**：一开始以为是"缩放至日本"没有把对马岛计入外包框——对马岛严格算国别是「海路中转站」（`side:"sea"`），不在 `side==="jp"` 的筛选范围内，只是因为留白半径够大才被"顺带"框了进来，不是真正参与外包框边界计算的一员。把它显式并入日本这份外包框后，两侧留白量在数学上确实对称了，但**实测截图依然是同一个不对称结果，一点没变**——说明第一次排查的方向根本不是主因，只是顺手也修了一个真实存在但非本质的小问题。

**第二次排查（真正原因）**：仔细打印缩放后的实际平移量才发现，`MapZoom.x` 精确卡在 `clampZoomState` 的平移边界上（`-maxX` 分毫不差）——问题不在外包框的留白算得对不对称，而在于**中国、日本两片区域在整张地图里本来就不是各自居中的**（日本整体偏在地图右侧）。只按"刚好能装下外包框"来选缩放倍数时，选出来的倍数（约 2.15 倍）太低，能平移的范围（随缩放倍数增大而增大）也跟着很小，根本不够把视野从"整张地图正中心"挪到"日本片区正中心"——于是被平移边界卡住，实际画面没能真正居中在日本这片区域上，一侧贴着边界、一侧却还有富余，看着就是明显的不对称，而这跟外包框留白量本身算得对不对完全是两回事。

**修复**：新增按"平移能挪多远"反推的最低缩放倍数——已知外包框中心相对整张地图正中心偏了多远（`kx`/`ky`），能反推出"至少要放大到多少倍，平移才有足够余量把视野真正挪到这个中心点上"（`1/(1-2|k|)`），与"刚好装下外包框"所需的倍数取较大者作为最终缩放倍数。这样一来，缩放至日本全境时会自动多放大一档（约 2.63 倍），确保画面真正居中在日本这片区域，两侧留白差距从原先的 119px vs 54px 收窄到 59px vs 65px（≈6px 的残余差距来自不同城市图标本身尺寸不同，可以接受）。

**验证**：Playwright 逐次核验缩放后的实际平移量、两侧留白像素差、全部城池可见性；中国一侧的缩放（其外包框中心同样明显偏离整张地图正中心）一并复核，未发现同类问题。

### 二、城池图标缩放策略重新设计：小幅随缩放联动，而非完全固定

**排查/测算过程**：完全固定视觉大小（`--zk` 严格等于 `1/MapZoom.scale`，這是七十九轮定下的方案）有个没预料到的副作用——地图缩到最小（默认视图）时，城池锚点本就靠得很近，图标尺寸已经没法再往小调，遇到本就密集的历史城池群只能眼睁睁堆叠；地图放大到最大时，锚点已经被拉开很远，图标却还是当初设计的那个固定尺寸，相对空出来的大片空间反而显得微不足道，体现不出"放大细看"应有的效果。用户要求的原则很明确：默认视图要尽量不堆叠（图标应该更小），放大后要凸显（图标应该更大）——这正是"完全不联动"和"完全跟着缩放等比例放大"（改造前的旧毛病）两个极端之间的一个折中点。

**方案**：把最终显示大小设计成随缩放倍数线性插值——缩放倍数最小（1 倍，默认视图）时，图标只呈现设计基准尺寸（已经过 `--rk` 设备自适应换算）的 55%；缩放倍数最大（8 倍）时放大到 150%；中间按缩放进度线性过渡。反推出每一级缩放对应的 `--zk` 取值代入原有换算公式即可，图标锚点仍随地图缩放/平移正常移动，改变的只是"这一级缩放下应该呈现多大"这个映射关系。

**验证**：Playwright 分别在缩放 1 倍（最小）、约 4.5 倍（聚焦城市默认倍数）、8 倍（最大）三档下测得成都图标渲染宽度依次为 15.7px、29.2px、42.8px，与设计的 55%→100%→150% 曲线吻合；截图核验默认视图下密集城池群的堆叠程度较上一版明显缓解，聚焦城市后的放大视图里图标清晰醒目、不再是"放大了但还是那么小一点"。

**其余验证**：`node --check` 复核通过；重新核验单击城池跳转、拖拽平移边界不露空白等既有功能均正常。

## 八十三、取消城池防重叠位移，改为回归真实经纬度；缩放锚点改为跟随当前观察中心（第八十一轮）

用户提两点：①城池所在地图位置"好像不对了"，要求完全按真实地理位置布局；②点击缩放按钮、双指捏合缩放地图时，画面观察点总是被带偏，要求以当前观察中心而非地图几何中心为基准缩放。

### 一、防重叠位移是真实的地理准确性 bug

排查方式：整屏渲染一版标注全部 41 座城名字的地图截图，逐一按真实的三国/战国地理常识核对相对方位，只找出一处明显异常——近江（大津，与京都在真实地理上几乎接壤）在图上被摆到了尾张（名古屋）附近，比例尺上看近江到京都的距离反而比近江到尾张更远，这在真实地理里是错的。追根溯源，正是七十七轮为解决"城池图标重叠"而加入的防重叠松弛算法（互相推开+弹簧拉回真实位置附近）——这段松弛只按"两点离得够不够远"判断要不要推开，完全不管推开的方向会不会打乱两座城原本的真实相对方位，近江和京都恰好是被松弛judge"离得太近"而彼此推开、又因为弹簧强度不够把近江拉得偏到了尾张附近。

**修复**：直接删除防重叠松弛这一整段计算，城池坐标完全信任真实经纬度换算的结果，不做任何位置层面的人工调整。重新跑一遍地图数据生成脚本，全部 41 座城市的坐标较修复前都对不上（drift 均为 0，即完全等于纯经纬度换算值），代入 `js/app.js` 的 `CITIES` 数组。至于原本想解决的"城池图标重叠"问题，交给这几轮已经陆续加上的其它手段去缓解——双击/按钮定点放大、缩放至中日全境按钮、默认视图下图标只呈现 55% 视觉大小——而不是继续牺牲地理准确性去騰位置。

**验证**：Playwright 重新渲染整屏标注地图截图，核对近江与京都现在几乎重合（相距仅 0.23 个归一化单位，此前是 3.3+ 个单位），其余全部城市方位未见异常；重跑既有回归用例（单击跳转、极限拖拽边界、缩放至中/日全境可见性、双击定点放大）全部通过。

### 二、缩放按钮/双指捏合/滚轮缩放锚点改为当前观察中心

排查：`+`/`-` 按钮的 `zoomStep`、双指捏合的 `onMove` 分支、鼠标滚轮的 `onwheel` 三处都是直接修改 `MapZoom.scale` 后调 `clampZoomState`+`applyZoom`，而 `.map-zoom-layer` 的 CSS `transform-origin` 固定是地图正中心——这意味着每次缩放实际上都是"以地图几何中心为不动点"重新缩放，一旦玩家已经拖拽平移到别处观察，缩放会让画面重新往地图中心"跳"，看起来就是观察点总在漂移。而双击/双触摸点按放大用的 `zoomAtPoint` 早就是正确写法（先反推出目标屏幕点当前对应的地图内容坐标，缩放后再反解出让该点仍留在同一屏幕位置所需的新平移量），只是没有复用到另外三处。

**修复**：`zoomStep` 改为以地图框当前的屏幕正中心为锚点调用 `zoomAtPoint`；双指捏合改为以两指当前中点为锚点；滚轮改为以光标当前位置为锚点（同一类问题，一并顺手修掉）。过程中还发现 `zoomAtPoint` 自身一个潜藏 bug：当目标倍数超出 `[1, MAP_ZOOM_MAX]` 时，函数先按越界的目标倍数解出平移量，再调用 `clampZoomState` 把倍数拉回合法范围——但平移量并没有跟着倍数被拉回而重新计算，导致缩放触顶/触底时锚点仍会跳漂。修复为先把目标倍数夹到合法范围，再用夹紧后的倍数去解平移量。

**验证**：Playwright 模拟"先放大再拖拽平移、画面偏离几何中心"的场景，分别测试点击 `+` 按钮、鼠标滚轮缩放，核验缩放前后屏幕锚点（按钮用框体正中心、滚轮用光标位置）对应的地图内容坐标完全不变（drift=0，滚轮场景 drift≈0.004 属浮点误差）；重跑既有回归用例（单击跳转、极限拖拽边界、缩放至中/日全境可见性、双击定点放大）全部通过。

**其余验证**：`node --check` 复核通过。

## 八十四、聚焦中/日全境图标偏小、道路改从图标底部连接、修复聚焦边缘城市/国别露出地图外空白（第八十二轮）

用户提三点：①聚焦中/日全境时城池图标和城名还是偏小；②道路线不要从城池图标"身上"穿过去连接，应该从图标底部开始连接；③聚焦日本全境时右侧和地图右边缘之间出现了空档。

### 一、聚焦全境时图标偏小——上调缩放-视觉大小换算曲线

上一轮（八十轮）把图标/城名视觉大小设计成"缩放倍数越小呈现越小（55%）、缩放倍数越大呈现越大（150%）"的线性插值，是为了缓解默认全景视图下的堆叠。但没考虑到"缩放至中/日全境"这两个按钮算出来的缩放倍数通常落在 2~3 倍这个偏低区间（比如缩放至日本全境约 2.4~2.6 倍），按原曲线只能呈现六七成大小，比玩家已经习惯的默认大小还要小，看着偏小。把曲线下限从 55% 上调到 75%、上限从 150% 上调到 180%，聚焦全境时的图标/城名明显更清楚，默认全景视图和最大缩放两端的堆叠/醒目程度基本不受影响。

### 二、道路连接点改到图标底部

排查：城池锚点坐标（`c.x%`/`c.y%`）既用于给道路 `<line>` 定端点，也用于给 `.map-city` 这个装图标+城名的竖排容器做 `left/top` 定位，容器本身用 `translate(-50%,-50%)` 让锚点落在"图标+城名"整个竖排区块的正中心——这个正中心大致穿过图标中段，道路线自然就像从图标身上穿出来一样，不好看也不直观。

**修复**：把 `.map-city` 改成只包住图标本身（`translate(-50%,-100%)`，锚点钉在图标的底边而不是整个区块的正中心，`transform-origin` 也改成底边正中，缩放时锚点不跟着挪动），城名改用 `position:absolute; top:100%` 单独挂在图标下方、不再计入这个容器自身的高度。道路端点坐标不用改——它们本就是同一个锚点坐标，锚点位置变了，道路自然就跟着落到图标底边上。

**验证**：Playwright 截图核对多处城池（越后、常陆、小田原等），道路线清晰地落在图标下沿，不再穿过图标本体；重跑单击城池跳转等既有用例确认点击热区未受影响。

### 三、聚焦全境/聚焦城市露出地图外空白——`clampZoomState` 只按整层 0~100 算边界，没扣掉底图实际没画到的四边留白

排查：`zoomToFitSide`（缩放至中/日全境按钮）取城池外包框时没有把留白（`pad`）撑出的范围卡在实际有效范围内——比如日本一侧留白后外包框右边能到归一化坐标 x≈101，而卫星底图（`MAP_RELIEF_RECT`）右边缘只画到 x≈99.02，两者之间的空档在缩放至日本全境时被撑大到一整条肉眼可见的空白色块。往深一层查，`clampZoomState` 本身也只是把平移量卡在"不超出整张 0~100 归一化层"的范围内，而不是卡在底图实际覆盖的那个更窄范围——这意味着不止缩放至全境按钮，任何聚焦到贴近地图裁切边缘的城市（比如成都本就贴着地图裁切时特意留的西边一点点空间，见七十七轮"稍微留点空间不要完全截到成都边上"）放大聚焦时，都会把这一小圈"归一化层里有、底图里没画"的空白一并拉进画面、放大成一大片纯色空档——这是排查这次问题时顺带在自测截图里发现的一个同类回归，此前的聚焦城市/拖拽平移测试从没测过贴边城市在高倍聚焦下的极端情况，没有暴露过。

**修复**：`zoomToFitSide` 的外包框先卡进 `MAP_RELIEF_RECT` 实际范围内，不再无限制往外撑；`clampZoomState` 从"按整层 0~100 算平移边界"改为"按底图实际覆盖范围算平移边界"——当前缩放倍数下可视窗口的宽/高换算成归一化比例后，要求这个窗口的两侧都落在底图范围内，倍数不够、窗口本身比底图还宽时（比如缩放倍数=1 的全景默认视图）退化为居中对齐、双侧各自留一点点无法避免的极小留白（不到 1%，肉眼不可见）。两个轴分别独立计算，因为底图四边留白量本就不对称。

**验证**：Playwright 分别重新截图核验缩放至日本/中国全境（右侧、西侧均不再露出空白）、单独聚焦成都（此前会在左侧露出一大片空白，现在紧贴底图边缘填满画面，成都图标随之略微偏离正中心，是必要的取舍而非 bug）；重跑全部既有回归用例（单击跳转、极限拖拽边界、缩放至中/日全境可见性、双击定点放大、缩放锚点跟随观察中心）全部通过。

**其余验证**：`node --check` 复核通过。

## 八十五、地图放到最大时日本城池挤成一团，改用按地理密度区分的图标放大上限（第八十三轮）

用户提一点：地图放到最大时，日本那片城池图标相应放大太多、彼此都叠在一起了；中国那边地方大，同样放到最大却没有这个问题，要求针对性调整。

**排查**：量出全部 41 座城池两两之间的真实（归一化坐标系下的）最近邻距离——中国一侧最近的一对（徐州/下邳）相距 1.62 个单位，其余普遍在 3~12 个单位；日本一侧除了近江/京都这一对因为真实地理位置本就几乎贴在一起（仅 0.23 个单位，属实际地理决定、动不了）之外，其余最近邻距离普遍落在 1.15~2.5 个单位，明显比中国紧凑得多。而图标放大曲线（八十二轮刚把上限从 150% 调到 180%）是全地图一条曲线，不分中日——中国那边城池间距大，图标放大到 180% 照样拉得开；日本这边城池间距本就窄，放大到 180% 后图标本身的像素宽度反而超过了城池间距，看着就是糊成一团。用 Playwright 分别把镜头顶格放大到最大倍数（8 倍）、对准两边密度最高的城池群截图核对，中国（徐州/下邳）依旧清晰分开，日本（近江/京都/大坂、美浓/尾张/三河）确实大片重叠，复现了问题。

**修复**：不改任何城池坐标（真实地理位置继续保留，这不是又一轮"防重叠位移"）,而是让图标放大曲线按"城池的真实地理归属"分成两条：中国一侧继续沿用 75%~180% 这条曲线（`--zk`）；日本/海路一侧新增一条上限低得多的 75%~105% 曲线（`--zk-jp`，MapUI.applyZoom 里与 `--zk` 同步算出），通过给每座城池的 DOM 节点新挂的 `geo-cn`/`geo-jp`/`geo-sea` 类（取自城池数据本身的 `side` 字段，不随占领方变化，与已有的、会随占领变化的 `owner` 类分开）路由到对应曲线上。

**验证**：Playwright 重新在最大缩放倍数下截图核对，美浓/尾张/三河三座城池不再糊成一团、彼此已能分辨为独立图标；近江/京都这一对因真实地理距离过近仍会有少量重叠（符合预期的取舍，非本次要解决的问题）；大坂与近江/京都的重叠范围也明显收窄。重跑全部既有回归用例（单击跳转、极限拖拽边界、缩放至中/日全境可见性、双击定点放大、缩放锚点跟随观察中心）全部通过；`node --check` 复核通过。

## 八十六、城池重叠问题的根源——名单本身，砍近江并修复道路网结构性缺陷（第八十四轮）

用户直接点出：无论怎么调图标缩放曲线，近江和京都在真实地理上就是几乎贴在一起的两个点，这不是缩放算法能解决的，要求排查全部城池名单的合理性，兼顾"两国代表性"和"地图分布均匀"，并追加了"中日城池数量（对马岛除外）要一致"这一条平衡原则。讨论过程中用户还追问了一个独立问题：汝南是不是只有一条路通许昌、这合不合理。

### 一、城池名单：砍近江，数量因此天然打平

排查全部 41 座城池两两最近邻距离：中国最近的一对（徐州/下邳）相距 1.62 个归一化单位，日本除近江/京都外普遍在 1.15~2.5，只有近江/京都这一对只有 0.23——查证后这是真实地理决定的（近江/大津与京都实质是同一都会区），任何图标缩放策略都无法让两个几乎重合的经纬度点在视觉上分开，此前几轮的曲线调整都只是治标。

排查各势力内部有没有可以精简的"重复卫星城"，发现只有近江够格：它所属的京都幕府势力还有京都、越前，砍掉近江后该势力仍保留 2 座城，代表性不受影响；其余城池两两之间的距离虽然有紧凑（如美浓/尾张、大坂/京都），但都是历史上真实存在、彼此接壤但各自独立的知名大名领地（织田/尾张、齐藤后来的织田/美浓、丰臣/大坂等），砍掉任何一座都会实质损失代表性，不能简单类比近江这种"同一都会区两个点"的情况。

用户追加的"中日数量对等"原则，把对马岛计入日本一侧后（对马岛地理上紧邻九州、玩法上也算日本一侧海路），天然打平：中国 20 座不动，日本原 20 座（不含对马岛）砍掉近江变 19 座，加对马岛共 20 座，与中国持平，不需要额外从中国砍城池凑数字。

### 二、道路网排查：中国一侧两处结构性缺陷，日本一侧全部合格

用户问到汝南只通许昌是否合理，借此机会写脚本给全部 41 座城池、61 条道路做了一次系统性核验（每座城池的连接度、每个势力内部城池是否需要绕道敌对势力才能互通），发现：

- **汝南**：连接度为 1（唯一硬伤级别），且它与同属袁术势力的寿春之间没有任何直连或经自家城池中转的通路，往来必须绕道曹魏的许昌——不合理。
- **邺城**：连接度为 1，虽然该势力（袁绍）只有这一座城、不存在绕道敌对势力的问题，但作为官渡之战的历史核心节点，只有一条出路显得孤立。
- **下邳**：连接度为 1，但它与同势力的徐州本就直接相邻（相距仅 1.62，实质是"姊妹城"），不算真正的问题，本轮不动。
- **合肥**（曹魏）：与自家的许昌、宛城之间没有直连道路，只能绕道寿春（袁术）或建业（东吴）——但合肥历史上本就是曹魏抵御东吴的孤悬前哨，这个"绕道"某种程度上符合史实的前线孤城设定，本轮保留不动。

日本一侧用同样的脚本核验，10 家势力（含京都幕府）全部合格，没有连接度为 1 的城池，也没有绕道敌对势力的情况——这个问题是中国一侧独有的。

**修复**：汝南新增两条路——通宛城（4.19，比原有的许昌 4.75 还近）、通寿春（5.84，解决袁术自家两城互通问题）；邺城新增一条通许昌的路（10.68，虽不算近，但正是官渡之战袁绍、曹操两军对垒的官道，史实呼应）。删除近江之后，原本连接近江的美浓也被连带削去一条路、意外掉成连接度 1——补一条通三河（2.33），尾张/美浓/三河三国边境本就互相接壤，也对应织田、德川结盟的清洲同盟路线。

**验证**：写脚本核验全部道路引用的城池都存在、无孤立节点残留（下邳仍保持度 1，是本轮明确保留的既有设计）、全部势力内部连通性核验通过、全图 40 座城池连成一整个连通图；Playwright 分别截图核对汝南、邺城新增道路正确渲染，日本核心区最大缩放下重新核验京都/大坂已能清楚分辨为独立图标；重跑全部既有回归用例（单击跳转、极限拖拽边界、缩放至中/日全境可见性、双击定点放大、缩放锚点跟随观察中心）全部通过；`node --check` 复核通过。

## 八十七、快速点两下城池图标会误触双击放大、桌面浏览器图标封顶太早（第八十五轮）

用户提两点：①点击一个城市后图标放大，再缩小地图后图标和之前不一致；②电脑浏览器上图标偏小，好像跟手机不是同一套自适应规则。

### 一、点城池图标"变大"——不是缩放算法的锅，是手势识别没把城池图标排除在外

**排查过程**：先复核了 `--zk`/`--rk` 这套缩放换算本身，写了一轮完整的"放大再缩小"数值回归测试（记录同一座城池在多次缩放操作前后的 `--zk`/`--rk`/图标像素宽度），结果完全可逆、分毫不差，说明缩放公式本身没有 bug。这就排除了"数值计算错误"这个方向，转而怀疑是不是某个交互手势被误触发了。

进一步查证 `bindZoom` 里 `box.onpointerdown` 的手势识别代码，发现它只排除了缩放控制条（`.map-zoom-ctl`）不参与拖拽/双击缩放的手势判定，唯独没有排除城池图标（`.map-city`）——城池图标虽然自己绑了 `click`（跳转/移动），但点击事件同时也会被地图背景的拖拽/双击缩放手势系统"看见"。用 Playwright 直接派发原生指针事件复现：连续快速点击同一座够不着的远方城池两次（这种点击不会真正移动，只会弹提示或确认框），地图缩放倍数从 1 直接跳到了 2——两次点击被误判成了"双击定点放大"！这正好完整解释了用户的报告：点一个城池（可能因为够不着弹了提示/确认框，又忍不住点了第二下）触发了意外的双击放大，图标跟着地图一起变大；之后如果不是用"显示全景"精确复位、而是用 -/拖拽之类的方式缩回去，落点未必和最初分毫不差，看着就像"图标和之前不一致"。

**修复**：`box.onpointerdown` 里追加排除 `.map-city`（连同已有的 `.map-zoom-ctl` 排除），点在城池图标上的指针事件不再被地图背景的拖拽/双击缩放手势接管，只走图标自己的 `click` 跳转逻辑。

**验证**：Playwright 复现用例——修复前连续两次点击同一城池图标 scale 从 1 变 2，修复后维持 1 不变；重跑全部既有回归用例（单击城池跳转、双击地图空白处定点放大、拖拽边界、缩放至中/日全境、缩放锚点跟随观察中心）全部通过，确认没有误伤"点空白处双击放大"这个本该保留的正常手势。

### 二、桌面浏览器图标偏小——响应式换算的上限设错了参照系

**排查**：`applyResponsiveScale` 按 `.map-svg-box` 实际渲染宽度相对 800px 这个参照宽度算 `--rk`，但上限被封死在 100%（`Math.min(1, ...)`）——原意是"手机上因为窄要缩小、桌面不用再缩小"，但实际效果是只要宽度 ≥800px（不管是 800px 的平板还是 1400px+ 的桌面显示器）一律拉平成同一个 100% 基准，桌面显示器物理尺寸大得多、看着自然比手机上更不起眼，这正是用户说的"电脑上图标太小，好像跟手机不是同一套规则"——手机端有专门的"越窄越小"，桌面端却没有对应的"越宽越大"，两端规则确实不对等。

**修复**：把上限从 100% 放宽到 150%（约 1200px 宽处封顶，避免超宽显示器继续无限放大）。Playwright 分别在 390/420/800/1200/1400/1920 六档视口下测得图标渲染宽度：手机端两档不变（21.4px），800px 平板从原来封顶的 42.75px 略降到 41.5px（更精确的比例换算，属预期内的微调），桌面 1200px 以上从原来固定的 42.75px 提升到 62.8~64px，明显更醒目，且 1400px 和 1920px 数值一致（确认封顶生效，不会在超宽屏上继续失控变大）。

**其余验证**：重新核验最大缩放下日本核心区（京都/大坂/美浓/尾张/三河）在放大后的桌面尺寸下依然能清楚分辨为独立图标，未重新引入上上轮解决的重叠问题；`node --check` 复核通过。

## 八十八、上一轮的桌面放大修复本身引入了新的重叠回归（第八十六轮）

用户反馈"这两个问题都没解决"：移动到另一城市后，所有城池图标突然变大很多，缩小地图后全叠在一起了。先按"点击城池图标本身触发了误判的双击缩放"排查一轮，Playwright 复现测试显示 `MapZoom.scale` 全程没变过，排除了缩放倍数被意外改动这个方向；又按"当前城市高亮样式（城名字号 18→21px）"追问，用户明确回复不是这个、而是**所有**图标一起变大，问题范围比这两个猜测都大。

**真正原因**：直接截图对比默认视图（scale=1，还没做任何缩放操作）就发现，好几处本来紧凑但不重叠的城池群（中国的许昌/宛城/汝南一带、日本的核心区）已经糊在一起——这跟"移动到另一城市"本身没有直接关系，只是移动后大概率会看到城池更密集的区域，才显得突出。根源是上一轮为解决"桌面浏览器图标太小"而把 `--rk`（设备自适应系数）上限从 100% 放宽到 150%——但这个放大倍数不分缩放级别，在任何倍数下都生效，包括默认全景视图。默认视图这个尺寸此前是按"最挤的几对城池刚好不重叠"精细调校过的（如徐州/下邳只相距 1.62 个单位），桌面上统一乘 1.5 倍直接把这条精细平衡打破，多处城池被挤到重叠——这是上一轮修复"桌面太小"时自己引入的新回归，两个问题看似独立，实则是同一次改动的一体两面。

**修复**：把"桌面放大加成"和当前缩放进度挂钩，而不是在所有缩放级别一律生效——缩放倍数为默认最小值时，加成完全不生效（等同未加成前的 100%，默认视图的防重叠调校原样保留，不因设备宽度而改变）；玩家主动放大（点击聚焦、双击定点、缩放至全境等）后，加成随缩放进度线性趋近满额 150%，且按 3 倍速趋近（缩放到约 1/3 全程就已接近满额，不用拉满到 8 倍才见效）。手机端的"越窄越小"这半条规则不受影响，照常在任何缩放级别下都生效。

**验证**：Playwright 核验默认视图（scale=1）在 390~1920px 六档宽度下 `--rk` 均不超过 100%、成都图标宽度稳定在 42.75px，与本轮改动前完全一致（不再触发重叠）；核验聚焦当前城市（scale≈4.5）时，桌面（1400px）图标从 42.75px 放大到 109px、手机端（420px）保持 36px 不变，确认"桌面放大"确实只在放大操作后才生效；重新截图核验默认全景视图与日本核心区最大缩放视图均未出现重叠；重跑全部既有回归用例（单击跳转、极限拖拽边界、缩放至中/日全境可见性、双击定点放大、缩放锚点跟随观察中心、点击城池图标不再误触双击缩放）全部通过；`node --check` 复核通过。

## 八十九、折叠屏展开态图标重叠、参照宽度改用真实比例；首次打开程序量到 0 宽度的陈年 bug（第八十七轮）

用户提两点：①折叠屏展开后图标明显交叠，折叠状态和电脑浏览器上却是合适的；②初次打开程序时的图标大小和后续点任何一种缩放后不一样。

### 一、折叠屏展开态重叠——参照宽度 800px 只适配了窄屏和桌面两头，没考虑中间这一段

**排查**：用 Playwright 分别模拟折叠状态（390px 宽）、展开状态（673px/817px 宽，折叠屏典型展开尺寸）、电脑浏览器（1400px 宽）四档截图对比，折叠状态和电脑浏览器都干净，展开状态那两档在中国许昌/宛城一带和日本核心区都明显糊成一片——复现成功。测了实际渲染尺寸才看出问题：展开态图标的绝对像素宽度（34.67px）反而比电脑浏览器的（42.75px）**更小**，但地图框的实际宽度只有电脑浏览器的不到一半（649px vs 1376px）——城池间距是按框宽等比换算的像素距离，框窄了间距跟着变窄，图标却没跟着等比例缩得够多，相对间距的占比反而更大，这才是重叠的真正原因，绝对像素大小本身说明不了问题。

追根溯源，`--rk` 的参照宽度取的是 800px，这个数字只在"明显是手机"（远小于 800）和"明显是桌面"（远大于 800，且已用另一套按缩放进度换算的封顶逻辑兜底）这两头验证过，唯独 600~900px 这个折叠屏展开后最常落入的中间地带，从来没人专门核验过——按 800 这个参照换算出来的比例（0.81~1.0）看着"没怎么缩"，但这个宽度相对于已验证正确的桌面基准（1376px）其实应该缩到 0.47~0.59 才对。

**修复**：参照宽度改用电脑浏览器已验证正确的实际值（1376px，对应常见 1400px 视口），不再用 800 这个仅在两端验证过、中间地带失真的旧参照——折叠、展开、桌面三种状态下地图框的实际宽度差好几倍，只有严格按真实宽度比例换算，城池间距和图标大小的比例关系才能在所有设备上保持一致。改完以后又发现一个连带问题：原来"桌面放大后能到 150%"这个加成，是让参照宽度自己的比值超过 100% 来体现的——换了参照宽度以后，常见的 1400px 窗口的比值正好卡在 100%整，再放大也涨不出"超过 100%"的空间，等于给这类窗口的放大功能失效了。于是把"默认基准"和"放大后能涨到多高"彻底拆成两个独立变量：基准值严格按真实宽度比例算（各设备都一样，是防重叠的安全线）；放大后的上限只留给真正意义上的桌面/大屏（≥1000px，折叠屏展开态被排除在外），且封顶到 150%，与设备实际宽度具体数值脱钩，任何达标的桌面宽度放大后都能摸到这个封顶。

**验证**：Playwright 重新核验四档设备默认视图，折叠展开态图标宽度从问题状态的 34.67px 收窄到 21.4px（横向展开为 25.4px），截图确认重叠完全消失、与折叠/桌面两档同样干净；核验放大后桌面（1400px）图标仍能从 42.75px 涨到 109px（与放宽 --rk 上限那一轮验证过的桌面手感一致），折叠屏展开态（<1000px）放大后维持基准值不涨，避免重新引入重叠。

### 二、首次打开程序图标偏小——量的时候屏幕还没显示出来

**排查**：这个问题此前几轮的数值回归测试从未复现过，因为测试脚本里手动切换 `.screen.active` 的时机（先切可见再 render）和真实入口 `MapUI.open()` 的实际顺序（先 render 再 `showScreen("map")`）刚好反了——测试脚本无意中绕开了这个 bug。改用和真实入口完全一致的调用顺序重新测试才复现：`render()` 里 `bindZoom`/`applyResponsiveScale` 会去量 `.map-svg-box` 的 `getBoundingClientRect()`，但这时候地图屏还是 `display:none`（`showScreen("map")` 还没执行），隐藏元素测出来的宽高恒为 0，`--rk` 直接被地板值（50%）卡死，图标显得比正常小一大截；一旦玩家随手点一次任何缩放操作，重新测量到真实宽度，才恢复到该有的大小——这跟设备宽度毫无关系，纯粹是"测量时机比屏幕真正可见早了一步"这个时序问题，只要地图不是玩家看到的第一个屏幕，就一定会踩到。

顺着这个思路又找到三处使用同一错误顺序（`render()` 在 `showScreen("map")` 之前）的入口——边境战报"返回天下地图"、武将大会战报"返回天下地图"、"全部城市"表格发起的驿站直达——但凡玩家不是从地图屏、而是从别的界面触发这几个跳转，同样会踩到这个 0 宽度的坑。

**修复**：四处入口统一把顺序改成"先 `showScreen("map")` 切到可见，再 `render()`"，让测量的时候屏幕已经真正显示出来。

**验证**：Playwright 完全按真实入口顺序调用 `MapUI.open()`，首次打开的 `--rk` 从问题状态的 0.500（地板值）恢复到与后续任意缩放操作后完全一致的 1.000；重新核验修复过的三处跳转入口未引入其它回归；重跑全部既有回归用例（单击跳转、极限拖拽边界、缩放至中/日全境可见性、双击定点放大、缩放锚点跟随观察中心、点击城池图标不再误触双击缩放）全部通过；`node --check` 复核通过。

## 九十、手机拖拽误触城池、当前城池光晕不够醒目、城池图标改为按星级分造型的宝塔剪影（第八十八轮）

用户一次提五点：①手机上手指按到城池图标再拖动会失效；②当前城池的光晕不够明显；③每座城池顶端的黄色小旗子看不出区分意义；④有的城池图标显得透明、能看见底下道路；⑤图标像乐高积木，希望换成更真实、且不同星级用不同造型（不是简单缩放）的城池样式。

### 一、手指按到城池图标再拖动会失效——上一轮修双击误触时把手势路径也顺带堵死了

**排查**：上一轮（八十六轮）为了修"快速点两下城池图标会被误判成双击定点放大"，直接把 `.map-city` 整体排除出了地图背景的拖拽/双击缩放手势跟踪（`box.onpointerdown` 里 `e.target.closest(".map-city")` 直接 `return`）——这解决了误触发缩放的问题，但也带来一个没预料到的副作用：手指从城池图标上开始按下的这次触点，从一开始就没有被计入拖拽跟踪，导致"从图标上开始滑动"这个手势彻底没有任何响应（既不会拖动地图，城池自己的 `click` 又因为手指有位移而通常不会触发跳转），在触屏上尤其明显——手指经常不偏不倚正好按在图标上，一按就"死"了。

**修复**：不再把城池图标整体排除在拖拽跟踪之外，而是给每个触点单独记一个"是否落在城池图标上"的标记，正常参与拖拽/捏合的位移跟踪（拖动照常生效）；只在手势结束、判定"这是一次没有位移的点按"要不要触发双击缩放时，才依据这个标记跳过——城池图标上的点按完全不参与双击缩放的计时/记录，两件事互不干扰。

**验证**：Playwright 模拟"从城池图标正中心按下、拖动一段距离再松开"，确认地图确实跟着位移、且没有被误判为移动到该城池；重新核验"快速点两下同一座城池"的既有回归用例，确认双击误触发缩放的问题依然修复着。

### 二、当前城池光晕加强；三、旧的黄色小旗子区分度不够；四、远处城池显得透明；五、图标改为分星级造型的宝塔

这四点放在一起处理，因为图标本身要重做，正好一并解决。

- **当前城池光晕**：原来只有图标本身的 `drop-shadow` 辉光，卫星底图纹理复杂的区域很容易被盖过去。现在在图标落地的锚点位置额外叠一圈径向渐变的金色光晕，并配一个缓慢的呼吸脉动动画，不管背景多花都能一眼看到"我在这"，同时把原有的 `drop-shadow` 辉光半径也调大了一档。
- **旧的黄色小旗子**：原来是"三级起挂旗、五级旗更大"，但旗子本身只有"有没有、大不大"两态，区分度确实弱，还容易被当成某种未知的游戏内提示符号。这次连同图标一起重做，不再单独维护一套旗子逻辑。
- **远处城池透明**：查到是 `.map-city.far { opacity: .62 }` 这条规则——大多数城池平时都是"远处"状态（只有 1 座当前、少数几座相邻），压到 62% 透明度以后，尤其是画在纹理复杂的底图上，看着就像图标本身镂空、能看穿到下面的道路，其实是整体调淡了，不是真镂空。调高到 85%，层次感还在（仍比当前/相邻城池弱一档），但不会被底图纹理吃掉。
- **图标改为分星级造型的宝塔**：不再用同一个"乐高积木"形状简单缩放，改成按繁荣度星级（1~5）逐级加盖楼层的宝塔剪影——一级是单层小屋，二级两层，三级三层且两侧开始出现院墙，四级四层、院墙更宽更高，五级五层、院墙最宽且顶端加一面旗（原来的旗子逻辑收窄成"只在封顶星级出现"，不再是看不出意义的泛用符号）。塔身用势力色区分归属，屋檐/院墙统一深褐色，全程实心填充，不再有透明观感的问题；不同星级不再是同一个形状的大小差异，而是从楼层数、有没有院墙、有没有旗这些"造型本身"就能看出规模差异。

**验证**：Playwright 分别渲染 1~5 星城池图标截图核对，五档造型层次分明、无透明镂空；核对成都（3 星、当前城池）同时呈现三层宝塔、两侧院墙、金色光晕脉动三重效果；重新核验日本核心区最大缩放视图下新造型仍能清楚分辨各座城池、未比旧图标更容易重叠；重跑全部既有回归用例（单击跳转、极限拖拽边界、缩放至中/日全境可见性、双击定点放大、缩放锚点跟随观察中心、折叠屏/首次打开图标尺寸一致性）全部通过；`node --check` 复核通过。

## 九十一、二级城池图标比三级还大——宝塔重做时留下的尺寸计算漏洞（第八十九轮）

用户发现二级城池的图标看着比三级还大，一、三、四、五级之间倒是看着都正常。

**排查**：算了一下每一级图标里"塔身"本身（不含三级起加的院墙）实际渲染出来的像素宽度——一级 38.6px，二级 54.9px，三级反而缩水到 37.5px，四级 37.1px，五级 38.2px。根源是 `cityIconSvg` 给每一级图标固定了一个 `width` 属性（沿用旧版 `CITY_ICON_SIZE` 数组的 39/48/57/66/78），`height` 按 viewBox 的宽高比换算——一二级没有院墙，viewBox 只包住塔身本身；三级起两侧一加院墙，viewBox 的宽度直接从二级的 16.8 涨到 29.2（涨了 74%），但 `width` 属性只从 48 涨到 57（只涨了 19%），涨得完全不够抵消 viewBox 变宽的效果，等于把变宽后的整个画面（塔身+院墙）硬塞进只多了 19% 的固定宽度里，实际渲染出来塔身反而被院墙"拖累"缩小了。

**修复**：不再用固定的 `width` 属性反推 `height`，而是先反推出"每个 viewBox 单位该占多少像素"（塔身在各级之间的最宽处——首层屋檐出挑后——恒为 19.2 个单位，用这个恒定值和设计好的目标像素宽度 39/48/57/66/78 反推比例），再用这个比例分别换算整个 viewBox（含院墙）的实际渲染宽高。这样院墙不管多宽，都只会让整个图标"看起来更宽阔"，不会反过来压缩塔身本身的像素大小。改完后顺带发现新问题：换算出来的图标高度涨得比宽度快得多（五级能到 199px 高，比宽度 110px 高出近一倍，图标又瘦又高很不协调）——把每层楼身/屋檐的高度参数调小（从 4/3.4 降到 2.3/2），重新算出五档图标宽高分别为 39×23、42×39、62×59、83×83、110×119，宽高比例更均衡。

**验证**：Playwright 重新截图核对成都（三级）明显大于天水/长安（二级）、天水/长安明显大于上庸/白帝城（一级），级别越高图标越大的顺序完全恢复正常；重新核验日本核心区最大缩放视图未因塔身变宽而重新出现重叠；重跑全部既有回归用例（单击跳转、极限拖拽边界、缩放至中/日全境可见性、双击定点放大、缩放锚点跟随观察中心、拖拽从城池图标开始不失效）全部通过；`node --check` 复核通过。

## 九十二、AI 城建速度过快、招揽敌将过于容易——两处数值调整（第九十轮）

用户提两点：①AI 势力城建太快，不到一年各城就冲到 5 星；②从武将界面招揽敌方武将太容易，只要给钱就大概率能笼络。先给出排查结论与两套备选方案，用户确认后按"AI 城建：A（补上政治资质门槛）+ C（提高 5 星门槛）"、"招揽：A（失败加冷却）+ B（失败加忠诚反噬）"执行，并追加要求玩家自选势力亲自捐修城建时同样要看政治资质。

### 一、AI 城建过快——政治资质从未真正影响过营建

**排查**：`FactionAI.build()` 每次命中就直接把某城某项城建升 1 级，只要出得起钱必成功，跟这家势力政治资质高低毫无关系——代码注释写着"政治高则营建快"，但函数体压根没用到传入的政治前五均值 `q`，各家诸侯修城速度整齐划一。城市繁荣度只需六项城建等级总和达到 48（满级 60）即可封 5 星，单城势力按现有节奏约 60~70 天、双城势力约 130~140 天就能达标，正好落在"不到一年"这个区间。

**修复**：
- 新增 `Buildings.buildChance(zheng)`：`clamp(0.25, 0.95, zheng/100)`——政治资质越差，营建越容易"烂尾"（钱粮照掏，不长等级），资质好的势力维持接近原有速度，诸侯之间第一次真正出现发展快慢的差异。
- `FactionAI.build()` 补上这一道判定；`Buildings.build()`（玩家自选势力亲自捐修的入口）同样补上，用 `RPG.heroGeneral().zheng`（含装备加成的政治资质）作判定——按用户追加要求，玩家自己动手营建也不再是稳赚不赔。
- `Prosper.BUILD_SUM_STEP` 的 5 星门槛从 48 提到 56，最后一星要啃下总和的九成三，越往后越难。

**验证**：Playwright 统计 300 次调用，政治资质 40 时 AI 营建成功率约 33%（公式预期 40%，样本噪声内）、资质 90 时约 89%（预期 90%）；玩家侧统计 200 次，资质 40 时成功率约 42%（预期 40%），均与设计公式吻合；`node --check` 复核通过。

### 二、招揽过于容易——失败没有任何代价，等于"反复给钱直到成功"

**排查**：招揽成功率公式本身（0.15 基础 + 友谊/忠诚加成 + 名声加成 - 势力抵抗，封顶 85%）对一个刚认识的陌生敌将只有 25%~35%，并不离谱；但失败后金币照扣、友谊不受影响、没有冷却、也不会引起对方警觉——只要金币够多，可以对同一目标反复尝试，单次 30% 成功率试 5 次累计成功率就有约 83%，"给钱大概率能笼络"说的其实是"反复给钱"。

**修复**：`Loyalty.persuade()` 新增两道保险——①失败后进入 20 天冷却，冷却期内该目标的招揽按钮直接置灰并提示剩余天数，无法再次出手（也不会被扣钱）；②失败时额外拉高对方现有忠诚 +6（"越劝反而越坚定"），让屡败屡战本身变得不划算。

**验证**：Playwright 直接调用 `Loyalty.persuade`，失败后忠诚从 78 涨到 84（+6，与设定一致）、冷却剩余天数为 20；冷却生效期间立即再次调用，返回失败且未扣金币，确认冷却期内完全挡住了重复尝试。

**其余验证**：`node --check` 复核通过；未改动地图相关代码，重跑既有地图回归用例（单击跳转、缩放至中/日全境、双击不误触）确认无连带影响。

## 九十四、存档导出/导入，防清浏览器数据丢进度（第九十二轮）

用户提出：本作是纯静态 PWA，存档全落在浏览器 `localStorage` 里，一旦清除浏览器数据（很多浏览器"清缓存"选项本身就顺带勾了"网站数据"）就再也找不回，问有没有独立于浏览器之外的存储方式。排查确认全部 8 个存档 key（角色/战役/武将库/友谊金币/宝物库×2/百人斩纪录/设置）确实都只落在 localStorage，没有任何备份手段。给出方案并经确认后按"手动导出/导入存档文件 + 申请持久化存储权限"两项实施（云端同步需要账号体系+后台服务器，工程量不对等，本轮不做）。

**实施**：
- `navigator.storage.persist()`：启动时静默请求持久化存储权限，降低浏览器在设备存储紧张时"自动清理不常用网站数据"误删存档的概率——防不住玩家自己手动清数据，只是多一层保险，无需任何界面。
- 新增 `SaveManager` 模块，动态扫描全部 `wujiang_` 前缀的 localStorage key（不写死列表，以后新增存档字段无需改这里）：
  - 「导出存档」：打包成 JSON 文件触发浏览器下载，文件躺在设备下载目录里，跟浏览器数据无关，清多少次缓存都不受影响，也可自行转存到网盘/聊天工具当云备份。
  - 「导入存档」：读取之前导出的文件，校验格式后覆盖写回对应 key，刷新页面生效——换设备、换浏览器、清完数据后都能用它找回进度。
- 两个按钮加入"⚙️ 设置"面板新增的"💾 存档管理"区块，并附一句提醒文案。

**验证**：Playwright 通过真实 UI 触发导出、用下载事件捕获文件内容，核对导出的 JSON 里金币字段与游戏内实际值一致；随后清空全部 localStorage 模拟"清浏览器数据"，用捕获到的文件内容调用导入，核对刷新后金币字段正确恢复为清空前的值，导入导出全链路走通；截图核对设置面板新区块正常渲染、无控制台报错；`node --check` 复核通过。

## 九十三、营建成功率二度调整、招揽成功率改为忠诚硬门槛（第九十一轮）

上一轮上线后用户又反馈两点：①后期武将一多，随便凑个前五政治均值就能上 90，营建门槛形同虚设，要再压低；②招揽成功率还要再结合忠诚度进一步降低——哪怕友谊拉满（300），忠诚一旦够高也该很难策反，但忠诚不能一刀切地把友谊完全抵消，友谊 300、忠诚 60（默认值）时原方案的 64% 不用变，忠诚 30（已有离心迹象）时至少要有 75%，忠诚 90 时压到 3% 左右即可。

### 一、营建成功率：曲线整体右移、天花板压低

**修复**：`Buildings.buildChance(zheng)` 从 `zheng/100`（封顶 95%、下限 25%）改为 `(zheng-30)/100`（封顶 80%、下限 15%）——90 分只给 60% 成功率，要摸到 80% 封顶得堆到 110 分以上的顶尖名将级别。

**验证**：Playwright 核验新曲线在 40/60/80/90/100/110/125 各点分别给出 15%/30%/50%/60%/70%/80%/80%，与设计吻合。

### 二、招揽成功率：忠诚 60 以下沿用旧公式，超过 60 起指数急坠

**排查**：直接用一套"友谊/忠诚各占一份加法"的公式时，两者可以互相抵消——友谊拉满、忠诚封顶（100）时仍有约 50% 成功率，明显不合理，但完全推倒重来又会破坏用户认可的"忠诚 60、友谊 300 时 64% 没问题"这个既有手感。

**修复**：以忠诚 60（游戏默认值）为分界点分段处理——60 分以下沿用原公式（友谊照常线性起作用，且把友谊权重从 0.35 微调到 0.36 以精确命中锚点）；超过 60 后，改为在"忠诚正好等于 60 时的基础值"上按指数衰减，每超出 30 点衰减到约 4.6%。这样忠诚 30 时是 75%（友谊照常给力，因为忠诚还没到分界点之上）、忠诚 60 时是 64.5%（与此前手感基本一致）、忠诚 90 时骤降到 3%（友谊拉满也无济于事）——忠诚超过默认值之后才开始变成真正压得住友谊的硬门槛，而不是全程线性可被友谊抵消。

**验证**：Playwright 核验友谊=300 时，忠诚 30/60/90/100 分别给出 75.0%/64.5%/3.0%/3.0%，三个用户指定的锚点全部精确命中；重跑既有地图回归用例（本轮未改动地图代码）确认无连带影响；`node --check` 复核通过。

## 九十四、边境战参战武将限定驻地、相邻城市按地理距离延迟支援（第九十三轮）

用户提出两点：①城市间对战此前是所在势力全部已现身武将都能上场，不符合"人在千里之外却出现在这场边境战"的逻辑，应改为只有真正驻扎在交战城池的武将才能出战；②出征/防守时应能从相邻己方城池呼叫援军，且援军到场要按真实地理距离折算时间，不能瞬移。方案讨论历经数轮：先明确"野战即时开打、援军按天数延迟"，用户又指出援军的到场应该是回合制、贯穿在野战/攻城战两个阶段里，多数情况下能在野战中后期赶到，赶不上野战的接着在攻城战里继续等，两阶段的路程只算一次不能重复计算，AI 也应当会视战况自行呼叫援军、援军带兵比例玩家自选/AI 按局势自决，没赶上仗的援军原样退回原城不折损。以下按最终确认方案实施。

### 一、参战武将限定驻地

**排查**：`resolveBorderWar` 组建野战双方候选池（`poolA`/`poolB`）时只按势力筛选已现身武将，不看 `m.assign`（武将当前驻扎的城池）——同一势力麾下无论驻扎在哪座城，只要已现身就可能被抽中出现在任意一场边境战里。攻城战 `resolveSiege` 的守方候选池（`defenderPool`）其实早就按 `m.assign === 目标城池` 筛过，只是野战这一侧一直没跟上。

**修复**：`poolA`/`poolB` 的筛选条件补上 `m.assign[g.id] === a`/`=== b`，与攻城战同一口径。主角本人与团队成员（`Bond.myRoster()`）不受影响——这两类是玩家私交/亲领的班底，不看驻地，逻辑上"随你出征"，与士兵一样不用问户籍。

### 二、援军：回合制到场，一套计时贯穿野战与攻城战

**机制**：出征/防守选定目标城池后，可从目标城池相邻的己方城池（`Reinforce.candidates`）里选一座作援军来源，选好或跳过后野战当场开打，不因援军而等待。援军到场所需天数 `Reinforce.days()` 按真实地理坐标折算（`clamp(2, 20, round(距离))`，与地图既有的城池坐标同一套单位），换算成网格战斗的回合数（`ROUNDS_PER_DAY = 3`，估算值）。`GridBattle` 每打完一整回合就替尚未到场的援军把剩余回合数减 1（`checkReinforceArrivals`），减到 0 立即插入战场（`spawnReinforcement`，按其自身兵力/武将现算一套独立缩放系数，就近摆进本方后排空位）。若野战打完了援军还没到，剩余回合数原样带到紧随其后的攻城战里继续倒数（`finishExternal` 把未到场的援军放进 `pendingReinforcements` 交回调用方，`resolveSiege` 原样续算，不重新按路程起算）；连攻城战都打完还没到，或者压根用不上（比如己方在野战倒是打赢了，原本准备防守用的援兵没了用武之地），一律原样退回来源城池的驻军（`Garrison.add`），不折损一兵一卒。

**归属判定**：野战阶段，援军直接加入自己一方的战场（不管最终谁攻谁守）；转入攻城战后，进攻方（此刻已由野战胜负确定）的援军永远有效，防守方援军则只有当初呼叫时瞄准的锚点恰好就是这座真被攻城的城池才继续算数（比如己方主力在野战反倒获胜、追杀到了敌城，原本备着防守本城的援军自然没了用处），判定不通过的援军直接退回原城，判定通过的才继续参与攻城战。

**呼叫方**：亲征一方由玩家在点将界面（`renderBorderWarPicker` 新增"呼叫援军"区块）自选来源城池与出兵比例（复用既有的出阵比例滑杆样式）；对面/未亲自掌兵的一方由 `Reinforce.aiDecide` 判断——己方此战兵力若不明显吃亏就不叫，明显吃亏时挑候选城中驻军最多的一座，按劣势幅度在 20%~60% 间调兵支援。来源城若没有已现身武将可领兵（`draftGenerals` 为空），直接不列入候选，避免"抽了兵却没人带队"的死角。

**验证**：Playwright 分层验证——① `Reinforce.days/etaRounds/candidates/draftGenerals/build/aiDecide` 各函数单独核对换算结果、驻军扣减、空候选/零比例返回 null、劣势触发/优势不触发；② 直接给 `GridBattle.beginExternal` 塞入一份 `remainingRounds:2` 的援军，实测在第 3 回合准点插入战场（多出 2 员指定武将）；③ 塞入 `remainingRounds:200`（超过百回合硬顶）的援军，实测野战速胜结束后正确出现在 `pendingReinforcements` 里、未标记为已到场；④ 走完整真实 UI 流程（`MapUI.openBorderWarPicker` → 点选援军来源与比例 → 「开战」），确认候选城池列表、比例滑杆文案与真实驻军数字一致，点开战后来源城驻军立即按比例扣减，且能观察到对面 AI 一方也按 `aiDecide` 自行叫了援军；`node --check` 复核通过，未改动地图代码，地图回归不受影响。
