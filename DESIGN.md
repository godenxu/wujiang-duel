# 《中日武将大单挑》武将扮演版 · 改造设计方案（定稿）

> 状态：**第一、二、三期均已完成并交付**。三期全部完成后，主线玩法闭环已完整（新游戏→游历→名声→悬赏/设施/奇遇→武道会→天下无双终局）。
> 本文档为大改造的总蓝图，分期实施时以此为准；实施中的数值微调需回写本文档。

## 实施记录

- **第一期·骨架**（已完成）：新主页 5 模块（新游戏/继续游戏/武将图鉴/宝物阁/小游戏）、存档架构（全局层 `wujiang_campaign_v1` 战役元数据 + 战役层沿用既有 `wujiang_rpg_v1`/`wujiang_bond_v1`/`wujiang_armory_v1`）、开局三步向导（自创/史实+少年60%本命天赋+50%成长·巅峰原值减半成长、天下格局预选、确认覆盖）、武将图鉴全局化（`showDetail(g,{global:true})` 剥离友谊/装备数据）、宝物阁全局模板层（`wujiang_armory_global_v1` 存 overrides/custom，编辑加成限幅 ≤15）、小游戏子菜单（原 8 个玩法迁移，经审计确认不触碰战役存档）。
- **第二期·地图**（已完成）：真实地理风格 SVG 地图（中原十城+战国十城+对马岛海路，`CITIES`/`ROADS` 数据表）、移动/天数/行动力系统（`Campaign.meta.map` 存 day/ap/apMax/curCity，历练与移动各耗 1 点行动力，宿营恢复满行动力并推进一天）、折叠屏双栏布局（`min-width:600px` CSS Grid，地图占左 55%、城池面板+状态条占右 45%）、城池界面通用四功能（拜访=本地武将名录点击进详情、历练、集市、铁匠铺，后两者直接跳转宝物库对应页签）、武将按城分布与渐进登场（`buildCityAssignment` 按史实/乱入两种格局分配，`CITY_HINTS` 覆盖约 180 位主要武将的势力归属+姓名哈希兜底其余武将；开局每城现身 2~3 名，之后每宿营跨 5 天批量揭示 3~5 名新武将，不按名声分层）。
  - **导航架构调整**（第二期新增，第一期文档未预见）：天下地图成为战役的新顶层主页，原「角色扮演」主页降为地图下的"角色详情"子页（通过状态条「🎭 详情」按钮进入，返回固定回地图）；「宝物库」（仓库/商店/锻造）可从地图（集市/铁匠铺直达对应页签）或角色详情页（宝物库入口）两处进入，新增 `homeBase` 机制记录战斗/宝物库结算后应返回地图还是角色详情——`showScreen(id)` 每次显示 "rpg" 或 "map" 时更新 `homeBase`，所有"返回养成"类按钮改为统一调用 `goHome()`，从而正确返回发起该动作时所在的那一层。
  - **与设计的差异**：史实分布的势力归属为风味设计而非严谨考据——约 180 位主要武将（各方主公、五虎上将/五子良将、大名/主要家臣等）有具体城池指向，其余约 220 位次要武将按姓名哈希稳定兜底分配到同阵营的城池范围内；行动力上限暂固定为 1（名声系统等第三期成长渠道尚未实现）；悬赏榜、赶路奇遇、特色设施穿插、天下大事件、唯一奇珍、武道会均按原计划留给第三期。
- **第三期·血肉**（已完成）：
  - **名声系统**：四阶阈值（无名之辈0/小有名气60/威震一方200/名满天下500），`Campaign.addFame(n)` 增加名声并在跨阶时提示；`Campaign.recalcApMax()` 按"1+名声阶梯+武道会首冠(+1)+已装备唯一奇珍(each+1)"重算行动力上限，封顶 6，上限提升时同步补给当天行动力。名声来源：任意切磋胜利(+3)、悬赏完成(+8~35)、设施挑战胜利(+8~50)、威名榜战胜高手(+25)、世界杯按名次(+12~60，夺冠额外首冠+1上限)。
  - **行动力扩展**：`spendAP()` 通用助手，历练/车轮/百人斩/阵营/组队/国战/世界杯的 RPG 入口方法统一在真正开战前扣减（2v2 在实际提交处扣减，避免取消选副将浪费）；不再局限于第二期仅有的"历练+移动"两项。
  - **悬赏榜**：每城 2~3 条悬赏（`Campaign.reset` 时经 `genBounty` 生成），讨伐令(击败指定武将，优先本地已现身者出题)与车轮令(连胜N场)两类，约 15% 为高级悬赏(奖励更丰厚，首次达成额外掉「千里靴」)；接取耗 1 行动力并直接发起对应战斗，`RPG.onBattleEnd`/`onGauntletResult` 结算时判定完成、发奖、刷新新悬赏。
  - **赶路奇遇**：移动时约 30% 触发六选一（山贼拦路按评分测算胜率给金/路遇奇珍掉神秘宝物/行脚商队集市临时八折/世外高人加点+1/路遇名将直接加友谊/客栈奇遇临时行动力+1）；触及对马岛的海路额外 20% 概率触发风暴（延误耗尽当日行动力 或 漂流到随机港口）；对马岛集市常驻八折黑市。
  - **特色设施穿插**：`CITY_FACILITY` 表为 20 城各配一项特色设施（天下擂台/车轮战武场/百人斩道场/双人比武场/军团攻城/远征/武道会预选，7 种玩法循环覆盖 20 城），城池面板按当前所在城显示专属设施按钮；duel 类设施弹出本地已现身武将挑选框，其余直接调用对应 RPG 入口。
  - **唯一奇珍**：赤兔·千里神驹(mount，百人斩首次达 15 层掉落)、千里靴(attire，首个高级悬赏达成掉落)，`Armory.makeUniqueTreasure(key)` 固定造出传说级特殊物品，额外带 `apBonus:1` 字段供 `recalcApMax` 识别；`uniqueOwned` 标记确保全局各仅能获得一次。
  - **武道会与终局**：世界杯 32 强报名需名声达「威震一方」（`fameTierIndex>=2`）方可解锁；首次夺冠永久 +1 行动力上限；`RIVAL_NAMES` 八大高手（吕布/关羽/张飞/赵云/织田信长/武田信玄/上杉谦信/本多忠胜，按姓名匹配以兼容武将库编辑）逐一击败记录于 `rivalsDefeated`；集齐八大高手 + 至少一次武道会夺冠触发一次性「天下无双」终局弹窗，之后照常继续自由游历（`ending` 标记防止重复触发）。
  - **与设计的差异**：威名榜按姓名而非固定 id 匹配（兼容武将图鉴改名/重建默认库场景）；"行脚商队/对马黑市"统一实现为集市价格 ×0.8 而非详细的差异化折价表；特色设施与"每城限定不重复"的设想不同，出于 20 城需覆盖 7 种玩法的现实，改为循环复用（每种玩法在 2~3 座城都能找到），但仍保留"每城都有一个专属去处"的体验。
- **第三期后续微调**（已完成）：
  - **车轮大战/百人斩免确认连战**：单场获胜结算浮层保留（仍展示战果与「立即迎战/攀上」「鸣金收兵」按钮），但新增 900ms 自动推进定时器，玩家无操作时自动进入下一阵/下一层，无需每场手动点击确认；百人斩每 5 层的「天赐机缘」三选一仍需手动挑选，不受影响。
  - **名声阶梯拉宽**：`Campaign.FAME_TIERS` 阈值由 0/60/200/500 调整为 0/500/2000/5000（约放大 8~10 倍），新增 `Campaign.FAME_MAX = 9999`、`addFame()` 封顶写入，避免早期数值范围过窄导致几场切磋/悬赏就冲满全部阶梯。
  - **折叠屏展开布局重排**：宽屏（≥600px）布局由原"地图+面板左右各半"改为"地图横贯顶部（`clamp(200px,30vh,340px)`，吸顶固定）+ 下方左（角色资料卡+状态条）右（城池事务：历练/集市/铁匠铺/设施、悬赏榜、本地武将）两栏"，窄屏（<600px）保持原纵向单栏堆叠不受影响。
- **地图扩容与交互增强**（已完成）：
  - **城市扩容至 40 座**：`CITIES`/`ROADS`/`CITY_FACILITY` 由"中原十城+战国十城"扩至"中原二十城+战国二十城"（各新增 10 座，如天水/白帝城/合肥/武昌，肥后/近江/美浓/常陆等），新城通过新增 `ROADS` 边接入既有路网保持全图连通；`CITY_FACILITY` 循环延续 7 种玩法覆盖全部 40 城。新增 20 城的武将归属未做逐一史实考据，全部走姓名哈希兜底分配（与既有约 220 位次要武将同一机制），是本轮明确的取舍，记录于此供后续如需补充史实提示时参考。
  - **地图缩放/拖动**：`MapUI.bindZoom()` 实现鼠标拖拽/触控拖拽/双指捏合/滚轮缩放/右上角 ＋－ 按钮，缩放状态 `MapZoom` 为模块级变量、跨 `render()` 重渲染持久。踩过一个坑：最初用 `element.setPointerCapture()` 实现拖拽，实测会导致后续 `click` 事件的目标被浏览器重定向到发起 capture 的祖先元素（而非实际点击的子元素），使城池点击彻底失效——不只是拖拽后的一次误触发，而是任何一次点击都失效。改为在 `document` 上于 `pointerdown` 时临时挂载 `pointermove`/`pointerup` 监听、手势结束即摘除的经典模式，规避了该问题。
  - **地图背景陆地剪影**：新增 `CHINA_LAND_PATH`/`JAPAN_LAND_PATH` 两条手绘简化 SVG 路径（与城池同一 0~100 相对坐标系），渲染于道路线下方，替代原先的抽象渐变色块，视觉上更接近真实地图但仍为风格化剪影而非精确测绘（未接入任何外部地图数据源，保持 PWA 离线可用、无版权顾虑）。
  - **名声九阶重构**：`FAME_TIERS` 由 4 档细分为 9 档，阈值 0/150/500/1200/2200/3500/5000/7000/9000；行动力上限贡献公式由"1+阶梯"改为"1+⌊阶梯÷2⌋"（新增的细分档不再每档都加 AP，仍在相近区间达到与旧 4 档版本类似的总封顶节奏）；`RPG.TRAIN_FAME_TIER`（历练解锁）由 1 调整为 2（对应"小有名气"新阈值 500），新增 `RPG.CUP_FAME_TIER = 4`（武道会报名，对应"威震一方"新阈值 2200）替代原硬编码 `< 2` 判断；新增三处细分档专属福利（集市货摊数、悬赏榜条数、铁匠铺折扣），使每次跨阶都有具体可感的变化而不仅是数字与 AP 上限。
  - **布局紧凑化**：城池面板"悬赏榜"（`.mc-bounty`）与"本地武将"（`.mc-gen`）卡片的内边距/间距普遍收窄约 30~40%，字号不变（尊重此前"字号放大"的明确诉求，紧凑通过留白而非缩小文字实现）。
- **地图信息架构精简**（已完成）：
  - **城市名字号缩小**：`.mcity-name` 由 14px/16px（当前城）调小为 9px/11px，缓解 40 城密集分布时的文字重叠。
  - **武将不定期自行迁移**：新增 `MapUI.wanderGenerals(m)`，每次宿营对每名已现身武将独立判定 3% 概率迁往其阵营内、与当前城池相邻的另一城池（复用 `adjCities`，天然不会跨海路/跨阵营）；`camp()` 中在天下快报（新登场提示）为空时改为提示本次迁移人数，天下版图因而持续流动而非一成不变。
  - **全部武将清单**：`MapUI.openAllGenerals()` 新增入口（本地武将标题栏右侧「全部武将」按钮），弹出跨全图的已现身武将清单，按友谊值降序，逐条显示当前所在城池与友谊值，复用 `showDetail` 承接点击进入详情。
  - **地图信息去重构+布局重排**：发现"角色资料卡"与原"天数/行动力/金币/名声状态条"两处都在展示名声/等级，属于明确的信息重复。改为将天数/行动力/金币/名声/等级五项状态统一上移至顶栏（`#map-topbar-status`，位于标题"天下游历"与右侧音乐按钮之间，含宿营快捷按钮），角色资料卡精简为仅头像/姓名/评分；原状态条腾出的位置改为承载"本地武将"区块（`localGeneralsHtml`，从右侧城池面板搬到左侧信息栏），每条武将卡片相应收窄（`.mc-roster.narrow`，`auto-fill, minmax(78px,1fr)`），标题文案由"X 位已现身 · 另有 Y 位未现身"精简为"已现身X/Y"。窄屏与宽屏（≥600px 双栏）布局同步调整，宽屏布局说明相应更新为"左：角色资料+本地武将，右：城池事务"。
  - **小游戏菜单居中**：`#screen-minigames` 补充 `align-items:center`（此前遗漏，与首页 `#screen-home` 同一做法），使 8 个玩法按钮网格在宽屏下水平居中而非贴左。
- **地图信息架构精简 · 续**（已完成）：
  - **城市名字号再缩小**：反馈上一轮 9px/11px 在 40 城密度下仍会重叠，进一步调小至 6px/8px（当前城），配合已有的地图缩放功能按需放大细看。
  - **顶栏状态字号放大 + 宿营按钮归位**：`#map-topbar-status` 字号由 12/13px 提升至 15/17px，更易辨读；「🏕️ 宿营」按钮从顶栏移出，并入 `cityPanelHtml` 的 `.menu.map-menu` 行动卡网格（与历练/集市/铁匠铺/设施同排同风格），顶栏因此腾出空间、只保留纯状态展示。
  - **本地武将条目精简**：`localGeneralsHtml` 的 `.mc-gen` 去掉 `avatarChar()` 生成的大字单姓头像（`.mcg-av`），只保留完整姓名，`.mc-roster.narrow` 的 `minmax` 相应从 78px 调宽到 92px 以容纳更长姓名不被省略号截断。
  - **全部武将改版为图鉴风格只读表格**：原先的卡片式弹窗（`buff-list`）替换为新增的独立页面 `#screen-allgen`（`AllGenUI` 模块），结构、CSS（`.db-table`/`.side-tabs`/`.search-box`）均与武将图鉴 `#screen-db` 一致——可按任意列排序、按三国/战国/全部筛选、按姓名搜索，但去除新增/编辑/删除等修改类操作；六维与评分不再取图鉴默认值，而是用 `Armory.geared(g, g.id)` 叠加该武将当前已装备宝物后的实时数值（与详情弹窗、战斗结算所见一致），并新增"友谊""所在城"两列（默认按友谊降序）。导航上固定挂在天下地图之下（`data-back` 处理逻辑新增 `#screen-allgen → MapUI.open()` 分支），点击任意武将行仍复用 `showDetail` 弹出完整详情（含赠礼/切磋/装备等所有既有交互）。
- **地图交互与全局返回体验**（已完成）：
  - **城市标记改矩形徽章**：`.map-city` 由"圆点 `.mcity-dot` + 下方悬浮 `.mcity-name`"改为单一矩形（圆角 3px，按阵营着色）容器，姓名直接排布在矩形内部，不再是两个视觉元素堆叠，客观上也略微缓解了 40 城密集时的重叠观感。
  - **地图聚焦/全览按钮**：`.map-zoom-ctl` 新增 `#map-zoom-focus`（🎯，`MapUI.focusCurCity()`——按 `transform-origin:center` 的缩放数学关系换算出让当前城池坐标居中所需的 `translate`，缩放至 2.2 倍，超出可平移边界时被既有的 `clampZoomState` 安全收紧）与 `#map-zoom-overview`（🗺️，直接复位 `scale(1) translate(0,0)`）两个按钮。
  - **全部武将详情改为只读**：`showDetail(g, opts)` 新增 `opts.readonly`——`bondable` 判定加入 `&& !opts.readonly`（不显示拜访/切磋/招募等交友板块），装备槽位仍显示当前真实装备（复用 `eqSlotsHtml`）但外层容器加 `.readonly` 类（CSS `pointer-events:none`），且不再调用 `bindEqSlots()`，双重确保不可点击更换；`AllGenUI` 的行点击从 `showDetail(g)` 改为 `showDetail(g, {readonly:true})`。
  - **顶栏信息去重排版**：等级信息从顶栏状态条移除，改在 `heroCardHtml` 的姓名下方、评分左侧显示（`Lv.${c.level} · 评分 ...`）；顶栏状态条本身通过 `flex-basis:100%` + `order` 强制独占一行，排在"返回箭头+标题+音乐按钮"那一行的下方，不论折叠屏展开与否都固定两行布局，不再出现文字挤在一起换行错位的问题。
  - **同步手机系统返回键**：原先内联在 `[data-back]` 点击事件里的路由判断逻辑抽成独立函数 `handleBackAction()`，`showScreen(id)` 在非"返回导航触发"时正常 `history.pushState`；新增 `popstate` 监听——弹窗打开时优先 `closeOverlay()` 并立即补推一条历史记录（不消耗"画面"层级，避免关闭弹窗被计成一次导航）；否则置位 `backNavActive` 标记后调用同一个 `handleBackAction()`（该标记使 `showScreen` 内部不再重复 push，避免历史栈因"返回导致的切屏"而越返越深）。首次进入的 `screen-home` 本就是 HTML 静态 `active`，从未走过 `showScreen()`，因此天然充当历史栈的"地基"——在首页再按一次返回键即为退出 PWA/关闭标签页的默认浏览器行为，符合预期。
- **顶栏三度重排 + 悬赏丰富化**（已完成）：
  - **全部武将详情恢复交友区**：`showDetail` 拆出 `showFriendBox`（同阵营即显示，不再受 `opts.readonly` 影响）与 `bondable`（决定是否渲染 `.bond-gifts` 拜访/切磋/招募三按钮，仍受 `opts.readonly` 约束）两个独立判定，`AllGenUI` 因此重新看得到目标武将的友谊进度条与等级，但保持不可互动。
  - **顶栏信息第三次重排**：`#map-topbar-status` 精简到只剩当前游历天数（换算为下述天下历），行动力/金币移至 `cityPanelHtml` 的 `.mc-head-stats`（当前城池名称同一行右侧对齐），名声（含名声等级、等级在前）移至 `heroCardHtml` 新增的 `.mh-action-col`（`.mh-fame`，在「🎭 角色详情」按钮正上方），四类此前散落各处的状态首次实现"各自贴着与之最相关的元素显示"而非集中堆在一处。
  - **天下历**：新增 `calYMD`/`calLabel`/`isMonthEnd` 三个纯函数，把 `Campaign.mapState().day` 这个从第 1 天起递增的原始计数器，换算为每年 12 月、每月 30 天的"X 年 X 月 X 日"展示格式；`isMonthEnd` 同时作为下述边境阵营大战的月度触发条件复用。
  - **悬赏榜四类穿插 + 交互简化**：`genBounty` 新增"登塔令"（百人斩爬至指定层）与"双雄令"（2v2 取胜一场）两类，与既有讨伐令/车轮令按 4/2/2/2 权重穿插；`RPG.duo()` 拆分出 `RPG.duoPicker(apSpent)`，悬赏接取时以 `duoPicker(true)` 跳过二次扣行动力（`acceptBounty` 已统一扣过）。UI 上 `.mc-bounty-list` 由纵向单列改为两列网格，原先卡片内嵌套的"领取"按钮去掉，整张卡片本身即可点击接取。
- **边境阵营大战与刺杀系统**（已完成）：
  - **城池归属独立于武将分布**：新增 `m.cityOwner`（城池 id → `'cn'|'jp'`，随边境战事可变）与静态的 `c.side`（武将分布/悬赏出题等沿用的固有阵营标记）两套并行概念，`cityOwnerSide(m, id)` 统一读取入口；`initCityOwner()` 令海路中转站对马岛（唯一 `side==="sea"` 的城池）初始归属战国一方，其余城池归属与其固有阵营一致。
  - **边境战自动识别**：`borderEdges(m)` 遍历 `ROADS`，筛出两端 `cityOwnerSide` 不同的边——天然只在对马岛与其两座三国邻城（建业/徐州）之间起步，但会随城池易主自然向外蔓延，无需为"前线"单独维护一份数据。
  - **月末阵营大战**：`camp()` 中若 `isMonthEnd(m.day)` 成立则调用 `checkBorderWar`，逐条边境弹出 `openBorderWarPicker`——若涉及主角所在阵营可选择率团队亲征其中一场（`resolveBorderWars` 中把主角与同阵营队友强制编入该场角色阵营的队列），否则各地驻军自行迎战。战斗本体复用 `js/engine.js` 现成的 `autoBattle(g1,g2)` 单场极速判定（同一函数已用于既有的「阵营大战」小游戏），双方各出「已现身武将」中随机等量人马（`Math.min` 取两侧较小值），依次淘汰直到一方队列耗尽，胜方直接把 `m.cityOwner[捕获城]` 改写为己方，前线因而逐月推移；亲征分支额外结算主角经验/金币/名声。
  - **潜入敌境唯有刺杀**：`showDetail` 新增 `assassinable`（同阵营改交友、异阵营且非 readonly 视图改「🗡️ 刺杀」，二者互斥），点击后进入与切磋相同的经典单挑通道，`m.activeAssassin` 记录目标；`RPG.onBattleEnd` 新增判定块——胜则从 `DIMS` 随机挑一维给敌将扣 1~3，败则反过来扣自己，全部写入战役内 `m.statPenalty`（`Armory.geared()` 在既有装备加成基础上一并减去，六维不低于 10，不写回武将图鉴全局数值）；`Bond.data.assassinDay` 提供与 `visitDay`/`sparDay` 一致的每日一次限制。为避免刺杀战胜的"不打不相识"惯例逻辑意外拉近与敌将的友谊，`RPG.onBattleEnd` 通用胜利分支的 `Bond.addF` 改为仅在 `opp.side === c.side` 时才执行。
  - **宿营反向夜袭**：`MapUI.checkAmbush(m)` 在 `camp()` 天数递增后、月末边境战判定之后运行——若当前城池的本地已现身武将中恰好存在敌方阵营成员，15% 概率弹出夜袭提示，「应战」直接复用与主动刺杀完全相同的 `m.activeAssassin` + `RPG.onBattleEnd` 结算通道（胜负判定对称，无需为"谁发起的"另写一套逻辑）。
- **边境战接入真实小游戏 + 悬赏/UI 细节修复**（已完成）：
  - **宽屏双栏改 50/50**：`.map-bottom` 的 `grid-template-columns` 由 `38% 62%` 改为 `1fr 1fr`，此前的偏向比例（城池事务区更宽）经反馈后统一为等宽。
  - **天下历纪年归零**：`calYMD` 的 `year` 计算去掉 `+1`，改为年份自 0 起（月、日仍各自 1 起），与"天数从第 1 天算起"的既有惯例分离——玩家更习惯"元年"从 0 数起而非 1。
  - **顶栏字号与溢出防护**：`.mc-head-stats`（当前城池行的行动力/金币）字号由 14px 提至 17px，同时补 `min-width:0; overflow:hidden; text-overflow:ellipsis` 与 `.mc-head` 的 `min-width:0`，配合既有的 `flex-wrap:wrap`，字号放大后仍不会把内容推出视口右边界。
  - **悬赏榜提示精简**：`.mc-sect small` 由「点击整条领取，耗 1⚡」精简为纯「1⚡」。
  - **本地武将空列表提示宽度**：`.mc-roster.narrow.triple` 是 3 列网格，作为唯一子元素的空状态提示此前只占其中 1 列宽度、显得局促；改为内联 `grid-column:1/-1;width:100%`，使其撑满与主角信息区同宽的整行，文案同时精简去掉"已"字。
  - **修复子玩法退出误回首页**：`handleBackAction()` 此前只对 `screen-armory`/`screen-allgen`/`screen-rpg` 三个屏幕特判"应回到发起它的那一层"，遗漏了 `screen-war`/`screen-teamwar`/`screen-conquest`/`screen-cup`——从天下地图城池特色设施（如汝南屯田营→国战）进入这些子玩法后按返回键，会直接摔回首页而非回到地图。新增统一分支，按各模块既有的 `War.rpg`/`TeamBattle.rpg`/`Conquest.rpg`/`Tournament.rpgMode` 标记判断是「小游戏自由试玩」（回首页）还是「角色扮演/地图发起」（回发起的那一层，`goHome()`），其中 `War` 模块此前从未追踪过这个标记，本轮一并补上 `this.rpg = !!hero`。
  - **刺杀 UI 细节**：刺杀按钮文案去掉多余的「（经典单挑）」备注，改标注与切磋一致的耗时提示「（-1⚡）」，并补上此前遗漏的行动力检查/扣减（原先无论行动力是否耗尽都能点击刺杀，现与切磋同规格：耗尽时置灰、点击提示、成功扣 1）。
  - **修复六维扣减显示 `+-N`**：`statRow`（武将详情弹窗）与 RPG 主页 `renderHub` 内联的六维展示，装备/惩罚增量此前固定拼接 `(+${gear})`——`gear` 为负数（如刺杀惩罚生效后）时就会显示成 `(+-3)`；改为按正负号动态决定前缀 `${gear > 0 ? '+' : ''}${gear}`。
  - **刺杀得手追加名声**：`RPG.onBattleEnd` 的刺杀结算分支此前只处理六维惩罚，未给主角任何名声收益；补上取胜时 `Campaign.addFame(15)` 并在战报文案中注明。
  - **悬赏榜新增「刺杀令」**：`genBounty` 新增第五种任务类型——仅当目标城池当前已现身的本地武将中存在与主角阵营不同者时才计入候选（`enemyLocal`），命中该前提时以 15% 概率优先生成，命中失败或不满足前提则退回原有四类 40/20/20/20 权重分配；由于生成时机（开局 `Campaign.reset`、名声跨阶追加、悬赏完成刷新）早于/独立于 `RPG.char` 创建时机，`Onboard.finish()` 改为先从选定的自创/史实武将解出 `side` 并随 `mode.side` 一并传入 `Campaign.reset`，使初始生成时就能正确识别敌我。刺杀令的接取与结算复用玩家主动发起刺杀的同一套 `m.activeAssassin` 通道（`acceptBounty` 内新增 `assassin` 分支设置目标并直接开战，`RPG.onBattleEnd` 内新增与「讨伐令」平行的悬赏完成判定），因而刺杀令的高额悬赏名声（45/高级 90）与刺杀本身固定的 +15 名声是两笔独立发放、按流程自然叠加。
  - **边境战改为每月单场 + 真正的阵营大战小游戏 + 夺城后部署调整**：`checkBorderWar` 从"逐条边境依次弹窗"改为候选边境中随机只挑一条；`openBorderWarPicker` 不再列出多场选择列表，只针对这唯一一条边境询问"率团队亲征"或"各地驻军自行迎战"（不涉及主角阵营时直接自动结算，不弹窗）。原先"亲征"分支只是把主角/队友塞进即时模拟队列、瞬间给出文字战报，现在改为真正调用 `War.start(hero, {customRoster, onDone})` 进入交互式的阵营大战画面（见上方 `War.start` 改造），玩家能看到战斗日志逐条滚动、击杀榜实时更新，而非直接弹出结果；不亲征的分支仍保留原先的即时模拟（`autoBattle` 队列淘汰）以维持轻量。胜负结算后新增 `applyBorderWarOutcome(m, edge, winnerSide)` 共用逻辑：败方原驻守被占城池的武将退守至己方相邻城池（复用 `adjCities`+`cityOwnerSide` 过滤），胜方从已现身武将中随机抽 2~4 名进驻占领的城池，使占领不再是"纯换色"而带有部署上的实际后果。
- **地图连通性修复 + 边境战改用组队大战 + 阵营大战亲历模式 + 武将大会**（已完成）：
  - **地图视觉假连通修复**：写脚本对 `CITIES`/`ROADS` 做几何检测——对每条道路的线段，检查是否有其他（与该线段两端都无直接道路）的城池坐标恰好落在该线段上（垂距 <2.5、投影参数落在线段内 8%~92% 区间），发现安艺↔萨摩、大坂↔尾张两条线分别与丰后（垂距仅 0.23）、近江（垂距仅 0.37）几乎完全重合，另有建业↔徐州/京都↔尾张/对马岛↔萨摩三处距离稍远（1.3~2.3）但同样构成视觉误导；据此微调 `hefei`/`higo`/`bungo`/`bizen`/`omi` 五城坐标（如丰后由 `(62,76)` 移至 `(59,72)`），重新跑检测确认「视觉假连通」全部清除、仅剩三处「已有直连、问题较小」（该第三城本就与线段一端相连，不构成误导）且未引入新的城池间距过近问题。
  - **边境战改用组队大战**：亲征分支不再调用 `War.start`（阵营大战），改为 `TeamBattle.begin(mine, heroSide, {exact:true, enemies:theirs, rpg:true, onDone})`——武将选择原则不变（双方均取「已现身武将」、主角与队友必上阵、其余随机补足、双方数量相等），因 `TeamBattle` 单场硬性上限 10 将（与国战攻城 `Conquest` 的既有做法一致），双方数量额外取 `min(...,10)`。`onDone` 回调收到 `{playerWon, mySurvivors, theirSurvivors, kills}`，据此判定胜负阵营、主角是否阵亡（`mySurvivors` 中是否仍含 `id===-1`）、结算经验/金币/名声，city 归属与部署调整仍走既有的 `applyBorderWarOutcome`。
  - **War 模块支持逐场询问 + 补齐 `rpg` 追踪**：`War.start(hero, opts)` 循环中新增判定——快捷模式下若即将交手的一方是主角本人或现有队友（`isHeroOrMate(g) = g.id===-1 || Bond.inTeam(g.id)`），先 `await this.askJoinDuel(cnFighter,jpFighter)` 弹窗询问「亲自应战」（进入经典单挑画面）或「自动观战」（照常瞬间结算），不强制打断已在进行的快捷推进节奏；`abort()` 新增对该 pending promise 的清理（`this._askResolve`），避免中途退出留下无法 resolve 的 await。顺带补上 `War.rpg = !!hero`（此前 War 模块从未追踪这一标记，是第九轮"子玩法退出误回首页"修复遗漏的一环——因为当时 War 还没有 hero 可传入的入口）。由于此功能需要一个"主角在阵营大战里"的可达路径，而角色详情页在早期改版中移除了全部 8 个玩法快捷按钮、边境战本轮又改投组队大战，`War.start` 事实上已无任何 hero 可达入口——因此在角色详情页新增「⚔️ 阵营大战」入口按钮（`RPG.war()`，耗 1⚡ 于「开战」时结算），重新给这一新功能提供落地场景。
  - **武将大会**：`isTournamentDay(day)` 判定每季度第二月（2/5/8/11）第 1 天，于 `camp()` 中月末边境战判定之前检查（`MapUI.checkTournament`）。报名费 `≈ratingScore(hero)×2`，报名即扣费、杀入四强（`Tournament.heroPlacement().label` 命中半决赛及以上）全额退还；32 强名单除主角外从 `m.appeared` 随机抽取（含队友，一视同仁），不足以 `MapUI.byeFighter` 占位补满，`Tournament.size` 报名前强制重置为 32 避免沿用小游戏残留设置。不参加时不放弃赛事本身——新增 `Tournament.simulate(parts)` 无 UI 纯逻辑模拟（小组循环赛取前二 + 单败淘汰，全程 `autoBattle`）在后台产生冠亚军；参加时复用现有交互式 `Tournament.begin`/`runKnockoutRpg` 流程，但新增 `Tournament.onDone` 可选回调（`settlePrediction` 两处收尾分支都优先判断），设置后接管默认的 `RPG.onCupResult`。冠亚军奖励统一走 `MapUI.applyTournamentPrize`：主角六维加成直接改 `RPG.char.alloc`（封顶 120 保护）；非主角武将新增战役内 `m.statGrowth` 字段（与 `m.statPenalty` 同构、符号相反，同样经 `Armory.geared()` 叠加展示不写回全局）。冠军额外触发 `grantChampionTreasure`：主角直接入库；非主角按稀有度/加成比较是否优于当前同类型装备，更好换装，更差则退化为再一次 `statGrowth +3`（与冠军基础奖励独立叠加，运气差时可能同维度或不同维度累计到 +6）。
  - **主角信息区精简 + 城市功能按钮真置灰**：`heroCardHtml` 的 `.mh-sub` 去掉 `评分 ${ratingScore(hg)}` 数字，只保留 `ratingChip(hg)` 等级字母。审查发现城市面板的 `.map-menu .menu-btn` 此前虽已正确带上 `disabled` 属性（行动力为 0 时），但项目里唯一负责禁用态视觉（灰化+`grayscale`）的 CSS 规则 `.menu-main .menu-btn:disabled` 选择器只覆盖首页主菜单的 `.menu-main`，未覆盖天下地图城池面板用的 `.map-menu`——按钮功能上已禁用但外观和平时一模一样；补充选择器 `.map-menu .menu-btn:disabled` 命中同一条规则即修复。
- **友谊上限收紧、切磋习得、宝物经济数值上调**（已完成）：
  - **友谊上限改 300 + 招募改满上限重金**：`Bond.addF` 的封顶由 `999` 改为新增的 `Bond.MAX_FRIEND = 300`；`Bond.LEVELS` 顶档阈值同步由 250 改为 300（与上限对齐，使"已至最高"提示准确对应真正的招募解锁点）。`Bond.recruit` 的门槛由原先"挚友（150）可招募、生死之交（250）免费"改为唯一门槛"友谊满 300"，且**取消免费档**——`recruitCost` 由 `评分×2` 上调为 `评分×10`（"重金"），`showDetail` 的招募按钮文案/禁用态相应改为二态（未满上限锁定 / 满上限显示实际重金费用），不再有"免费"文案分支。`Bond.visit` 新增前置检查：友谊达上限即直接拒绝并提示，`showDetail` 的拜访按钮同步在满上限时置灰、文案改「友谊已满」（与"今日已访"的每日限次提示互斥显示，二者共用同一 `dim` 态但文案优先级不同）。
  - **切磋习得**：`RPG.trySparLearn(m, heroWon, opp)` 在既有的切磋友谊结算之后追加判定——命中概率 `1% + (对手当前友谊/300)×30%`（友谊越高概率越高，封顶 31%），命中后用 `Armory.geared()` 取双方含装备/惩罚/成长的实际六维，`DIMS.reduce` 找出败方最高一项，若严格大于胜方同一项则该项 +1：主角一侧直接改 `RPG.char.alloc[dim]`（沿用 `RPG.eff()` 判断 120 封顶，与既有加点成长走同一套读取路径，无需额外改动展示逻辑）；对手一侧新增战役内 `Campaign.mapState().statGrowth[gid][dim]` 字段——与刺杀机制引入的 `statPenalty` 结构完全对称（同为 `{ti,wu,tong,zhi,zheng,mei}`），`Armory.geared()` 补一行 `if (growth) ...+= growth[k]` 即可复用同一套装备加成叠加管线，只是符号相反（`statPenalty` 减、`statGrowth` 加）。
  - **宝物加成区间化 + 售价上调**：`Armory` 新增 `BONUS_RANGE`（`normal:[1,2], fine:[3,5], rare:[6,8], legend:[9,10]`）与 `rollBonus(rarityK)`，`makeItem` 里所有此前直接取 `rar.bonus`（含 curio 的"体魄"效果，取 `curioVals('ti')` 时同源于 `RARITIES.bonus`）的默认取值路径统一改为调用 `rollBonus`——`bonusOverride`（自定义宝物/唯一奇珍的显式覆盖值）路径不受影响，仍取固定值。`shopPrice(rarityK, discount)` 由固定表 `{normal:220, fine:520, rare:1100, legend:2600}` 改为公式 `rarityDef(rarityK).bonus × 1000`（普通1000/精良3000/稀有6000/传说10000），取的是稀有度的**基准**加成值（`RARITIES[].bonus` 本身未改，仍是 1/3/6/10）而非每件实际浮动后的加成——因为货摊/商店展示价格的时间点上具体宝物实例（及其随机加成）尚未生成，只能按档位基准定价；因此实际到手的加成有可能略高于"按标价该得的量"，是有意保留的轻度惊喜感，不做逐件动态定价。锻造 `forge()`、集市 `buyShop`/`market-buy`、悬赏与冠军奖励等一切经 `makeItem`/`shopPrice` 生成物品或计价的路径均自动获得新行为，无需分别修改调用点。
- **阵营大战入口回收、团队随行、六维基础上限收紧、宝物出售**（已完成）：
  - **移除角色详情页阵营大战入口**：上一轮为让"逐场询问是否亲自单挑"这一新功能可达，临时在角色详情页新增了 `#rpg-war` 快捷入口按钮；本轮按反馈移除该按钮及其绑定（`RPG.war()` 方法本身保留，但重新变为不可达的死代码，与更早期"移除 8 个玩法快捷按钮"时的状态一致）——`War` 模块的"阵营大战"玩法此后仅能从首页「小游戏」自由试玩进入，边境战事本就已在更早一轮改投「组队大战」，二者互不影响。
  - **团队随行**：`MapUI.moveTo(id)` 在写入新 `m.curCity` 后，新增 `Bond.data.team.forEach(gid => { m.assign[gid] = id; })`——团队成员的 `m.assign` 城池归属与主角同步更新，使其在新城池的"本地武将"名录中同步现身，不再滞留在招募时的原城池。
  - **组队遭遇战**：新增 `MapUI.triggerTeamEncounter(m)`，约 12% 概率命中（`moveTo` 中先判定该函数，命中则直接 `return`，未命中才落回既有的 `triggerEncounter`——两者互斥，同次移动只触发其一）。命中后从对方阵营已现身武将中随机抽取与"主角+团队成员"等量的对手，`TeamBattle.begin(mine, heroSide, {exact:true, enemies:theirs, rpg:true, onDone})` 进入组队大战画面，结算模式与经验/金币/名声公式仿照边境战亲征分支（略调低基础值以体现"遭遇战"而非"边境战事"的规模差异）。
  - **六维基础上限 120→110**：`RPG.allocate(k)`、`RPG.trySparLearn` 主角分支、`MapUI.grantHeroStatGrowth`（武道会奖励）三处判断 `RPG.eff(c,k) >= 120` / `< 120` / `120 - RPG.eff(...)` 统一改为 `110`，`allocate` 的封顶提示文案同步改为「该维度已达上限 110」。装备/宝物加成不受此限制，仍可叠加至基础值 110 之上；雷达图坐标系的固定刻度 `max=120`（纯展示用，与本次调整的"基础值可分配上限"是两个独立概念）未作改动。
  - **友谊满上限仍显示增加的文案修正**：根因是 `Bond.addF(id, n)` 此前无返回值，各处结算文案直接用传入的名义增量 `n` 拼接「友谊 +N」，即使该武将友谊早已被 `Math.min(300, ...)` 封顶、实际值分毫未涨，文案仍按 `n` 显示涨幅。修正为 `addF` 返回 `实际新值 - 原值` 的真实增量，`RPG.onBattleEnd` 切磋获胜分支、`Bond.maybeGiftFriend`（赠礼）、`Bond.visit`（拜访）三处结算文案改用该返回值渲染，返回值为 0 时切磋分支改显示「友谊已至上限」提示，其余同类调用点（百人斩/阵营大战/组队大战/国战/武道会同组的 `Bond.addMany` 批量结算）经排查均无配套的"+N"专属文案、不受此问题影响，未作改动。
  - **宝物出售**：`Armory.itemCard()` 未装备宝物的操作区由单一「拆解取材」按钮改为并列两个各占 50% 宽度的按钮——「拆解」（原逻辑不变，仅改名）与新增「出售」；对应新增 `Armory.sellItem(uid)`：校验已鉴定且未装备后，按 `shopPrice(rarity) × SELL_FACTOR(0.5)` 结算金币（低于市价），从仓库移除该宝物，并以其自身的名称/简介/属性（而非重新查询图鉴模板池，避免自建模板事后被删导致挂空引用）重建为一条货摊记录追加进 `Armory.data.shop`，使其可在「商店」页签被其他时机再次购回（重新购得时的加成值会依 `BONUS_RANGE` 重新随机，不保留出售前的原始加成）。CSS 新增 `.ic-btn-row`（flex 均分）承载两个按钮。
- **边境战犒赏、行动力奇珍改制、天赐机缘封顶保护、悬赏权重再平衡**（已完成）：
  - **边境战胜利犒赏**：`MapUI` 新增常量 `BORDER_WAR_WIN_BONUS = 1000`。`resolveBorderWar` 的两条结算分支——不亲征的即时模拟（判定 `winnerSide === heroSide`）与亲征的 `TeamBattle.begin(...).onDone`（判定 `result.playerWon`，二者本质同一条件的两种表达）——命中时均调用 `Bond.addGold(this.BORDER_WAR_WIN_BONUS)`，在战报文案中单独一行展示，不与战斗本身的经验/金币结算混同，无论主角是否亲自出征、只要己方阵营取胜就必发。
  - **行动力奇珍改由常规奇珍池承担**：此前"赤兔·千里神驹""千里靴"两件全局唯一奇珍各自额外附带 `apBonus:1` 字段。本轮移除这两件的 `apBonus`（`Armory.UNIQUE_TREASURES`/`makeUniqueTreasure`），改为在常规奇珍效果表 `CURIO_EFFECTS`/`CURIO_VALS` 新增 `apBonus`（`[1,1,1,1]`，不随稀有度浮动），并将 `Armory.TEMPLATES.curio` 中「传国玉玺」（原 `critBonus`）、「九鼎」（原 `guardBonus`）两个模板的 `effect` 改为 `apBonus`；`makeItem()` 生成宝物时若 `stat === "apBonus"` 则额外置 `item.apBonus = 1`，供 `Campaign.recalcApMax()` 沿用原有的 `Armory.itemsOf("hero").filter(i => i.apBonus).length` 计数逻辑识别。由于奇珍只有一个装备槽位，二者功能相同、无法同时叠加，行动力上限的可达途径整体从"两件全局仅一份的唯一奇珍（分属坐骑/服饰两槽，理论上限 +2）"收窄为"任意一件可反复获取的行动力奇珍（奇珍单槽，上限 +1）"——顺带修复一个关联的既有缺口：`Armory.equip`/`unequip` 此前完全不触发 `recalcApMax()`（该函数只在获得唯一奇珍/名声跨阶/武道会夺冠三个时机被动调用），意味着换装行动力奇珍并不会让上限即时生效，只能等下一次某个不相关事件顺带重算；本轮在 `equip`/`unequip` 内对 `owner === "hero"` 的情形各自补上一次 `Campaign.recalcApMax()` 调用。
  - **天赐机缘不突破六维基础上限**：`Tower.offerBuffs()` 生成三选一候选前，若 `this.rpg && RPG.char`（角色扮演模式，非小游戏自由试玩），先按 `RPG.eff(RPG.char, o.k) < 110` 过滤掉已封顶维度对应的成长类机缘（"疗养生息"不涉及维度增长恒保留），候选不足 3 项时回退为未过滤的全量 `BUFFS`（复用与 `grantHeroStatGrowth` 相同的"多数封顶时放开限制保底出选项"思路）。
  - **悬赏车轮令权重下调**：`genBounty` 非刺杀悬赏四类权重由 `40%/20%/20%/20%`（讨伐/车轮/登塔/双雄）调整为 `35%/15%/25%/25%`，车轮令反馈过于密集，压低其占比、向登塔令与双雄令匀出空间，刺杀令的独立 15% 优先判定逻辑不变。
- **行动力奇珍限定传说品阶、天赐机缘按加成量预判、武将大会择优选人、取消武道会首冠行动力**（已完成）：
  - **传国玉玺/九鼎仅传说品阶才具行动力效果**：上一轮把这两个模板的 `effect` 直接改成了 `apBonus`（任意稀有度佩戴皆 +1 行动力），本轮反馈"其余稀有度的加成可以随意"——改为给两个模板各自恢复原本的默认 `effect`（传国玉玺→`critBonus`、九鼎→`guardBonus`）并新增 `legendEffect: "apBonus"` 字段；`makeItem()` 内 curio 分支新增判断：仅当 `t.legendEffect && rarityK === "legend"` 时才用 `legendEffect` 覆盖 `stat`（`bonus` 固定为 1），其余情形维持原有的 `t.effect` 走 `curioVals` 按稀有度正常浮动。`item.apBonus = 1` 的置位判断本就基于 `stat === "apBonus"`，无需改动即自动只在传说品阶生效。用 mock `Math.random()` 序列强制 `rollRarity`/模板选取的 Playwright 测试确认：传说品阶生成的传国玉玺/九鼎 `stat` 为 `apBonus`、`apBonus:1`；其余品阶维持原效果、无 `apBonus` 字段。
  - **天赐机缘按加成量预判上限**：`Tower.offerBuffs()` 的过滤条件从"当前值是否已达 110"改为"当前值 + 该机缘固定加成量是否会超过 110"（`RPG.eff(RPG.char, o.k) + (o.k === "ti" ? 16 : 7) <= 110`）——此前的判断只挡了"已经封顶"的维度，像武力 109 这种"仅差 1 点就封顶、但机缘固定 +7 会一举冲到 116"的临界情况仍会被提供，本轮堵上这个漏洞。测试中把武力设为 109（其余维度留有余量，避免触发"候选不足回退全量"的兜底分支），确认第 5 层候选中不再出现"武艺精进"。
  - **武将大会参赛武将改评分择优**：`MapUI.runTournament(m, pool, joining)` 原先 `shuffle(others)` 随机抽取 31 名非主角参赛席位，改为 `pool.slice().sort((a,b) => ratingScore(b) - ratingScore(a))` 按总评分降序取前 31——`Tournament.draw()` 内仍会 `shuffle(this.participants)`，但那只打乱分组签位顺序，不影响已经选定的这 32 人本身。Playwright 测试独立复算"已现身武将中评分最高的 31 名"与实际赛程草稿页 `.cg-member` 展示的 31 个非主角名字逐一比对，31/31 完全吻合。
  - **取消武道会首冠行动力 +1**：`Campaign.recalcApMax()` 移除 `if (m.cupWon) cap++;` 这一项，`RPG.onCupResult` 首冠分支的战报文案同步去掉"行动力上限永久 +1"的表述；`m.cupWon` 标记本身继续保留、继续在夺冠时置位，仅不再参与行动力上限的计算，"天下无双"终局条件（八大高手 + 至少一次武道会夺冠）不受影响。测试中预先在战役状态里置 `cupWon:true` 并装备一件传说行动力奇珍，确认最终 `apMax` 只计入奇珍的 +1、不再额外计入首冠的 +1。
- **城市经营一期：置业收租 + 掌柜委任 + 易主查封**（已完成）：
  - **设计取向**：经营玩法不做独立重度内政（征税/征兵/民忠等 4X 数值会与阵营大战抢戏），而是围绕"主角走南闯北"的核心循环做**私人产业**——给玩家重返各城的理由，并与既有系统（边境战夺城、友谊满上限、铁匠铺材料）互相咬合。一期落地置业/收租/掌柜/查封/变卖五件事；经营事件（劫掠/贵客等穿插战斗的收取事件）与城市繁荣度留待二期。
  - **数据与模块**：新增 `ESTATE_TYPES`（酒馆 1200金/日35、马场 1600/48、矿山 800/18+材料、农庄 800/25、商队 2000/60 行情浮动 0.6~1.4）、`CITY_ESTATE`（40 城风味映射：西凉天水/甲斐产马、汉中/石见出云有矿、天府成都/浓尾尾张产粮、丝路长安/堺港大坂通商……对马岛无产业）与 `Estate` 模块；存档为战役层 `m.estate[cityId] = {type, lastDay, pending, manager, matCarry?, matPending?}`，`Campaign.reset`/`ensureMap` 分别负责新档初始化与旧档补建。
  - **懒结算**：收益不做每日 tick，而是 `Estate.accrue(m, cityId)` 在任何读取时机（地图渲染总览行、面板打开、收取、易主）按 `m.day - lastDay` 补记，待收上限 = 日进 × 15 天（满仓停积，逼玩家规划巡视路线）；矿山按同一天数流水另记 `matCarry`，每满 5 天转 1 份 `matPending`（上限 3 份），材料类型与本城铁匠铺专精同式（`Armory.TYPES[hashStr(cityId)%5]`）。
  - **掌柜**：候选 = 已现身 + 友谊满 300 + 不在团队 + 未在他处任职（不限阵营——给友谊满上限一个招募之外的新出口）；加成 = `min(50, round((智+政)/4))`%，取 `Armory.geared` 装备后数值；委任/更换/解任/入队卸任前一律先 `accrue` 按旧费率结清再切换。掌柜上任即 `m.assign[gid]=cityId` 常驻本城：`wanderGenerals` 云游与 `applyBorderWarOutcome` 败军撤离两处均以 `Estate.managerIds(m)` 跳过；`Bond.recruit` 成功后调用 `Estate.onRecruit(gid)` 自动卸任（队伍随主角移动，无法再驻店）。
  - **易主查封**：`Estate.sealed` 即 `cityOwnerSide !== 主角阵营`——查封中不积攒、不可收取、不可变卖，夺回自动恢复。懒结算无法自知"两次读取之间何时易主"，故 `applyBorderWarOutcome` 在改写 `m.cityOwner` **之前**先对被占城 `Estate.accrue`：占领前的天数照常入账、占领期间的读取因 sealed 分支只推进 `lastDay` 而颗粒无收、夺回后从夺回时点续积，三段账目互不污染。附带效果：己方新攻占的敌城随即可置业，边境战的切身利益进一步加重。
  - **UI**：城池功能区铁匠铺与特色设施之间新增「产业」按钮，三态（置办[含敌占禁购置灰]/待收金额/⛔查封）；点开 overlay 面板承载置办确认、收取、委任/更换/解任掌柜（候选列表按加成降序、显示智政与加成百分比）、变卖（回收置办价五成+全部待收）;地图左栏角色卡下方新增「🏪 名下产业 N 处 · 待收 X 金」总览行（渲染时顺带全量懒结算）。全部复用既有 `result-card`/`menu-btn`/`mc-sect` 样式，未新增 CSS。
  - **验证**：Playwright 27 项断言一次全过——置办扣款/扣行动力/建档、3 天待收 105、收取入账清零、100 天只积 15 天上限 525、掌柜加成公式与候选列表自洽、委任后费率生效且常驻本城、查封三禁（积攒/收取/变卖）与夺回恢复、变卖回收额与档案删除、矿山 12 天 216 金+2 材料入库、商队收取浮动落在 [360,840]、敌占京都禁购、总览行计数。
- **一键拜访 + 掌柜代收 + 城市经营二期（经营事件与繁荣度）**（已完成）：
  - **一键拜访**：本地武将区在「全部武将」上方新增同风格大按钮「🚶 一键拜访」（`MapUI.oneClickVisit`）——一次访遍当前城池所有「已现身 + 己方阵营 + 友谊未满 300 + 今日未访」的武将，逐一写 `visitDay` 并 `Bond.addF(randInt(1,2))`，与逐个拜访完全同规则同额度（纯省点击、无额外收益、不耗行动力），汇总一条 toast（超 8 人截断为「等 N 位」）；当日再点只提示无人可访、友谊分毫不动。
  - **掌柜代收金币**：`Estate.accrue` 分流——有掌柜的产业金币不再进 `pending` 待收，而是按天数直接 `Bond.addGold` 入账（商队行情浮动对整段天数一次判定），累计代收额记入 `est.banked` 供产业面板展示（「掌柜逐日代收金币直接入账（累计已代收 X 金）」）；代收不受 15 天积攒上限约束（掌柜天天清账，本就不会积压）、也不触发经营事件；矿山材料明确不在代收之列，仍走 `matPending`（上限 3 份）等主角亲取。产业按钮小字相应改为多段拼接：掌柜代收中·日进 X 金 / 待收 Y 金 / 材料 Z 份。
  - **繁荣度**：新增 `Prosper` 模块（`m.prosper[cityId]={lv,pts}`，懒取初始档：洛阳/许昌/成都/建业/京都/大坂/尾张/骏府 3 星、白帝城/上庸/奥州/肥后/常陆 1 星、其余 2 星）。繁荣点来源四类：设施挑战获胜 +1（duel/duo 结算、车轮全清、阵营/组队/国战三处 `facilityBonus` 分支）、完成悬赏 +1（`completeBountyReward`）、置办产业 +2（`Estate.buy`）、己方边境战夺城/光复 +2（`applyBorderWarOutcome` 内仅 `winnerSide===主角阵营` 时）。每满 5 点升 1 星（toast 即时相告），5 星封顶。影响三处：`Estate.dailyRate` 乘 `×(0.8+0.1×lv)`、`cityMarketStalls` 摊位数 ≥4 星 +1（与名声第 1 阶的 4→5 叠加）、`openForge` 专精工钱 ≥4 星 ×0.7（与名声第 7 阶的 30→20 叠加，四舍五入）。城池面板抬头城名旁以金色 ★ 实时显示星级。
  - **经营事件（仅亲自收取触发）**：`MapUI.collectEstate` 取代直连 `Estate.collect`，单次 `Math.random()` 按固定区间判定：[0,0.12) 强人劫掠（农庄免疫，命中视同无事件）、[0.12,0.22) 大市（`Estate.collect` 新增 `goldMult` 参数传 2 翻倍）、[0.22,0.32) 贵客临门（酒馆特有，随机己方已现身武将友谊 +10 按实际增量）。劫掠流程：`Estate.seizeForRaid` 先把待收连同材料清账"押走"（商队行情此时判定），弹窗二选一——「应战」置 `m.activeEstateRaid={cityId,gold,mats}` 后 `startClassicBattle` 打真实经典单挑（对手 `makeRaider()`：五个浪人名随机、六维贴主角 −12~+8 浮动夹在 40~110、id=-77 不入图鉴不产友谊），结算在 `RPG.onBattleEnd` 新增通道：胜 = 追回 2 倍 + 名声 +5、败 = 保住 50%；「破财消灾」= 保住 70%。矿山材料不为强人所图，三种结局（胜/败/破财）均全额 `Estate.deliverMats` 送回。`Campaign.reset`/`ensureMap` 补 `prosper:{}` 与 `activeEstateRaid:null` 字段。
  - **验证**：Playwright 13 项断言全过——洛阳初始 ★★★ 与 4 星后 ★★★★ 显示、一键拜访 N 人各 +1~2 且当日重复无效、置办 +2 繁荣点、无掌柜日进 39（35×1.1）、掌柜代收 3 天自动入账且 pending 恒 0 banked 精确、4 星集市摊位 4→5、4 星铁匠铺专精工钱 21 金、mock 随机数窗口法强制触发大市（+200）/劫掠破财（+70）/劫掠应战（实战获胜 +215 含历练金15，`activeEstateRaid` 清除）/贵客临门（+100 金且随机武将友谊 +10）。
- **悬赏轮换、登塔令奖赏加码、顶栏图标按钮与全部城市总览**（已完成）：
  - **悬赏轮换**：`camp()` 在武将云游判定后新增约 20% 概率的静默轮换——随机挑一座有悬赏的城、随机一条槽位以 `genBounty` 换新，各城条数不变；已接取悬赏的结算走 `m.activeBounty` 快照，原槽位被轮换也不影响奖励发放（找不到原 uid 时仅跳过"完成槽位刷新"）。
  - **登塔令奖赏按层数二次方加码**：`genBounty` tower 分支 `rewardGold: 50+N×10+N²×2`、`rewardFame: N+round(N²/4)`（原 55+N×10 / 6+N），5 层 150金/11名声 → 20 层 1050金/120名声——20 层高级登塔令难度极高，奖赏从原本与 12 层普通令几乎无差（255金/35名声）大幅拉开。
  - **顶栏图标按钮**：「全部武将」从地图左栏的 cup-go 大按钮迁至顶部标题栏音乐按钮左侧，图标改为切合按钮名的 👥（原来用的 🌐 与"武将"无关联）；其左新增「🏙️ 全部城市」。两枚按钮直接放进 `.audio-btns` 容器复用现成的圆形图标按钮样式，`MapUI.bind` 每次渲染重绑（顶栏为静态 HTML，幂等无副作用）。左栏仅保留「🚶 一键拜访」。
  - **全部城市总览（`AllCityUI` + `screen-allcity`）**：与全部武将同风格的只读 `db-table` 表格页，41 城逐行列出：城市名（按归属方着色、当前所在城 📍 标记）、归属阵营、繁荣星（金色 ★）、特色设施名、铁匠专精、产业状态（未置办/待收X/代收中/查封/—）、已现身/驻城武将数、悬赏条数；城市/归属/繁荣/武将/悬赏五列可排序（复用 AllGenUI 的 sortBy 模式）。行点击弹出该城详情卡：归属、设施、专精、集市行情（复用 `cityPriceFactor` 文案）、产业细况（含掌柜与累计代收）、本地已现身武将名单（按阵营着色）、悬赏列表、相邻城池。返回键与硬件返回均固定回天下地图（`handleBack` 补 `screen-allcity` 分支）。
  - **验证**：Playwright 9 项断言全过——18 条登塔令样本全量吻合新公式且 20 层样本恰为 1050/120、25 天宿营后 4 条悬赏换新且各城条数不变、顶栏按钮顺序 🏙️→👥→♪→🔊 且左栏旧按钮移除/一键拜访保留、👥 打开全部武将、🏙️ 打开 41 行总览表且表头齐全、按繁荣降序首行 ★★★、行点击详情卡内容齐全、返回回到地图。
- **登塔令层数固定四档、悬赏轮换概率大幅提高**（已完成）：
  - **登塔令层数**：`genBounty` tower 分支的层数生成由 `legendary ? 20 : [5,8,12][randInt]` 改为不区分 legendary 标记、统一 `[5,10,15,20][randInt(0,3)]` 等概率随机——上一轮新增的二次方奖励公式（`50+N×10+N²×2` 金 / `N+round(N²/4)` 名声）原样保留不变，只是层数取值收敛为这四个整齐档位（5层150金/11名声 · 10层350/34 · 15层650/71 · 20层1050/120），不再出现 8/12 层这类中间值。`legendary` 标记本身仍保留（继续影响千里靴掉落判定等其他逻辑），只是不再驱动登塔令的层数选择。
  - **悬赏轮换概率提高**：`camp()` 内的轮换逻辑从「整局宿营约 20% 概率仅挑一城一条」改为「遍历每座城的每一条悬赏，各自独立掷一次 50% 概率，命中则以 `genBounty` 换新」——一次宿营后榜单近半数悬赏面貌一新，刷新力度远高于此前"多次宿营才轮到一条"的频率。已接取中的悬赏仍走 `m.activeBounty` 独立快照结算，不受轮换影响。
  - **验证**：Playwright 3 项断言全过——15 次宿营后 29 条登塔令样本层数均落在 {5,10,15,20} 且奖励公式逐条吻合；6 轮宿营共 606 条悬赏槽位样本中 296 条发生变化，实测比例 48.8%，精确贴合 50% 独立轮换预期。
- **城建捐修、产业升级链、守将委任与敌境自营（城市经营三·四期）**（已完成）：
  - **城建捐修（`BUILD_TYPES`/`Buildings`，存 `m.builds[cityId][type]=lv`）**：除对马岛外每城开放 3 种公共建筑——五选三按 `hashStr(cityId+"|"+type)` 排序稳定选取（同城每局相同），各 1~3 级；修至 1/2/3 级花 500/1000/2000 金 + 0/1/2 份本城铁匠铺专精材料，不耗行动力且每级 +2 繁荣点。效果：🏥医馆 = 回魂丹 100→80/65/50（`Buildings.reviveCost` 接入 `maybeRevive`，仅当前所在城归属己方）；📚书院 = 本城单挑经验 ×1.1/1.2/1.3（`onBattleEnd` 在入账前乘 `expMult`，战报加注一行）；🏇驿站 = 两端均为己方且建有驿站的城市间快马直达，1⚡ + BFS 最短跳数×60 金驿费（本城驿站 2/3 级再 −25%/−50%），无奇遇无风浪、团队随行；🏯城墙 = 边境战报模拟中本城一方全员六维 +2/+4/+6（攻守对称，敌占期间为敌所用）；⚔️演武场 = 本城设施获胜名声额外 +4/+8/+12（`drillBonus` 接入 duel/duo、车轮全清、阵营/组队/国战五处结算）。建筑归属跟随城池：敌占期间面板只读（城墙照常为敌方生效），夺回原级保留。
  - **产业升级链**：`m.estate[cityId]` 新增 `lv`（缺省视为 1），各类型 `lvN` 三段名（酒馆→酒楼→天下名楼等）；扩建花置办价 ×1.25（升 2 级）/×2（升 3 级）且城市繁荣须达 ★★/★★★，收益倍率 ×1/×1.6/×2.4 乘入 `dailyRate`；矿山按级出材 5/4/3 天一份、待收上限 3/4/5；变卖与接管计价基础统一改为**累计造价**（`cumCost`）——变卖回收 = 累计造价五成 + 待收。
  - **守将委任（`Guard`，存 `m.guards`/`m.captives`）**：己方城市可委任友谊满 300、不在团队且未任掌柜的**己方**武将驻守（掌柜候选与守将候选互斥）；守将 `m.assign` 钉在本城、`wanderGenerals` 跳过；边境战报模拟中必上阵（置于本方阵前）且六维 +3（与城墙叠加，`gearBuff` 于 `Armory.geared` 之后加注）；亲征时若战线上己方城池有守将则必编入主角队伍。**城破被俘**：`applyBorderWarOutcome` 夺城后调 `Guard.capture`——守将从 `m.appeared` 暂时消失（囹圄之中不可拜访/上阵/委任）、记入 `m.captives[gid]={cityId,day}`；败军撤离过滤同时跳过俘虏。营救三途：亲至关押之城付赎金（评分×5 金）、劫牢营救（1⚡ 经典单挑 vs `makeJailer` 狱吏[-8~+10 浮动]，走 `m.activeRescue` 通道：胜 = 放还 + 名声 +10 + 友谊 +20，败 = 留待再来）、己方夺回该城自动放人（`Guard.heldAt`→`free`）。入队自动卸任（`Guard.onRecruit`）。
  - **敌境自营（`AIDev.tick`，每次宿营调用）**：敌方治下城市随机抽 2 座，各 60% 概率发生一次发展动作——45% 繁荣 +1 点（`Prosper.add` 新增 silent 参数静默积攒，升星才播报）、30% 修筑/升级城建（等级上限 3，与玩家共用 `m.builds`，夺城即归玩家所用）、25% 兴办/扩建**敌营产业**（`m.npcEstate[cityId]={lv}`，与玩家产业互斥占位，玩家旧产业查封中的城不另起炉灶）。动向拼入宿营 toast（「敌境风闻：大坂修筑城墙至 2 级」），繁荣/建筑/产业升级节奏刻意放缓、约每次宿营 1.2 个动作。**敌产接管**：己方夺城后产业面板/产业按钮转为「接管」——按累计造价五成收归己有且等级保留（耗 1⚡ + 2 繁荣点），未接管期间闲置，敌方夺回则复归敌营。
  - **UI**：城池功能区产业按钮之后新增「🏗️ 城建」按钮（小字列已建建筑与等级，敌占提示只可远观）与条件显示的「⛓️ 牢狱营救」按钮；城建面板承载三建筑捐修行（含下一级效果预览与费用）、守将区（委任/更换/解任 + 城破被俘警示）与「🏇 驿站快马」入口（目的地按驿费升序）；产业面板新增扩建按钮（含所需繁荣提示）与敌产接管确认页；全部城市总览的行内产业列新增「敌营/可接管」态、详情卡新增「🏗️ 城建」「🛡️ 守将（含 ⛓️ 狱中名单）」两行。`Campaign.reset`/`ensureMap` 补 `builds/npcEstate/guards/captives/activeRescue` 五字段。
  - **验证**：Playwright 11+1 项断言全过——城建面板 3 行、捐修 1 级入档且繁荣 +2、守将委任入档、产业扩建 2 级、mock 随机窗口强制敌境自营两次城建动作（肥后演武场 2 级）、被俘武将牢狱按钮显示、付赎金放还回名录、驿站快马洛阳→成都 3 跳 180 金且当城切换、敌产接管等级保留且 `npcEstate` 清除、全部城市详情含城建/守将行；另以随机窗口法强制边境战选中建业-对马边（城墙 3 级 + 守将上阵）模拟结算无异常。回归：第十六轮 13 项、第十七轮追加 3 项全过。
- **城市经营 UI 排版修复五项**（已完成）：
  - **弹窗内嵌 `.menu` 越界导致横向滚动**：根因是 `.menu` 基类硬编码 `width: min(470px, 94vw)`，而承载它的 `.detail-card`/`.result-card` 只有 `width: min(420px, 92vw)`——嵌套在弹窗里的候选列表（驿站目的地、委任掌柜/守将候选）子元素比父卡片更宽，撑出横向滚动。新增 `.detail-card .menu, .result-card .menu { width: 100%; }` 收敛为跟随父卡片宽度；驿站目的地列表另加 `.post-dest-list { grid-template-columns: 1fr; }` 改单列，彻底不再横向拥挤。
  - **产业面板按钮"纵向挤压"**：`.result-card .btns` 原是无换行的 flex 行，5、6 个按钮（收取/扩建/委任/解任/变卖/离开）被迫等分挤进一行，长文案的按钮（如「扩建为『酒楼』（1500 金·收益×1.6）」）文字被压成逐字纵向折行。改 `.result-card .btns { flex-wrap: wrap; }` + 按钮 `flex: 1 1 calc(50% - 5px); min-width: 110px;`——2 个按钮时行为不变（各占 50%），单个按钮时因 flex-grow 仍撑满整行，多按钮时自然按 2 个一行分行摆布，长文案有了合理宽度可正常换行 1~2 行而非逐字断行。
  - **产业按钮去掉"产业"字样**：`estateBtnHtml` 已置办分支的标签从 `` `${Estate.lvName(...)}产业` `` 简化为 `` `${Estate.lvName(...)}` ``——按钮直接显示「酒馆」「酒楼」「天下名楼」等本名，未置办/接管分支本就没有此后缀，无需改动。
  - **地图点击已通驿远城直接确认驿传**：`moveTo(id)` 原逻辑只要目标不在 `adjCities` 中就一律报「距离太远」；改为先查 `Buildings.postDests(m)`（当前城已建驿站、目标城己方且也建有驿站）里是否含该城，命中则调新增的 `confirmPostTravel(m, dest)` 弹出确认框（驿费、行动力、余额一览），确认后复用既有 `postTravel` 完成移动；未命中才照旧报错。与既有「🏇 驿站快马」按钮共用 `postTravel`，行为完全一致，只是多了一条从地图直接触发的入口。
  - **城池功能区分行重排**：不耗行动力的一组（集市/铁匠铺/产业/城建/守将/驿站快马/牢狱营救，后二者独立抽出）合并进新的 `.map-menu-free`（`grid-template-columns: repeat(3,1fr)`）三列容器置顶；耗行动力的历练、特色设施与宿营移入下方 `.map-menu-ap`（同样三列），宿营天然排在最后一位即历练/设施的右侧。守将委任与驿站快马原先内嵌在「城建」弹窗底部，现拆成独立的 `guardBtnHtml`/`postBtnHtml` 按钮与 `openGuard()`/`openPostTravel()` 独立入口（`openGuard` 内部仍复用既有 `openGuardPicker`，返回目标从 `openBuild()` 改为 `openGuard()`）；`openBuild()` 相应瘦身为只承载三项建筑捐修行。
  - **验证**：Playwright 14 项断言全过——free 行 3 列且含 ≥3 个免行动力按钮、ap 行历练在前宿营收尾、产业按钮标签不含"产业"二字、产业弹窗按钮占卡片宽度 >35%（不再逐字纵向）且无横向滚动、驿站快马已独立成城池按钮且弹窗单列无横向滚动、守将委任独立按钮且城建面板内不再含守将入口、点击已通驿远城（洛阳→成都）弹确认框成功走驿传扣费到账、未通驿远城（洛阳→京都）仍报「距离太远」。回归：第十八轮 11 项、边境战报模拟冒烟、第十七轮追加 3 项、第十六轮 13 项全过。
- **边境战亲历判定与可视化、医馆扩效、宿敌系统等六项（第二十轮）**（已完成）：
  - **全部城市「日进」列**：`AllCityUI.rowData` 新增 `dailyGold`/`dailyTxt`——已置办产业按 `Estate.dailyRate`（含等级/掌柜/繁荣倍率）、敌营产业按新增的 `Estate.npcDailyRate`（无掌柜加成）标注「（敌）」、未置办显示基础费率为「潜力N」、查封显示「查封」；表头 `th("dailyGold","日进")` 可排序。
  - **边境战重写（`resolveBorderWar` 签名去掉 joining 参数）**：`openBorderWarPicker` 不再弹「亲征/不亲征」二选一，只留「开战」；是否亲历由 `m.curCity` 自动判定——恰为交战两城之一且归属己方即 `heroForced`（主角+队友经 `War.start` 既有强制上阵逻辑排本方阵前），否则主角 `heroGeneral()` 混入本方候选池 shuffle 随机抽取。无论亲历与否一律 `showScreen("war") + War.start(hero?, {customRoster, onDone})` 以阵营大战快捷模式**可视化演出**（守将置前+3、城墙 ±2×lv 照旧折算进 roster，主角 id=-1 不叠加）；轮到主角/队友时 War 既有的「亲自应战/自动观战」询问照常。战报按亲历与否两版：个人战果（heroKills×15 经验等）vs 简报+犒赏；结束后 `War.rpg=true` 强制置位，保证硬件返回回天下地图（hero=null 时 War.start 会误置 false）。
  - **医馆扩效**：新增 `Buildings.hpRegenBonus(m)`（等级×2，注入 `RPG.heroGeneral()` 返回对象的 `regenBonus`，与奇珍「气血回复」同通道在 engine.js `endTurn` 叠加结算）与 `Buildings.troopHealBonus(m)`（等级×3%，`TeamBattle.doScheme` 中己方 rally/reinforce 命中后按 `maxTroops` 比例追加，仅 rpg 模式，日志加注「🏥 医馆额外 +N」）；`BUILD_TYPES.hospital` 的 desc/eff 三档文案同步改写。
  - **对马岛第二条战国航线**：ROADS 新增 `["tsushima","higo"]`——两侧各两港（建业/徐州 ↔ 对马 ↔ 萨摩/肥后），BFS 驿费、渡海风暴、边境战边候选自然全部生效，无需其他改动。
  - **城池功能区再排**：`.map-menu-free/.map-menu-ap` 3 列改 2 列；「历练」按钮从 ap 行移除（`RPG.train` 函数与「再历练」链路保留，但地图无入口——练级动线归拢到切磋/悬赏/设施/边境战）；ap 行现为 [特色设施]→宿营。
  - **宿敌系统（`Nemesis` 模块，存 `m.nemesis={id,wins,nemesisWins,ambush,finaleDone}` + `m.activeNemesis`/`m.nemesisChallenge`）**：结缘双通道——`onBattleEnd` 主角首败于敌方阵营真实武将（id≥0）即指定，或名声达「声名初显」后每次宿营 8% 兜底随缘指定；战力 `buffedOpponent` = 真实数值 + 全维 `round((主角Lv-1)×1.5)+wins×4`（仅本场，不写 statGrowth）；宿营寻衅 15% 三选一（拦路标记→`moveTo` 强制应战 / 抢先夺走本城一条悬赏 / 踏营下战书→`checkCampChallenge` 接管宿营流程尾段）；对决走 `m.activeNemesis` 结算通道——胜 +20 名声、80+wins×20 金，满 3 胜「恩怨了结」再 +80 名声与 `guaranteedItem("legend")`，`finaleDone` 后停止寻衅；地图左栏角色卡下新增宿敌卡（战绩/进度/下战书按钮，耗 1⚡）。`Campaign.reset`/`ensureMap` 补三字段。
  - **验证**：Playwright 8+12 项断言全过——日进列存在且可排序、对马→肥后直达（避风暴窗口）、历练按钮移除、free/ap 两行均 2 列、医馆新描述、边境战亲历（建业）进 War 界面且战报含个人战果、不亲历（洛阳）同样进 War 界面且战报为简报版、战后回地图、极弱主角刺杀必败自动结缘宿敌（id 精确匹配）、2 胜后下战书第 3 胜触发恩怨了结（finaleDone/wins=3、传说宝物入库）。回归：第十九轮 14 项（2 项断言按新规范更新）、第十八轮 11 项全过（18b 边境冒烟已被本轮 20b 取代）。
- **边境战组队化、宿营夜报、宿敌降幅与弹窗防误触等五项（第二十一轮）**（已完成）：
  - **城池按钮对换**：free 行顺序调整为 铁匠铺→集市→城建→产业→守将→驿站快马→牢狱营救（集市/铁匠铺对换、产业/城建对换，纯模板行序调整）。
  - **边境战改组队大战（`resolveBorderWar` 再重写，替代上一轮的 War 快捷模式）**：名单构建/守将+3/城墙±2×lv/亲历判定全部沿用，但每方收紧至 10 将上限，改经 `TeamBattle.begin(myRoster, heroSide, { exact, enemies, rpg, observe, onDone })` 开打——亲历时主角+队友手动排到本方阵前（原先靠 `War.start` 的强制上阵逻辑，TeamBattle 无此逻辑故在 roster 构建时自排）；未亲历时主角混入候选池，**被抽中即为可亲自指挥的组队大战**，未抽中则走新增的 `opts.observe`（`begin` 内 `delegated` 开局即置真，己方全程 AI 代打、AI 挑唆单挑以旁观模式自动演出）。战报个人战果字段改用 TeamBattle 回传的 `{playerWon, mySurvivors, kills}`（`kills` 为己方总歼敌数，经验 kills×12、胜 30+kills×4 金）；夺城结算 `applyBorderWarOutcome` 原样复用。上一轮的 `War.rpg=true` 补丁与 `showScreen("war")` 随 War 路径退役（`customRoster` 保留为 War 的通用接口，暂无内置玩法使用）。
  - **宿营夜报**：`camp()` 尾段有附加消息（敌境风闻/宿敌风闻）时不再挤进一条 toast，改弹「🏕️ 宿营夜报」卡片逐条列出（非 modal——点遮罩任意处或「知道了」即关）；无附加消息仍走 toast。
  - **宿敌降幅**：`buffedOpponent` 加成由 `round((Lv-1)×1.5)+wins×4` 降为 `round((Lv-1)×0.6)+wins×2`，另设 25 上限。
  - **弹窗 modal 机制（修复单挑卡死）**：病灶——`#overlay` 的遮罩点击监听无条件 `closeOverlay()`，而「回魂丹/认输」确认框是战斗循环 `await` 的 Promise（`maybeRevive`），误触遮罩把按钮连同 Promise 唯一出路一起销毁，战斗永久挂死只能杀 APP。`openOverlay(html, {modal:true})` 新增第二参数，modal 弹窗遮罩点击不关闭；已 modal 化：回魂丹确认、War「亲自应战/自动观战」询问（同为 await 的 Promise）、TeamBattle onDone 战果卡（漏点会丢夺城结算回调）、世界杯竞猜表、宿营夜袭、宿敌拦路/踏营应战、边境战事开战与战报。
  - **验证**：Playwright 22 项断言全过——按钮两组对换生效、宿营夜报弹窗出现且点击屏幕消失、宿敌公式源码校验、极弱主角倒地弹回魂丹后点遮罩弹窗仍在（modal 防误触）且认输正常结算（对方记 1 胜）、边境战事弹窗文案改「组队大战」、亲历（建业）进 teamwar 界面且主角在阵中出现行动面板、委托 AI 打完后战果卡 modal 防误触、战报含个人战果与「返回天下地图」、城池归属易主、不亲历（洛阳，己方 14 人池 r=0 洗牌主角必落选）进 teamwar 观战模式（无行动面板、AI 自动演完）、战报为「知道了」简报版、战后回地图。回归：test20b 12 项（边境战 A/B 断言按组队化更新）、test20 8 项、test19 14 项、test18 11 项全过。
- **掌柜代收材料、宿敌更替、敌将武备自集与单挑夺宝等四项（第二十二轮）**（已完成）：
  - **掌柜代收矿山材料**：`Estate.accrue` 矿山分支尾部——有掌柜时当期折算出的材料份数立即 `deliverMats` 入库并计入新字段 `est.matBanked`、`matPending` 清零（不再受待收上限约束）；无掌柜路径原样。产业面板/委任提示文案同步改写。
  - **宿敌恩怨已了即隐卡、新宿怨可再结**：`nemesisHtml` 对 `finaleDone` 提前返回空（「重温旧怨」入口随卡片一并退役，相关死分支清理）；`onHeroLoss` 放行「旧怨已了且败给另一位敌将」的重新结怨（全新战绩对象）；`ensureFallback` 同样放行并排除旧宿敌本人。
  - **敌将武备自集（新 `AIGear` 模块，宿营钩子插在 AIDev 之后）**：敌方已现身武将抽 2 人、各 25% 概率 `makeItem`（类型均匀、稀有度按掉落权重）→ 新增通用的 `Armory.npcAutoEquip(gid,item)` 择优穿戴（比同槽现役强才穿；换下旧装弃毁、`Armory.isUnique` 判定的唯一奇珍退回玩家库；不如现役则整件弃之不取）；「⚔️ 武备风闻」并入宿营夜报 extras。
  - **单挑夺宝与赎回（`RPG.onBattleEnd` 尾段重构）**：新增 `hostileDuel` 判定（`opp.id≥0 && opp.side≠主角`）——主角胜 35% 随机夺取败方随身宝物直接入库（`equippedBy=null`）；主角败 25% 被夺（夺宝池排除唯一奇珍），战报弹窗前先弹 modal 赎回框：付市价五成（新 `Armory.LOOT_PRICE` 500/1500/3000/5000）当场赎回，或拒绝 → `npcAutoEquip` 决定敌将穿走（可再夺回）还是销毁遗失，随手 `recalcApMax` 防行动力奇珍被夺后上限悬空。原战报 openOverlay 收进 `showResult()` 闭包供两条路径复用。
  - **验证**：Playwright 14 项断言全过——矿山 10 天掌柜代收 2 份材料入库且 matBanked/matPending 正确、宿营夜报含武备风闻且宝物库出现敌将随身宝物（equippedBy 为武将 id）、恩怨已了宿敌卡隐藏、败于新敌将结新怨（id 更替、战绩清零）且卡片复现、主角胜夺得敌将「青釭剑」入库（equippedBy 置空）、主角败弹「宝物被夺」赎回框（modal 防误触）、赎回花 500 金原物归位、拒绝赎回宝物转为敌将随身穿戴。回归：test21 22 项、test20b 12 项、test20 8 项、test19 14 项、test18 11 项全过。
- **夺宝概率对齐、赎金上调与全员成长两项（第二十三轮）**（已完成）：
  - **单挑夺宝调参**：落败被夺 25%→35%（与获胜侧对齐）；`Armory.LOOT_PRICE` 由市价五成（500/1500/3000/5000）改市价一倍二（1200/3600/7200/12000）。test22 相应断言同步更新（赎金 500→1200）。
  - **全员成长（新 `Growth` 模块，用户拍板 B+A、去累计上限）**：`bump(m,gid,dim)` 统一入口——校验「基础值+statGrowth ≥ 110 则不涨」（单项不带宝物不破 110，累计不设额外上限），写战役层 `m.statGrowth`；`tick`（宿营岁月修行）随机抽 3 人、概率 `clamp((750−评分)/500, 0.1, 0.5)` 评分越低越易成长、随机一维 +1、按维度配动词生成「📈 修行风闻」并入宿营夜报；`battle`（历战成长）胜 12%/败 6% 武力/统帅/体力随机 +1，挂 `RPG.onBattleEnd`（单挑对手，战报「历战砥砺」行）与 `TeamBattle.finish`（战役内组队大战全员遍历 cn+jp 单位，日志「历战磨砺：N 位」）。
  - **验证**：Playwright 10 项断言全过——宿营夜报含修行风闻且被抽中弱将 statGrowth.ti=3（r=0.1 三抽同一人）、单挑落败方历战砥砺武力 +1（战报行+存档）、预置 wu 顶 110 后历战不再上涨（封顶校验）、r=0.3 触发新 35% 被夺概率（旧 25% 下不会命中）、赎金显示/扣款均为 1200 金且原物归位、TeamBattle 挂钩与 CAP 校验源码断言。回归：test22 14 项（赎金断言更新）、test21 22 项、test20b 12 项、test20 8 项、test19 14 项、test18 11 项全过。
- **野战演武小游戏（两军对圆·斗将破阵，第二十四轮）**（已完成）：
  - **背景**：用户要求把「双方大战」重做为全游戏最精彩的部分且不复用阵营/组队大战玩法，先以小游戏试水（不接战役）。方案历经三稿拍板为「A 演义流骨架（排兵→斗将→破阵→追击）+ B 推线演出层（实时红蓝兵潮）」。
  - **实现（新 `FieldBattle` 模块 + `#screen-field` + `#fb-content` 动态四相 UI + 全套 fb-* CSS）**：排兵布阵（10 将自动填五位、点两 chip 互换、四阵形卡（active 高亮）、随机地形横幅、斥候揭敌阵耗军令）；阵前斗将（敌最强武力者搦战，点将弹窗 modal，`startTeamDuel` 新增 `opts.backScreen` 使中途退出回野战界面而非组队界面；胜负±12 士气（河畔×1.5）、连斩三将 -15、败者入 `dead` 集）；挥军破阵（600ms setInterval + `gen` 代际防串场，四线 meter 0~100、DOM 增量更新（`updateClashDom` 只改宽度/数字避免打断点击）、军令五式（擂鼓/鸣金/后备/火攻/奇袭）走「点军令再点战线」的 orderMode 交互、敌军 AI 每 8 刻投后备或擂鼓、破线邻线连锁 ±8、逾 100 刻决胜时刻烈度×2 保证必然终局）；追亡逐北（穷追 60/40 对赌 or 收兵）+ 战果卡（斗将/破阵/阵亡/士气四行统计，再战/回菜单）。
  - **接线**：小游戏菜单新增 `data-go="field"` 入口（组队大战之后）；`handleBackAction` 新增 field 分支（`FieldBattle.abort()` 清定时器+代际递增后回首页）；BGM 沿用战术曲；纯自由玩法零存档读写。
  - **验证**：Playwright 16 项断言全过——入口/五位十将四阵形/地形横幅、阵形切换高亮、两将对调、斥候揭阵且军令 4→3、斗将三按钮、点将弹窗 10 人且 modal 防误触、避战士气下降、四线战场+军令台（余 3）、擂鼓选线消耗军令且入战报、整场自然打完（含决胜时刻）弹战果统计、返回首页、二局出将应战进单挑引擎（标题「阵前斗将」）打完回野战界面、战斗中硬件返回中止回首页无卡死；另截图目检排兵/破阵两屏布局。回归：test23 10 项、test22 14 项、test21 22 项、test20b 12 项、test20 8 项（一次海路奇遇窗口抖动复跑即过，非本轮回归）、test19 14 项、test18 11 项全过。
- **野战演武五项打磨（第二十五轮）**（已完成，全部来自用户试玩反馈）：
  - 按钮统一金边风格（阵形卡/武将 chip/军令钮与 cup-go 同系）；排兵改 3×3 阵位网格（前锋上、左右翼分列、中军居中、后路殿后，「后备」更名「后路」，grid-template-areas 实现）。
  - 斗将：`usedDuelists` 集合保证三阵各遣一将（胜者/中退者均不得再出，点将名单剔除+无将可遣兜底提示）；新增 `controllable(g)`（`RPG.char && Bond.data.team.includes(id)`）——仅团队成员可亲自操控，其余 `spectate:true` 自动接战，名单逐一标注；斗将阶段士气保底 5、删除提前终局分支，破阵正戏必打。
  - 破阵：五线（后路参战，`LANES` 加 reserve）；开打前点选主攻线（+12%、🚩）→ 手动「击鼓总攻」（`beginAssault`）才启动 tick，敌军随机暗定主攻线（⚠️、同 +12%）；「投入后备」军令与敌 AI 后备逻辑退役（军令四式），奇袭改遣后路智将（失手阵亡直接削弱后路线）；溃线邻接表补 center↔reserve；终局改 3 破线 / 士气归零 / 五线俱了比线数。
  - **验证**：test24 扩至 21 项全过——新增 grid-area 五区断言、金边色断言、主攻选线→🚩→总攻启用、军令台无「后备」钮、五线可点、第二阵点将名单 9 人（排除已出战者）；截图目检新阵位布局。回归：test23 10 项、test22 14 项、test21 22 项全过。
- **野战演武四项体验打磨（第二十六轮）**（已完成，全部来自用户试玩反馈）：
  - 按钮再统一：`fb-go`/`fb-fight`/`fb-decline`/`fb-allout`/`fb-assault` 改 `cup-go`（主操作叠加 `primary`），单钮行走 `cup-start-btns`（`flex:1` 撑满整行，双手皆可点按），三钮行新增 `.fb-btnrow`（`flex-wrap` 防挤压）。
  - 士气条由纵向堆叠改左右并排（`.fb-morales` flex row），敌方一侧 `row-reverse` 镜像、血条锚定外侧向中央递减，视觉上对齐单挑血条的两端对垒式布局。
  - 新增 `usedFoeDuelists` 集合，`challenger()` 排除其中记录过的敌将 id、`renderDuel()` 选定挑战者即计入——阵前斗将双方均须各阵不同将，敌方也不例外。
  - 总攻节奏放缓：`tick` 间隔 600ms→1100ms，推移系数 8→5、随机扰动 ±1.4→±1，决胜时刻阈值 100 刻→60 刻但倍率 2×→1.6×；擂鼓/鸣金持续 6 秒→9 秒，敌军 AI 擂鼓判定周期与增益时长同步拉长，给玩家留出观战下令的余裕。
  - **验证**：test24 扩至 26 项全过——新增按钮 class 断言（fb-go/斗将三钮/fb-assault 均含 `cup-go` 且主操作含 `primary`）、士气条 `flexDirection` row/row-reverse 断言、敌方两阵挑战武将姓名不同断言、总攻开战瞬间战线未被首刻推动（首刻延后验证节奏放缓）。回归：test23 10 项、test22 14 项、test21 22 项全过。
- **野战演武：智谋军令、计谋与兵力体系（第二十七轮）**（已完成，全部来自用户试玩反馈）：
  - 军令数改由全军智谋折算（`calcOrders`：均值 (−30)/12 取整、clamp 2~6），我/敌各自独立（`orders`/`foeOrders`），排兵界面明示双方道数。
  - 移除「全军掩杀（跳过斗将）」按钮，阵前斗将只留应战/避战两选；无将可遣提示同步改文。
  - 士气条弃双色方案，双方同走 `hpColor(值/100)` 按数值变色（绿/黄/红，与单挑体力条同语义），`moraleBarHtml`/`updateClashDom` 双处内联背景色。
  - 兵力体系：`myTroops/foeTroops` = Σ统帅×100，士气区下方新增 `.fb-troopline` 全程实时显示；破阵每刻按对方线战力互耗、破线额外折损 1,500~3,000（战报记斩获/折兵）、兵力比反哺战力（0.7+0.3×存兵比）、兵力耗尽为新增终局条件；战果卡加「余兵」行。
  - 敌军军令+计谋 AI：每 5 刻 40% 概率耗 1 敌令（军令台显示「敌令余 N」），半数擂鼓/半数施计（择智谋差最大线）；计谋公式 `clamp(0.45+(智差)/150, 0.15, 0.85)`，成则线 ±20、对方士气 −8，败则自损 −5；我方军令台同列新增「🧠 计谋」（同公式，战报明示两侧智谋和）。
  - **验证**：test24 扩至 30 项全过——新增军令数 2~6 断言、掩杀钮移除断言、双条同色（50 均为中段黄）断言、兵力显示断言、计谋军令战报（含智谋对比与扣道数）断言、战果余兵行断言。回归：test23 10 项、test22 14 项、test21 22 项全过。
- **野战演武：兵力色条、战线兵力制与调速（第二十八轮）**（已完成，全部来自用户试玩反馈）：
  - 页头兵力条置于士气条上方（同左右对垒布局，`#fb-trbar-my/foe`，按存兵比走 `hpColor` 变色）；士气/兵力条改用 id 定位（两行 `.fb-morales` 并存后类选择器会串行）。
  - 布阵武将 chip 加显智力（武/统/智）；野战顶栏音乐钮左侧新增 `#fb-speed` 调速钮（×1/×2/×4，`bindSpeedBtn` 战中切换重建定时器）。
  - 斗将士气摆幅减半：±6（河畔 9）、连斩三将 -8、避战 -4/-6。
  - 战线兵力制：`lanes[k].myTr/foeTr` = 该线两将统帅×100；meter 仅决定互耗不对称度（己损 ∝ 敌线战力×(1.6−推进度)、敌损 ∝ 我线战力×(0.6+推进度)，±0.1 随机、决胜 ×1.6），**破线唯一条件是该线兵力耗尽**（双尽比 meter），meter 触边不再破线；线战力按该线存兵比衰减；每线实时显示双方兵数（`#fb-ltrm/ltrf-*`）；总兵力=Σ线兵力；破线额外折兵取消。
  - **验证**：test24 扩至 35 项全过——新增 chip 三维断言、调速钮就位与 ×1→×2→×4 循环断言、兵力色条在士气条上方且满编绿断言、每线兵数显示断言、避战降幅 4~6 断言。回归：test23 10 项、test22 14 项、test21 22 项全过。
- **野战演武：兵力对推条、战线攻防与各线士气（第二十九轮）**（已完成，全部来自用户试玩反馈）：
  - 五线对推条宽度改为兵力比（`laneW` = 我线兵/两军线兵之和），兵力数字嵌条内（我左敌右）；`meter` 字段整体退役。
  - `lanePower` 拆为 `laneStat(side, lane, "atk"/"def")`：平均武力/统帅加权（攻偏武 65/35、防偏统 65/35）、攻吃阵形/地形/主攻/相克、方圆改防 ×1.25、乘该线士气系数；每刻折损 = 220×敌攻/我守×随机×决胜。擂鼓攻×2、鸣金守×2。
  - 各线独立士气 `L.myMor/foeMor`（开局取全军总士气，随折损差涨落、破线邻线 -8、军令直击、雁行流失，保底 5）；战线头行改「我线士气 | 线名居中 16px | 敌线士气」，武将名单降为条下小字两端分列。
  - 点将名单与敌方搦战卡显示 `ratingScore` 总评分。
  - 奇袭并入选线流程（用户自选目标线，得手斩 1,500~2,500 众+该线敌士气 -10；火攻烧 800~1,500 众；计谋改打击该线士气 -15），敌军 AI 目标选择同步改为按存兵比/线上士气。
  - **验证**：test24 扩至 40 项全过——新增总评分（点将 10/10、搦战卡）断言、兵力数字嵌条内断言、条宽=兵力比断言（误差 <1%）、线名居中 16px+两端线士气断言、奇袭选线横幅断言、首刻不动改为条宽与开战前一致。回归：test23 10 项、test22 14 项、test21 22 项全过。
- **野战演武：擂鼓振军、雁行修正与唯兵力定胜负（第三十轮）**（已完成，全部来自用户试玩反馈）：
  - 擂鼓由「攻 ×2（9 秒）」改为即时提振该线士气 `max(3, round(线上魅力总值/12))`（新 `laneMei` 助手），敌 AI 同公式；`myBoost/foeBoost` 定时增益整体退役，攻守限时增益仅剩鸣金守 ×2。
  - 核对全部阵形/地形效果均在 `laneStat` 正确应用；修复雁行乱箭（原蚀全军总士气——总攻后无意义、且 0.15/刻不可见）→ 直击敌各线士气 0.4/刻；阵形卡描述改攻/守口径（方圆=守 +25%）。
  - 总攻页头去掉总士气行（`moraleBarHtml(showMorale=false)` + `.fb-morales.big` 字体放大），敌军兵力「字样↔数值」对换成镜像；战果卡撤「终局士气」。
  - 终局唯兵力：删去全军士气归零判负与连破三线判负，仅剩一方五线兵力尽墨（或五线俱了比存兵）；破线/计谋/奇袭全军士气加减全部改为线士气（奇袭殉国动摇后路线、计谋被识破乱本线）。
  - **验证**：test24 扩至 43 项全过——新增总攻页单行大字兵力+label/value 对换断言、擂鼓振军（前锋线士气 +≥2、战报含魅力合计）断言、雁行蚀敌实测断言（×4 观测 6 秒敌各线平均士气下降）；终局战果显示「五线兵力折损殆尽」。回归：test23 10 项、test22 14 项、test21 22 项全过。

## 〇、改造总目标

把游戏从"模式菜单合集"改造为**以武将扮演为主线**的游历养成游戏，现有各战斗玩法穿插其中；同时保留一个"小游戏"入口收纳原有模式的自由版。

### 已拍板的关键决策

| 决策点 | 结论 |
|---|---|
| 史实武将开局属性 | 新建游戏时由玩家选择：少年模式（60% 起步+本命天赋）或 巅峰模式（原值开局、成长减半） |
| 老存档迁移 | **不做**。更新后旧的历练/友谊/宝物数据废弃，一切从"新游戏"开始 |
| 自建/编辑宝物数值 | 限幅 **≤15**（含奇珍特效值），输入框做上限校验 |
| 城池数量 | 中原 10 城 + 战国 10 城（名单见第四节，已确认） |
| 行动力成长渠道 | 四渠道方案（名声主线+首冠+唯一奇珍+客栈临时），封顶 6 点/天，已确认 |
| 武将登场节奏 | 渐进登场，**不按名声分层**——顶级高手也可能随时现身 |
| 名声阶梯与终局 | 四阶名声 + 威名榜八大高手 + 武道会 → "天下无双"，已确认 |

---

## 一、新主页（5 个主模块）

```
🆕 新游戏      —— 开创新的武将人生（会覆盖旧存档，需确认）
▶️ 继续游戏    —— 读取自动存档（按钮上显示：武将名 · 第N天 · 名声；无存档则置灰）
📜 武将图鉴    —— 全局资料库，只显示默认数值，不受游戏进度影响
🎁 宝物阁      —— 全局宝物大全，可编辑、可自建，不受游戏进度影响
🕹️ 小游戏      —— 二级菜单，收纳原有 8 个战斗模式的自由版
```

**小游戏收纳**：经典单挑、车轮大战、百人斩·爬塔、2v2 主副将、阵营大战、组队大战、国战、武将世界杯。
全部为"自由模式"：任选武将、使用图鉴默认数值、不吃不产任何存档数据。

---

## 二、新游戏 · 开局三步向导

### 第 1 步 · 选角色

- **自创武将**：沿用现有角色创建（起名 + 加点），白手起家。
- **扮演史实武将**：从图鉴任选一人，随后二选一：
  - **少年模式**（推荐）：初始六维 = 默认值 60%（四舍五入），最高两项属性为"**本命天赋**"——历练成长 +50%，且成长上限可突破默认值；
  - **巅峰模式**：默认原值开局，但历练成长减半。适合想立刻横着走的玩家。

### 第 2 步 · 选天下格局

- **史实分布**：武将按真实势力落位（蜀将在成都、武田家在甲斐、岛津在萨摩……）；
- **群雄乱入**：全部武将随机洗牌分配到 20 城，每局天下大不同。

### 第 3 步 · 确认覆盖旧档

弹确认框后建立全新战役存档：角色、金币、友谊、队伍、宝物背包、地图进度全部从零开始。

### 武将渐进登场

- 开局每城只"现身"2~3 名武将，其余处于"未登场"状态；
- **每过 5 天登场一批（3~5 人）**，弹出**天下快报**："⚡ 快报：关羽现身荆州！"；
- 登场批次完全随机，**不按名声分层**——吕布、本多忠胜级别的高手也可能开局不久就现身；
- 图鉴内可查看"已登场/未登场"状态（战役内视图），寻访名将是游历动力之一。

---

## 三、主游戏界面 ——「天下游历」

### 竖屏三段式布局（宽 < 600px）

```
┌───────────────┐
│  🗺️ 天下地图 45% │
│  📍 城池操作 35% │
│  🎭 状态条  20%  │ ← 头像·六维缩略·💰金·⭐名声·⚡2/3·📅第N天
└───────────────┘
```

### 折叠屏展开 / 平板 / 横屏双栏布局（宽 ≥ 600px，CSS Grid + 媒体查询自动切换）

```
┌──────────────────┬─────────────────┐
│                  │  📍 当前城池信息    │
│   🗺️ 天下地图      │  行动按钮（双列排布） │
│   （完整不裁切，     ├─────────────────┤
│    城池名全显示）    │  🎭 角色状态卡      │
│                  │  头像+六维雷达图+装备 │
│       55%        │  💰 ⭐ ⚡ 📅 一行排开 │
└──────────────────┴─────────────────┘
```

- 展开态最大优势给地图：20 城 + 海路一屏尽览，不用滚动；
- 右栏下半为**扩展版状态卡**：常驻雷达图 + 装备栏（竖屏下仅缩略数字）；
- 战斗界面沿用现有左右对峙布局，宽屏只做安全边距微调；
- 断点 `min-width: 600px`，兼顾折叠屏展开（约 2200×1840）、平板与桌面浏览器。

---

## 四、地图 ——「真实地理版天下图」

SVG 手绘风：左侧简化中国东部轮廓 + 右侧日本列岛轮廓，城池按**真实相对经纬度**落点，道路/海路连线。

### 中原十城（三国势力分布）

| 城池 | 地理位置 | 常驻势力 |
|---|---|---|
| 成都 | 西南盆地 | 蜀 |
| 汉中 | 秦岭之南 | 蜀 |
| 长安 | 关中 | 魏 |
| 洛阳 | 中原腹地 | 魏 |
| 许昌 | 中原 | 魏 |
| 邺城 | 华北 | 魏 |
| 徐州 | 华东 | 魏 |
| 荆州 | 长江中游 | 吴（争地） |
| 柴桑 | 江右 | 吴 |
| 建业 | 江东 | 吴 |

### 战国十城（大名居城）

| 城池 | 地理位置 | 常驻势力 |
|---|---|---|
| 萨摩 | 九州最南 | 岛津家 |
| 安艺 | 本州西部 | 毛利家 |
| 京都 | 近畿 | 足利·明智 |
| 大坂 | 近畿 | 丰臣家 |
| 尾张 | 中部 | 织田家 |
| 甲斐 | 中部山地 | 武田家 |
| 骏府 | 东海道 | 德川·今川 |
| 小田原 | 关东 | 北条家 |
| 越后 | 日本海侧 | 上杉家 |
| 奥州 | 东北 | 伊达家 |

### 海路

**建业 / 徐州 ⇄ 对马岛 ⇄ 萨摩**

- 对马岛为中转补给点，常驻**黑市商人**（特殊商店）；
- 渡海一次耗 1 天，可能触发"风暴"事件：延误 1 天，或漂流到随机港口（也算奇遇）。

### 赶路奇遇（移动时约 30% 概率触发）

| 事件 | 效果 |
|---|---|
| 山贼拦路 | 小战斗，赢了得金币 |
| 路遇奇珍 | 捡到神秘宝物（未鉴定） |
| 行脚商队 | 折扣商店 |
| 世外高人 | +1 属性点 |
| 路遇名将 | 直接结交，获友谊 |
| 客栈奇遇（小概率） | 当天行动力临时 +1 |

---

## 五、行动力系统

- 每天拥有 **⚡行动力**，初始上限 **1 点/天**；
- **耗 1 点**的重行动：历练、切磋/擂台/道场等一切战斗、承接悬赏战斗、移动一段路（移动同时触发赶路奇遇判定）；
- **免费**的轻行动：拜访、赠宝、逛集市、锻造、看悬赏榜、查看队伍；
- 行动力用尽后出现"🏕️ 宿营至次日"按钮，进入下一天并恢复满行动力（可提前宿营）。

### 行动力上限成长（四渠道，封顶 6 点/天）

| 渠道 | 效果 | 设计意图 |
|---|---|---|
| ⭐ 名声阶梯 | 每升一阶 +1（无名1 → 小有名气2 → 威震一方3 → 名满天下4） | 主渠道：越有名，天下事越多 |
| 🏆 武道会夺冠 | 永久 +1（仅首冠） | 终局前最后一点 |
| 🐎 唯一奇珍「赤兔马」「千里靴」 | 装备时各 +1 | 全地图仅各 1 件，藏在高级悬赏/深层爬塔中 |
| 🍵 客栈奇遇 | 当天临时 +1 | 赶路惊喜，不破坏上限体系 |

界面常驻显示 `⚡ 当前/每日上限`（状态条 + 宿营按钮旁）。

---

## 六、城池行动与玩法穿插

每城 = 通用功能 + 1 个特色设施。

### 通用功能

| 功能 | 说明 |
|---|---|
| 🚶 拜访名将 | 武将按城分布，结交/赠宝/切磋/招募需"人在哪座城就去哪座城" |
| 🏋️ 历练 | 现有历练玩法接入，一次耗 1 点行动力 |
| 🏪 集市 | 现有商店，**各城价格 ±20% 浮动**，可倒卖 |
| ⚒️ 铁匠铺 | 现有锻造 |
| 📋 悬赏榜 | 每城随机 2~3 个任务（"击败张飞""车轮战三连胜"等），赏金+名声+宝物 |

### 特色设施（现有玩法穿插，扮演角色亲自上阵）

| 城池 | 特色设施 | 对应玩法 |
|---|---|---|
| 洛阳 | 天下擂台 | 经典单挑挑战名将 |
| 京都 | 百人斩道场 | 爬塔模式 |
| 成都 | 车轮战武场 | 车轮战 |
| 大坂 | 双人比武 | 2v2（副将从队伍选） |
| 小田原 | 军团攻城 | 组队大战 |
| 奥州 | 远征 | 国战入口 |
| （其余城池） | 待第三期细化 | 每城都有"必去一次"的理由 |

### 天下大事件

名声达标后触发：阵营大战、国战（玩家队伍全员参战）。

---

## 七、名声与终极目标

- 新资源 **⭐名声**：赢切磋、完成悬赏、赢设施挑战都涨；
- 名声四阶：**无名之辈 → 小有名气 → 威震一方 → 名满天下**；
- 名声作用：提升行动力上限（主渠道）、解锁高级悬赏、解锁"天下第一武道会"（世界杯玩法，每 30 天一届）、触发天下大事件；
- **注意**：名声**不**控制武将登场（已拍板移除该设定）；
- **终局目标**：击败「威名榜」八大高手（吕布、关羽、本多忠胜等）+ 武道会夺冠 → 获封"**天下无双**"，此后进入自由沙盒继续游历。

---

## 八、全局武将图鉴 / 宝物阁

### 武将图鉴（全局）

- 保留现有浏览、编辑、新增武将功能；
- **去掉**友谊、队伍、装备等一切进度数据显示，详情弹窗只展示默认六维；
- 定位是"设定集"，不是存档查看器。

### 宝物阁（全局）

- **全部宝物明牌展示**（不再有"未发现"遮罩）；
- 每件可编辑名称、简介、类型、属性、加成值，也可**自建新宝物**；
- 自建/改动后的宝物进入新游戏的掉落与商店池；
- **数值限幅 ≤15**（含奇珍特效值），输入框做上限校验。

---

## 九、存档架构

| 层 | 内容 | 生命周期 |
|---|---|---|
| **全局数据** | 自定义武将、宝物编辑、音频设置 | 永不随新游戏重置 |
| **战役存档** | 角色、模式选择（少年/巅峰、史实/乱入）、金币、友谊、队伍、宝物背包、地图位置、天数、行动力、名声、已登场武将名单、悬赏进度 | 单一自动档 `wujiang_campaign_v1`，每次行动后自动保存；新游戏覆盖（需确认） |

- **不做老档迁移**（已拍板）；
- "继续游戏"按钮读取自动档并显示摘要（武将名 · 第 N 天 · 名声）。

---

## 十、分期实施计划

每期单独交付、可玩可测；每期完成后本地提交并打包交付。

### 第一期 · 骨架

1. 新主页（5 模块）+ 小游戏二级菜单（8 模式自由版迁移）；
2. 全局武将图鉴改造（剥离进度数据）；
3. 全局宝物阁改造（明牌+编辑+自建，限幅 ≤15）；
4. 存档架构（全局层/战役层分离，自动存档机制）；
5. 开局三步向导（选角色/少年巅峰、选格局、确认覆盖）。

### 第二期 · 地图

1. 真实地理 SVG 地图（20 城 + 对马岛海路）；
2. 移动 / 天数 / 行动力系统（含宿营）；
3. 折叠屏双栏布局（`min-width: 600px` 断点）；
4. 城池界面（通用功能：拜访/历练/集市/铁匠铺）；
5. 武将按城分布（史实/乱入两种格局）与渐进登场（每 5 天一批+天下快报）。

### 第三期 · 血肉

1. 名声系统（四阶+行动力联动+武道会解锁）；
2. 悬赏榜（每城随机任务）；
3. 赶路奇遇（六类事件+渡海风暴+对马黑市）；
4. 特色设施穿插（各城玩法入口）+ 天下大事件；
5. 唯一奇珍「赤兔马」「千里靴」；
6. 天下第一武道会与"天下无双"终局。

---

## 附：与现有代码的衔接要点（实施参考）

- 所有战斗仍走 `makeFighter` / `makeTroopUnit` 统一装备加成通道，扮演模式与小游戏共用战斗引擎；
- 小游戏自由版 = 现有各模式去掉 RPG/Bond/Armory 读写（选人池用图鉴默认值）；
- 现有 `Bond`（友谊/金币/队伍）与 `Armory`（宝物）模块整体收编进战役存档层；
- 宝物模板编辑存全局层，战役开局时以"全局模板（含自建）"为掉落/商店池快照；
- Service Worker 缓存版本与 `APP_VERSION` 按现有约定每次交付时同步更新。
